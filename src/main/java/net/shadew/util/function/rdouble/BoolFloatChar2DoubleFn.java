package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code char} and returns a {@code double}.
 */
public interface BoolFloatChar2DoubleFn {
    double invoke(boolean p1, float p2, char p3);
}
