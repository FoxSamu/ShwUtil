package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code double} and returns a {@code double}.
 */
public interface BoolDoubleDouble2DoubleFn {
    double invoke(boolean p1, double p2, double p3);
}
