package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns a {@code double}.
 */
public interface ShortDoubleInt2DoubleFn {
    double invoke(short p1, double p2, int p3);
}
