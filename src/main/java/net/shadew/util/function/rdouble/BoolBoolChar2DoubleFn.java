package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code char} and returns a {@code double}.
 */
public interface BoolBoolChar2DoubleFn {
    double invoke(Bool p1, Bool p2, Char p3);
}
