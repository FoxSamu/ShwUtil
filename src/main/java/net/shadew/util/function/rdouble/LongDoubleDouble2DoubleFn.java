package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code double} and returns a {@code double}.
 */
public interface LongDoubleDouble2DoubleFn {
    double invoke(long p1, double p2, double p3);
}
