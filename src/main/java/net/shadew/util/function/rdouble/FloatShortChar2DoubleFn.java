package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code char} and returns a {@code double}.
 */
public interface FloatShortChar2DoubleFn {
    double invoke(float p1, short p2, char p3);
}
