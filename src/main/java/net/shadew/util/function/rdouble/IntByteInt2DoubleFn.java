package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns a {@code double}.
 */
public interface IntByteInt2DoubleFn {
    double invoke(int p1, byte p2, int p3);
}
