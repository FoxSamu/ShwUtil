package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code double} and returns a {@code double}.
 */
public interface ByteShortDouble2DoubleFn {
    double invoke(byte p1, short p2, double p3);
}
