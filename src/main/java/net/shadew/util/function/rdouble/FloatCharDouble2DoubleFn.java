package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code double} and returns a {@code double}.
 */
public interface FloatCharDouble2DoubleFn {
    double invoke(float p1, char p2, double p3);
}
