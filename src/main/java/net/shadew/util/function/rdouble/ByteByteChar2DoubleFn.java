package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code char} and returns a {@code double}.
 */
public interface ByteByteChar2DoubleFn {
    double invoke(byte p1, byte p2, char p3);
}
