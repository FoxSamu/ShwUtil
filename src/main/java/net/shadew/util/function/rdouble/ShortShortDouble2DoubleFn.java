package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code double} and returns a {@code double}.
 */
public interface ShortShortDouble2DoubleFn {
    double invoke(short p1, short p2, double p3);
}
