package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long} and a {@code char} and returns a {@code double}.
 */
public interface LongChar2DoubleFn {
    double invoke(long p1, char p2);
}
