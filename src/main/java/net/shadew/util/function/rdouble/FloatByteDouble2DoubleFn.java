package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns a {@code double}.
 */
public interface FloatByteDouble2DoubleFn {
    double invoke(float p1, byte p2, double p3);
}
