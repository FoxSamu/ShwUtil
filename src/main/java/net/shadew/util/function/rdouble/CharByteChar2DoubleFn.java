package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns a {@code double}.
 */
public interface CharByteChar2DoubleFn {
    double invoke(char p1, byte p2, char p3);
}
