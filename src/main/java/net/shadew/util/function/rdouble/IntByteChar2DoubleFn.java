package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns a {@code double}.
 */
public interface IntByteChar2DoubleFn {
    double invoke(int p1, byte p2, char p3);
}
