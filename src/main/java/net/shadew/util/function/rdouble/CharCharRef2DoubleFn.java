package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code char} and an object and returns a {@code double}.
 */
public interface CharCharRef2DoubleFn<P3> {
    double invoke(char p1, char p2, P3 p3);
}
