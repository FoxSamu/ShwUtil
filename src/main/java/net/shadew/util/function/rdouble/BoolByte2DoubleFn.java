package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean} and a {@code byte} and returns a {@code double}.
 */
public interface BoolByte2DoubleFn {
    double invoke(boolean p1, byte p2);
}
