package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code short} and returns a {@code double}.
 */
public interface FloatDoubleShort2DoubleFn {
    double invoke(float p1, double p2, short p3);
}
