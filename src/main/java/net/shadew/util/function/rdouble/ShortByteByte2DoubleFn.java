package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code byte} and returns a {@code double}.
 */
public interface ShortByteByte2DoubleFn {
    double invoke(short p1, byte p2, byte p3);
}
