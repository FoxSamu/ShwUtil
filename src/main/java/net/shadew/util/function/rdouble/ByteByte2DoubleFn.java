package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte} and a {@code byte} and returns a {@code double}.
 */
public interface ByteByte2DoubleFn {
    double invoke(byte p1, byte p2);
}
