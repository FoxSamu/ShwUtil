package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long} and an {@code int} and returns a {@code double}.
 */
public interface LongInt2DoubleFn {
    double invoke(Long p1, Int p2);
}
