package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code double} and returns a {@code double}.
 */
public interface LongBoolDouble2DoubleFn {
    double invoke(long p1, boolean p2, double p3);
}
