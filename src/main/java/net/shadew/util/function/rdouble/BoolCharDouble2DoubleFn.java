package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code double} and returns a {@code double}.
 */
public interface BoolCharDouble2DoubleFn {
    double invoke(boolean p1, char p2, double p3);
}
