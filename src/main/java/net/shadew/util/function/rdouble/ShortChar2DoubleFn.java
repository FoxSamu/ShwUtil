package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short} and a {@code char} and returns a {@code double}.
 */
public interface ShortChar2DoubleFn {
    double invoke(short p1, char p2);
}
