package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface ByteDoubleChar2DoubleFn {
    double invoke(byte p1, double p2, char p3);
}
