package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code boolean} and returns a {@code double}.
 */
public interface FloatShortBool2DoubleFn {
    double invoke(float p1, short p2, boolean p3);
}
