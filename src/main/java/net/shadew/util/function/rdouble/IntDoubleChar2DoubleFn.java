package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface IntDoubleChar2DoubleFn {
    double invoke(int p1, double p2, char p3);
}
