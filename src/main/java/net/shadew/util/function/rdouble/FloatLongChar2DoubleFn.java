package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code char} and returns a {@code double}.
 */
public interface FloatLongChar2DoubleFn {
    double invoke(float p1, long p2, char p3);
}
