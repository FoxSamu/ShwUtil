package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code byte} and returns a {@code double}.
 */
public interface ShortBoolByte2DoubleFn {
    double invoke(short p1, boolean p2, byte p3);
}
