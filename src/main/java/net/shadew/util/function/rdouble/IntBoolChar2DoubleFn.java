package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code char} and returns a {@code double}.
 */
public interface IntBoolChar2DoubleFn {
    double invoke(int p1, boolean p2, char p3);
}
