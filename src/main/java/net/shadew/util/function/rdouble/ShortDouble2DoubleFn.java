package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short} and a {@code double} and returns a {@code double}.
 */
public interface ShortDouble2DoubleFn {
    double invoke(short p1, double p2);
}
