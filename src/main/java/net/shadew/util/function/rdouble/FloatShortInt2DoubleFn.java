package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code short} and an {@code int} and returns a {@code double}.
 */
public interface FloatShortInt2DoubleFn {
    double invoke(float p1, short p2, int p3);
}
