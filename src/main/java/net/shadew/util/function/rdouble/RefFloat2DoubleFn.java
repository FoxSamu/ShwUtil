package net.shadew.util.function.rdouble;

/**
 * A function that takes an object and a {@code float} and returns a {@code double}.
 */
public interface RefFloat2DoubleFn<P1> {
    double invoke(P1 p1, float p2);
}
