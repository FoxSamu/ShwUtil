package net.shadew.util.function.rdouble;

/**
 * A function that takes an object, a {@code boolean} and a {@code byte} and returns a {@code double}.
 */
public interface RefBoolByte2DoubleFn<P1> {
    double invoke(P1 p1, boolean p2, byte p3);
}
