package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char} and a {@code byte} and returns a {@code double}.
 */
public interface CharByte2DoubleFn {
    double invoke(char p1, byte p2);
}
