package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code short} and returns a {@code double}.
 */
public interface IntByteShort2DoubleFn {
    double invoke(int p1, byte p2, short p3);
}
