package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code byte} and returns a {@code double}.
 */
public interface FloatLongByte2DoubleFn {
    double invoke(float p1, long p2, byte p3);
}
