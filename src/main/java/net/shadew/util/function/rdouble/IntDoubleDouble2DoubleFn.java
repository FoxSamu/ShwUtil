package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code double} and returns a {@code double}.
 */
public interface IntDoubleDouble2DoubleFn {
    double invoke(int p1, double p2, double p3);
}
