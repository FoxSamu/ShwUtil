package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns a {@code double}.
 */
public interface BoolBool2DoubleFn {
    double invoke(boolean p1, boolean p2);
}
