package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code double} and an {@code int} and returns a {@code double}.
 */
public interface DoubleInt2DoubleFn {
    double invoke(double p1, int p2);
}
