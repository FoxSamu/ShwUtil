package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns a {@code double}.
 */
public interface ByteBool2DoubleFn {
    double invoke(byte p1, boolean p2);
}
