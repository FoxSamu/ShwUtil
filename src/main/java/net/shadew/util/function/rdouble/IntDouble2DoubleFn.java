package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int} and a {@code double} and returns a {@code double}.
 */
public interface IntDouble2DoubleFn {
    double invoke(int p1, double p2);
}
