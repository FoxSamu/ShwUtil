package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code short} and an {@code int} and returns a {@code double}.
 */
public interface ByteShortInt2DoubleFn {
    double invoke(byte p1, short p2, int p3);
}
