package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code short} and returns a {@code double}.
 */
public interface BoolByteShort2DoubleFn {
    double invoke(boolean p1, byte p2, short p3);
}
