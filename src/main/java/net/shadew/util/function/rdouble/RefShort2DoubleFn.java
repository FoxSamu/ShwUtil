package net.shadew.util.function.rdouble;

/**
 * A function that takes an object and a {@code short} and returns a {@code double}.
 */
public interface RefShort2DoubleFn<P1> {
    double invoke(P1 p1, short p2);
}
