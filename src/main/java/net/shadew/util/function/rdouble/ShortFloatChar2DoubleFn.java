package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code char} and returns a {@code double}.
 */
public interface ShortFloatChar2DoubleFn {
    double invoke(short p1, float p2, char p3);
}
