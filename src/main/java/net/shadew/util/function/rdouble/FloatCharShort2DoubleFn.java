package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code short} and returns a {@code double}.
 */
public interface FloatCharShort2DoubleFn {
    double invoke(float p1, char p2, short p3);
}
