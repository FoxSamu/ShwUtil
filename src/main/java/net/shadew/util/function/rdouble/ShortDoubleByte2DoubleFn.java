package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code byte} and returns a {@code double}.
 */
public interface ShortDoubleByte2DoubleFn {
    double invoke(short p1, double p2, byte p3);
}
