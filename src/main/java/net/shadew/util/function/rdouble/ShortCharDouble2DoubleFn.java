package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code double} and returns a {@code double}.
 */
public interface ShortCharDouble2DoubleFn {
    double invoke(short p1, char p2, double p3);
}
