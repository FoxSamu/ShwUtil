package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short} and a {@code boolean} and returns a {@code double}.
 */
public interface ShortBool2DoubleFn {
    double invoke(short p1, boolean p2);
}
