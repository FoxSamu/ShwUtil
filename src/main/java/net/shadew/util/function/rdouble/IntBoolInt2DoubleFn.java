package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code boolean} and an {@code int} and returns a {@code double}.
 */
public interface IntBoolInt2DoubleFn {
    double invoke(int p1, boolean p2, int p3);
}
