package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an {@code int} and returns a {@code double}.
 */
public interface BoolByteInt2DoubleFn {
    double invoke(boolean p1, byte p2, int p3);
}
