package net.shadew.util.function.rdouble;

/**
 * A function that takes an object, a {@code char} and a {@code byte} and returns a {@code double}.
 */
public interface RefCharByte2DoubleFn<P1> {
    double invoke(P1 p1, char p2, byte p3);
}
