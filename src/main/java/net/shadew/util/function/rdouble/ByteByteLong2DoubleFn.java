package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code long} and returns a {@code double}.
 */
public interface ByteByteLong2DoubleFn {
    double invoke(byte p1, byte p2, long p3);
}
