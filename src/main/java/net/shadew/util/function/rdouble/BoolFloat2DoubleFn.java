package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean} and a {@code float} and returns a {@code double}.
 */
public interface BoolFloat2DoubleFn {
    double invoke(boolean p1, float p2);
}
