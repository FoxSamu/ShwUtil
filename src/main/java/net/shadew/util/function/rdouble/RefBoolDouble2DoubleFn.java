package net.shadew.util.function.rdouble;

/**
 * A function that takes an object, a {@code boolean} and a {@code double} and returns a {@code double}.
 */
public interface RefBoolDouble2DoubleFn<P1> {
    double invoke(P1 p1, boolean p2, double p3);
}
