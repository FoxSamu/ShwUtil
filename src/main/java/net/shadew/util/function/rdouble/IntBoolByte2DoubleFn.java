package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code byte} and returns a {@code double}.
 */
public interface IntBoolByte2DoubleFn {
    double invoke(int p1, boolean p2, byte p3);
}
