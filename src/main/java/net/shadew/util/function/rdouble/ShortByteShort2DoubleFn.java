package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code short} and returns a {@code double}.
 */
public interface ShortByteShort2DoubleFn {
    double invoke(short p1, byte p2, short p3);
}
