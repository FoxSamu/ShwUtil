package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code char} and returns a {@code double}.
 */
public interface ShortShortChar2DoubleFn {
    double invoke(short p1, short p2, char p3);
}
