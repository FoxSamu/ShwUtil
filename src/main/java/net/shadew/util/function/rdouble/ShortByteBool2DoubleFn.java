package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code boolean} and returns a {@code double}.
 */
public interface ShortByteBool2DoubleFn {
    double invoke(short p1, byte p2, boolean p3);
}
