package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code byte} and returns a {@code double}.
 */
public interface FloatDoubleByte2DoubleFn {
    double invoke(float p1, double p2, byte p3);
}
