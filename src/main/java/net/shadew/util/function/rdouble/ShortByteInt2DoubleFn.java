package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code byte} and an {@code int} and returns a {@code double}.
 */
public interface ShortByteInt2DoubleFn {
    double invoke(short p1, byte p2, int p3);
}
