package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code char} and returns a {@code double}.
 */
public interface FloatBoolChar2DoubleFn {
    double invoke(float p1, boolean p2, char p3);
}
