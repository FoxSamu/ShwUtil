package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code byte} and returns a {@code double}.
 */
public interface FloatCharByte2DoubleFn {
    double invoke(float p1, char p2, byte p3);
}
