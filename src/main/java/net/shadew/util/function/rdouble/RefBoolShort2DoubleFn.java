package net.shadew.util.function.rdouble;

/**
 * A function that takes an object, a {@code boolean} and a {@code short} and returns a {@code double}.
 */
public interface RefBoolShort2DoubleFn<P1> {
    double invoke(P1 p1, boolean p2, short p3);
}
