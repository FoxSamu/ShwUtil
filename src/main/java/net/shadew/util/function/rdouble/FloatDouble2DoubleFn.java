package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float} and a {@code double} and returns a {@code double}.
 */
public interface FloatDouble2DoubleFn {
    double invoke(float p1, double p2);
}
