package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns a {@code double}.
 */
public interface ByteBoolChar2DoubleFn {
    double invoke(byte p1, boolean p2, char p3);
}
