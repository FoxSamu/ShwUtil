package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code char} and returns a {@code double}.
 */
public interface FloatIntChar2DoubleFn {
    double invoke(float p1, int p2, char p3);
}
