package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code char} and an {@code int} and returns a {@code double}.
 */
public interface BoolCharInt2DoubleFn {
    double invoke(boolean p1, char p2, int p3);
}
