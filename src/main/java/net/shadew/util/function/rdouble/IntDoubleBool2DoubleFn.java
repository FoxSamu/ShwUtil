package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code boolean} and returns a {@code double}.
 */
public interface IntDoubleBool2DoubleFn {
    double invoke(int p1, double p2, boolean p3);
}
