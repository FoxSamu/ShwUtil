package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code double} and an {@code int} and returns a {@code double}.
 */
public interface CharDoubleInt2DoubleFn {
    double invoke(char p1, double p2, int p3);
}
