package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns a {@code double}.
 */
public interface FloatByteBool2DoubleFn {
    double invoke(float p1, byte p2, boolean p3);
}
