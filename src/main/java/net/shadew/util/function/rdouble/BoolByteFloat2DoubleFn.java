package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code float} and returns a {@code double}.
 */
public interface BoolByteFloat2DoubleFn {
    double invoke(boolean p1, byte p2, float p3);
}
