package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code char} and returns a {@code double}.
 */
public interface LongByteChar2DoubleFn {
    double invoke(long p1, byte p2, char p3);
}
