package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code double} and returns a {@code double}.
 */
public interface LongByteDouble2DoubleFn {
    double invoke(long p1, byte p2, double p3);
}
