package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code char} and returns a {@code double}.
 */
public interface ShortByteChar2DoubleFn {
    double invoke(short p1, byte p2, char p3);
}
