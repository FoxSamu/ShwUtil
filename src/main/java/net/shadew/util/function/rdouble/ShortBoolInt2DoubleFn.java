package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code boolean} and an {@code int} and returns a {@code double}.
 */
public interface ShortBoolInt2DoubleFn {
    double invoke(short p1, boolean p2, int p3);
}
