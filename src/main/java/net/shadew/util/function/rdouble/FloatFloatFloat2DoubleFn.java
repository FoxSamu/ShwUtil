package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code float} and returns a {@code double}.
 */
public interface FloatFloatFloat2DoubleFn {
    double invoke(Float p1, Float p2, Float p3);
}
