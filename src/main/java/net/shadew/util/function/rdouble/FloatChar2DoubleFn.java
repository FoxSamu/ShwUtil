package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float} and a {@code char} and returns a {@code double}.
 */
public interface FloatChar2DoubleFn {
    double invoke(float p1, char p2);
}
