package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long} and a {@code boolean} and returns a {@code double}.
 */
public interface LongBool2DoubleFn {
    double invoke(long p1, boolean p2);
}
