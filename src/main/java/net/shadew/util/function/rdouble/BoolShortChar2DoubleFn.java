package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code char} and returns a {@code double}.
 */
public interface BoolShortChar2DoubleFn {
    double invoke(boolean p1, short p2, char p3);
}
