package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, an {@code int} and a {@code byte} and returns a {@code double}.
 */
public interface BoolIntByte2DoubleFn {
    double invoke(Bool p1, Int p2, Byte p3);
}
