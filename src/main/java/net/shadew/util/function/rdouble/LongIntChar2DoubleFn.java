package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code char} and returns a {@code double}.
 */
public interface LongIntChar2DoubleFn {
    double invoke(long p1, int p2, char p3);
}
