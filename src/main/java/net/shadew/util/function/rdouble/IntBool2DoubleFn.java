package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int} and a {@code boolean} and returns a {@code double}.
 */
public interface IntBool2DoubleFn {
    double invoke(int p1, boolean p2);
}
