package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long} and a {@code byte} and returns a {@code double}.
 */
public interface LongByte2DoubleFn {
    double invoke(long p1, byte p2);
}
