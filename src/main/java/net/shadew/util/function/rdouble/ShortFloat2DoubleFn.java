package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short} and a {@code float} and returns a {@code double}.
 */
public interface ShortFloat2DoubleFn {
    double invoke(short p1, float p2);
}
