package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code boolean} and returns a {@code double}.
 */
public interface IntCharBool2DoubleFn {
    double invoke(int p1, char p2, boolean p3);
}
