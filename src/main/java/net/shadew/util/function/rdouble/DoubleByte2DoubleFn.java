package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code double} and a {@code byte} and returns a {@code double}.
 */
public interface DoubleByte2DoubleFn {
    double invoke(double p1, byte p2);
}
