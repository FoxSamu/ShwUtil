package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code double} and an {@code int} and returns a {@code double}.
 */
public interface FloatDoubleInt2DoubleFn {
    double invoke(float p1, double p2, int p3);
}
