package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code float} and returns a {@code double}.
 */
public interface BoolDoubleFloat2DoubleFn {
    double invoke(boolean p1, double p2, float p3);
}
