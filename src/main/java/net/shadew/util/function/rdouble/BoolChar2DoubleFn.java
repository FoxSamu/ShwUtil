package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns a {@code double}.
 */
public interface BoolChar2DoubleFn {
    double invoke(boolean p1, char p2);
}
