package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface ShortDoubleChar2DoubleFn {
    double invoke(short p1, double p2, char p3);
}
