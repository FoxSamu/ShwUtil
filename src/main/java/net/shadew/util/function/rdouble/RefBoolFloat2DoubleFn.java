package net.shadew.util.function.rdouble;

/**
 * A function that takes an object, a {@code boolean} and a {@code float} and returns a {@code double}.
 */
public interface RefBoolFloat2DoubleFn<P1> {
    double invoke(P1 p1, boolean p2, float p3);
}
