package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code long} and an {@code int} and returns a {@code double}.
 */
public interface FloatLongInt2DoubleFn {
    double invoke(Float p1, Long p2, Int p3);
}
