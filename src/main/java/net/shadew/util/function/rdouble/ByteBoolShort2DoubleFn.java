package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code short} and returns a {@code double}.
 */
public interface ByteBoolShort2DoubleFn {
    double invoke(byte p1, boolean p2, short p3);
}
