package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns a {@code double}.
 */
public interface ByteByteDouble2DoubleFn {
    double invoke(byte p1, byte p2, double p3);
}
