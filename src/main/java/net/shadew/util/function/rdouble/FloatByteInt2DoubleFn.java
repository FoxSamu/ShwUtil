package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns a {@code double}.
 */
public interface FloatByteInt2DoubleFn {
    double invoke(float p1, byte p2, int p3);
}
