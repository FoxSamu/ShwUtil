package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code char} and returns a {@code double}.
 */
public interface FloatCharChar2DoubleFn {
    double invoke(float p1, char p2, char p3);
}
