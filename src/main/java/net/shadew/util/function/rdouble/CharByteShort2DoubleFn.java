package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code short} and returns a {@code double}.
 */
public interface CharByteShort2DoubleFn {
    double invoke(char p1, byte p2, short p3);
}
