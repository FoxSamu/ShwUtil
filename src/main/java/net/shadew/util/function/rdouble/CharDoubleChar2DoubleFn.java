package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface CharDoubleChar2DoubleFn {
    double invoke(char p1, double p2, char p3);
}
