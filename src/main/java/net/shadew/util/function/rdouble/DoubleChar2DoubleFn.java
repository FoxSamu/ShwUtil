package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface DoubleChar2DoubleFn {
    double invoke(double p1, char p2);
}
