package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code double} and a {@code double} and returns a {@code double}.
 */
public interface DoubleDouble2DoubleFn {
    double invoke(double p1, double p2);
}
