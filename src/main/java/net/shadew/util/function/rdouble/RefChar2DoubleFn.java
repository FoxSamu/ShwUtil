package net.shadew.util.function.rdouble;

/**
 * A function that takes an object and a {@code char} and returns a {@code double}.
 */
public interface RefChar2DoubleFn<P1> {
    double invoke(P1 p1, char p2);
}
