package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code char} and returns a {@code double}.
 */
public interface BoolByteChar2DoubleFn {
    double invoke(boolean p1, byte p2, char p3);
}
