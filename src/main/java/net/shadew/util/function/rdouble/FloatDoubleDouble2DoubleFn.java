package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code double} and returns a {@code double}.
 */
public interface FloatDoubleDouble2DoubleFn {
    double invoke(float p1, double p2, double p3);
}
