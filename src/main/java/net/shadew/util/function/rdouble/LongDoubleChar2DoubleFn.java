package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface LongDoubleChar2DoubleFn {
    double invoke(long p1, double p2, char p3);
}
