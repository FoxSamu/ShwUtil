package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code char} and returns a {@code double}.
 */
public interface FloatByteChar2DoubleFn {
    double invoke(float p1, byte p2, char p3);
}
