package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code short} and a {@code double} and returns a {@code double}.
 */
public interface CharShortDouble2DoubleFn {
    double invoke(char p1, short p2, double p3);
}
