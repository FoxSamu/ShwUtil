package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns a {@code double}.
 */
public interface ByteByteBool2DoubleFn {
    double invoke(byte p1, byte p2, boolean p3);
}
