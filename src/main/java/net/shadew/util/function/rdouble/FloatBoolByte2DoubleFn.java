package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code byte} and returns a {@code double}.
 */
public interface FloatBoolByte2DoubleFn {
    double invoke(float p1, boolean p2, byte p3);
}
