package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code byte} and returns a {@code double}.
 */
public interface IntByteByte2DoubleFn {
    double invoke(int p1, byte p2, byte p3);
}
