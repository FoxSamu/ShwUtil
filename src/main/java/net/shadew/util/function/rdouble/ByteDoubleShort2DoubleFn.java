package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code short} and returns a {@code double}.
 */
public interface ByteDoubleShort2DoubleFn {
    double invoke(byte p1, double p2, short p3);
}
