package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code boolean} and returns a {@code double}.
 */
public interface LongByteBool2DoubleFn {
    double invoke(long p1, byte p2, boolean p3);
}
