package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte} and a {@code float} and returns a {@code double}.
 */
public interface ByteFloat2DoubleFn {
    double invoke(byte p1, float p2);
}
