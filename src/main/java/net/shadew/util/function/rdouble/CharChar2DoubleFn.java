package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char} and a {@code char} and returns a {@code double}.
 */
public interface CharChar2DoubleFn {
    double invoke(char p1, char p2);
}
