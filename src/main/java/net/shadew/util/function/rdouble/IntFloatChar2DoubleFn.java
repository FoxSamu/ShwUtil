package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code char} and returns a {@code double}.
 */
public interface IntFloatChar2DoubleFn {
    double invoke(int p1, float p2, char p3);
}
