package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float} and a {@code boolean} and returns a {@code double}.
 */
public interface FloatBool2DoubleFn {
    double invoke(float p1, boolean p2);
}
