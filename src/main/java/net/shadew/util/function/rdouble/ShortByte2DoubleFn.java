package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short} and a {@code byte} and returns a {@code double}.
 */
public interface ShortByte2DoubleFn {
    double invoke(short p1, byte p2);
}
