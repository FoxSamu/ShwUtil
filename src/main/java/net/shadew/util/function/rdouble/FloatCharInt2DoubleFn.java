package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code char} and an {@code int} and returns a {@code double}.
 */
public interface FloatCharInt2DoubleFn {
    double invoke(float p1, char p2, int p3);
}
