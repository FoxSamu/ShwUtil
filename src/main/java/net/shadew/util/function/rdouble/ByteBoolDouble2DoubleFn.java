package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code double} and returns a {@code double}.
 */
public interface ByteBoolDouble2DoubleFn {
    double invoke(byte p1, boolean p2, double p3);
}
