package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte} and a {@code short} and returns a {@code double}.
 */
public interface ByteShort2DoubleFn {
    double invoke(byte p1, short p2);
}
