package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code boolean} and returns a {@code double}.
 */
public interface ByteDoubleBool2DoubleFn {
    double invoke(byte p1, double p2, boolean p3);
}
