package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code byte} and returns a {@code double}.
 */
public interface BoolShortByte2DoubleFn {
    double invoke(boolean p1, short p2, byte p3);
}
