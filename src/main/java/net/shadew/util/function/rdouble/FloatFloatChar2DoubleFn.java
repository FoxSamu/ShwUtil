package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code char} and returns a {@code double}.
 */
public interface FloatFloatChar2DoubleFn {
    double invoke(float p1, float p2, char p3);
}
