package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns a {@code double}.
 */
public interface IntDoubleByte2DoubleFn {
    double invoke(int p1, double p2, byte p3);
}
