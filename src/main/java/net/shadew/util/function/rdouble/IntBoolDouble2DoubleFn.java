package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code double} and returns a {@code double}.
 */
public interface IntBoolDouble2DoubleFn {
    double invoke(int p1, boolean p2, double p3);
}
