package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char} and an {@code int} and returns a {@code double}.
 */
public interface CharInt2DoubleFn {
    double invoke(char p1, int p2);
}
