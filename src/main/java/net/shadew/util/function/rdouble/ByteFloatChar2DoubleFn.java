package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code char} and returns a {@code double}.
 */
public interface ByteFloatChar2DoubleFn {
    double invoke(byte p1, float p2, char p3);
}
