package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an object and returns a {@code double}.
 */
public interface ByteBoolRef2DoubleFn<P3> {
    double invoke(byte p1, boolean p2, P3 p3);
}
