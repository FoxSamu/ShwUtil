package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code char} and returns a {@code double}.
 */
public interface IntCharChar2DoubleFn {
    double invoke(int p1, char p2, char p3);
}
