package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface BoolDoubleChar2DoubleFn {
    double invoke(boolean p1, double p2, char p3);
}
