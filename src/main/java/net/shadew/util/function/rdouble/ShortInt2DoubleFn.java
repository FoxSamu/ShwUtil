package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short} and an {@code int} and returns a {@code double}.
 */
public interface ShortInt2DoubleFn {
    double invoke(short p1, int p2);
}
