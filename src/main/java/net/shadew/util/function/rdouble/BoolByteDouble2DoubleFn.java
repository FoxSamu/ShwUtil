package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code double} and returns a {@code double}.
 */
public interface BoolByteDouble2DoubleFn {
    double invoke(boolean p1, byte p2, double p3);
}
