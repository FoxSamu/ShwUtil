package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns a {@code double}.
 */
public interface BoolDouble2DoubleFn {
    double invoke(boolean p1, double p2);
}
