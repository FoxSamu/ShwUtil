package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an object and returns a {@code double}.
 */
public interface BoolBoolRef2DoubleFn<P3> {
    double invoke(Bool p1, Bool p2, P3 p3);
}
