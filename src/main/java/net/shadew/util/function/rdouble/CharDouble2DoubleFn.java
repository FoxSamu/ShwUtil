package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char} and a {@code double} and returns a {@code double}.
 */
public interface CharDouble2DoubleFn {
    double invoke(char p1, double p2);
}
