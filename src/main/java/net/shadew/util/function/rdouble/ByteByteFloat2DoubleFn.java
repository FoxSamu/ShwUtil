package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code float} and returns a {@code double}.
 */
public interface ByteByteFloat2DoubleFn {
    double invoke(byte p1, byte p2, float p3);
}
