package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char} and a {@code boolean} and returns a {@code double}.
 */
public interface CharBool2DoubleFn {
    double invoke(char p1, boolean p2);
}
