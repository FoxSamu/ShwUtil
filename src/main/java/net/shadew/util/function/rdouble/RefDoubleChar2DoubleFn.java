package net.shadew.util.function.rdouble;

/**
 * A function that takes an object, a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface RefDoubleChar2DoubleFn<P1> {
    double invoke(P1 p1, double p2, char p3);
}
