package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float} and a {@code short} and returns a {@code double}.
 */
public interface FloatShort2DoubleFn {
    double invoke(float p1, short p2);
}
