package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code short} and an {@code int} and returns a {@code double}.
 */
public interface ShortShortInt2DoubleFn {
    double invoke(short p1, short p2, int p3);
}
