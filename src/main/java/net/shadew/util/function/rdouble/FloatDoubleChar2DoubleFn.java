package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code char} and returns a {@code double}.
 */
public interface FloatDoubleChar2DoubleFn {
    double invoke(float p1, double p2, char p3);
}
