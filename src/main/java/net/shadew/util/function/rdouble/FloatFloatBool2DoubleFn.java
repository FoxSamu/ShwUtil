package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code boolean} and returns a {@code double}.
 */
public interface FloatFloatBool2DoubleFn {
    double invoke(float p1, float p2, boolean p3);
}
