package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code boolean} and returns a {@code double}.
 */
public interface IntByteBool2DoubleFn {
    double invoke(int p1, byte p2, boolean p3);
}
