package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code short} and a {@code char} and returns a {@code double}.
 */
public interface CharShortChar2DoubleFn {
    double invoke(char p1, short p2, char p3);
}
