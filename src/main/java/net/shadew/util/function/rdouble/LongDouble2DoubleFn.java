package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code long} and a {@code double} and returns a {@code double}.
 */
public interface LongDouble2DoubleFn {
    double invoke(long p1, double p2);
}
