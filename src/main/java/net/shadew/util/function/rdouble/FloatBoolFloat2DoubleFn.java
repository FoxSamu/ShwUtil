package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code float} and returns a {@code double}.
 */
public interface FloatBoolFloat2DoubleFn {
    double invoke(float p1, boolean p2, float p3);
}
