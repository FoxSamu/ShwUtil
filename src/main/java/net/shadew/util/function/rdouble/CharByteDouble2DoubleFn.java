package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code double} and returns a {@code double}.
 */
public interface CharByteDouble2DoubleFn {
    double invoke(char p1, byte p2, double p3);
}
