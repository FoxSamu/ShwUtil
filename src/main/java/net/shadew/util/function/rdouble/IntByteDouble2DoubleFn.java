package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code double} and returns a {@code double}.
 */
public interface IntByteDouble2DoubleFn {
    double invoke(int p1, byte p2, double p3);
}
