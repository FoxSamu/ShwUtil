package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns a {@code double}.
 */
public interface ShortCharByte2DoubleFn {
    double invoke(short p1, char p2, byte p3);
}
