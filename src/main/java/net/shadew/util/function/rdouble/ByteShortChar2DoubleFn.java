package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code char} and returns a {@code double}.
 */
public interface ByteShortChar2DoubleFn {
    double invoke(byte p1, short p2, char p3);
}
