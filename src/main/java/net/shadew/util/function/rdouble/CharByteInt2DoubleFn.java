package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code char}, a {@code byte} and an {@code int} and returns a {@code double}.
 */
public interface CharByteInt2DoubleFn {
    double invoke(char p1, byte p2, int p3);
}
