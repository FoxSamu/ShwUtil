package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code double} and returns a {@code double}.
 */
public interface ShortByteDouble2DoubleFn {
    double invoke(short p1, byte p2, double p3);
}
