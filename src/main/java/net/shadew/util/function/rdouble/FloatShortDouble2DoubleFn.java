package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code double} and returns a {@code double}.
 */
public interface FloatShortDouble2DoubleFn {
    double invoke(float p1, short p2, double p3);
}
