package net.shadew.util.function.rdouble;

/**
 * A function that takes an object, a {@code float} and a {@code short} and returns a {@code double}.
 */
public interface RefFloatShort2DoubleFn<P1> {
    double invoke(P1 p1, float p2, short p3);
}
