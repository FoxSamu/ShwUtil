package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte} and a {@code char} and returns a {@code double}.
 */
public interface ByteChar2DoubleFn {
    double invoke(byte p1, char p2);
}
