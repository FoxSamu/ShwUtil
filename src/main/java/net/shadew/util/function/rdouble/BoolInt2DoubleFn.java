package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean} and an {@code int} and returns a {@code double}.
 */
public interface BoolInt2DoubleFn {
    double invoke(boolean p1, int p2);
}
