package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code short} and returns a {@code double}.
 */
public interface FloatBoolShort2DoubleFn {
    double invoke(float p1, boolean p2, short p3);
}
