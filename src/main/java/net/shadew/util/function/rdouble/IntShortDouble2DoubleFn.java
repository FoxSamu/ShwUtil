package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code double} and returns a {@code double}.
 */
public interface IntShortDouble2DoubleFn {
    double invoke(int p1, short p2, double p3);
}
