package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code double} and returns a {@code double}.
 */
public interface ShortBoolDouble2DoubleFn {
    double invoke(short p1, boolean p2, double p3);
}
