package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int} and a {@code byte} and returns a {@code double}.
 */
public interface IntByte2DoubleFn {
    double invoke(int p1, byte p2);
}
