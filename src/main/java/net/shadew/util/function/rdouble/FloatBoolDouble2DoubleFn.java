package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code double} and returns a {@code double}.
 */
public interface FloatBoolDouble2DoubleFn {
    double invoke(float p1, boolean p2, double p3);
}
