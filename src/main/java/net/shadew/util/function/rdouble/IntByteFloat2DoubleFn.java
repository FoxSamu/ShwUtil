package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code float} and returns a {@code double}.
 */
public interface IntByteFloat2DoubleFn {
    double invoke(int p1, byte p2, float p3);
}
