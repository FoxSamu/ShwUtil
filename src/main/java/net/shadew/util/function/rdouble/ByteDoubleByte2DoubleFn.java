package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code byte} and returns a {@code double}.
 */
public interface ByteDoubleByte2DoubleFn {
    double invoke(byte p1, double p2, byte p3);
}
