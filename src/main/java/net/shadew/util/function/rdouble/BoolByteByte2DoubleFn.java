package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code byte} and returns a {@code double}.
 */
public interface BoolByteByte2DoubleFn {
    double invoke(boolean p1, byte p2, byte p3);
}
