package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code boolean} and returns a {@code double}.
 */
public interface ShortCharBool2DoubleFn {
    double invoke(short p1, char p2, boolean p3);
}
