package net.shadew.util.function.rdouble;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code char} and returns a {@code double}.
 */
public interface IntShortChar2DoubleFn {
    double invoke(int p1, short p2, char p3);
}
