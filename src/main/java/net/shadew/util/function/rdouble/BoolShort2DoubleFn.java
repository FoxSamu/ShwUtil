package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns a {@code double}.
 */
public interface BoolShort2DoubleFn {
    double invoke(boolean p1, short p2);
}
