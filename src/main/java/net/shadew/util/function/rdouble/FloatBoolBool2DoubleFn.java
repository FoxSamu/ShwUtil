package net.shadew.util.function.rdouble;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code boolean} and returns a {@code double}.
 */
public interface FloatBoolBool2DoubleFn {
    double invoke(float p1, boolean p2, boolean p3);
}
