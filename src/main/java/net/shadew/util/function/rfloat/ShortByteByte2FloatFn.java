package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code byte} and returns a {@code float}.
 */
public interface ShortByteByte2FloatFn {
    float invoke(short p1, byte p2, byte p3);
}
