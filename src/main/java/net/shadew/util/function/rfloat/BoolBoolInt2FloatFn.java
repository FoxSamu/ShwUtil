package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an {@code int} and returns a {@code float}.
 */
public interface BoolBoolInt2FloatFn {
    float invoke(boolean p1, boolean p2, int p3);
}
