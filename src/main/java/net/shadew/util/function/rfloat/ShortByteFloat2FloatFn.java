package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code float} and returns a {@code float}.
 */
public interface ShortByteFloat2FloatFn {
    float invoke(short p1, byte p2, float p3);
}
