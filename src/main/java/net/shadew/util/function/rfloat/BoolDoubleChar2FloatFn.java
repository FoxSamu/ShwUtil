package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code char} and returns a {@code float}.
 */
public interface BoolDoubleChar2FloatFn {
    float invoke(boolean p1, double p2, char p3);
}
