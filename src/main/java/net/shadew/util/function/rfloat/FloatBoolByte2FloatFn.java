package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code byte} and returns a {@code float}.
 */
public interface FloatBoolByte2FloatFn {
    float invoke(float p1, boolean p2, byte p3);
}
