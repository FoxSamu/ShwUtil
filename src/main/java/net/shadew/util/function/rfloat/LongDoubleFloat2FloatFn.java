package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code float} and returns a {@code float}.
 */
public interface LongDoubleFloat2FloatFn {
    float invoke(long p1, double p2, float p3);
}
