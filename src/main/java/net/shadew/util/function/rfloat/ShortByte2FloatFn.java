package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short} and a {@code byte} and returns a {@code float}.
 */
public interface ShortByte2FloatFn {
    float invoke(short p1, byte p2);
}
