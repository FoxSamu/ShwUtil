package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code char} and returns a {@code float}.
 */
public interface LongFloatChar2FloatFn {
    float invoke(long p1, float p2, char p3);
}
