package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code char} and an {@code int} and returns a {@code float}.
 */
public interface ShortCharInt2FloatFn {
    float invoke(short p1, char p2, int p3);
}
