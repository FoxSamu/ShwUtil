package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code short} and returns a {@code float}.
 */
public interface ByteDoubleShort2FloatFn {
    float invoke(byte p1, double p2, short p3);
}
