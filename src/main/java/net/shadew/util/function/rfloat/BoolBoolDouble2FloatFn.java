package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code double} and returns a {@code float}.
 */
public interface BoolBoolDouble2FloatFn {
    float invoke(boolean p1, boolean p2, double p3);
}
