package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code byte} and returns a {@code float}.
 */
public interface BoolShortByte2FloatFn {
    float invoke(boolean p1, short p2, byte p3);
}
