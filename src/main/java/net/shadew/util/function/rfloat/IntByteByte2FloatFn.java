package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code byte} and returns a {@code float}.
 */
public interface IntByteByte2FloatFn {
    float invoke(int p1, byte p2, byte p3);
}
