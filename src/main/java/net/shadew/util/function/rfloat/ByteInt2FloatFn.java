package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte} and an {@code int} and returns a {@code float}.
 */
public interface ByteInt2FloatFn {
    float invoke(byte p1, int p2);
}
