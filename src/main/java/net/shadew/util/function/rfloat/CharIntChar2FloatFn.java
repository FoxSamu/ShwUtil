package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, an {@code int} and a {@code char} and returns a {@code float}.
 */
public interface CharIntChar2FloatFn {
    float invoke(char p1, int p2, char p3);
}
