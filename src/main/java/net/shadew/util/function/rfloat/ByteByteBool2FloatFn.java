package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns a {@code float}.
 */
public interface ByteByteBool2FloatFn {
    float invoke(byte p1, byte p2, boolean p3);
}
