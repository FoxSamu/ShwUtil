package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code double} and an {@code int} and returns a {@code float}.
 */
public interface LongDoubleInt2FloatFn {
    float invoke(long p1, double p2, int p3);
}
