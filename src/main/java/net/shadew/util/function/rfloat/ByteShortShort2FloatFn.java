package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code short} and returns a {@code float}.
 */
public interface ByteShortShort2FloatFn {
    float invoke(byte p1, short p2, short p3);
}
