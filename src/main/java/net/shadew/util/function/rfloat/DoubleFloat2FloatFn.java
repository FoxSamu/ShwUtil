package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double} and a {@code float} and returns a {@code float}.
 */
public interface DoubleFloat2FloatFn {
    float invoke(double p1, float p2);
}
