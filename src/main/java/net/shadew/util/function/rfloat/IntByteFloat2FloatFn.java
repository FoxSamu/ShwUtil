package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code float} and returns a {@code float}.
 */
public interface IntByteFloat2FloatFn {
    float invoke(int p1, byte p2, float p3);
}
