package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code byte} and returns a {@code float}.
 */
public interface ByteDoubleByte2FloatFn {
    float invoke(byte p1, double p2, byte p3);
}
