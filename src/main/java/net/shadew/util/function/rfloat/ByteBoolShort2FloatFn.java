package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code short} and returns a {@code float}.
 */
public interface ByteBoolShort2FloatFn {
    float invoke(byte p1, boolean p2, short p3);
}
