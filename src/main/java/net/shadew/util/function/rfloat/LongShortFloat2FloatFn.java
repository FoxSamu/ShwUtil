package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code float} and returns a {@code float}.
 */
public interface LongShortFloat2FloatFn {
    float invoke(long p1, short p2, float p3);
}
