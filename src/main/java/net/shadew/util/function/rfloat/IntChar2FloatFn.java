package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int} and a {@code char} and returns a {@code float}.
 */
public interface IntChar2FloatFn {
    float invoke(int p1, char p2);
}
