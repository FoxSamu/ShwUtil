package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long} and a {@code boolean} and returns a {@code float}.
 */
public interface LongBool2FloatFn {
    float invoke(long p1, boolean p2);
}
