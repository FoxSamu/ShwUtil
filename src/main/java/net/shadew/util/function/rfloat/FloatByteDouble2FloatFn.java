package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns a {@code float}.
 */
public interface FloatByteDouble2FloatFn {
    float invoke(float p1, byte p2, double p3);
}
