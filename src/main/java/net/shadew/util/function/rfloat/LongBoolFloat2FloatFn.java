package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code float} and returns a {@code float}.
 */
public interface LongBoolFloat2FloatFn {
    float invoke(long p1, boolean p2, float p3);
}
