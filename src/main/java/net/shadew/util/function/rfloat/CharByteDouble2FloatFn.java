package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code double} and returns a {@code float}.
 */
public interface CharByteDouble2FloatFn {
    float invoke(char p1, byte p2, double p3);
}
