package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code double} and returns a {@code float}.
 */
public interface ByteFloatDouble2FloatFn {
    float invoke(byte p1, float p2, double p3);
}
