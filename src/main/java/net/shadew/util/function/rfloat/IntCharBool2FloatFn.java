package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code boolean} and returns a {@code float}.
 */
public interface IntCharBool2FloatFn {
    float invoke(int p1, char p2, boolean p3);
}
