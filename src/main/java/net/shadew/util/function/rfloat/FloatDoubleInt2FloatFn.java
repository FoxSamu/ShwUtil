package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code double} and an {@code int} and returns a {@code float}.
 */
public interface FloatDoubleInt2FloatFn {
    float invoke(float p1, double p2, int p3);
}
