package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code float} and returns a {@code float}.
 */
public interface FloatIntFloat2FloatFn {
    float invoke(float p1, int p2, float p3);
}
