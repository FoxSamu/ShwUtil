package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code long} and a {@code float} and returns a {@code float}.
 */
public interface IntLongFloat2FloatFn {
    float invoke(Int p1, Long p2, Float p3);
}
