package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code double} and returns a {@code float}.
 */
public interface BoolShortDouble2FloatFn {
    float invoke(boolean p1, short p2, double p3);
}
