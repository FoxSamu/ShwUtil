package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean} and a {@code float} and returns a {@code float}.
 */
public interface BoolFloat2FloatFn {
    float invoke(boolean p1, float p2);
}
