package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code byte} and returns a {@code float}.
 */
public interface CharByteByte2FloatFn {
    float invoke(char p1, byte p2, byte p3);
}
