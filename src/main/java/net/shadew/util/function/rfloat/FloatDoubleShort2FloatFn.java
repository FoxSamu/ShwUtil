package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code short} and returns a {@code float}.
 */
public interface FloatDoubleShort2FloatFn {
    float invoke(float p1, double p2, short p3);
}
