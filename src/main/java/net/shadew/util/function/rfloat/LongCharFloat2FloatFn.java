package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code float} and returns a {@code float}.
 */
public interface LongCharFloat2FloatFn {
    float invoke(long p1, char p2, float p3);
}
