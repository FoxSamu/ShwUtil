package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int} and a {@code double} and returns a {@code float}.
 */
public interface IntDouble2FloatFn {
    float invoke(int p1, double p2);
}
