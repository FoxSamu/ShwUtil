package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float} and a {@code short} and returns a {@code float}.
 */
public interface FloatShort2FloatFn {
    float invoke(float p1, short p2);
}
