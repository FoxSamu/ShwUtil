package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte} and a {@code double} and returns a {@code float}.
 */
public interface ByteDouble2FloatFn {
    float invoke(byte p1, double p2);
}
