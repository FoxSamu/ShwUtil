package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns a {@code float}.
 */
public interface ByteByteDouble2FloatFn {
    float invoke(byte p1, byte p2, double p3);
}
