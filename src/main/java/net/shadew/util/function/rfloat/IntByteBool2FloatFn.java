package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code boolean} and returns a {@code float}.
 */
public interface IntByteBool2FloatFn {
    float invoke(int p1, byte p2, boolean p3);
}
