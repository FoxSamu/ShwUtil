package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code boolean} and returns a {@code float}.
 */
public interface BoolCharBool2FloatFn {
    float invoke(boolean p1, char p2, boolean p3);
}
