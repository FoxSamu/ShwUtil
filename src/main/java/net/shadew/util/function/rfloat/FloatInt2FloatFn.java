package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float} and an {@code int} and returns a {@code float}.
 */
public interface FloatInt2FloatFn {
    float invoke(float p1, int p2);
}
