package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code byte} and an {@code int} and returns a {@code float}.
 */
public interface CharByteInt2FloatFn {
    float invoke(char p1, byte p2, int p3);
}
