package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code byte} and returns a {@code float}.
 */
public interface IntCharByte2FloatFn {
    float invoke(int p1, char p2, byte p3);
}
