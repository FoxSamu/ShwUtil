package net.shadew.util.function.rfloat;

/**
 * A function that takes an object and a {@code short} and returns a {@code float}.
 */
public interface RefShort2FloatFn<P1> {
    float invoke(P1 p1, short p2);
}
