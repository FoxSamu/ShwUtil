package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code float} and returns a {@code float}.
 */
public interface ShortCharFloat2FloatFn {
    float invoke(short p1, char p2, float p3);
}
