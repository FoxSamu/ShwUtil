package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code short} and returns a {@code float}.
 */
public interface FloatBoolShort2FloatFn {
    float invoke(float p1, boolean p2, short p3);
}
