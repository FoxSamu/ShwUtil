package net.shadew.util.function.rfloat;

/**
 * A function that takes an object and a {@code byte} and returns a {@code float}.
 */
public interface RefByte2FloatFn<P1> {
    float invoke(P1 p1, byte p2);
}
