package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code char} and returns a {@code float}.
 */
public interface CharDoubleChar2FloatFn {
    float invoke(char p1, double p2, char p3);
}
