package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code float} and returns a {@code float}.
 */
public interface LongByteFloat2FloatFn {
    float invoke(long p1, byte p2, float p3);
}
