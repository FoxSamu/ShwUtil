package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code double} and returns a {@code float}.
 */
public interface ShortByteDouble2FloatFn {
    float invoke(short p1, byte p2, double p3);
}
