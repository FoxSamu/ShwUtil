package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code byte} and returns a {@code float}.
 */
public interface ByteFloatByte2FloatFn {
    float invoke(byte p1, float p2, byte p3);
}
