package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code char} and an {@code int} and returns a {@code float}.
 */
public interface FloatCharInt2FloatFn {
    float invoke(float p1, char p2, int p3);
}
