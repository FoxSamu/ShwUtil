package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns a {@code float}.
 */
public interface BoolShort2FloatFn {
    float invoke(boolean p1, short p2);
}
