package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code double} and returns a {@code float}.
 */
public interface BoolCharDouble2FloatFn {
    float invoke(boolean p1, char p2, double p3);
}
