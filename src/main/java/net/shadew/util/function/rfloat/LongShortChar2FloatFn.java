package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code char} and returns a {@code float}.
 */
public interface LongShortChar2FloatFn {
    float invoke(long p1, short p2, char p3);
}
