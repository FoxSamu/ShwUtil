package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float} and a {@code boolean} and returns a {@code float}.
 */
public interface FloatBool2FloatFn {
    float invoke(float p1, boolean p2);
}
