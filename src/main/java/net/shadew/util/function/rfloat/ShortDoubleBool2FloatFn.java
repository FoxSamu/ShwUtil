package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code boolean} and returns a {@code float}.
 */
public interface ShortDoubleBool2FloatFn {
    float invoke(short p1, double p2, boolean p3);
}
