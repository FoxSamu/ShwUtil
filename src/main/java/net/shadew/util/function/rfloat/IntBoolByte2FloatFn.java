package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code byte} and returns a {@code float}.
 */
public interface IntBoolByte2FloatFn {
    float invoke(int p1, boolean p2, byte p3);
}
