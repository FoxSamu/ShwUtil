package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, an {@code int} and a {@code float} and returns a {@code float}.
 */
public interface ByteIntFloat2FloatFn {
    float invoke(byte p1, int p2, float p3);
}
