package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code char} and returns a {@code float}.
 */
public interface FloatFloatChar2FloatFn {
    float invoke(float p1, float p2, char p3);
}
