package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float} and a {@code double} and returns a {@code float}.
 */
public interface FloatDouble2FloatFn {
    float invoke(float p1, double p2);
}
