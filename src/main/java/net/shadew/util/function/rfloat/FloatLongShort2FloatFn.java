package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code short} and returns a {@code float}.
 */
public interface FloatLongShort2FloatFn {
    float invoke(float p1, long p2, short p3);
}
