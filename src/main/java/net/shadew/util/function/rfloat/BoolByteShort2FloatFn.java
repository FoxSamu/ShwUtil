package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code short} and returns a {@code float}.
 */
public interface BoolByteShort2FloatFn {
    float invoke(boolean p1, byte p2, short p3);
}
