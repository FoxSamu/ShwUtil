package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an {@code int} and returns a {@code float}.
 */
public interface ByteBoolInt2FloatFn {
    float invoke(byte p1, boolean p2, int p3);
}
