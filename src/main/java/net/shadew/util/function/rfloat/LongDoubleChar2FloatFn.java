package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code char} and returns a {@code float}.
 */
public interface LongDoubleChar2FloatFn {
    float invoke(long p1, double p2, char p3);
}
