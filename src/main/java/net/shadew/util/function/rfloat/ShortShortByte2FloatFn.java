package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code byte} and returns a {@code float}.
 */
public interface ShortShortByte2FloatFn {
    float invoke(short p1, short p2, byte p3);
}
