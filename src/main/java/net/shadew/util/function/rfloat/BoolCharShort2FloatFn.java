package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code short} and returns a {@code float}.
 */
public interface BoolCharShort2FloatFn {
    float invoke(boolean p1, char p2, short p3);
}
