package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte} and a {@code short} and returns a {@code float}.
 */
public interface ByteShort2FloatFn {
    float invoke(byte p1, short p2);
}
