package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short} and a {@code long} and returns a {@code float}.
 */
public interface ShortLong2FloatFn {
    float invoke(short p1, long p2);
}
