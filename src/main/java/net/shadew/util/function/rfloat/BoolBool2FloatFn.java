package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns a {@code float}.
 */
public interface BoolBool2FloatFn {
    float invoke(boolean p1, boolean p2);
}
