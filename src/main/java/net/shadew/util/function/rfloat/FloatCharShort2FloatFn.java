package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code short} and returns a {@code float}.
 */
public interface FloatCharShort2FloatFn {
    float invoke(float p1, char p2, short p3);
}
