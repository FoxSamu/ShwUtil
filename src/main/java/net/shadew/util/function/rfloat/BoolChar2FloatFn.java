package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns a {@code float}.
 */
public interface BoolChar2FloatFn {
    float invoke(boolean p1, char p2);
}
