package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code byte} and returns a {@code float}.
 */
public interface DoubleByteByte2FloatFn {
    float invoke(double p1, byte p2, byte p3);
}
