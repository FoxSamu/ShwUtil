package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long} and an {@code int} and returns a {@code float}.
 */
public interface LongInt2FloatFn {
    float invoke(long p1, int p2);
}
