package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code double} and returns a {@code float}.
 */
public interface BoolDoubleDouble2FloatFn {
    float invoke(boolean p1, double p2, double p3);
}
