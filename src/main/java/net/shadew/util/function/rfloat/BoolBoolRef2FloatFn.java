package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an object and returns a {@code float}.
 */
public interface BoolBoolRef2FloatFn<P3> {
    float invoke(boolean p1, boolean p2, P3 p3);
}
