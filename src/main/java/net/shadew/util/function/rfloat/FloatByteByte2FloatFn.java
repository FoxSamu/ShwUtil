package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code byte} and returns a {@code float}.
 */
public interface FloatByteByte2FloatFn {
    float invoke(float p1, byte p2, byte p3);
}
