package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code long} and a {@code short} and returns a {@code float}.
 */
public interface ByteLongShort2FloatFn {
    float invoke(byte p1, long p2, short p3);
}
