package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code double} and returns a {@code float}.
 */
public interface FloatShortDouble2FloatFn {
    float invoke(float p1, short p2, double p3);
}
