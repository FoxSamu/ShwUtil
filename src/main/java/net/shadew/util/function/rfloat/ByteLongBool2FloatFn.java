package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code long} and a {@code boolean} and returns a {@code float}.
 */
public interface ByteLongBool2FloatFn {
    float invoke(byte p1, long p2, boolean p3);
}
