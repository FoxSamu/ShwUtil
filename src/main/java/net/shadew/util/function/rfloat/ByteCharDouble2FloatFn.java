package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code double} and returns a {@code float}.
 */
public interface ByteCharDouble2FloatFn {
    float invoke(byte p1, char p2, double p3);
}
