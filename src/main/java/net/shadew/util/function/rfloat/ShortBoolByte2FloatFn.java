package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code byte} and returns a {@code float}.
 */
public interface ShortBoolByte2FloatFn {
    float invoke(short p1, boolean p2, byte p3);
}
