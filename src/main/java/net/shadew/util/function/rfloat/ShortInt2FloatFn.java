package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short} and an {@code int} and returns a {@code float}.
 */
public interface ShortInt2FloatFn {
    float invoke(Short p1, Int p2);
}
