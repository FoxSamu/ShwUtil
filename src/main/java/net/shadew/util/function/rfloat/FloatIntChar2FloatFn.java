package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code char} and returns a {@code float}.
 */
public interface FloatIntChar2FloatFn {
    float invoke(float p1, int p2, char p3);
}
