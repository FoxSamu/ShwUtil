package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float} and a {@code byte} and returns a {@code float}.
 */
public interface FloatByte2FloatFn {
    float invoke(float p1, byte p2);
}
