package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int} and a {@code short} and returns a {@code float}.
 */
public interface IntShort2FloatFn {
    float invoke(int p1, short p2);
}
