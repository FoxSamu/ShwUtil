package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short} and a {@code char} and returns a {@code float}.
 */
public interface ShortChar2FloatFn {
    float invoke(short p1, char p2);
}
