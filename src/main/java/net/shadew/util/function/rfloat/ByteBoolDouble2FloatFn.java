package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code double} and returns a {@code float}.
 */
public interface ByteBoolDouble2FloatFn {
    float invoke(byte p1, boolean p2, double p3);
}
