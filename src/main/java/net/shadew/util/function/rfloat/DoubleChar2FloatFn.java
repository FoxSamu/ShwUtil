package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double} and a {@code char} and returns a {@code float}.
 */
public interface DoubleChar2FloatFn {
    float invoke(Double p1, Char p2);
}
