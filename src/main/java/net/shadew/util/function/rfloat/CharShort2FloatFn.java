package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char} and a {@code short} and returns a {@code float}.
 */
public interface CharShort2FloatFn {
    float invoke(char p1, short p2);
}
