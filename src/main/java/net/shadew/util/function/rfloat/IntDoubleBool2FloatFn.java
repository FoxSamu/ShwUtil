package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code boolean} and returns a {@code float}.
 */
public interface IntDoubleBool2FloatFn {
    float invoke(int p1, double p2, boolean p3);
}
