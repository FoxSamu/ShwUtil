package net.shadew.util.function.rfloat;

/**
 * A function that takes an object, an {@code int} and a {@code float} and returns a {@code float}.
 */
public interface RefIntFloat2FloatFn<P1> {
    float invoke(P1 p1, Int p2, Float p3);
}
