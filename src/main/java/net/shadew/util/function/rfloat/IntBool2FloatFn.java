package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int} and a {@code boolean} and returns a {@code float}.
 */
public interface IntBool2FloatFn {
    float invoke(int p1, boolean p2);
}
