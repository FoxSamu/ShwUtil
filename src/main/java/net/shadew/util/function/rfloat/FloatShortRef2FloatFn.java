package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code short} and an object and returns a {@code float}.
 */
public interface FloatShortRef2FloatFn<P3> {
    float invoke(float p1, short p2, P3 p3);
}
