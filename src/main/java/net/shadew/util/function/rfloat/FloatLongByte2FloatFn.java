package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code byte} and returns a {@code float}.
 */
public interface FloatLongByte2FloatFn {
    float invoke(float p1, long p2, byte p3);
}
