package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code double} and returns a {@code float}.
 */
public interface IntByteDouble2FloatFn {
    float invoke(int p1, byte p2, double p3);
}
