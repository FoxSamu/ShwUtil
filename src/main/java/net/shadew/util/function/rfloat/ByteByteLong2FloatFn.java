package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code long} and returns a {@code float}.
 */
public interface ByteByteLong2FloatFn {
    float invoke(byte p1, byte p2, long p3);
}
