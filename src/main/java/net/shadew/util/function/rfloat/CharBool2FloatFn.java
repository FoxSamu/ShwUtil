package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char} and a {@code boolean} and returns a {@code float}.
 */
public interface CharBool2FloatFn {
    float invoke(char p1, boolean p2);
}
