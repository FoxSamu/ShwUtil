package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double} and a {@code short} and returns a {@code float}.
 */
public interface DoubleShort2FloatFn {
    float invoke(double p1, short p2);
}
