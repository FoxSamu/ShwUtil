package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code short} and returns a {@code float}.
 */
public interface FloatByteShort2FloatFn {
    float invoke(float p1, byte p2, short p3);
}
