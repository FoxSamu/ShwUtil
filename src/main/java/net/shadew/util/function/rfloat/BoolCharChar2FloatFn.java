package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code char} and returns a {@code float}.
 */
public interface BoolCharChar2FloatFn {
    float invoke(boolean p1, char p2, char p3);
}
