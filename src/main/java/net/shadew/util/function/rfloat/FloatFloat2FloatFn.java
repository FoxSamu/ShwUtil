package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float} and a {@code float} and returns a {@code float}.
 */
public interface FloatFloat2FloatFn {
    float invoke(float p1, float p2);
}
