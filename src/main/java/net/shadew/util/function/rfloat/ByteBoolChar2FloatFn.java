package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns a {@code float}.
 */
public interface ByteBoolChar2FloatFn {
    float invoke(byte p1, boolean p2, char p3);
}
