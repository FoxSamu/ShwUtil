package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code boolean} and returns a {@code float}.
 */
public interface ShortCharBool2FloatFn {
    float invoke(short p1, char p2, boolean p3);
}
