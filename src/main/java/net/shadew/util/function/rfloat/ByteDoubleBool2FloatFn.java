package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code boolean} and returns a {@code float}.
 */
public interface ByteDoubleBool2FloatFn {
    float invoke(byte p1, double p2, boolean p3);
}
