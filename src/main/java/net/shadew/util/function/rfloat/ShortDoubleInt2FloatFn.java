package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns a {@code float}.
 */
public interface ShortDoubleInt2FloatFn {
    float invoke(short p1, double p2, int p3);
}
