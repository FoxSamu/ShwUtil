package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code boolean} and returns a {@code float}.
 */
public interface FloatBoolBool2FloatFn {
    float invoke(float p1, boolean p2, boolean p3);
}
