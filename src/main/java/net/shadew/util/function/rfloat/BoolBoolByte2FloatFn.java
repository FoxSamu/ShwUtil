package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code byte} and returns a {@code float}.
 */
public interface BoolBoolByte2FloatFn {
    float invoke(boolean p1, boolean p2, byte p3);
}
