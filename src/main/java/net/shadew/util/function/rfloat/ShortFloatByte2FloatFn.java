package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code byte} and returns a {@code float}.
 */
public interface ShortFloatByte2FloatFn {
    float invoke(short p1, float p2, byte p3);
}
