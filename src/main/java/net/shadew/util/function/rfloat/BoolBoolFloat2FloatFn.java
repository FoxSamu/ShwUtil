package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code float} and returns a {@code float}.
 */
public interface BoolBoolFloat2FloatFn {
    float invoke(boolean p1, boolean p2, float p3);
}
