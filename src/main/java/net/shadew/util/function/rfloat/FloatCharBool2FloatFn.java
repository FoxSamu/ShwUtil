package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code boolean} and returns a {@code float}.
 */
public interface FloatCharBool2FloatFn {
    float invoke(float p1, char p2, boolean p3);
}
