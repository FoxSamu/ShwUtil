package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code byte} and returns a {@code float}.
 */
public interface LongDoubleByte2FloatFn {
    float invoke(long p1, double p2, byte p3);
}
