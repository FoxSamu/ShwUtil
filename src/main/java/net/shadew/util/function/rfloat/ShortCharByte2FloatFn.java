package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns a {@code float}.
 */
public interface ShortCharByte2FloatFn {
    float invoke(short p1, char p2, byte p3);
}
