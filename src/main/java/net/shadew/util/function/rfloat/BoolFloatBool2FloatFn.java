package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code boolean} and returns a {@code float}.
 */
public interface BoolFloatBool2FloatFn {
    float invoke(boolean p1, float p2, boolean p3);
}
