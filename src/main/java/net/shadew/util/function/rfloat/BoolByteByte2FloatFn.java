package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code byte} and returns a {@code float}.
 */
public interface BoolByteByte2FloatFn {
    float invoke(boolean p1, byte p2, byte p3);
}
