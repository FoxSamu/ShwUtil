package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code byte} and returns a {@code float}.
 */
public interface BoolDoubleByte2FloatFn {
    float invoke(boolean p1, double p2, byte p3);
}
