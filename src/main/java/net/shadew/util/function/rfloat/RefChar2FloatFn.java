package net.shadew.util.function.rfloat;

/**
 * A function that takes an object and a {@code char} and returns a {@code float}.
 */
public interface RefChar2FloatFn<P1> {
    float invoke(P1 p1, char p2);
}
