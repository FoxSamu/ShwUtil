package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte} and a {@code long} and returns a {@code float}.
 */
public interface ByteLong2FloatFn {
    float invoke(byte p1, long p2);
}
