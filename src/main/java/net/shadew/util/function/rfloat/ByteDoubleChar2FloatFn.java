package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code char} and returns a {@code float}.
 */
public interface ByteDoubleChar2FloatFn {
    float invoke(byte p1, double p2, char p3);
}
