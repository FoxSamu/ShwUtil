package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code double} and returns a {@code float}.
 */
public interface FloatBoolDouble2FloatFn {
    float invoke(Float p1, Bool p2, Double p3);
}
