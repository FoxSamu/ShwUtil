package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code char} and returns a {@code float}.
 */
public interface IntBoolChar2FloatFn {
    float invoke(int p1, boolean p2, char p3);
}
