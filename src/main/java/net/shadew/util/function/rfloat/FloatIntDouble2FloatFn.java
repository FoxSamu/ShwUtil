package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code double} and returns a {@code float}.
 */
public interface FloatIntDouble2FloatFn {
    float invoke(float p1, int p2, double p3);
}
