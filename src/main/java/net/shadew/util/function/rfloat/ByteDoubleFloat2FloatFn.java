package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code float} and returns a {@code float}.
 */
public interface ByteDoubleFloat2FloatFn {
    float invoke(byte p1, double p2, float p3);
}
