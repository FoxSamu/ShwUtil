package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code float} and returns a {@code float}.
 */
public interface BoolByteFloat2FloatFn {
    float invoke(boolean p1, byte p2, float p3);
}
