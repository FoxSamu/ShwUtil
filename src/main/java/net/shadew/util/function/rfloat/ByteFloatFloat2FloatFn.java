package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code float} and returns a {@code float}.
 */
public interface ByteFloatFloat2FloatFn {
    float invoke(byte p1, float p2, float p3);
}
