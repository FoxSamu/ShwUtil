package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code double} and returns a {@code float}.
 */
public interface ByteDoubleDouble2FloatFn {
    float invoke(byte p1, double p2, double p3);
}
