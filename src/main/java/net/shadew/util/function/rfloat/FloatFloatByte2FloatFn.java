package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code byte} and returns a {@code float}.
 */
public interface FloatFloatByte2FloatFn {
    float invoke(float p1, float p2, byte p3);
}
