package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long} and a {@code float} and returns a {@code float}.
 */
public interface LongFloat2FloatFn {
    float invoke(long p1, float p2);
}
