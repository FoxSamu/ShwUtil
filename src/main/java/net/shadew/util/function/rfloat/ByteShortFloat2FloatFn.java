package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code float} and returns a {@code float}.
 */
public interface ByteShortFloat2FloatFn {
    float invoke(byte p1, short p2, float p3);
}
