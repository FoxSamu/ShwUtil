package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code char} and returns a {@code float}.
 */
public interface ShortIntChar2FloatFn {
    float invoke(short p1, int p2, char p3);
}
