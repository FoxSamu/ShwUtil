package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code float} and an object and returns a {@code float}.
 */
public interface ByteFloatRef2FloatFn<P3> {
    float invoke(byte p1, float p2, P3 p3);
}
