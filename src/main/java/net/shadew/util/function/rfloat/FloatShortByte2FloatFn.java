package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code byte} and returns a {@code float}.
 */
public interface FloatShortByte2FloatFn {
    float invoke(float p1, short p2, byte p3);
}
