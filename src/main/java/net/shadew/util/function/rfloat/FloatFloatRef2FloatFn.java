package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code float} and an object and returns a {@code float}.
 */
public interface FloatFloatRef2FloatFn<P3> {
    float invoke(float p1, float p2, P3 p3);
}
