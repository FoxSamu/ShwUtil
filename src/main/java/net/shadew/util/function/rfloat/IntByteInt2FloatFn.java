package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns a {@code float}.
 */
public interface IntByteInt2FloatFn {
    float invoke(int p1, byte p2, int p3);
}
