package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code byte} and returns a {@code float}.
 */
public interface FloatDoubleByte2FloatFn {
    float invoke(float p1, double p2, byte p3);
}
