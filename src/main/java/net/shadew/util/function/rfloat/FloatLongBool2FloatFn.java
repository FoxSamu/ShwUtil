package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code boolean} and returns a {@code float}.
 */
public interface FloatLongBool2FloatFn {
    float invoke(Float p1, Long p2, Bool p3);
}
