package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, an {@code int} and a {@code byte} and returns a {@code float}.
 */
public interface CharIntByte2FloatFn {
    float invoke(char p1, int p2, byte p3);
}
