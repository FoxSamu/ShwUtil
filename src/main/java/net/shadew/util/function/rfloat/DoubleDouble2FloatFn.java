package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double} and a {@code double} and returns a {@code float}.
 */
public interface DoubleDouble2FloatFn {
    float invoke(double p1, double p2);
}
