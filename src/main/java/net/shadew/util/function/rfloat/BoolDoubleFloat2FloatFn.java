package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code float} and returns a {@code float}.
 */
public interface BoolDoubleFloat2FloatFn {
    float invoke(boolean p1, double p2, float p3);
}
