package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code byte} and returns a {@code float}.
 */
public interface IntFloatByte2FloatFn {
    float invoke(int p1, float p2, byte p3);
}
