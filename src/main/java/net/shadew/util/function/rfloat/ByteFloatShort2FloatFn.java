package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code short} and returns a {@code float}.
 */
public interface ByteFloatShort2FloatFn {
    float invoke(byte p1, float p2, short p3);
}
