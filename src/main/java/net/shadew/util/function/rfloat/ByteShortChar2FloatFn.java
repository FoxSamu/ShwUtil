package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code char} and returns a {@code float}.
 */
public interface ByteShortChar2FloatFn {
    float invoke(byte p1, short p2, char p3);
}
