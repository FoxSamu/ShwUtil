package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code float} and returns a {@code float}.
 */
public interface ByteCharFloat2FloatFn {
    float invoke(byte p1, char p2, float p3);
}
