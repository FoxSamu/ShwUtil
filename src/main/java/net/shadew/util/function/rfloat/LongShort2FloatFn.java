package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long} and a {@code short} and returns a {@code float}.
 */
public interface LongShort2FloatFn {
    float invoke(long p1, short p2);
}
