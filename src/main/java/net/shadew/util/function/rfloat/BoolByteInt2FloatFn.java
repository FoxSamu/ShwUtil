package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an {@code int} and returns a {@code float}.
 */
public interface BoolByteInt2FloatFn {
    float invoke(boolean p1, byte p2, int p3);
}
