package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code double} and returns a {@code float}.
 */
public interface ShortBoolDouble2FloatFn {
    float invoke(short p1, boolean p2, double p3);
}
