package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code double} and returns a {@code float}.
 */
public interface DoubleByteDouble2FloatFn {
    float invoke(double p1, byte p2, double p3);
}
