package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code short} and returns a {@code float}.
 */
public interface FloatShortShort2FloatFn {
    float invoke(float p1, short p2, short p3);
}
