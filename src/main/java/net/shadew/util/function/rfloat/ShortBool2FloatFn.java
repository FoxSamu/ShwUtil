package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short} and a {@code boolean} and returns a {@code float}.
 */
public interface ShortBool2FloatFn {
    float invoke(short p1, boolean p2);
}
