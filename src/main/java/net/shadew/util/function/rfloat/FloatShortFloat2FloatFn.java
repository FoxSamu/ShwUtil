package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code float} and returns a {@code float}.
 */
public interface FloatShortFloat2FloatFn {
    float invoke(float p1, short p2, float p3);
}
