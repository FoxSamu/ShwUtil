package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long} and a {@code byte} and returns a {@code float}.
 */
public interface LongByte2FloatFn {
    float invoke(long p1, byte p2);
}
