package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code double} and returns a {@code float}.
 */
public interface IntCharDouble2FloatFn {
    float invoke(int p1, char p2, double p3);
}
