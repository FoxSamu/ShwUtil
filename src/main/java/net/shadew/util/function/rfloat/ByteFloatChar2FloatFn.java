package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code char} and returns a {@code float}.
 */
public interface ByteFloatChar2FloatFn {
    float invoke(byte p1, float p2, char p3);
}
