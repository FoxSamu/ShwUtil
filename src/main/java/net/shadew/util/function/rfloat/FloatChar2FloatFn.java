package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float} and a {@code char} and returns a {@code float}.
 */
public interface FloatChar2FloatFn {
    float invoke(float p1, char p2);
}
