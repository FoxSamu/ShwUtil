package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code char} and returns a {@code float}.
 */
public interface ShortDoubleChar2FloatFn {
    float invoke(short p1, double p2, char p3);
}
