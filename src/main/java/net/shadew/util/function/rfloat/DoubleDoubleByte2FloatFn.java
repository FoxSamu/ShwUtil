package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double}, a {@code double} and a {@code byte} and returns a {@code float}.
 */
public interface DoubleDoubleByte2FloatFn {
    float invoke(double p1, double p2, byte p3);
}
