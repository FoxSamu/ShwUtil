package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns a {@code float}.
 */
public interface IntDoubleByte2FloatFn {
    float invoke(int p1, double p2, byte p3);
}
