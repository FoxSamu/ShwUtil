package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code short} and returns a {@code float}.
 */
public interface ByteByteShort2FloatFn {
    float invoke(byte p1, byte p2, short p3);
}
