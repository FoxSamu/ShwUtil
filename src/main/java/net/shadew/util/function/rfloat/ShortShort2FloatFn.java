package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short} and a {@code short} and returns a {@code float}.
 */
public interface ShortShort2FloatFn {
    float invoke(short p1, short p2);
}
