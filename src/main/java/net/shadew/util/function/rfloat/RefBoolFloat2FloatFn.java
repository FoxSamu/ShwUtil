package net.shadew.util.function.rfloat;

/**
 * A function that takes an object, a {@code boolean} and a {@code float} and returns a {@code float}.
 */
public interface RefBoolFloat2FloatFn<P1> {
    float invoke(P1 p1, boolean p2, float p3);
}
