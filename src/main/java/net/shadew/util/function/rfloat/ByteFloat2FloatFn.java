package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte} and a {@code float} and returns a {@code float}.
 */
public interface ByteFloat2FloatFn {
    float invoke(byte p1, float p2);
}
