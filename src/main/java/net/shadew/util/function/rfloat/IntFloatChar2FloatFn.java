package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code char} and returns a {@code float}.
 */
public interface IntFloatChar2FloatFn {
    float invoke(int p1, float p2, char p3);
}
