package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns a {@code float}.
 */
public interface ByteBool2FloatFn {
    float invoke(byte p1, boolean p2);
}
