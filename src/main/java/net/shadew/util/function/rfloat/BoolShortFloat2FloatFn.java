package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code float} and returns a {@code float}.
 */
public interface BoolShortFloat2FloatFn {
    float invoke(boolean p1, short p2, float p3);
}
