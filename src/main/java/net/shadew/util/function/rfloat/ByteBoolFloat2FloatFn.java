package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code float} and returns a {@code float}.
 */
public interface ByteBoolFloat2FloatFn {
    float invoke(byte p1, boolean p2, float p3);
}
