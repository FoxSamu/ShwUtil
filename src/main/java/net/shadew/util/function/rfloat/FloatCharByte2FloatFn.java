package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code byte} and returns a {@code float}.
 */
public interface FloatCharByte2FloatFn {
    float invoke(float p1, char p2, byte p3);
}
