package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code short} and an {@code int} and returns a {@code float}.
 */
public interface ByteShortInt2FloatFn {
    float invoke(byte p1, short p2, int p3);
}
