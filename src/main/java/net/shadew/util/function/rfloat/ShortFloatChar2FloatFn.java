package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code char} and returns a {@code float}.
 */
public interface ShortFloatChar2FloatFn {
    float invoke(short p1, float p2, char p3);
}
