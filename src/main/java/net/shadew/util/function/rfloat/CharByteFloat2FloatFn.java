package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code float} and returns a {@code float}.
 */
public interface CharByteFloat2FloatFn {
    float invoke(Char p1, Byte p2, Float p3);
}
