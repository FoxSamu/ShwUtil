package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code float} and returns a {@code float}.
 */
public interface FloatByteFloat2FloatFn {
    float invoke(float p1, byte p2, float p3);
}
