package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char} and a {@code double} and returns a {@code float}.
 */
public interface CharDouble2FloatFn {
    float invoke(char p1, double p2);
}
