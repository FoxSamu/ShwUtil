package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code char} and returns a {@code float}.
 */
public interface ByteByteChar2FloatFn {
    float invoke(byte p1, byte p2, char p3);
}
