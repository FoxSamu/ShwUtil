package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code float} and an {@code int} and returns a {@code float}.
 */
public interface ByteFloatInt2FloatFn {
    float invoke(byte p1, float p2, int p3);
}
