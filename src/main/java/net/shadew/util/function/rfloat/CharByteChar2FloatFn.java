package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns a {@code float}.
 */
public interface CharByteChar2FloatFn {
    float invoke(char p1, byte p2, char p3);
}
