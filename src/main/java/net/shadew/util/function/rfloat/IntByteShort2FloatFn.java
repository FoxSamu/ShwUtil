package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code short} and returns a {@code float}.
 */
public interface IntByteShort2FloatFn {
    float invoke(int p1, byte p2, short p3);
}
