package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code short} and returns a {@code float}.
 */
public interface CharDoubleShort2FloatFn {
    float invoke(char p1, double p2, short p3);
}
