package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code float} and returns a {@code float}.
 */
public interface ShortShortFloat2FloatFn {
    float invoke(short p1, short p2, float p3);
}
