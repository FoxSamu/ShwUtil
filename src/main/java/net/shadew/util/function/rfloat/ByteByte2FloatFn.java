package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte} and a {@code byte} and returns a {@code float}.
 */
public interface ByteByte2FloatFn {
    float invoke(byte p1, byte p2);
}
