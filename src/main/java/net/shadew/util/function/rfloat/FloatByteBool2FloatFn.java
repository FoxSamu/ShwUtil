package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns a {@code float}.
 */
public interface FloatByteBool2FloatFn {
    float invoke(Float p1, Byte p2, Bool p3);
}
