package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code double} and returns a {@code float}.
 */
public interface ShortShortDouble2FloatFn {
    float invoke(short p1, short p2, double p3);
}
