package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code char} and returns a {@code float}.
 */
public interface IntDoubleChar2FloatFn {
    float invoke(int p1, double p2, char p3);
}
