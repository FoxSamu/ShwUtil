package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code float} and returns a {@code float}.
 */
public interface FloatBoolFloat2FloatFn {
    float invoke(float p1, boolean p2, float p3);
}
