package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code double} and returns a {@code float}.
 */
public interface CharDoubleDouble2FloatFn {
    float invoke(char p1, double p2, double p3);
}
