package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code byte} and returns a {@code float}.
 */
public interface LongCharByte2FloatFn {
    float invoke(long p1, char p2, byte p3);
}
