package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char} and a {@code float} and returns a {@code float}.
 */
public interface CharFloat2FloatFn {
    float invoke(char p1, float p2);
}
