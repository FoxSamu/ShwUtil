package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code char} and returns a {@code float}.
 */
public interface LongBoolChar2FloatFn {
    float invoke(long p1, boolean p2, char p3);
}
