package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code float} and returns a {@code float}.
 */
public interface IntBoolFloat2FloatFn {
    float invoke(int p1, boolean p2, float p3);
}
