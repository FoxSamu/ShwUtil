package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code boolean} and returns a {@code float}.
 */
public interface LongByteBool2FloatFn {
    float invoke(long p1, byte p2, boolean p3);
}
