package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code byte} and returns a {@code float}.
 */
public interface FloatIntByte2FloatFn {
    float invoke(float p1, int p2, byte p3);
}
