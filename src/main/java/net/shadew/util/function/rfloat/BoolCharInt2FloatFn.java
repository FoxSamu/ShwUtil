package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code char} and an {@code int} and returns a {@code float}.
 */
public interface BoolCharInt2FloatFn {
    float invoke(boolean p1, char p2, int p3);
}
