package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code byte} and an {@code int} and returns a {@code float}.
 */
public interface ShortByteInt2FloatFn {
    float invoke(short p1, byte p2, int p3);
}
