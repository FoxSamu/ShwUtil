package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code char} and a {@code double} and returns a {@code float}.
 */
public interface CharCharDouble2FloatFn {
    float invoke(char p1, char p2, double p3);
}
