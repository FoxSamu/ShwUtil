package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte} and a {@code char} and returns a {@code float}.
 */
public interface ByteChar2FloatFn {
    float invoke(byte p1, char p2);
}
