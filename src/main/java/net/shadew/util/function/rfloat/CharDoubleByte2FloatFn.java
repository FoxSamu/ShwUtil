package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code byte} and returns a {@code float}.
 */
public interface CharDoubleByte2FloatFn {
    float invoke(char p1, double p2, byte p3);
}
