package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long} and a {@code double} and returns a {@code float}.
 */
public interface LongDouble2FloatFn {
    float invoke(long p1, double p2);
}
