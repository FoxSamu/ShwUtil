package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean} and an {@code int} and returns a {@code float}.
 */
public interface BoolInt2FloatFn {
    float invoke(boolean p1, int p2);
}
