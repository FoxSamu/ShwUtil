package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code char} and an object and returns a {@code float}.
 */
public interface FloatCharRef2FloatFn<P3> {
    float invoke(float p1, char p2, P3 p3);
}
