package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns a {@code float}.
 */
public interface BoolDouble2FloatFn {
    float invoke(boolean p1, double p2);
}
