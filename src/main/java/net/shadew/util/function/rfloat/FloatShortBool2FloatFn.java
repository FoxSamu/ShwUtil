package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code boolean} and returns a {@code float}.
 */
public interface FloatShortBool2FloatFn {
    float invoke(float p1, short p2, boolean p3);
}
