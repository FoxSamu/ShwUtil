package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code double} and an {@code int} and returns a {@code float}.
 */
public interface ByteDoubleInt2FloatFn {
    float invoke(byte p1, double p2, int p3);
}
