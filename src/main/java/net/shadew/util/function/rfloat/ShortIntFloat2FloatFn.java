package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code float} and returns a {@code float}.
 */
public interface ShortIntFloat2FloatFn {
    float invoke(short p1, int p2, float p3);
}
