package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code char} and returns a {@code float}.
 */
public interface BoolShortChar2FloatFn {
    float invoke(boolean p1, short p2, char p3);
}
