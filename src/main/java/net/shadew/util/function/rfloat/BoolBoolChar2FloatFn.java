package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code char} and returns a {@code float}.
 */
public interface BoolBoolChar2FloatFn {
    float invoke(boolean p1, boolean p2, char p3);
}
