package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code short} and returns a {@code float}.
 */
public interface ByteCharShort2FloatFn {
    float invoke(byte p1, char p2, short p3);
}
