package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code char} and returns a {@code float}.
 */
public interface ShortCharChar2FloatFn {
    float invoke(short p1, char p2, char p3);
}
