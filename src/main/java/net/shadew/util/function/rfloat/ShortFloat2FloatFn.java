package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short} and a {@code float} and returns a {@code float}.
 */
public interface ShortFloat2FloatFn {
    float invoke(short p1, float p2);
}
