package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code char} and returns a {@code float}.
 */
public interface FloatBoolChar2FloatFn {
    float invoke(float p1, boolean p2, char p3);
}
