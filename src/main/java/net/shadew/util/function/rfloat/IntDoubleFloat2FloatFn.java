package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code float} and returns a {@code float}.
 */
public interface IntDoubleFloat2FloatFn {
    float invoke(int p1, double p2, float p3);
}
