package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code double} and returns a {@code float}.
 */
public interface ByteShortDouble2FloatFn {
    float invoke(byte p1, short p2, double p3);
}
