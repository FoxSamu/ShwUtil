package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code byte} and returns a {@code float}.
 */
public interface ShortLongByte2FloatFn {
    float invoke(short p1, long p2, byte p3);
}
