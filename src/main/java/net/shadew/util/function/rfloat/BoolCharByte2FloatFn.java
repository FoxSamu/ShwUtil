package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code byte} and returns a {@code float}.
 */
public interface BoolCharByte2FloatFn {
    float invoke(boolean p1, char p2, byte p3);
}
