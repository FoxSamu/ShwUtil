package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code boolean} and an {@code int} and returns a {@code float}.
 */
public interface FloatBoolInt2FloatFn {
    float invoke(float p1, boolean p2, int p3);
}
