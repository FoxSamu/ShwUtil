package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code char} and returns a {@code float}.
 */
public interface ShortShortChar2FloatFn {
    float invoke(short p1, short p2, char p3);
}
