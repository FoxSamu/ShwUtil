package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code boolean} and returns a {@code float}.
 */
public interface FloatIntBool2FloatFn {
    float invoke(float p1, int p2, boolean p3);
}
