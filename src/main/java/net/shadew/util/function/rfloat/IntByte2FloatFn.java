package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int} and a {@code byte} and returns a {@code float}.
 */
public interface IntByte2FloatFn {
    float invoke(int p1, byte p2);
}
