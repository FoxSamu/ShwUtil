package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code double} and an {@code int} and returns a {@code float}.
 */
public interface BoolDoubleInt2FloatFn {
    float invoke(boolean p1, double p2, int p3);
}
