package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns a {@code float}.
 */
public interface FloatByteInt2FloatFn {
    float invoke(float p1, byte p2, int p3);
}
