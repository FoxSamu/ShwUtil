package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code long} and returns a {@code float}.
 */
public interface FloatByteLong2FloatFn {
    float invoke(float p1, byte p2, long p3);
}
