package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double} and a {@code byte} and returns a {@code float}.
 */
public interface DoubleByte2FloatFn {
    float invoke(double p1, byte p2);
}
