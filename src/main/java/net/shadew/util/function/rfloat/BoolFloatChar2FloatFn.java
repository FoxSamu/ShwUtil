package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code char} and returns a {@code float}.
 */
public interface BoolFloatChar2FloatFn {
    float invoke(boolean p1, float p2, char p3);
}
