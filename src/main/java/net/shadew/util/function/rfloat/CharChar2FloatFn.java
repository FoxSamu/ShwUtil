package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char} and a {@code char} and returns a {@code float}.
 */
public interface CharChar2FloatFn {
    float invoke(char p1, char p2);
}
