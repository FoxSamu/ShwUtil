package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code double} and returns a {@code float}.
 */
public interface BoolFloatDouble2FloatFn {
    float invoke(boolean p1, float p2, double p3);
}
