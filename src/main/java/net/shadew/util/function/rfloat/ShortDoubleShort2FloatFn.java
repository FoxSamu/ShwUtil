package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code short} and returns a {@code float}.
 */
public interface ShortDoubleShort2FloatFn {
    float invoke(short p1, double p2, short p3);
}
