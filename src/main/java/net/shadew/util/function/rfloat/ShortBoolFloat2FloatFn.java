package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code float} and returns a {@code float}.
 */
public interface ShortBoolFloat2FloatFn {
    float invoke(short p1, boolean p2, float p3);
}
