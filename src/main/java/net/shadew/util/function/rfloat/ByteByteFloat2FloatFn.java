package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code float} and returns a {@code float}.
 */
public interface ByteByteFloat2FloatFn {
    float invoke(byte p1, byte p2, float p3);
}
