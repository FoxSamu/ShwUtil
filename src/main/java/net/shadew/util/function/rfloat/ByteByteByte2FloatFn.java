package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code byte} and returns a {@code float}.
 */
public interface ByteByteByte2FloatFn {
    float invoke(byte p1, byte p2, byte p3);
}
