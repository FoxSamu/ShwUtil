package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code long} and a {@code char} and returns a {@code float}.
 */
public interface LongChar2FloatFn {
    float invoke(long p1, char p2);
}
