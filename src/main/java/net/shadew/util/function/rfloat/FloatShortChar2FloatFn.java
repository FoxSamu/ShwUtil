package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code char} and returns a {@code float}.
 */
public interface FloatShortChar2FloatFn {
    float invoke(float p1, short p2, char p3);
}
