package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code char} and returns a {@code float}.
 */
public interface ShortBoolChar2FloatFn {
    float invoke(short p1, boolean p2, char p3);
}
