package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code boolean} and a {@code byte} and returns a {@code float}.
 */
public interface BoolByte2FloatFn {
    float invoke(boolean p1, byte p2);
}
