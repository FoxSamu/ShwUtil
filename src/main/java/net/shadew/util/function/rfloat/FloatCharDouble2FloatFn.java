package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code double} and returns a {@code float}.
 */
public interface FloatCharDouble2FloatFn {
    float invoke(float p1, char p2, double p3);
}
