package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code double} and an {@code int} and returns a {@code float}.
 */
public interface DoubleInt2FloatFn {
    float invoke(double p1, int p2);
}
