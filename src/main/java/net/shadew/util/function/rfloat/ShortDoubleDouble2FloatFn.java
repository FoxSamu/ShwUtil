package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code double} and returns a {@code float}.
 */
public interface ShortDoubleDouble2FloatFn {
    float invoke(short p1, double p2, double p3);
}
