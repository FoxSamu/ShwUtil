package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int} and a {@code float} and returns a {@code float}.
 */
public interface IntFloat2FloatFn {
    float invoke(int p1, float p2);
}
