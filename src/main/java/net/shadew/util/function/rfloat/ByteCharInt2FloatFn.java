package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code byte}, a {@code char} and an {@code int} and returns a {@code float}.
 */
public interface ByteCharInt2FloatFn {
    float invoke(byte p1, char p2, int p3);
}
