package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code char} and returns a {@code float}.
 */
public interface FloatCharChar2FloatFn {
    float invoke(float p1, char p2, char p3);
}
