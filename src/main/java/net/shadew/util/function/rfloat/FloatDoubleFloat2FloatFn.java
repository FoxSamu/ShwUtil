package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code float} and returns a {@code float}.
 */
public interface FloatDoubleFloat2FloatFn {
    float invoke(float p1, double p2, float p3);
}
