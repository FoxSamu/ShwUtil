package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code char} and returns a {@code float}.
 */
public interface FloatDoubleChar2FloatFn {
    float invoke(float p1, double p2, char p3);
}
