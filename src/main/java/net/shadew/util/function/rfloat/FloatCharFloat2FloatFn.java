package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code float} and returns a {@code float}.
 */
public interface FloatCharFloat2FloatFn {
    float invoke(float p1, char p2, float p3);
}
