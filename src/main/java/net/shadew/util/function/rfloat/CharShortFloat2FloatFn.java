package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code short} and a {@code float} and returns a {@code float}.
 */
public interface CharShortFloat2FloatFn {
    float invoke(char p1, short p2, float p3);
}
