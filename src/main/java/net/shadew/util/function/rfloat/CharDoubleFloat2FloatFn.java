package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code float} and returns a {@code float}.
 */
public interface CharDoubleFloat2FloatFn {
    float invoke(char p1, double p2, float p3);
}
