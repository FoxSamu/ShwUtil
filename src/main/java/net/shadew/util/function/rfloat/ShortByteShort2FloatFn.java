package net.shadew.util.function.rfloat;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code short} and returns a {@code float}.
 */
public interface ShortByteShort2FloatFn {
    float invoke(Short p1, Byte p2, Short p3);
}
