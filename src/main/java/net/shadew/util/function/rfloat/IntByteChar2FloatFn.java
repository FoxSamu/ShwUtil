package net.shadew.util.function.rfloat;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns a {@code float}.
 */
public interface IntByteChar2FloatFn {
    float invoke(int p1, byte p2, char p3);
}
