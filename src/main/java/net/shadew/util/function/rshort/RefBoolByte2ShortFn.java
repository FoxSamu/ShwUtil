package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code boolean} and a {@code byte} and returns a {@code short}.
 */
public interface RefBoolByte2ShortFn<P1> {
    short invoke(P1 p1, boolean p2, byte p3);
}
