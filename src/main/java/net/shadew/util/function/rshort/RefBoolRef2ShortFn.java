package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code boolean} and an object and returns a {@code short}.
 */
public interface RefBoolRef2ShortFn<P1, P3> {
    short invoke(P1 p1, boolean p2, P3 p3);
}
