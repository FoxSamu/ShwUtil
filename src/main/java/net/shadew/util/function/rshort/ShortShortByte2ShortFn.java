package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code byte} and returns a {@code short}.
 */
public interface ShortShortByte2ShortFn {
    short invoke(short p1, short p2, byte p3);
}
