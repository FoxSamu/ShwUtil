package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code float} and returns a {@code short}.
 */
public interface ShortCharFloat2ShortFn {
    short invoke(short p1, char p2, float p3);
}
