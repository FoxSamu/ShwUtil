package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code byte} and returns a {@code short}.
 */
public interface IntByteByte2ShortFn {
    short invoke(int p1, byte p2, byte p3);
}
