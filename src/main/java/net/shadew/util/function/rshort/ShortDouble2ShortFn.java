package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short} and a {@code double} and returns a {@code short}.
 */
public interface ShortDouble2ShortFn {
    short invoke(short p1, double p2);
}
