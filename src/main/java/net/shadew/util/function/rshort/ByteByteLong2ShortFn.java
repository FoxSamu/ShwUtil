package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code long} and returns a {@code short}.
 */
public interface ByteByteLong2ShortFn {
    short invoke(byte p1, byte p2, long p3);
}
