package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface IntDoubleByte2ShortFn {
    short invoke(int p1, double p2, byte p3);
}
