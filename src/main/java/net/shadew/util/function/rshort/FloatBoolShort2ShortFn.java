package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code short} and returns a {@code short}.
 */
public interface FloatBoolShort2ShortFn {
    short invoke(float p1, boolean p2, short p3);
}
