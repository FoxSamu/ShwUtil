package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code char} and returns a {@code short}.
 */
public interface FloatByteChar2ShortFn {
    short invoke(float p1, byte p2, char p3);
}
