package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code char} and returns a {@code short}.
 */
public interface IntCharChar2ShortFn {
    short invoke(int p1, char p2, char p3);
}
