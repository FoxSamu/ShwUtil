package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code boolean} and returns a {@code short}.
 */
public interface FloatShortBool2ShortFn {
    short invoke(float p1, short p2, boolean p3);
}
