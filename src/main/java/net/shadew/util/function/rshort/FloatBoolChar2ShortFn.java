package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface FloatBoolChar2ShortFn {
    short invoke(float p1, boolean p2, char p3);
}
