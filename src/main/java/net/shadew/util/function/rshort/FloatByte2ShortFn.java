package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float} and a {@code byte} and returns a {@code short}.
 */
public interface FloatByte2ShortFn {
    short invoke(float p1, byte p2);
}
