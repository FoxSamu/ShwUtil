package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code short} and returns a {@code short}.
 */
public interface CharDoubleShort2ShortFn {
    short invoke(char p1, double p2, short p3);
}
