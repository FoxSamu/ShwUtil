package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code byte} and returns a {@code short}.
 */
public interface ByteShortByte2ShortFn {
    short invoke(byte p1, short p2, byte p3);
}
