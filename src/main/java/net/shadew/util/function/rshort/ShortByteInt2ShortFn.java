package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface ShortByteInt2ShortFn {
    short invoke(short p1, byte p2, int p3);
}
