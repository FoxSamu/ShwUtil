package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code char} and returns a {@code short}.
 */
public interface FloatFloatChar2ShortFn {
    short invoke(float p1, float p2, char p3);
}
