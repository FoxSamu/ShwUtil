package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code double} and an {@code int} and returns a {@code short}.
 */
public interface BoolDoubleInt2ShortFn {
    short invoke(boolean p1, double p2, int p3);
}
