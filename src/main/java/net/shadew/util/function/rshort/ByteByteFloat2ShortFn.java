package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code float} and returns a {@code short}.
 */
public interface ByteByteFloat2ShortFn {
    short invoke(byte p1, byte p2, float p3);
}
