package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code short} and returns a {@code short}.
 */
public interface LongDoubleShort2ShortFn {
    short invoke(long p1, double p2, short p3);
}
