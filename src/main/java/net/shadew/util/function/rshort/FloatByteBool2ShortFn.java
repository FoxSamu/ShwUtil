package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface FloatByteBool2ShortFn {
    short invoke(float p1, byte p2, boolean p3);
}
