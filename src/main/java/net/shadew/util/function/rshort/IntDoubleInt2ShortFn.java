package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code double} and an {@code int} and returns a {@code short}.
 */
public interface IntDoubleInt2ShortFn {
    short invoke(int p1, double p2, int p3);
}
