package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code char} and a {@code float} and returns a {@code short}.
 */
public interface RefCharFloat2ShortFn<P1> {
    short invoke(P1 p1, char p2, float p3);
}
