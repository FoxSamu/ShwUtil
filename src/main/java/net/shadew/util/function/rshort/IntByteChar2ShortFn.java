package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns a {@code short}.
 */
public interface IntByteChar2ShortFn {
    short invoke(int p1, byte p2, char p3);
}
