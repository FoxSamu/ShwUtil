package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code float} and a {@code byte} and returns a {@code short}.
 */
public interface RefFloatByte2ShortFn<P1> {
    short invoke(P1 p1, float p2, byte p3);
}
