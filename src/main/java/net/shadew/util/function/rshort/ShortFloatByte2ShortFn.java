package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code byte} and returns a {@code short}.
 */
public interface ShortFloatByte2ShortFn {
    short invoke(short p1, float p2, byte p3);
}
