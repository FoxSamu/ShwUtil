package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code boolean} and an object and returns a {@code short}.
 */
public interface IntBoolRef2ShortFn<P3> {
    short invoke(int p1, boolean p2, P3 p3);
}
