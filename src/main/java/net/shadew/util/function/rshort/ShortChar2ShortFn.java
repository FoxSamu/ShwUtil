package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short} and a {@code char} and returns a {@code short}.
 */
public interface ShortChar2ShortFn {
    short invoke(short p1, char p2);
}
