package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char} and a {@code double} and returns a {@code short}.
 */
public interface CharDouble2ShortFn {
    short invoke(char p1, double p2);
}
