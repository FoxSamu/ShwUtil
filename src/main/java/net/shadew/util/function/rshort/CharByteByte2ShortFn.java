package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code byte} and returns a {@code short}.
 */
public interface CharByteByte2ShortFn {
    short invoke(char p1, byte p2, byte p3);
}
