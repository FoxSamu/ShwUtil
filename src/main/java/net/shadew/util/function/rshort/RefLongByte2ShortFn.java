package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code long} and a {@code byte} and returns a {@code short}.
 */
public interface RefLongByte2ShortFn<P1> {
    short invoke(P1 p1, long p2, byte p3);
}
