package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code boolean} and returns a {@code short}.
 */
public interface FloatBoolBool2ShortFn {
    short invoke(float p1, boolean p2, boolean p3);
}
