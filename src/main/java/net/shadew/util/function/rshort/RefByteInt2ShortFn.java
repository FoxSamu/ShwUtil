package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface RefByteInt2ShortFn<P1> {
    short invoke(P1 p1, byte p2, int p3);
}
