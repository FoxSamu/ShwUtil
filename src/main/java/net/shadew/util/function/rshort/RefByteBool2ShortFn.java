package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface RefByteBool2ShortFn<P1> {
    short invoke(P1 p1, byte p2, boolean p3);
}
