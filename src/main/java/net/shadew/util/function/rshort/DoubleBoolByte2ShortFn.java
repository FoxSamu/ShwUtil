package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code byte} and returns a {@code short}.
 */
public interface DoubleBoolByte2ShortFn {
    short invoke(double p1, boolean p2, byte p3);
}
