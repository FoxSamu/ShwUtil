package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code boolean} and returns a {@code short}.
 */
public interface LongBoolBool2ShortFn {
    short invoke(long p1, boolean p2, boolean p3);
}
