package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface ByteBoolChar2ShortFn {
    short invoke(byte p1, boolean p2, char p3);
}
