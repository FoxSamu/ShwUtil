package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code float} and returns a {@code short}.
 */
public interface ByteShortFloat2ShortFn {
    short invoke(byte p1, short p2, float p3);
}
