package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code char} and returns a {@code short}.
 */
public interface BoolShortChar2ShortFn {
    short invoke(boolean p1, short p2, char p3);
}
