package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code byte} and returns a {@code short}.
 */
public interface LongByteByte2ShortFn {
    short invoke(long p1, byte p2, byte p3);
}
