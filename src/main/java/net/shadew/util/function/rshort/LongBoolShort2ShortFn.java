package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code short} and returns a {@code short}.
 */
public interface LongBoolShort2ShortFn {
    short invoke(long p1, boolean p2, short p3);
}
