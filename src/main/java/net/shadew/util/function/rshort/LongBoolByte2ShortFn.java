package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code byte} and returns a {@code short}.
 */
public interface LongBoolByte2ShortFn {
    short invoke(long p1, boolean p2, byte p3);
}
