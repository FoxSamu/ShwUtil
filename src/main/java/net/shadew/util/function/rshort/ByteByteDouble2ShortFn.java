package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns a {@code short}.
 */
public interface ByteByteDouble2ShortFn {
    short invoke(byte p1, byte p2, double p3);
}
