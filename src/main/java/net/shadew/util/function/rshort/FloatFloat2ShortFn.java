package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float} and a {@code float} and returns a {@code short}.
 */
public interface FloatFloat2ShortFn {
    short invoke(float p1, float p2);
}
