package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code boolean} and returns a {@code short}.
 */
public interface ByteShortBool2ShortFn {
    short invoke(byte p1, short p2, boolean p3);
}
