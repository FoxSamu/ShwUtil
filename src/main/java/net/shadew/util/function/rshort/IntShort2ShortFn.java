package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int} and a {@code short} and returns a {@code short}.
 */
public interface IntShort2ShortFn {
    short invoke(int p1, short p2);
}
