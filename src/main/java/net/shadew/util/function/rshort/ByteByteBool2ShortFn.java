package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface ByteByteBool2ShortFn {
    short invoke(byte p1, byte p2, boolean p3);
}
