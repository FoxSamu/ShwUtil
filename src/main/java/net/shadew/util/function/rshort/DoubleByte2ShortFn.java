package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface DoubleByte2ShortFn {
    short invoke(double p1, byte p2);
}
