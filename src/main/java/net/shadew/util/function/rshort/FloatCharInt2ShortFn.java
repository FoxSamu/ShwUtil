package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code char} and an {@code int} and returns a {@code short}.
 */
public interface FloatCharInt2ShortFn {
    short invoke(float p1, char p2, int p3);
}
