package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code boolean} and a {@code short} and returns a {@code short}.
 */
public interface RefBoolShort2ShortFn<P1> {
    short invoke(P1 p1, boolean p2, short p3);
}
