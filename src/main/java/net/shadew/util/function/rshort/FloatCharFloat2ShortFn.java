package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code float} and returns a {@code short}.
 */
public interface FloatCharFloat2ShortFn {
    short invoke(float p1, char p2, float p3);
}
