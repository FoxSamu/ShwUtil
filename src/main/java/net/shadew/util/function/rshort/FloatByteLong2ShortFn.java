package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code long} and returns a {@code short}.
 */
public interface FloatByteLong2ShortFn {
    short invoke(float p1, byte p2, long p3);
}
