package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code double} and returns a {@code short}.
 */
public interface FloatDoubleDouble2ShortFn {
    short invoke(float p1, double p2, double p3);
}
