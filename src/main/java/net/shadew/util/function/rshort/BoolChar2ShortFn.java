package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface BoolChar2ShortFn {
    short invoke(boolean p1, char p2);
}
