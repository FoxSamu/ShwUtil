package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code short} and returns a {@code short}.
 */
public interface ShortByteShort2ShortFn {
    short invoke(short p1, byte p2, short p3);
}
