package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code long} and a {@code short} and returns a {@code short}.
 */
public interface ByteLongShort2ShortFn {
    short invoke(byte p1, long p2, short p3);
}
