package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code short} and returns a {@code short}.
 */
public interface FloatCharShort2ShortFn {
    short invoke(float p1, char p2, short p3);
}
