package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code boolean} and a {@code double} and returns a {@code short}.
 */
public interface RefBoolDouble2ShortFn<P1> {
    short invoke(P1 p1, boolean p2, double p3);
}
