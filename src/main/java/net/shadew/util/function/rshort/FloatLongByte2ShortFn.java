package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code byte} and returns a {@code short}.
 */
public interface FloatLongByte2ShortFn {
    short invoke(float p1, long p2, byte p3);
}
