package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long} and a {@code boolean} and returns a {@code short}.
 */
public interface LongBool2ShortFn {
    short invoke(long p1, boolean p2);
}
