package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char} and an {@code int} and returns a {@code short}.
 */
public interface CharInt2ShortFn {
    short invoke(char p1, int p2);
}
