package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an object and returns a {@code short}.
 */
public interface BoolByteRef2ShortFn<P3> {
    short invoke(boolean p1, byte p2, P3 p3);
}
