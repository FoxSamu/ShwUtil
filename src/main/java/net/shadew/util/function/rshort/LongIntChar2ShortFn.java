package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code char} and returns a {@code short}.
 */
public interface LongIntChar2ShortFn {
    short invoke(long p1, int p2, char p3);
}
