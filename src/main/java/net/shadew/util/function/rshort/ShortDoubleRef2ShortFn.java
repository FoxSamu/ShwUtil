package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code double} and an object and returns a {@code short}.
 */
public interface ShortDoubleRef2ShortFn<P3> {
    short invoke(short p1, double p2, P3 p3);
}
