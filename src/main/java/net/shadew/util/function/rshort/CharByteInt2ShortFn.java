package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface CharByteInt2ShortFn {
    short invoke(char p1, byte p2, int p3);
}
