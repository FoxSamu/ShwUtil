package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code float} and returns a {@code short}.
 */
public interface ShortIntFloat2ShortFn {
    short invoke(short p1, int p2, float p3);
}
