package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code byte} and returns a {@code short}.
 */
public interface FloatBoolByte2ShortFn {
    short invoke(float p1, boolean p2, byte p3);
}
