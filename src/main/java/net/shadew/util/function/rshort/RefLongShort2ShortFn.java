package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code long} and a {@code short} and returns a {@code short}.
 */
public interface RefLongShort2ShortFn<P1> {
    short invoke(P1 p1, long p2, short p3);
}
