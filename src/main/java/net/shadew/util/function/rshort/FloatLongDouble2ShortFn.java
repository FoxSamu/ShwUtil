package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code double} and returns a {@code short}.
 */
public interface FloatLongDouble2ShortFn {
    short invoke(float p1, long p2, double p3);
}
