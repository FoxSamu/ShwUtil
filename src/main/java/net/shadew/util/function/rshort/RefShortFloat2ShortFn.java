package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code short} and a {@code float} and returns a {@code short}.
 */
public interface RefShortFloat2ShortFn<P1> {
    short invoke(P1 p1, short p2, float p3);
}
