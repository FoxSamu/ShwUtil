package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code boolean} and an object and returns a {@code short}.
 */
public interface FloatBoolRef2ShortFn<P3> {
    short invoke(float p1, boolean p2, P3 p3);
}
