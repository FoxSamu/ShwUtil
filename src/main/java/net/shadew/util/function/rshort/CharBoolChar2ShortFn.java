package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface CharBoolChar2ShortFn {
    short invoke(char p1, boolean p2, char p3);
}
