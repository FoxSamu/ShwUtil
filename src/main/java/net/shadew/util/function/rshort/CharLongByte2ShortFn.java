package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code long} and a {@code byte} and returns a {@code short}.
 */
public interface CharLongByte2ShortFn {
    short invoke(char p1, long p2, byte p3);
}
