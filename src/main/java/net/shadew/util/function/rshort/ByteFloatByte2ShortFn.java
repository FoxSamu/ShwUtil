package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code byte} and returns a {@code short}.
 */
public interface ByteFloatByte2ShortFn {
    short invoke(byte p1, float p2, byte p3);
}
