package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte} and a {@code short} and returns a {@code short}.
 */
public interface ByteShort2ShortFn {
    short invoke(byte p1, short p2);
}
