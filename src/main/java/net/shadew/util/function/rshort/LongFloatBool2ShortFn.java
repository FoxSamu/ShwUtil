package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code boolean} and returns a {@code short}.
 */
public interface LongFloatBool2ShortFn {
    short invoke(long p1, float p2, boolean p3);
}
