package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns a {@code short}.
 */
public interface ShortCharByte2ShortFn {
    short invoke(short p1, char p2, byte p3);
}
