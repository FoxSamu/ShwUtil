package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code char} and returns a {@code short}.
 */
public interface CharDoubleChar2ShortFn {
    short invoke(char p1, double p2, char p3);
}
