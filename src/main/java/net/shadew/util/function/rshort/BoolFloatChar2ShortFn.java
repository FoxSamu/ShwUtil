package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code char} and returns a {@code short}.
 */
public interface BoolFloatChar2ShortFn {
    short invoke(boolean p1, float p2, char p3);
}
