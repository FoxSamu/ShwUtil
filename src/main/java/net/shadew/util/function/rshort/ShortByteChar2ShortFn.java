package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code char} and returns a {@code short}.
 */
public interface ShortByteChar2ShortFn {
    short invoke(short p1, byte p2, char p3);
}
