package net.shadew.util.function.rshort;

/**
 * A function that takes an object and a {@code boolean} and returns a {@code short}.
 */
public interface RefBool2ShortFn<P1> {
    short invoke(P1 p1, boolean p2);
}
