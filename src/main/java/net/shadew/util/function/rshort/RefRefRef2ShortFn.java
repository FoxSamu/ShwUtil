package net.shadew.util.function.rshort;

/**
 * A function that takes an object, an object and an object and returns a {@code short}.
 */
public interface RefRefRef2ShortFn<P1, P2, P3> {
    short invoke(P1 p1, P2 p2, P3 p3);
}
