package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte} and a {@code float} and returns a {@code short}.
 */
public interface ByteFloat2ShortFn {
    short invoke(byte p1, float p2);
}
