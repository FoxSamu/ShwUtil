package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte} and a {@code double} and returns a {@code short}.
 */
public interface ByteDouble2ShortFn {
    short invoke(byte p1, double p2);
}
