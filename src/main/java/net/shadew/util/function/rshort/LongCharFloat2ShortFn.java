package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code float} and returns a {@code short}.
 */
public interface LongCharFloat2ShortFn {
    short invoke(long p1, char p2, float p3);
}
