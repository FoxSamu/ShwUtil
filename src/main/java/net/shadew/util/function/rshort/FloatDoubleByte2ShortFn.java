package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface FloatDoubleByte2ShortFn {
    short invoke(float p1, double p2, byte p3);
}
