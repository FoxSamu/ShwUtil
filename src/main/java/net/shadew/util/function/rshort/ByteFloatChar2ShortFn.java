package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code char} and returns a {@code short}.
 */
public interface ByteFloatChar2ShortFn {
    short invoke(byte p1, float p2, char p3);
}
