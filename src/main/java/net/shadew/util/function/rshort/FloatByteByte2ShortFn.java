package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code byte} and returns a {@code short}.
 */
public interface FloatByteByte2ShortFn {
    short invoke(float p1, byte p2, byte p3);
}
