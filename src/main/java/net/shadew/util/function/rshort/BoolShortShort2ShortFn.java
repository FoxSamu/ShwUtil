package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code short} and returns a {@code short}.
 */
public interface BoolShortShort2ShortFn {
    short invoke(boolean p1, short p2, short p3);
}
