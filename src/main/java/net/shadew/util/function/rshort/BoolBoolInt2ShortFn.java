package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an {@code int} and returns a {@code short}.
 */
public interface BoolBoolInt2ShortFn {
    short invoke(boolean p1, boolean p2, int p3);
}
