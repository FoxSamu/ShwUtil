package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code byte} and returns a {@code short}.
 */
public interface BoolByteByte2ShortFn {
    short invoke(boolean p1, byte p2, byte p3);
}
