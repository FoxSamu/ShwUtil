package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code boolean} and returns a {@code short}.
 */
public interface ShortCharBool2ShortFn {
    short invoke(short p1, char p2, boolean p3);
}
