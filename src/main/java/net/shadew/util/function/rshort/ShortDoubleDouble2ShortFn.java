package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code double} and returns a {@code short}.
 */
public interface ShortDoubleDouble2ShortFn {
    short invoke(short p1, double p2, double p3);
}
