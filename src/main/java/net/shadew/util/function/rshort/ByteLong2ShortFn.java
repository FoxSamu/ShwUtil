package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte} and a {@code long} and returns a {@code short}.
 */
public interface ByteLong2ShortFn {
    short invoke(byte p1, long p2);
}
