package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double} and a {@code char} and returns a {@code short}.
 */
public interface DoubleChar2ShortFn {
    short invoke(double p1, char p2);
}
