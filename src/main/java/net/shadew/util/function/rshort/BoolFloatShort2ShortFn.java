package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code short} and returns a {@code short}.
 */
public interface BoolFloatShort2ShortFn {
    short invoke(boolean p1, float p2, short p3);
}
