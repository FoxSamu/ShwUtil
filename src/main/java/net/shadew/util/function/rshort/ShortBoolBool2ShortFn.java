package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code boolean} and returns a {@code short}.
 */
public interface ShortBoolBool2ShortFn {
    short invoke(short p1, boolean p2, boolean p3);
}
