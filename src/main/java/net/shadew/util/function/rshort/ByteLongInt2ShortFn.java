package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code long} and an {@code int} and returns a {@code short}.
 */
public interface ByteLongInt2ShortFn {
    short invoke(byte p1, long p2, int p3);
}
