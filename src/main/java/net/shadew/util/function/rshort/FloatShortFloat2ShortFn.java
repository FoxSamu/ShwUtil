package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code float} and returns a {@code short}.
 */
public interface FloatShortFloat2ShortFn {
    short invoke(float p1, short p2, float p3);
}
