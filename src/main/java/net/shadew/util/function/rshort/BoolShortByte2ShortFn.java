package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code byte} and returns a {@code short}.
 */
public interface BoolShortByte2ShortFn {
    short invoke(boolean p1, short p2, byte p3);
}
