package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code double} and returns a {@code short}.
 */
public interface ByteBoolDouble2ShortFn {
    short invoke(byte p1, boolean p2, double p3);
}
