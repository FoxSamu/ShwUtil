package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short} and a {@code byte} and returns a {@code short}.
 */
public interface ShortByte2ShortFn {
    short invoke(short p1, byte p2);
}
