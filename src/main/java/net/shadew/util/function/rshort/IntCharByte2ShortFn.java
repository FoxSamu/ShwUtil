package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code byte} and returns a {@code short}.
 */
public interface IntCharByte2ShortFn {
    short invoke(int p1, char p2, byte p3);
}
