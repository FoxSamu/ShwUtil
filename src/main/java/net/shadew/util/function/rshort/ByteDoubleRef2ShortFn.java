package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code double} and an object and returns a {@code short}.
 */
public interface ByteDoubleRef2ShortFn<P3> {
    short invoke(byte p1, double p2, P3 p3);
}
