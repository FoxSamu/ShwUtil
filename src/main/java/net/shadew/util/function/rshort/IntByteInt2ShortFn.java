package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface IntByteInt2ShortFn {
    short invoke(int p1, byte p2, int p3);
}
