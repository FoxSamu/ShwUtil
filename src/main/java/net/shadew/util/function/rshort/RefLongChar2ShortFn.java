package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code long} and a {@code char} and returns a {@code short}.
 */
public interface RefLongChar2ShortFn<P1> {
    short invoke(P1 p1, long p2, char p3);
}
