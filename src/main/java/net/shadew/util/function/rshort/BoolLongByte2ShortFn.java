package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code long} and a {@code byte} and returns a {@code short}.
 */
public interface BoolLongByte2ShortFn {
    short invoke(boolean p1, long p2, byte p3);
}
