package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code byte} and returns a {@code short}.
 */
public interface ByteCharByte2ShortFn {
    short invoke(byte p1, char p2, byte p3);
}
