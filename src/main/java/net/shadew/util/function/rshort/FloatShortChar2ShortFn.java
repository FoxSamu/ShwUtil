package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code char} and returns a {@code short}.
 */
public interface FloatShortChar2ShortFn {
    short invoke(float p1, short p2, char p3);
}
