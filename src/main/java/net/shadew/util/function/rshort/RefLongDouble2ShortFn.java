package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code long} and a {@code double} and returns a {@code short}.
 */
public interface RefLongDouble2ShortFn<P1> {
    short invoke(P1 p1, long p2, double p3);
}
