package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code short} and returns a {@code short}.
 */
public interface BoolByteShort2ShortFn {
    short invoke(boolean p1, byte p2, short p3);
}
