package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code char} and returns a {@code short}.
 */
public interface ByteDoubleChar2ShortFn {
    short invoke(byte p1, double p2, char p3);
}
