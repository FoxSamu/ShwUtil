package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code char} and returns a {@code short}.
 */
public interface IntDoubleChar2ShortFn {
    short invoke(int p1, double p2, char p3);
}
