package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code boolean} and returns a {@code short}.
 */
public interface BoolFloatBool2ShortFn {
    short invoke(boolean p1, float p2, boolean p3);
}
