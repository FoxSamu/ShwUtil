package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code boolean} and returns a {@code short}.
 */
public interface IntDoubleBool2ShortFn {
    short invoke(int p1, double p2, boolean p3);
}
