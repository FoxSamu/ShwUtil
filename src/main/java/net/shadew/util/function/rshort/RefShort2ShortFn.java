package net.shadew.util.function.rshort;

/**
 * A function that takes an object and a {@code short} and returns a {@code short}.
 */
public interface RefShort2ShortFn<P1> {
    short invoke(P1 p1, short p2);
}
