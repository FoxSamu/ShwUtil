package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns a {@code short}.
 */
public interface BoolDouble2ShortFn {
    short invoke(boolean p1, double p2);
}
