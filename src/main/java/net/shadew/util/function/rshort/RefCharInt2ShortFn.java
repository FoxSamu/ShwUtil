package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code char} and an {@code int} and returns a {@code short}.
 */
public interface RefCharInt2ShortFn<P1> {
    short invoke(P1 p1, char p2, int p3);
}
