package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code double} and returns a {@code short}.
 */
public interface FloatBoolDouble2ShortFn {
    short invoke(float p1, boolean p2, double p3);
}
