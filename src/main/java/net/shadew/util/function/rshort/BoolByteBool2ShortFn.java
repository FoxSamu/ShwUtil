package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface BoolByteBool2ShortFn {
    short invoke(boolean p1, byte p2, boolean p3);
}
