package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code boolean} and an {@code int} and returns a {@code short}.
 */
public interface RefBoolInt2ShortFn<P1> {
    short invoke(P1 p1, boolean p2, int p3);
}
