package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code char} and returns a {@code short}.
 */
public interface ShortFloatChar2ShortFn {
    short invoke(short p1, float p2, char p3);
}
