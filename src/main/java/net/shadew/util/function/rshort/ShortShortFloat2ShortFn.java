package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code float} and returns a {@code short}.
 */
public interface ShortShortFloat2ShortFn {
    short invoke(short p1, short p2, float p3);
}
