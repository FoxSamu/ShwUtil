package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code short} and returns a {@code short}.
 */
public interface FloatDoubleShort2ShortFn {
    short invoke(float p1, double p2, short p3);
}
