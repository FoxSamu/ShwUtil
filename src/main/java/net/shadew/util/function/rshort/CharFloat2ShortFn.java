package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char} and a {@code float} and returns a {@code short}.
 */
public interface CharFloat2ShortFn {
    short invoke(char p1, float p2);
}
