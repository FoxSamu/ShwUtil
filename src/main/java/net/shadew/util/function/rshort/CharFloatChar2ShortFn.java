package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code float} and a {@code char} and returns a {@code short}.
 */
public interface CharFloatChar2ShortFn {
    short invoke(char p1, float p2, char p3);
}
