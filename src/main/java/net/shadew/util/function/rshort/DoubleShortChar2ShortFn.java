package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code char} and returns a {@code short}.
 */
public interface DoubleShortChar2ShortFn {
    short invoke(double p1, short p2, char p3);
}
