package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code float} and returns a {@code short}.
 */
public interface ByteCharFloat2ShortFn {
    short invoke(byte p1, char p2, float p3);
}
