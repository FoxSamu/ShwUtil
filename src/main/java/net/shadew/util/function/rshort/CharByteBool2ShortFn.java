package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface CharByteBool2ShortFn {
    short invoke(char p1, byte p2, boolean p3);
}
