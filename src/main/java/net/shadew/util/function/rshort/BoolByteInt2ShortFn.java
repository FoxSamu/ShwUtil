package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface BoolByteInt2ShortFn {
    short invoke(boolean p1, byte p2, int p3);
}
