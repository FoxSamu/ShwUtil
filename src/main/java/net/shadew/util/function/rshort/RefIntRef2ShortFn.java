package net.shadew.util.function.rshort;

/**
 * A function that takes an object, an {@code int} and an object and returns a {@code short}.
 */
public interface RefIntRef2ShortFn<P1, P3> {
    short invoke(P1 p1, int p2, P3 p3);
}
