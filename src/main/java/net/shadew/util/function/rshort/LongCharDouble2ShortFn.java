package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code double} and returns a {@code short}.
 */
public interface LongCharDouble2ShortFn {
    short invoke(long p1, char p2, double p3);
}
