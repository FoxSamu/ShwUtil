package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an object and returns a {@code short}.
 */
public interface BoolBoolRef2ShortFn<P3> {
    short invoke(boolean p1, boolean p2, P3 p3);
}
