package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns a {@code short}.
 */
public interface FloatByteDouble2ShortFn {
    short invoke(float p1, byte p2, double p3);
}
