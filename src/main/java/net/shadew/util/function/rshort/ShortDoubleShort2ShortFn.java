package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code short} and returns a {@code short}.
 */
public interface ShortDoubleShort2ShortFn {
    short invoke(short p1, double p2, short p3);
}
