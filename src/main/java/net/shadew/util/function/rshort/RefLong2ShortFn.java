package net.shadew.util.function.rshort;

/**
 * A function that takes an object and a {@code long} and returns a {@code short}.
 */
public interface RefLong2ShortFn<P1> {
    short invoke(P1 p1, long p2);
}
