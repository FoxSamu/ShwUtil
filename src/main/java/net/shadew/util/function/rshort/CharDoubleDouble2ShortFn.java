package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code double} and returns a {@code short}.
 */
public interface CharDoubleDouble2ShortFn {
    short invoke(char p1, double p2, double p3);
}
