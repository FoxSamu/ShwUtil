package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code boolean} and an object and returns a {@code short}.
 */
public interface DoubleBoolRef2ShortFn<P3> {
    short invoke(double p1, boolean p2, P3 p3);
}
