package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code boolean} and an {@code int} and returns a {@code short}.
 */
public interface ShortBoolInt2ShortFn {
    short invoke(short p1, boolean p2, int p3);
}
