package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code double} and returns a {@code short}.
 */
public interface BoolBoolDouble2ShortFn {
    short invoke(boolean p1, boolean p2, double p3);
}
