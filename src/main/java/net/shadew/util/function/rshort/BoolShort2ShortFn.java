package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns a {@code short}.
 */
public interface BoolShort2ShortFn {
    short invoke(boolean p1, short p2);
}
