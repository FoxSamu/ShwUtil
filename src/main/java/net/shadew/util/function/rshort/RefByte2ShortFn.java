package net.shadew.util.function.rshort;

/**
 * A function that takes an object and a {@code byte} and returns a {@code short}.
 */
public interface RefByte2ShortFn<P1> {
    short invoke(P1 p1, byte p2);
}
