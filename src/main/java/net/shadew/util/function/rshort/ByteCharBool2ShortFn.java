package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code boolean} and returns a {@code short}.
 */
public interface ByteCharBool2ShortFn {
    short invoke(byte p1, char p2, boolean p3);
}
