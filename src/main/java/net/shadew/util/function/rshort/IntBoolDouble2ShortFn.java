package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code double} and returns a {@code short}.
 */
public interface IntBoolDouble2ShortFn {
    short invoke(int p1, boolean p2, double p3);
}
