package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code boolean} and returns a {@code short}.
 */
public interface CharDoubleBool2ShortFn {
    short invoke(char p1, double p2, boolean p3);
}
