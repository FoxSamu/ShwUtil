package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code char} and returns a {@code short}.
 */
public interface DoubleByteChar2ShortFn {
    short invoke(double p1, byte p2, char p3);
}
