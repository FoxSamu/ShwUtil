package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code long} and an {@code int} and returns a {@code short}.
 */
public interface RefLongInt2ShortFn<P1> {
    short invoke(P1 p1, long p2, int p3);
}
