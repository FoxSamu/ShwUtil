package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, an {@code int} and a {@code float} and returns a {@code short}.
 */
public interface BoolIntFloat2ShortFn {
    short invoke(boolean p1, int p2, float p3);
}
