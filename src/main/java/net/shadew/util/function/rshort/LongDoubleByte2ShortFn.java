package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface LongDoubleByte2ShortFn {
    short invoke(long p1, double p2, byte p3);
}
