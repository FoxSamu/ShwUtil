package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code short} and an {@code int} and returns a {@code short}.
 */
public interface ByteShortInt2ShortFn {
    short invoke(byte p1, short p2, int p3);
}
