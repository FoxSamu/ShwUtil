package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int} and a {@code boolean} and returns a {@code short}.
 */
public interface IntBool2ShortFn {
    short invoke(int p1, boolean p2);
}
