package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code byte} and returns a {@code short}.
 */
public interface LongShortByte2ShortFn {
    short invoke(long p1, short p2, byte p3);
}
