package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface ShortByteBool2ShortFn {
    short invoke(short p1, byte p2, boolean p3);
}
