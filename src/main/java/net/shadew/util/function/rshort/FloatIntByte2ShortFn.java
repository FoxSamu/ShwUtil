package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code byte} and returns a {@code short}.
 */
public interface FloatIntByte2ShortFn {
    short invoke(float p1, int p2, byte p3);
}
