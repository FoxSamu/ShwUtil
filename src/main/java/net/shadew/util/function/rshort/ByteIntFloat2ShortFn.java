package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, an {@code int} and a {@code float} and returns a {@code short}.
 */
public interface ByteIntFloat2ShortFn {
    short invoke(byte p1, int p2, float p3);
}
