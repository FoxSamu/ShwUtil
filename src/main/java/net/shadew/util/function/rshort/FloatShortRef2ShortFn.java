package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code short} and an object and returns a {@code short}.
 */
public interface FloatShortRef2ShortFn<P3> {
    short invoke(float p1, short p2, P3 p3);
}
