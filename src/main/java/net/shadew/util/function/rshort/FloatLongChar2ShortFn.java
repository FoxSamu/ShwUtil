package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code char} and returns a {@code short}.
 */
public interface FloatLongChar2ShortFn {
    short invoke(float p1, long p2, char p3);
}
