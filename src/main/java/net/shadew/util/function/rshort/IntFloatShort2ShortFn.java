package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code short} and returns a {@code short}.
 */
public interface IntFloatShort2ShortFn {
    short invoke(int p1, float p2, short p3);
}
