package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code short} and returns a {@code short}.
 */
public interface ShortFloatShort2ShortFn {
    short invoke(short p1, float p2, short p3);
}
