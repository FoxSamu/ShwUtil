package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code float} and returns a {@code short}.
 */
public interface LongFloatFloat2ShortFn {
    short invoke(long p1, float p2, float p3);
}
