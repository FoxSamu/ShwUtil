package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code double} and returns a {@code short}.
 */
public interface ShortByteDouble2ShortFn {
    short invoke(short p1, byte p2, double p3);
}
