package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code double} and an {@code int} and returns a {@code short}.
 */
public interface RefDoubleInt2ShortFn<P1> {
    short invoke(P1 p1, double p2, int p3);
}
