package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code float} and returns a {@code short}.
 */
public interface DoubleByteFloat2ShortFn {
    short invoke(double p1, byte p2, float p3);
}
