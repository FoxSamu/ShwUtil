package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code byte} and returns a {@code short}.
 */
public interface DoubleByteByte2ShortFn {
    short invoke(double p1, byte p2, byte p3);
}
