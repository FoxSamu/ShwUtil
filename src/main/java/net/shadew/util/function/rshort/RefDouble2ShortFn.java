package net.shadew.util.function.rshort;

/**
 * A function that takes an object and a {@code double} and returns a {@code short}.
 */
public interface RefDouble2ShortFn<P1> {
    short invoke(P1 p1, double p2);
}
