package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code byte} and returns a {@code short}.
 */
public interface FloatShortByte2ShortFn {
    short invoke(float p1, short p2, byte p3);
}
