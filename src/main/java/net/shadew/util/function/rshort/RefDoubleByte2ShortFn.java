package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface RefDoubleByte2ShortFn<P1> {
    short invoke(P1 p1, double p2, byte p3);
}
