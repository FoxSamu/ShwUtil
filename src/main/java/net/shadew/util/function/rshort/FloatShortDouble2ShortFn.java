package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code double} and returns a {@code short}.
 */
public interface FloatShortDouble2ShortFn {
    short invoke(float p1, short p2, double p3);
}
