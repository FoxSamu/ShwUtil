package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code double} and returns a {@code short}.
 */
public interface BoolFloatDouble2ShortFn {
    short invoke(boolean p1, float p2, double p3);
}
