package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code char} and an object and returns a {@code short}.
 */
public interface BoolCharRef2ShortFn<P3> {
    short invoke(boolean p1, char p2, P3 p3);
}
