package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code boolean} and returns a {@code short}.
 */
public interface FloatFloatBool2ShortFn {
    short invoke(float p1, float p2, boolean p3);
}
