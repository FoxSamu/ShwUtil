package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code boolean} and a {@code float} and returns a {@code short}.
 */
public interface RefBoolFloat2ShortFn<P1> {
    short invoke(P1 p1, boolean p2, float p3);
}
