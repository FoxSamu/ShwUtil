package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code double} and returns a {@code short}.
 */
public interface DoubleShortDouble2ShortFn {
    short invoke(double p1, short p2, double p3);
}
