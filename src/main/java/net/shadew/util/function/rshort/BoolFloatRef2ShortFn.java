package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code float} and an object and returns a {@code short}.
 */
public interface BoolFloatRef2ShortFn<P3> {
    short invoke(boolean p1, float p2, P3 p3);
}
