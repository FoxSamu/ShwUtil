package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code byte} and returns a {@code short}.
 */
public interface ByteByteByte2ShortFn {
    short invoke(byte p1, byte p2, byte p3);
}
