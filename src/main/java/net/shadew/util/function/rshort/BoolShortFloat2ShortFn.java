package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code float} and returns a {@code short}.
 */
public interface BoolShortFloat2ShortFn {
    short invoke(boolean p1, short p2, float p3);
}
