package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code char} and an {@code int} and returns a {@code short}.
 */
public interface LongCharInt2ShortFn {
    short invoke(long p1, char p2, int p3);
}
