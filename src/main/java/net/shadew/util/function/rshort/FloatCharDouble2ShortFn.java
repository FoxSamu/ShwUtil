package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code double} and returns a {@code short}.
 */
public interface FloatCharDouble2ShortFn {
    short invoke(float p1, char p2, double p3);
}
