package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code char} and returns a {@code short}.
 */
public interface ShortLongChar2ShortFn {
    short invoke(short p1, long p2, char p3);
}
