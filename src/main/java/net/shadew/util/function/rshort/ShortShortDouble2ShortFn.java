package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code double} and returns a {@code short}.
 */
public interface ShortShortDouble2ShortFn {
    short invoke(short p1, short p2, double p3);
}
