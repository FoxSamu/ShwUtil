package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int} and a {@code float} and returns a {@code short}.
 */
public interface IntFloat2ShortFn {
    short invoke(int p1, float p2);
}
