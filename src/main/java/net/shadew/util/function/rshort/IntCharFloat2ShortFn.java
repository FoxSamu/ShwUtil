package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code float} and returns a {@code short}.
 */
public interface IntCharFloat2ShortFn {
    short invoke(int p1, char p2, float p3);
}
