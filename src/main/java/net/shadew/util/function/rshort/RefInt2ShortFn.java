package net.shadew.util.function.rshort;

/**
 * A function that takes an object and an {@code int} and returns a {@code short}.
 */
public interface RefInt2ShortFn<P1> {
    short invoke(P1 p1, int p2);
}
