package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int} and a {@code char} and returns a {@code short}.
 */
public interface IntChar2ShortFn {
    short invoke(int p1, char p2);
}
