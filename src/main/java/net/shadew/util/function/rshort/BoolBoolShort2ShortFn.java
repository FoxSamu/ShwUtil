package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code short} and returns a {@code short}.
 */
public interface BoolBoolShort2ShortFn {
    short invoke(boolean p1, boolean p2, short p3);
}
