package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code byte} and returns a {@code short}.
 */
public interface LongFloatByte2ShortFn {
    short invoke(long p1, float p2, byte p3);
}
