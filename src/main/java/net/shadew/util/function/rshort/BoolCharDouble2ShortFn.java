package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code double} and returns a {@code short}.
 */
public interface BoolCharDouble2ShortFn {
    short invoke(boolean p1, char p2, double p3);
}
