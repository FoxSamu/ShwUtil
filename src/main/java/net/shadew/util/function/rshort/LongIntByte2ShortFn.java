package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code byte} and returns a {@code short}.
 */
public interface LongIntByte2ShortFn {
    short invoke(long p1, int p2, byte p3);
}
