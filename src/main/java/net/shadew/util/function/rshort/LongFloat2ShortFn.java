package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long} and a {@code float} and returns a {@code short}.
 */
public interface LongFloat2ShortFn {
    short invoke(long p1, float p2);
}
