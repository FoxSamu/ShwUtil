package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code long} and a {@code byte} and returns a {@code short}.
 */
public interface IntLongByte2ShortFn {
    short invoke(int p1, long p2, byte p3);
}
