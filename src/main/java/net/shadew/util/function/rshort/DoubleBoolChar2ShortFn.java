package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface DoubleBoolChar2ShortFn {
    short invoke(double p1, boolean p2, char p3);
}
