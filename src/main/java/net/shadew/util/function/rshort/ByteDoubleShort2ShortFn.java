package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code short} and returns a {@code short}.
 */
public interface ByteDoubleShort2ShortFn {
    short invoke(byte p1, double p2, short p3);
}
