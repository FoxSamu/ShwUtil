package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code byte} and returns a {@code short}.
 */
public interface BoolBoolByte2ShortFn {
    short invoke(boolean p1, boolean p2, byte p3);
}
