package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code char} and an object and returns a {@code short}.
 */
public interface IntCharRef2ShortFn<P3> {
    short invoke(Int p1, Char p2, P3 p3);
}
