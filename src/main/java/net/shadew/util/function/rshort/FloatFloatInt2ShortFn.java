package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code float} and an {@code int} and returns a {@code short}.
 */
public interface FloatFloatInt2ShortFn {
    short invoke(float p1, float p2, int p3);
}
