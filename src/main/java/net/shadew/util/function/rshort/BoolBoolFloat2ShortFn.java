package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code float} and returns a {@code short}.
 */
public interface BoolBoolFloat2ShortFn {
    short invoke(boolean p1, boolean p2, float p3);
}
