package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char} and a {@code boolean} and returns a {@code short}.
 */
public interface CharBool2ShortFn {
    short invoke(char p1, boolean p2);
}
