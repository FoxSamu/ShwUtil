package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code double} and returns a {@code short}.
 */
public interface LongShortDouble2ShortFn {
    short invoke(long p1, short p2, double p3);
}
