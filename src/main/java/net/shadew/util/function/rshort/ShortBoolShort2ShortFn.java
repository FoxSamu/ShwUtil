package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code short} and returns a {@code short}.
 */
public interface ShortBoolShort2ShortFn {
    short invoke(short p1, boolean p2, short p3);
}
