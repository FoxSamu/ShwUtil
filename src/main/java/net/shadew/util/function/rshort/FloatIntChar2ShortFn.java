package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code char} and returns a {@code short}.
 */
public interface FloatIntChar2ShortFn {
    short invoke(float p1, int p2, char p3);
}
