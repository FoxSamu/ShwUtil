package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code char} and a {@code double} and returns a {@code short}.
 */
public interface CharCharDouble2ShortFn {
    short invoke(char p1, char p2, double p3);
}
