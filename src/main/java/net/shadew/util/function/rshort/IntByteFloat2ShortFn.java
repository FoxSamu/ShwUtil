package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code float} and returns a {@code short}.
 */
public interface IntByteFloat2ShortFn {
    short invoke(int p1, byte p2, float p3);
}
