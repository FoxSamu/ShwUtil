package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float} and a {@code double} and returns a {@code short}.
 */
public interface FloatDouble2ShortFn {
    short invoke(float p1, double p2);
}
