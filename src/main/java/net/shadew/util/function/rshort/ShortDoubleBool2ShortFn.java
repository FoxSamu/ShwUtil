package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code boolean} and returns a {@code short}.
 */
public interface ShortDoubleBool2ShortFn {
    short invoke(short p1, double p2, boolean p3);
}
