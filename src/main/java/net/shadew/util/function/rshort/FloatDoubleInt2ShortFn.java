package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code double} and an {@code int} and returns a {@code short}.
 */
public interface FloatDoubleInt2ShortFn {
    short invoke(float p1, double p2, int p3);
}
