package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, an {@code int} and a {@code float} and returns a {@code short}.
 */
public interface CharIntFloat2ShortFn {
    short invoke(Char p1, Int p2, Float p3);
}
