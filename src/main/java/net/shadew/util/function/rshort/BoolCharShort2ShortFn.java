package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code short} and returns a {@code short}.
 */
public interface BoolCharShort2ShortFn {
    short invoke(boolean p1, char p2, short p3);
}
