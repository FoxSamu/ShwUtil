package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code short} and returns a {@code short}.
 */
public interface LongByteShort2ShortFn {
    short invoke(long p1, byte p2, short p3);
}
