package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface DoubleDoubleByte2ShortFn {
    short invoke(double p1, double p2, byte p3);
}
