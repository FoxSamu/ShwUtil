package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, an {@code int} and a {@code byte} and returns a {@code short}.
 */
public interface ByteIntByte2ShortFn {
    short invoke(byte p1, int p2, byte p3);
}
