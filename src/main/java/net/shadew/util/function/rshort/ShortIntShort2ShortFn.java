package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code short} and returns a {@code short}.
 */
public interface ShortIntShort2ShortFn {
    short invoke(short p1, int p2, short p3);
}
