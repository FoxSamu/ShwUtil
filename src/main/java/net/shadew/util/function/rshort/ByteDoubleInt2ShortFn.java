package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code double} and an {@code int} and returns a {@code short}.
 */
public interface ByteDoubleInt2ShortFn {
    short invoke(byte p1, double p2, int p3);
}
