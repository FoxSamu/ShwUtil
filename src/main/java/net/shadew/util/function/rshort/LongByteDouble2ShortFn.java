package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code double} and returns a {@code short}.
 */
public interface LongByteDouble2ShortFn {
    short invoke(long p1, byte p2, double p3);
}
