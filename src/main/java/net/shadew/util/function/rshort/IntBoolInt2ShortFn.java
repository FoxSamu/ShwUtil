package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code boolean} and an {@code int} and returns a {@code short}.
 */
public interface IntBoolInt2ShortFn {
    short invoke(int p1, boolean p2, int p3);
}
