package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte} and a {@code char} and returns a {@code short}.
 */
public interface ByteChar2ShortFn {
    short invoke(byte p1, char p2);
}
