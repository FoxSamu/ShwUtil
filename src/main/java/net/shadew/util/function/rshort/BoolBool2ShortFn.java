package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns a {@code short}.
 */
public interface BoolBool2ShortFn {
    short invoke(boolean p1, boolean p2);
}
