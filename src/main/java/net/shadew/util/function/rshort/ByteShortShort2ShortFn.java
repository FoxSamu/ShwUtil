package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code short} and returns a {@code short}.
 */
public interface ByteShortShort2ShortFn {
    short invoke(byte p1, short p2, short p3);
}
