package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code double} and an {@code int} and returns a {@code short}.
 */
public interface CharDoubleInt2ShortFn {
    short invoke(char p1, double p2, int p3);
}
