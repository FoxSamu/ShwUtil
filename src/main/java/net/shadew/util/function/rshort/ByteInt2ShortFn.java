package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface ByteInt2ShortFn {
    short invoke(byte p1, int p2);
}
