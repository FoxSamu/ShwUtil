package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long} and a {@code short} and returns a {@code short}.
 */
public interface LongShort2ShortFn {
    short invoke(long p1, short p2);
}
