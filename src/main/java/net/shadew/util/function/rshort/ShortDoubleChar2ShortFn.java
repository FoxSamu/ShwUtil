package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code char} and returns a {@code short}.
 */
public interface ShortDoubleChar2ShortFn {
    short invoke(short p1, double p2, char p3);
}
