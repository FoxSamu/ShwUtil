package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code char} and returns a {@code short}.
 */
public interface BoolByteChar2ShortFn {
    short invoke(boolean p1, byte p2, char p3);
}
