package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, an {@code int} and a {@code boolean} and returns a {@code short}.
 */
public interface ByteIntBool2ShortFn {
    short invoke(Byte p1, Int p2, Bool p3);
}
