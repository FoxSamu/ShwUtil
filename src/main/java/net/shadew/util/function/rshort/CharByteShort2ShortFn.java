package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code short} and returns a {@code short}.
 */
public interface CharByteShort2ShortFn {
    short invoke(char p1, byte p2, short p3);
}
