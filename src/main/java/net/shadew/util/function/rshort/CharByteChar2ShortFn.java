package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns a {@code short}.
 */
public interface CharByteChar2ShortFn {
    short invoke(char p1, byte p2, char p3);
}
