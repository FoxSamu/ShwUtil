package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code char} and an object and returns a {@code short}.
 */
public interface FloatCharRef2ShortFn<P3> {
    short invoke(float p1, char p2, P3 p3);
}
