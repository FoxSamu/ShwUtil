package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code char} and returns a {@code short}.
 */
public interface LongByteChar2ShortFn {
    short invoke(long p1, byte p2, char p3);
}
