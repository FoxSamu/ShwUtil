package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code float} and an {@code int} and returns a {@code short}.
 */
public interface ByteFloatInt2ShortFn {
    short invoke(byte p1, float p2, int p3);
}
