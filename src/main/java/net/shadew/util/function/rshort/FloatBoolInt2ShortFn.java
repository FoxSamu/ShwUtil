package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code boolean} and an {@code int} and returns a {@code short}.
 */
public interface FloatBoolInt2ShortFn {
    short invoke(float p1, boolean p2, int p3);
}
