package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double} and an {@code int} and returns a {@code short}.
 */
public interface DoubleInt2ShortFn {
    short invoke(double p1, int p2);
}
