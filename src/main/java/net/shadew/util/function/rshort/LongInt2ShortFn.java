package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long} and an {@code int} and returns a {@code short}.
 */
public interface LongInt2ShortFn {
    short invoke(long p1, int p2);
}
