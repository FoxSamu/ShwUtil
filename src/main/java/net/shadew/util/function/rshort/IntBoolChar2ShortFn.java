package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface IntBoolChar2ShortFn {
    short invoke(int p1, boolean p2, char p3);
}
