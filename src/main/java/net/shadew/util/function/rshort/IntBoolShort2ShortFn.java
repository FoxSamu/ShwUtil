package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code short} and returns a {@code short}.
 */
public interface IntBoolShort2ShortFn {
    short invoke(int p1, boolean p2, short p3);
}
