package net.shadew.util.function.rshort;

/**
 * A function that takes an object, an {@code int} and an {@code int} and returns a {@code short}.
 */
public interface RefIntInt2ShortFn<P1> {
    short invoke(P1 p1, int p2, int p3);
}
