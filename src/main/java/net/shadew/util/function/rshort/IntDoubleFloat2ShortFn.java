package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code float} and returns a {@code short}.
 */
public interface IntDoubleFloat2ShortFn {
    short invoke(int p1, double p2, float p3);
}
