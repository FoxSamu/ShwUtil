package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code float} and an {@code int} and returns a {@code short}.
 */
public interface RefFloatInt2ShortFn<P1> {
    short invoke(P1 p1, float p2, int p3);
}
