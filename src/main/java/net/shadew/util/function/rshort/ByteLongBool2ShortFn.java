package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code long} and a {@code boolean} and returns a {@code short}.
 */
public interface ByteLongBool2ShortFn {
    short invoke(byte p1, long p2, boolean p3);
}
