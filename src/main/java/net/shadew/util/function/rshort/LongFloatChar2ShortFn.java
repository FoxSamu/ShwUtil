package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code char} and returns a {@code short}.
 */
public interface LongFloatChar2ShortFn {
    short invoke(Long p1, Float p2, Char p3);
}
