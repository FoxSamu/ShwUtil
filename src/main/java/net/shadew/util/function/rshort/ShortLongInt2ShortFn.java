package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code long} and an {@code int} and returns a {@code short}.
 */
public interface ShortLongInt2ShortFn {
    short invoke(short p1, long p2, int p3);
}
