package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code char} and an {@code int} and returns a {@code short}.
 */
public interface CharCharInt2ShortFn {
    short invoke(char p1, char p2, int p3);
}
