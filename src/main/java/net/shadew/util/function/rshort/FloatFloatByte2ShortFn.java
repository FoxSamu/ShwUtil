package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code byte} and returns a {@code short}.
 */
public interface FloatFloatByte2ShortFn {
    short invoke(float p1, float p2, byte p3);
}
