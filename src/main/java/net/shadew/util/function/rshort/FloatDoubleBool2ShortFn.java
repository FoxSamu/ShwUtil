package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code boolean} and returns a {@code short}.
 */
public interface FloatDoubleBool2ShortFn {
    short invoke(float p1, double p2, boolean p3);
}
