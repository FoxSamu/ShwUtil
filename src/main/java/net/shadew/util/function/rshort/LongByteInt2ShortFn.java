package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface LongByteInt2ShortFn {
    short invoke(long p1, byte p2, int p3);
}
