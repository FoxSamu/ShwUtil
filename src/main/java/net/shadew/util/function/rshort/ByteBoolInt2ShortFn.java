package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an {@code int} and returns a {@code short}.
 */
public interface ByteBoolInt2ShortFn {
    short invoke(byte p1, boolean p2, int p3);
}
