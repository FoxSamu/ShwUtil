package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an object and returns a {@code short}.
 */
public interface ByteBoolRef2ShortFn<P3> {
    short invoke(byte p1, boolean p2, P3 p3);
}
