package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code boolean} and returns a {@code short}.
 */
public interface IntCharBool2ShortFn {
    short invoke(int p1, char p2, boolean p3);
}
