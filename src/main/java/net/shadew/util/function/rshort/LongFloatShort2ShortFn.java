package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code short} and returns a {@code short}.
 */
public interface LongFloatShort2ShortFn {
    short invoke(long p1, float p2, short p3);
}
