package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code char} and returns a {@code short}.
 */
public interface IntShortChar2ShortFn {
    short invoke(int p1, short p2, char p3);
}
