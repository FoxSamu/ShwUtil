package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, an {@code int} and a {@code boolean} and returns a {@code short}.
 */
public interface CharIntBool2ShortFn {
    short invoke(char p1, int p2, boolean p3);
}
