package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code boolean} and returns a {@code short}.
 */
public interface IntBoolBool2ShortFn {
    short invoke(int p1, boolean p2, boolean p3);
}
