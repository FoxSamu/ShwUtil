package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code double} and returns a {@code short}.
 */
public interface BoolDoubleDouble2ShortFn {
    short invoke(boolean p1, double p2, double p3);
}
