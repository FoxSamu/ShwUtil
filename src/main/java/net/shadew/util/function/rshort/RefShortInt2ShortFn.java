package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code short} and an {@code int} and returns a {@code short}.
 */
public interface RefShortInt2ShortFn<P1> {
    short invoke(P1 p1, short p2, int p3);
}
