package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface LongBoolChar2ShortFn {
    short invoke(long p1, boolean p2, char p3);
}
