package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code byte} and returns a {@code short}.
 */
public interface ShortByteByte2ShortFn {
    short invoke(short p1, byte p2, byte p3);
}
