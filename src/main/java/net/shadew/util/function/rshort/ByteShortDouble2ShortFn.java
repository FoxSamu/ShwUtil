package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code double} and returns a {@code short}.
 */
public interface ByteShortDouble2ShortFn {
    short invoke(byte p1, short p2, double p3);
}
