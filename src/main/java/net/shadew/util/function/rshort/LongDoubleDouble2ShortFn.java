package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code double} and returns a {@code short}.
 */
public interface LongDoubleDouble2ShortFn {
    short invoke(long p1, double p2, double p3);
}
