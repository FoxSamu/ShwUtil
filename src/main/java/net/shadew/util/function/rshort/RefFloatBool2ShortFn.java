package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code float} and a {@code boolean} and returns a {@code short}.
 */
public interface RefFloatBool2ShortFn<P1> {
    short invoke(P1 p1, float p2, boolean p3);
}
