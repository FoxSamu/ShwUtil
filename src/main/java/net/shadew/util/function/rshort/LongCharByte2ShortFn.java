package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code byte} and returns a {@code short}.
 */
public interface LongCharByte2ShortFn {
    short invoke(long p1, char p2, byte p3);
}
