package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code double} and returns a {@code short}.
 */
public interface BoolShortDouble2ShortFn {
    short invoke(boolean p1, short p2, double p3);
}
