package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code float} and returns a {@code short}.
 */
public interface DoubleBoolFloat2ShortFn {
    short invoke(double p1, boolean p2, float p3);
}
