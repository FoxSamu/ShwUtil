package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code boolean} and returns a {@code short}.
 */
public interface LongDoubleBool2ShortFn {
    short invoke(long p1, double p2, boolean p3);
}
