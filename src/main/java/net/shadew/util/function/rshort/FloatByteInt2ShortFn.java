package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface FloatByteInt2ShortFn {
    short invoke(float p1, byte p2, int p3);
}
