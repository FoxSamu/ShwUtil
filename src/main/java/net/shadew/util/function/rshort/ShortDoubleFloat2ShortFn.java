package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code float} and returns a {@code short}.
 */
public interface ShortDoubleFloat2ShortFn {
    short invoke(short p1, double p2, float p3);
}
