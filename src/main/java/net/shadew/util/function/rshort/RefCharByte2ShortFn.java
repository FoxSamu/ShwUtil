package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code char} and a {@code byte} and returns a {@code short}.
 */
public interface RefCharByte2ShortFn<P1> {
    short invoke(P1 p1, char p2, byte p3);
}
