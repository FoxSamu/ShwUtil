package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code long} and a {@code byte} and returns a {@code short}.
 */
public interface ByteLongByte2ShortFn {
    short invoke(byte p1, long p2, byte p3);
}
