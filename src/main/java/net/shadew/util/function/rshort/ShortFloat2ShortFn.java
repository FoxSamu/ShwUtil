package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short} and a {@code float} and returns a {@code short}.
 */
public interface ShortFloat2ShortFn {
    short invoke(short p1, float p2);
}
