package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code short} and returns a {@code short}.
 */
public interface DoubleByteShort2ShortFn {
    short invoke(double p1, byte p2, short p3);
}
