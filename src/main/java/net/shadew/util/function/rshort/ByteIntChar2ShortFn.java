package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, an {@code int} and a {@code char} and returns a {@code short}.
 */
public interface ByteIntChar2ShortFn {
    short invoke(byte p1, int p2, char p3);
}
