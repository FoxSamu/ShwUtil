package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code float} and returns a {@code short}.
 */
public interface FloatIntFloat2ShortFn {
    short invoke(float p1, int p2, float p3);
}
