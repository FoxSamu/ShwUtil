package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code boolean} and an object and returns a {@code short}.
 */
public interface ShortBoolRef2ShortFn<P3> {
    short invoke(short p1, boolean p2, P3 p3);
}
