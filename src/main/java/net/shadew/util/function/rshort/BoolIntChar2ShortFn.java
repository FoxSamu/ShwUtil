package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, an {@code int} and a {@code char} and returns a {@code short}.
 */
public interface BoolIntChar2ShortFn {
    short invoke(boolean p1, int p2, char p3);
}
