package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code double} and returns a {@code short}.
 */
public interface ShortCharDouble2ShortFn {
    short invoke(short p1, char p2, double p3);
}
