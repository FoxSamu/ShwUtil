package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code short} and returns a {@code short}.
 */
public interface FloatShortShort2ShortFn {
    short invoke(float p1, short p2, short p3);
}
