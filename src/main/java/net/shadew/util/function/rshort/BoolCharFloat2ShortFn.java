package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code float} and returns a {@code short}.
 */
public interface BoolCharFloat2ShortFn {
    short invoke(boolean p1, char p2, float p3);
}
