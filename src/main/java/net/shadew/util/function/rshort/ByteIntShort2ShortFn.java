package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, an {@code int} and a {@code short} and returns a {@code short}.
 */
public interface ByteIntShort2ShortFn {
    short invoke(byte p1, int p2, short p3);
}
