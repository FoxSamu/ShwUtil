package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code boolean} and returns a {@code short}.
 */
public interface ShortFloatBool2ShortFn {
    short invoke(short p1, float p2, boolean p3);
}
