package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean} and a {@code byte} and returns a {@code short}.
 */
public interface BoolByte2ShortFn {
    short invoke(boolean p1, byte p2);
}
