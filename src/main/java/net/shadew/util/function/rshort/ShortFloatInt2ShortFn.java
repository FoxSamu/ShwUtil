package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code float} and an {@code int} and returns a {@code short}.
 */
public interface ShortFloatInt2ShortFn {
    short invoke(short p1, float p2, int p3);
}
