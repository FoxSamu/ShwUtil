package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code char} and returns a {@code short}.
 */
public interface ShortCharChar2ShortFn {
    short invoke(short p1, char p2, char p3);
}
