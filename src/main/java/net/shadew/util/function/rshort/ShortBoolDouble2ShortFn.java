package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code double} and returns a {@code short}.
 */
public interface ShortBoolDouble2ShortFn {
    short invoke(short p1, boolean p2, double p3);
}
