package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface ShortDoubleByte2ShortFn {
    short invoke(short p1, double p2, byte p3);
}
