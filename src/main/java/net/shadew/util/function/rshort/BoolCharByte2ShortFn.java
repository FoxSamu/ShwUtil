package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code byte} and returns a {@code short}.
 */
public interface BoolCharByte2ShortFn {
    short invoke(boolean p1, char p2, byte p3);
}
