package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code double} and a {@code double} and returns a {@code short}.
 */
public interface RefDoubleDouble2ShortFn<P1> {
    short invoke(P1 p1, double p2, double p3);
}
