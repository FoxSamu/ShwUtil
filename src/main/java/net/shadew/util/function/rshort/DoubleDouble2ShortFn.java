package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double} and a {@code double} and returns a {@code short}.
 */
public interface DoubleDouble2ShortFn {
    short invoke(double p1, double p2);
}
