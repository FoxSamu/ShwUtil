package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code float} and returns a {@code short}.
 */
public interface FloatBoolFloat2ShortFn {
    short invoke(float p1, boolean p2, float p3);
}
