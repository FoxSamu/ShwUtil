package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code double} and returns a {@code short}.
 */
public interface IntDoubleDouble2ShortFn {
    short invoke(int p1, double p2, double p3);
}
