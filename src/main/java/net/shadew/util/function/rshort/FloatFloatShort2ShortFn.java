package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code short} and returns a {@code short}.
 */
public interface FloatFloatShort2ShortFn {
    short invoke(float p1, float p2, short p3);
}
