package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code byte} and returns a {@code short}.
 */
public interface ShortIntByte2ShortFn {
    short invoke(short p1, int p2, byte p3);
}
