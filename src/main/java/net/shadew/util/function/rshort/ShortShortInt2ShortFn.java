package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code short} and an {@code int} and returns a {@code short}.
 */
public interface ShortShortInt2ShortFn {
    short invoke(Short p1, Short p2, Int p3);
}
