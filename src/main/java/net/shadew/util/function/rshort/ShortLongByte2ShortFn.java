package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code byte} and returns a {@code short}.
 */
public interface ShortLongByte2ShortFn {
    short invoke(short p1, long p2, byte p3);
}
