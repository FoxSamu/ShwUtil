package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface ByteBool2ShortFn {
    short invoke(byte p1, boolean p2);
}
