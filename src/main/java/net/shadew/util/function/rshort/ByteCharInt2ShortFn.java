package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code char} and an {@code int} and returns a {@code short}.
 */
public interface ByteCharInt2ShortFn {
    short invoke(byte p1, char p2, int p3);
}
