package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code short} and an {@code int} and returns a {@code short}.
 */
public interface FloatShortInt2ShortFn {
    short invoke(float p1, short p2, int p3);
}
