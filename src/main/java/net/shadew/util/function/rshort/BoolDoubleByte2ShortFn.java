package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface BoolDoubleByte2ShortFn {
    short invoke(boolean p1, double p2, byte p3);
}
