package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code double} and returns a {@code short}.
 */
public interface ByteFloatDouble2ShortFn {
    short invoke(byte p1, float p2, double p3);
}
