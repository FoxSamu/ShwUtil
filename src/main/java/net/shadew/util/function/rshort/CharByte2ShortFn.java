package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char} and a {@code byte} and returns a {@code short}.
 */
public interface CharByte2ShortFn {
    short invoke(char p1, byte p2);
}
