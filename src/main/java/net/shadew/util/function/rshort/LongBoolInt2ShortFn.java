package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code boolean} and an {@code int} and returns a {@code short}.
 */
public interface LongBoolInt2ShortFn {
    short invoke(long p1, boolean p2, int p3);
}
