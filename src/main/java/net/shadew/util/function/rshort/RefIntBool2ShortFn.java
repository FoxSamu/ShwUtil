package net.shadew.util.function.rshort;

/**
 * A function that takes an object, an {@code int} and a {@code boolean} and returns a {@code short}.
 */
public interface RefIntBool2ShortFn<P1> {
    short invoke(P1 p1, int p2, boolean p3);
}
