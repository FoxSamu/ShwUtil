package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double} and a {@code boolean} and returns a {@code short}.
 */
public interface DoubleBool2ShortFn {
    short invoke(double p1, boolean p2);
}
