package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code short} and an object and returns a {@code short}.
 */
public interface ByteShortRef2ShortFn<P3> {
    short invoke(byte p1, short p2, P3 p3);
}
