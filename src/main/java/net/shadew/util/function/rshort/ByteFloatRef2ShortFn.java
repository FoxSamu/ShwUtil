package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code float} and an object and returns a {@code short}.
 */
public interface ByteFloatRef2ShortFn<P3> {
    short invoke(byte p1, float p2, P3 p3);
}
