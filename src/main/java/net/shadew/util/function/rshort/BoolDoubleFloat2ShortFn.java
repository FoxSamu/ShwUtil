package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code float} and returns a {@code short}.
 */
public interface BoolDoubleFloat2ShortFn {
    short invoke(boolean p1, double p2, float p3);
}
