package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code char} and returns a {@code short}.
 */
public interface FloatCharChar2ShortFn {
    short invoke(float p1, char p2, char p3);
}
