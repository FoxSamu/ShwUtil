package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code byte} and returns a {@code short}.
 */
public interface IntBoolByte2ShortFn {
    short invoke(int p1, boolean p2, byte p3);
}
