package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code float} and returns a {@code short}.
 */
public interface IntBoolFloat2ShortFn {
    short invoke(int p1, boolean p2, float p3);
}
