package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code double} and returns a {@code short}.
 */
public interface CharByteDouble2ShortFn {
    short invoke(char p1, byte p2, double p3);
}
