package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code float} and an object and returns a {@code short}.
 */
public interface FloatFloatRef2ShortFn<P3> {
    short invoke(float p1, float p2, P3 p3);
}
