package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code char} and returns a {@code short}.
 */
public interface ByteCharChar2ShortFn {
    short invoke(byte p1, char p2, char p3);
}
