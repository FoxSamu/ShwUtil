package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char} and a {@code char} and returns a {@code short}.
 */
public interface CharChar2ShortFn {
    short invoke(char p1, char p2);
}
