package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long} and a {@code byte} and returns a {@code short}.
 */
public interface LongByte2ShortFn {
    short invoke(long p1, byte p2);
}
