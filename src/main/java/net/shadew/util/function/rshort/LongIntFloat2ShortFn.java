package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code float} and returns a {@code short}.
 */
public interface LongIntFloat2ShortFn {
    short invoke(long p1, int p2, float p3);
}
