package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code byte} and an {@code int} and returns a {@code short}.
 */
public interface ByteByteInt2ShortFn {
    short invoke(byte p1, byte p2, int p3);
}
