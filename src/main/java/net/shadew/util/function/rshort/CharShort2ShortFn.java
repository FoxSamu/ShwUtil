package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char} and a {@code short} and returns a {@code short}.
 */
public interface CharShort2ShortFn {
    short invoke(char p1, short p2);
}
