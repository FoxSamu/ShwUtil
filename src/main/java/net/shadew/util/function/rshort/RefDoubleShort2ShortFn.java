package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code double} and a {@code short} and returns a {@code short}.
 */
public interface RefDoubleShort2ShortFn<P1> {
    short invoke(P1 p1, double p2, short p3);
}
