package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code float} and returns a {@code short}.
 */
public interface ShortByteFloat2ShortFn {
    short invoke(short p1, byte p2, float p3);
}
