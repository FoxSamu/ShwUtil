package net.shadew.util.function.rshort;

/**
 * A function that takes an object, a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface RefBoolChar2ShortFn<P1> {
    short invoke(P1 p1, boolean p2, char p3);
}
