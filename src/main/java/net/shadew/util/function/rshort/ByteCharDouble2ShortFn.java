package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code double} and returns a {@code short}.
 */
public interface ByteCharDouble2ShortFn {
    short invoke(byte p1, char p2, double p3);
}
