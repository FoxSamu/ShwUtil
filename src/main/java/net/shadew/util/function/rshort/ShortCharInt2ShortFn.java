package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code char} and an {@code int} and returns a {@code short}.
 */
public interface ShortCharInt2ShortFn {
    short invoke(short p1, char p2, int p3);
}
