package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short} and a {@code short} and returns a {@code short}.
 */
public interface ShortShort2ShortFn {
    short invoke(short p1, short p2);
}
