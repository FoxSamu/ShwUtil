package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code short} and a {@code char} and returns a {@code short}.
 */
public interface CharShortChar2ShortFn {
    short invoke(char p1, short p2, char p3);
}
