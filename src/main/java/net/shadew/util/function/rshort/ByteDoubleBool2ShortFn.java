package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code boolean} and returns a {@code short}.
 */
public interface ByteDoubleBool2ShortFn {
    short invoke(byte p1, double p2, boolean p3);
}
