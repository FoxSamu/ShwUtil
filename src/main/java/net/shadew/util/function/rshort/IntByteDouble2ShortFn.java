package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code double} and returns a {@code short}.
 */
public interface IntByteDouble2ShortFn {
    short invoke(int p1, byte p2, double p3);
}
