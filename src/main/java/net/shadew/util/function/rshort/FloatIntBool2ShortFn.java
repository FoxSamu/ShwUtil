package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code boolean} and returns a {@code short}.
 */
public interface FloatIntBool2ShortFn {
    short invoke(float p1, int p2, boolean p3);
}
