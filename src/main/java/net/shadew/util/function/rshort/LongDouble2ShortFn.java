package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long} and a {@code double} and returns a {@code short}.
 */
public interface LongDouble2ShortFn {
    short invoke(long p1, double p2);
}
