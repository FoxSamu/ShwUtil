package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, an {@code int} and a {@code char} and returns a {@code short}.
 */
public interface IntIntChar2ShortFn {
    short invoke(int p1, int p2, char p3);
}
