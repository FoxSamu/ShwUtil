package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code float} and an {@code int} and returns a {@code short}.
 */
public interface BoolFloatInt2ShortFn {
    short invoke(boolean p1, float p2, int p3);
}
