package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float} and a {@code boolean} and returns a {@code short}.
 */
public interface FloatBool2ShortFn {
    short invoke(float p1, boolean p2);
}
