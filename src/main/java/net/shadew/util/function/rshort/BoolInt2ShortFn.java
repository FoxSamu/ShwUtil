package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean} and an {@code int} and returns a {@code short}.
 */
public interface BoolInt2ShortFn {
    short invoke(boolean p1, int p2);
}
