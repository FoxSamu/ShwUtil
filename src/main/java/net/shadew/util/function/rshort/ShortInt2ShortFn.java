package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short} and an {@code int} and returns a {@code short}.
 */
public interface ShortInt2ShortFn {
    short invoke(short p1, int p2);
}
