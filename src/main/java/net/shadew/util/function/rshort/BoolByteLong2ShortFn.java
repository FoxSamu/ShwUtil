package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code long} and returns a {@code short}.
 */
public interface BoolByteLong2ShortFn {
    short invoke(boolean p1, byte p2, long p3);
}
