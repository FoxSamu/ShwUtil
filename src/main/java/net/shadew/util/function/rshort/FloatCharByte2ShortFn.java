package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code byte} and returns a {@code short}.
 */
public interface FloatCharByte2ShortFn {
    short invoke(float p1, char p2, byte p3);
}
