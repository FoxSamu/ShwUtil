package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code boolean} and returns a {@code short}.
 */
public interface ByteBoolBool2ShortFn {
    short invoke(byte p1, boolean p2, boolean p3);
}
