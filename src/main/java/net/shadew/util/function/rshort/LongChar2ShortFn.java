package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long} and a {@code char} and returns a {@code short}.
 */
public interface LongChar2ShortFn {
    short invoke(long p1, char p2);
}
