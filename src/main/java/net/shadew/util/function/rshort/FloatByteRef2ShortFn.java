package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code byte} and an object and returns a {@code short}.
 */
public interface FloatByteRef2ShortFn<P3> {
    short invoke(float p1, byte p2, P3 p3);
}
