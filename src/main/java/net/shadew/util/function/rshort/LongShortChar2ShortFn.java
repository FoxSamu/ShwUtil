package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code char} and returns a {@code short}.
 */
public interface LongShortChar2ShortFn {
    short invoke(long p1, short p2, char p3);
}
