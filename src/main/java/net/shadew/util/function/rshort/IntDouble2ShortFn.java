package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int} and a {@code double} and returns a {@code short}.
 */
public interface IntDouble2ShortFn {
    short invoke(int p1, double p2);
}
