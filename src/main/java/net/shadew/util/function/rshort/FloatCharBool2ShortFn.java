package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code boolean} and returns a {@code short}.
 */
public interface FloatCharBool2ShortFn {
    short invoke(float p1, char p2, boolean p3);
}
