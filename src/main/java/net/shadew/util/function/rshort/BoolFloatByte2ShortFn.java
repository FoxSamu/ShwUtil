package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code byte} and returns a {@code short}.
 */
public interface BoolFloatByte2ShortFn {
    short invoke(boolean p1, float p2, byte p3);
}
