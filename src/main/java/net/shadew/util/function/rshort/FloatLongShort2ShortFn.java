package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code short} and returns a {@code short}.
 */
public interface FloatLongShort2ShortFn {
    short invoke(float p1, long p2, short p3);
}
