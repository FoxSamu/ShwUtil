package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code float} and returns a {@code short}.
 */
public interface FloatDoubleFloat2ShortFn {
    short invoke(float p1, double p2, float p3);
}
