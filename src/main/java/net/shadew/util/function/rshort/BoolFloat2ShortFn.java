package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean} and a {@code float} and returns a {@code short}.
 */
public interface BoolFloat2ShortFn {
    short invoke(boolean p1, float p2);
}
