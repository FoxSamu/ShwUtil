package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code char} and returns a {@code short}.
 */
public interface LongDoubleChar2ShortFn {
    short invoke(long p1, double p2, char p3);
}
