package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code float} and returns a {@code short}.
 */
public interface BoolByteFloat2ShortFn {
    short invoke(boolean p1, byte p2, float p3);
}
