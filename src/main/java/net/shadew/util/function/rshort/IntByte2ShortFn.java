package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int} and a {@code byte} and returns a {@code short}.
 */
public interface IntByte2ShortFn {
    short invoke(int p1, byte p2);
}
