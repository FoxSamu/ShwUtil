package net.shadew.util.function.rshort;

/**
 * A function that takes an object and a {@code char} and returns a {@code short}.
 */
public interface RefChar2ShortFn<P1> {
    short invoke(P1 p1, char p2);
}
