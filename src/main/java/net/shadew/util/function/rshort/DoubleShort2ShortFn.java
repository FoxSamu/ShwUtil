package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double} and a {@code short} and returns a {@code short}.
 */
public interface DoubleShort2ShortFn {
    short invoke(double p1, short p2);
}
