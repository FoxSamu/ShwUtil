package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code byte} and returns a {@code short}.
 */
public interface CharDoubleByte2ShortFn {
    short invoke(char p1, double p2, byte p3);
}
