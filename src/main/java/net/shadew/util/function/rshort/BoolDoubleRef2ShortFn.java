package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code double} and an object and returns a {@code short}.
 */
public interface BoolDoubleRef2ShortFn<P3> {
    short invoke(boolean p1, double p2, P3 p3);
}
