package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code char} and returns a {@code short}.
 */
public interface FloatDoubleChar2ShortFn {
    short invoke(float p1, double p2, char p3);
}
