package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code byte} and an object and returns a {@code short}.
 */
public interface ShortByteRef2ShortFn<P3> {
    short invoke(short p1, byte p2, P3 p3);
}
