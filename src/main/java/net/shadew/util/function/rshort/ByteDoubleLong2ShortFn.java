package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code long} and returns a {@code short}.
 */
public interface ByteDoubleLong2ShortFn {
    short invoke(byte p1, double p2, long p3);
}
