package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code char} and returns a {@code short}.
 */
public interface BoolDoubleChar2ShortFn {
    short invoke(boolean p1, double p2, char p3);
}
