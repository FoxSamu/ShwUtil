package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code double} and returns a {@code short}.
 */
public interface FloatIntDouble2ShortFn {
    short invoke(float p1, int p2, double p3);
}
