package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float} and a {@code short} and returns a {@code short}.
 */
public interface FloatShort2ShortFn {
    short invoke(float p1, short p2);
}
