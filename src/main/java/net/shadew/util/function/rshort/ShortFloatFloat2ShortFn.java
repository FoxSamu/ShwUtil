package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code float} and returns a {@code short}.
 */
public interface ShortFloatFloat2ShortFn {
    short invoke(short p1, float p2, float p3);
}
