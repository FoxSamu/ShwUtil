package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code byte} and returns a {@code short}.
 */
public interface IntFloatByte2ShortFn {
    short invoke(int p1, float p2, byte p3);
}
