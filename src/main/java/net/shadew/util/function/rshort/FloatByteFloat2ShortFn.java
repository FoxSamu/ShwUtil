package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code float} and returns a {@code short}.
 */
public interface FloatByteFloat2ShortFn {
    short invoke(float p1, byte p2, float p3);
}
