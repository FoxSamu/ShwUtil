package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code char} and returns a {@code short}.
 */
public interface ShortShortChar2ShortFn {
    short invoke(short p1, short p2, char p3);
}
