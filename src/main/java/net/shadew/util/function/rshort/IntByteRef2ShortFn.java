package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code byte} and an object and returns a {@code short}.
 */
public interface IntByteRef2ShortFn<P3> {
    short invoke(int p1, byte p2, P3 p3);
}
