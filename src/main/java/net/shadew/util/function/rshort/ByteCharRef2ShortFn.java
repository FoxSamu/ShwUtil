package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code char} and an object and returns a {@code short}.
 */
public interface ByteCharRef2ShortFn<P3> {
    short invoke(byte p1, char p2, P3 p3);
}
