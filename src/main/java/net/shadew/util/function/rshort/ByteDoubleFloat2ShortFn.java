package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code float} and returns a {@code short}.
 */
public interface ByteDoubleFloat2ShortFn {
    short invoke(byte p1, double p2, float p3);
}
