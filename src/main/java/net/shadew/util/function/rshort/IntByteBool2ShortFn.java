package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface IntByteBool2ShortFn {
    short invoke(int p1, byte p2, boolean p3);
}
