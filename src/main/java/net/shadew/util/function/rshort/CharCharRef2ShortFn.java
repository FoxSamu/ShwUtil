package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code char} and an object and returns a {@code short}.
 */
public interface CharCharRef2ShortFn<P3> {
    short invoke(char p1, char p2, P3 p3);
}
