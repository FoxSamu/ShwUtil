package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float} and an {@code int} and returns a {@code short}.
 */
public interface FloatInt2ShortFn {
    short invoke(float p1, int p2);
}
