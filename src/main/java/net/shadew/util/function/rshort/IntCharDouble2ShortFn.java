package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code double} and returns a {@code short}.
 */
public interface IntCharDouble2ShortFn {
    short invoke(int p1, char p2, double p3);
}
