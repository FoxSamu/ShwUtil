package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, an {@code int} and a {@code byte} and returns a {@code short}.
 */
public interface CharIntByte2ShortFn {
    short invoke(char p1, int p2, byte p3);
}
