package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code float} and a {@code char} and returns a {@code short}.
 */
public interface FloatChar2ShortFn {
    short invoke(float p1, char p2);
}
