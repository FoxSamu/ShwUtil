package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code float} and returns a {@code short}.
 */
public interface LongDoubleFloat2ShortFn {
    short invoke(long p1, double p2, float p3);
}
