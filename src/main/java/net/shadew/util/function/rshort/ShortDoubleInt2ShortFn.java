package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns a {@code short}.
 */
public interface ShortDoubleInt2ShortFn {
    short invoke(short p1, double p2, int p3);
}
