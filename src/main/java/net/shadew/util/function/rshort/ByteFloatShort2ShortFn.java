package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code short} and returns a {@code short}.
 */
public interface ByteFloatShort2ShortFn {
    short invoke(byte p1, float p2, short p3);
}
