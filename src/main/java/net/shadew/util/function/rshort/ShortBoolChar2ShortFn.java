package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code char} and returns a {@code short}.
 */
public interface ShortBoolChar2ShortFn {
    short invoke(short p1, boolean p2, char p3);
}
