package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code short} and returns a {@code short}.
 */
public interface ByteCharShort2ShortFn {
    short invoke(byte p1, char p2, short p3);
}
