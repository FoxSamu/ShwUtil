package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code char} and an object and returns a {@code short}.
 */
public interface ShortCharRef2ShortFn<P3> {
    short invoke(short p1, char p2, P3 p3);
}
