package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code float} and returns a {@code short}.
 */
public interface LongBoolFloat2ShortFn {
    short invoke(long p1, boolean p2, float p3);
}
