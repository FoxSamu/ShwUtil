package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double} and a {@code float} and returns a {@code short}.
 */
public interface DoubleFloat2ShortFn {
    short invoke(double p1, float p2);
}
