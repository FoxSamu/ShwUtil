package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code char} and an {@code int} and returns a {@code short}.
 */
public interface BoolCharInt2ShortFn {
    short invoke(boolean p1, char p2, int p3);
}
