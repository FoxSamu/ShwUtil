package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code char} and returns a {@code short}.
 */
public interface BoolCharChar2ShortFn {
    short invoke(boolean p1, char p2, char p3);
}
