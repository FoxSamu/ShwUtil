package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code short} and returns a {@code short}.
 */
public interface IntDoubleShort2ShortFn {
    short invoke(int p1, double p2, short p3);
}
