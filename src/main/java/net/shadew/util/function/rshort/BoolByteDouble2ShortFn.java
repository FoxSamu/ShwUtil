package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code double} and returns a {@code short}.
 */
public interface BoolByteDouble2ShortFn {
    short invoke(boolean p1, byte p2, double p3);
}
