package net.shadew.util.function.rshort;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code byte} and returns a {@code short}.
 */
public interface IntShortByte2ShortFn {
    short invoke(int p1, short p2, byte p3);
}
