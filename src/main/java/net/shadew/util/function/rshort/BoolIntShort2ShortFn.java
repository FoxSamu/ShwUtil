package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code boolean}, an {@code int} and a {@code short} and returns a {@code short}.
 */
public interface BoolIntShort2ShortFn {
    short invoke(boolean p1, int p2, short p3);
}
