package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code char}, a {@code boolean} and a {@code byte} and returns a {@code short}.
 */
public interface CharBoolByte2ShortFn {
    short invoke(char p1, boolean p2, byte p3);
}
