package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code boolean} and returns a {@code short}.
 */
public interface DoubleByteBool2ShortFn {
    short invoke(double p1, byte p2, boolean p3);
}
