package net.shadew.util.function.rshort;

/**
 * A function that takes an object and a {@code float} and returns a {@code short}.
 */
public interface RefFloat2ShortFn<P1> {
    short invoke(P1 p1, float p2);
}
