package net.shadew.util.function.rshort;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code float} and returns a {@code short}.
 */
public interface ShortBoolFloat2ShortFn {
    short invoke(short p1, boolean p2, float p3);
}
