package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code short} and returns a {@code byte}.
 */
public interface BoolCharShort2ByteFn {
    byte invoke(boolean p1, char p2, short p3);
}
