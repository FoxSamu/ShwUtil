package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code byte} and returns a {@code byte}.
 */
public interface FloatIntByte2ByteFn {
    byte invoke(float p1, int p2, byte p3);
}
