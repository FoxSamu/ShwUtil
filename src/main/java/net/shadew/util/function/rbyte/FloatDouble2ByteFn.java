package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float} and a {@code double} and returns a {@code byte}.
 */
public interface FloatDouble2ByteFn {
    byte invoke(float p1, double p2);
}
