package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code float} and returns a {@code byte}.
 */
public interface ShortBoolFloat2ByteFn {
    byte invoke(short p1, boolean p2, float p3);
}
