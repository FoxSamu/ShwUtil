package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double} and a {@code short} and returns a {@code byte}.
 */
public interface DoubleShort2ByteFn {
    byte invoke(double p1, short p2);
}
