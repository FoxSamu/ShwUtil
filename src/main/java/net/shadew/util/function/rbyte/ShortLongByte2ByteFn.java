package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code byte} and returns a {@code byte}.
 */
public interface ShortLongByte2ByteFn {
    byte invoke(short p1, long p2, byte p3);
}
