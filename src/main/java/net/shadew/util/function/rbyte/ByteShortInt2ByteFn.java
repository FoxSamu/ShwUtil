package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code short} and an {@code int} and returns a {@code byte}.
 */
public interface ByteShortInt2ByteFn {
    byte invoke(byte p1, short p2, int p3);
}
