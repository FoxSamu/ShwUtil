package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code long} and returns a {@code byte}.
 */
public interface ShortByteLong2ByteFn {
    byte invoke(short p1, byte p2, long p3);
}
