package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface BoolByteInt2ByteFn {
    byte invoke(boolean p1, byte p2, int p3);
}
