package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code byte} and an object and returns a {@code byte}.
 */
public interface ShortByteRef2ByteFn<P3> {
    byte invoke(short p1, byte p2, P3 p3);
}
