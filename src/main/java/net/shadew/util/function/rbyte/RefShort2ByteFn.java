package net.shadew.util.function.rbyte;

/**
 * A function that takes an object and a {@code short} and returns a {@code byte}.
 */
public interface RefShort2ByteFn<P1> {
    byte invoke(P1 p1, short p2);
}
