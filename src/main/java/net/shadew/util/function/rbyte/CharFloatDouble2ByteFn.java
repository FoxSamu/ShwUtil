package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code float} and a {@code double} and returns a {@code byte}.
 */
public interface CharFloatDouble2ByteFn {
    byte invoke(char p1, float p2, double p3);
}
