package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code byte} and returns a {@code byte}.
 */
public interface ByteDoubleByte2ByteFn {
    byte invoke(byte p1, double p2, byte p3);
}
