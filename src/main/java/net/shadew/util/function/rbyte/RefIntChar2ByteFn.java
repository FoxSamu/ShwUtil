package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, an {@code int} and a {@code char} and returns a {@code byte}.
 */
public interface RefIntChar2ByteFn<P1> {
    byte invoke(P1 p1, int p2, char p3);
}
