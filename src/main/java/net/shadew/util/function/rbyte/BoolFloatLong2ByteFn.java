package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code long} and returns a {@code byte}.
 */
public interface BoolFloatLong2ByteFn {
    byte invoke(Bool p1, Float p2, Long p3);
}
