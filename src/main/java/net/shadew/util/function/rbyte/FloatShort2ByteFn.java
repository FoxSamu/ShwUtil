package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float} and a {@code short} and returns a {@code byte}.
 */
public interface FloatShort2ByteFn {
    byte invoke(float p1, short p2);
}
