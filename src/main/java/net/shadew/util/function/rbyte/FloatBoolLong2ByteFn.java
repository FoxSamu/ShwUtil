package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code long} and returns a {@code byte}.
 */
public interface FloatBoolLong2ByteFn {
    byte invoke(float p1, boolean p2, long p3);
}
