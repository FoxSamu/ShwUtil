package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code boolean} and returns a {@code byte}.
 */
public interface LongShortBool2ByteFn {
    byte invoke(long p1, short p2, boolean p3);
}
