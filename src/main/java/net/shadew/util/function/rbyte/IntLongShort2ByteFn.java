package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code long} and a {@code short} and returns a {@code byte}.
 */
public interface IntLongShort2ByteFn {
    byte invoke(Int p1, Long p2, Short p3);
}
