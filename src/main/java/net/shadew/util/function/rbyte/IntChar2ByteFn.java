package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int} and a {@code char} and returns a {@code byte}.
 */
public interface IntChar2ByteFn {
    byte invoke(int p1, char p2);
}
