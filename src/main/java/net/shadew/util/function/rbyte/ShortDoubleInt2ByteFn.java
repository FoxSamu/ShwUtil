package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns a {@code byte}.
 */
public interface ShortDoubleInt2ByteFn {
    byte invoke(short p1, double p2, int p3);
}
