package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface ByteBoolShort2ByteFn {
    byte invoke(byte p1, boolean p2, short p3);
}
