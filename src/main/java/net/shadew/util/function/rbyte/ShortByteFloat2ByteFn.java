package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code float} and returns a {@code byte}.
 */
public interface ShortByteFloat2ByteFn {
    byte invoke(short p1, byte p2, float p3);
}
