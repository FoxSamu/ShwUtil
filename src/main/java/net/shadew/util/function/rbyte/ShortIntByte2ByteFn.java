package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code byte} and returns a {@code byte}.
 */
public interface ShortIntByte2ByteFn {
    byte invoke(short p1, int p2, byte p3);
}
