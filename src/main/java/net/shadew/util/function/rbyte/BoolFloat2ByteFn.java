package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean} and a {@code float} and returns a {@code byte}.
 */
public interface BoolFloat2ByteFn {
    byte invoke(boolean p1, float p2);
}
