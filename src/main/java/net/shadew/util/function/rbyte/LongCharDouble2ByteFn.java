package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code double} and returns a {@code byte}.
 */
public interface LongCharDouble2ByteFn {
    byte invoke(long p1, char p2, double p3);
}
