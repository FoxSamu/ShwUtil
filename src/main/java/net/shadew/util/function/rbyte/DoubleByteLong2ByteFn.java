package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code long} and returns a {@code byte}.
 */
public interface DoubleByteLong2ByteFn {
    byte invoke(double p1, byte p2, long p3);
}
