package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code byte} and returns a {@code byte}.
 */
public interface LongShortByte2ByteFn {
    byte invoke(long p1, short p2, byte p3);
}
