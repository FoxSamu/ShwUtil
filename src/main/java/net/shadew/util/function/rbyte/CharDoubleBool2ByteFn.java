package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code boolean} and returns a {@code byte}.
 */
public interface CharDoubleBool2ByteFn {
    byte invoke(char p1, double p2, boolean p3);
}
