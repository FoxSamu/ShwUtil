package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code double} and returns a {@code byte}.
 */
public interface ShortBoolDouble2ByteFn {
    byte invoke(short p1, boolean p2, double p3);
}
