package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns a {@code byte}.
 */
public interface ByteBoolChar2ByteFn {
    byte invoke(byte p1, boolean p2, char p3);
}
