package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code float} and returns a {@code byte}.
 */
public interface FloatBoolFloat2ByteFn {
    byte invoke(float p1, boolean p2, float p3);
}
