package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code float} and returns a {@code byte}.
 */
public interface LongDoubleFloat2ByteFn {
    byte invoke(long p1, double p2, float p3);
}
