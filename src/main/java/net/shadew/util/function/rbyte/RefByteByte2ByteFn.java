package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code byte} and a {@code byte} and returns a {@code byte}.
 */
public interface RefByteByte2ByteFn<P1> {
    byte invoke(P1 p1, byte p2, byte p3);
}
