package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code float} and returns a {@code byte}.
 */
public interface BoolCharFloat2ByteFn {
    byte invoke(boolean p1, char p2, float p3);
}
