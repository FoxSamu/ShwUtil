package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code float} and returns a {@code byte}.
 */
public interface CharByteFloat2ByteFn {
    byte invoke(char p1, byte p2, float p3);
}
