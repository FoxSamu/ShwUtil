package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code byte} and returns a {@code byte}.
 */
public interface BoolShortByte2ByteFn {
    byte invoke(Bool p1, Short p2, Byte p3);
}
