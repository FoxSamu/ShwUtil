package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an {@code int} and returns a {@code byte}.
 */
public interface BoolBoolInt2ByteFn {
    byte invoke(boolean p1, boolean p2, int p3);
}
