package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code boolean} and an object and returns a {@code byte}.
 */
public interface FloatBoolRef2ByteFn<P3> {
    byte invoke(float p1, boolean p2, P3 p3);
}
