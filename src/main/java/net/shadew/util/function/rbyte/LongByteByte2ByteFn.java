package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code byte} and returns a {@code byte}.
 */
public interface LongByteByte2ByteFn {
    byte invoke(long p1, byte p2, byte p3);
}
