package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code short} and returns a {@code byte}.
 */
public interface ByteDoubleShort2ByteFn {
    byte invoke(byte p1, double p2, short p3);
}
