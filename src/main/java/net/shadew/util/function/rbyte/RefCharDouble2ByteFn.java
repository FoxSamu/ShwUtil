package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code char} and a {@code double} and returns a {@code byte}.
 */
public interface RefCharDouble2ByteFn<P1> {
    byte invoke(P1 p1, char p2, double p3);
}
