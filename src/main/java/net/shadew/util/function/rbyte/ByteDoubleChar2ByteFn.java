package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code char} and returns a {@code byte}.
 */
public interface ByteDoubleChar2ByteFn {
    byte invoke(byte p1, double p2, char p3);
}
