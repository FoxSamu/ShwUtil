package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code char} and an {@code int} and returns a {@code byte}.
 */
public interface LongCharInt2ByteFn {
    byte invoke(long p1, char p2, int p3);
}
