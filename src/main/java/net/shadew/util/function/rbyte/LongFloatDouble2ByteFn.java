package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code double} and returns a {@code byte}.
 */
public interface LongFloatDouble2ByteFn {
    byte invoke(long p1, float p2, double p3);
}
