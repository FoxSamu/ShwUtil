package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code float} and returns a {@code byte}.
 */
public interface BoolDoubleFloat2ByteFn {
    byte invoke(boolean p1, double p2, float p3);
}
