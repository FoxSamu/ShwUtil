package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code boolean} and returns a {@code byte}.
 */
public interface FloatBoolBool2ByteFn {
    byte invoke(float p1, boolean p2, boolean p3);
}
