package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code boolean} and returns a {@code byte}.
 */
public interface FloatIntBool2ByteFn {
    byte invoke(float p1, int p2, boolean p3);
}
