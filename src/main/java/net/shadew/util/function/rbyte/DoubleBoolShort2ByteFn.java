package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface DoubleBoolShort2ByteFn {
    byte invoke(double p1, boolean p2, short p3);
}
