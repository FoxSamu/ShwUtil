package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code byte} and returns a {@code byte}.
 */
public interface BoolCharByte2ByteFn {
    byte invoke(boolean p1, char p2, byte p3);
}
