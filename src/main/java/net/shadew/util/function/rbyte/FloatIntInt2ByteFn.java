package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, an {@code int} and an {@code int} and returns a {@code byte}.
 */
public interface FloatIntInt2ByteFn {
    byte invoke(float p1, int p2, int p3);
}
