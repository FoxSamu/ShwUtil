package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code float} and returns a {@code byte}.
 */
public interface FloatIntFloat2ByteFn {
    byte invoke(float p1, int p2, float p3);
}
