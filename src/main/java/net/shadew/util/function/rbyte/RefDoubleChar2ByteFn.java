package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code double} and a {@code char} and returns a {@code byte}.
 */
public interface RefDoubleChar2ByteFn<P1> {
    byte invoke(P1 p1, double p2, char p3);
}
