package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface ByteByteChar2ByteFn {
    byte invoke(byte p1, byte p2, char p3);
}
