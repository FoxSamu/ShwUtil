package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code double} and returns a {@code byte}.
 */
public interface DoubleShortDouble2ByteFn {
    byte invoke(double p1, short p2, double p3);
}
