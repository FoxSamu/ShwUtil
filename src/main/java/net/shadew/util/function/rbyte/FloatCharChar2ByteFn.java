package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code char} and returns a {@code byte}.
 */
public interface FloatCharChar2ByteFn {
    byte invoke(float p1, char p2, char p3);
}
