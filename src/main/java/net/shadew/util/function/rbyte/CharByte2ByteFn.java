package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char} and a {@code byte} and returns a {@code byte}.
 */
public interface CharByte2ByteFn {
    byte invoke(char p1, byte p2);
}
