package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code double} and returns a {@code byte}.
 */
public interface ShortLongDouble2ByteFn {
    byte invoke(short p1, long p2, double p3);
}
