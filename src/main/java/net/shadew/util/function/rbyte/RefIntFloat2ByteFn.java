package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, an {@code int} and a {@code float} and returns a {@code byte}.
 */
public interface RefIntFloat2ByteFn<P1> {
    byte invoke(P1 p1, int p2, float p3);
}
