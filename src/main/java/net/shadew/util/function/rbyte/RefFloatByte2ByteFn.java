package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code float} and a {@code byte} and returns a {@code byte}.
 */
public interface RefFloatByte2ByteFn<P1> {
    byte invoke(P1 p1, float p2, byte p3);
}
