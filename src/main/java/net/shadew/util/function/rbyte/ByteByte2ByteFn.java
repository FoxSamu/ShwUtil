package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte} and a {@code byte} and returns a {@code byte}.
 */
public interface ByteByte2ByteFn {
    byte invoke(byte p1, byte p2);
}
