package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface IntByteInt2ByteFn {
    byte invoke(int p1, byte p2, int p3);
}
