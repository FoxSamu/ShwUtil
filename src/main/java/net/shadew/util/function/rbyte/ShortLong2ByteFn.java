package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short} and a {@code long} and returns a {@code byte}.
 */
public interface ShortLong2ByteFn {
    byte invoke(short p1, long p2);
}
