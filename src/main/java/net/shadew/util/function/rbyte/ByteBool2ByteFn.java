package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns a {@code byte}.
 */
public interface ByteBool2ByteFn {
    byte invoke(byte p1, boolean p2);
}
