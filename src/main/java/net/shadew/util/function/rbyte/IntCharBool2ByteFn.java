package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code boolean} and returns a {@code byte}.
 */
public interface IntCharBool2ByteFn {
    byte invoke(int p1, char p2, boolean p3);
}
