package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code float} and returns a {@code byte}.
 */
public interface ShortShortFloat2ByteFn {
    byte invoke(short p1, short p2, float p3);
}
