package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code double} and returns a {@code byte}.
 */
public interface BoolDoubleDouble2ByteFn {
    byte invoke(boolean p1, double p2, double p3);
}
