package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code byte} and returns a {@code byte}.
 */
public interface CharByteByte2ByteFn {
    byte invoke(char p1, byte p2, byte p3);
}
