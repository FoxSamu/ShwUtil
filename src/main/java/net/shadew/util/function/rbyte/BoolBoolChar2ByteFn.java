package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code char} and returns a {@code byte}.
 */
public interface BoolBoolChar2ByteFn {
    byte invoke(boolean p1, boolean p2, char p3);
}
