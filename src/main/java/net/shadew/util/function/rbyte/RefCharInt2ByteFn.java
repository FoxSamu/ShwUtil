package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code char} and an {@code int} and returns a {@code byte}.
 */
public interface RefCharInt2ByteFn<P1> {
    byte invoke(P1 p1, char p2, int p3);
}
