package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code char} and returns a {@code byte}.
 */
public interface LongIntChar2ByteFn {
    byte invoke(long p1, int p2, char p3);
}
