package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code double} and an {@code int} and returns a {@code byte}.
 */
public interface RefDoubleInt2ByteFn<P1> {
    byte invoke(P1 p1, double p2, int p3);
}
