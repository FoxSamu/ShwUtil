package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code short} and returns a {@code byte}.
 */
public interface IntByteShort2ByteFn {
    byte invoke(int p1, byte p2, short p3);
}
