package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, an {@code int} and a {@code boolean} and returns a {@code byte}.
 */
public interface IntIntBool2ByteFn {
    byte invoke(int p1, int p2, boolean p3);
}
