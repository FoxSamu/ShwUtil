package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an object and returns a {@code byte}.
 */
public interface ByteBoolRef2ByteFn<P3> {
    byte invoke(byte p1, boolean p2, P3 p3);
}
