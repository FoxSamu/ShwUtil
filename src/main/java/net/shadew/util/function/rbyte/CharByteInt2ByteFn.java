package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface CharByteInt2ByteFn {
    byte invoke(char p1, byte p2, int p3);
}
