package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code double} and returns a {@code byte}.
 */
public interface FloatDoubleDouble2ByteFn {
    byte invoke(float p1, double p2, double p3);
}
