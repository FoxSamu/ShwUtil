package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code double} and returns a {@code byte}.
 */
public interface LongByteDouble2ByteFn {
    byte invoke(long p1, byte p2, double p3);
}
