package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface RefByteChar2ByteFn<P1> {
    byte invoke(P1 p1, byte p2, char p3);
}
