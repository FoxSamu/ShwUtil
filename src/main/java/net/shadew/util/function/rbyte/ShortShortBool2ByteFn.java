package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code boolean} and returns a {@code byte}.
 */
public interface ShortShortBool2ByteFn {
    byte invoke(short p1, short p2, boolean p3);
}
