package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code double} and returns a {@code byte}.
 */
public interface CharByteDouble2ByteFn {
    byte invoke(char p1, byte p2, double p3);
}
