package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code byte} and returns a {@code byte}.
 */
public interface ShortShortByte2ByteFn {
    byte invoke(short p1, short p2, byte p3);
}
