package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double} and a {@code long} and returns a {@code byte}.
 */
public interface DoubleLong2ByteFn {
    byte invoke(double p1, long p2);
}
