package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, an {@code int} and a {@code short} and returns a {@code byte}.
 */
public interface BoolIntShort2ByteFn {
    byte invoke(boolean p1, int p2, short p3);
}
