package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code byte} and returns a {@code byte}.
 */
public interface FloatFloatByte2ByteFn {
    byte invoke(float p1, float p2, byte p3);
}
