package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code float} and returns a {@code byte}.
 */
public interface BoolBoolFloat2ByteFn {
    byte invoke(boolean p1, boolean p2, float p3);
}
