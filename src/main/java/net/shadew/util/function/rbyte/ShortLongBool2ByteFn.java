package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code boolean} and returns a {@code byte}.
 */
public interface ShortLongBool2ByteFn {
    byte invoke(short p1, long p2, boolean p3);
}
