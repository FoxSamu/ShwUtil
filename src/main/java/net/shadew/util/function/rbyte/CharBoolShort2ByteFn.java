package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface CharBoolShort2ByteFn {
    byte invoke(char p1, boolean p2, short p3);
}
