package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char} and a {@code boolean} and returns a {@code byte}.
 */
public interface CharBool2ByteFn {
    byte invoke(char p1, boolean p2);
}
