package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code char} and returns a {@code byte}.
 */
public interface LongFloatChar2ByteFn {
    byte invoke(long p1, float p2, char p3);
}
