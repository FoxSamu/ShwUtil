package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code double} and returns a {@code byte}.
 */
public interface ByteCharDouble2ByteFn {
    byte invoke(byte p1, char p2, double p3);
}
