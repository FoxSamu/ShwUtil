package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code char} and returns a {@code byte}.
 */
public interface ShortCharChar2ByteFn {
    byte invoke(short p1, char p2, char p3);
}
