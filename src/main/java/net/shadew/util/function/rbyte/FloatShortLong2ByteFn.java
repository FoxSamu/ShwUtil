package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code long} and returns a {@code byte}.
 */
public interface FloatShortLong2ByteFn {
    byte invoke(float p1, short p2, long p3);
}
