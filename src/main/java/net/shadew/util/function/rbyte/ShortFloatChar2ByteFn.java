package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code char} and returns a {@code byte}.
 */
public interface ShortFloatChar2ByteFn {
    byte invoke(short p1, float p2, char p3);
}
