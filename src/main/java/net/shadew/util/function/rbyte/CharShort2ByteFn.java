package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char} and a {@code short} and returns a {@code byte}.
 */
public interface CharShort2ByteFn {
    byte invoke(char p1, short p2);
}
