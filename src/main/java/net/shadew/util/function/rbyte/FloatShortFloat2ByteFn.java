package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code float} and returns a {@code byte}.
 */
public interface FloatShortFloat2ByteFn {
    byte invoke(float p1, short p2, float p3);
}
