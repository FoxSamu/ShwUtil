package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char} and a {@code long} and returns a {@code byte}.
 */
public interface CharLong2ByteFn {
    byte invoke(char p1, long p2);
}
