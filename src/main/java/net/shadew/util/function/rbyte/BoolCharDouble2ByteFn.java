package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code double} and returns a {@code byte}.
 */
public interface BoolCharDouble2ByteFn {
    byte invoke(boolean p1, char p2, double p3);
}
