package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code boolean} and an {@code int} and returns a {@code byte}.
 */
public interface LongBoolInt2ByteFn {
    byte invoke(long p1, boolean p2, int p3);
}
