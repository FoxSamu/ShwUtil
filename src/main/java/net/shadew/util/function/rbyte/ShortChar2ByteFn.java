package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short} and a {@code char} and returns a {@code byte}.
 */
public interface ShortChar2ByteFn {
    byte invoke(short p1, char p2);
}
