package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code char} and returns a {@code byte}.
 */
public interface ShortShortChar2ByteFn {
    byte invoke(short p1, short p2, char p3);
}
