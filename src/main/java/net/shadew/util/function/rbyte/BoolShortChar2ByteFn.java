package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code char} and returns a {@code byte}.
 */
public interface BoolShortChar2ByteFn {
    byte invoke(boolean p1, short p2, char p3);
}
