package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface BoolBoolShort2ByteFn {
    byte invoke(boolean p1, boolean p2, short p3);
}
