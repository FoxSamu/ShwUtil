package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns a {@code byte}.
 */
public interface ByteByteBool2ByteFn {
    byte invoke(byte p1, byte p2, boolean p3);
}
