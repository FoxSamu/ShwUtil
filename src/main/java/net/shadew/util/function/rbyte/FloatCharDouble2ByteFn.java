package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code double} and returns a {@code byte}.
 */
public interface FloatCharDouble2ByteFn {
    byte invoke(float p1, char p2, double p3);
}
