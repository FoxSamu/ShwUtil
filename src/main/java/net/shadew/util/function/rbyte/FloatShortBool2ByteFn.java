package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code boolean} and returns a {@code byte}.
 */
public interface FloatShortBool2ByteFn {
    byte invoke(float p1, short p2, boolean p3);
}
