package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code byte} and returns a {@code byte}.
 */
public interface FloatCharByte2ByteFn {
    byte invoke(float p1, char p2, byte p3);
}
