package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float} and a {@code byte} and returns a {@code byte}.
 */
public interface FloatByte2ByteFn {
    byte invoke(float p1, byte p2);
}
