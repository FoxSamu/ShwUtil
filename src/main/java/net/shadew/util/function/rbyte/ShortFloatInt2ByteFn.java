package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code float} and an {@code int} and returns a {@code byte}.
 */
public interface ShortFloatInt2ByteFn {
    byte invoke(short p1, float p2, int p3);
}
