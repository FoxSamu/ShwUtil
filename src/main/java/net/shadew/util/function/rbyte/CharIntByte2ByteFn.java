package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, an {@code int} and a {@code byte} and returns a {@code byte}.
 */
public interface CharIntByte2ByteFn {
    byte invoke(char p1, int p2, byte p3);
}
