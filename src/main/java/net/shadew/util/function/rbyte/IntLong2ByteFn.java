package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int} and a {@code long} and returns a {@code byte}.
 */
public interface IntLong2ByteFn {
    byte invoke(int p1, long p2);
}
