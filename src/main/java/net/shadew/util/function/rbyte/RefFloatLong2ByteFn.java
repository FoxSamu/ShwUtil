package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code float} and a {@code long} and returns a {@code byte}.
 */
public interface RefFloatLong2ByteFn<P1> {
    byte invoke(P1 p1, float p2, long p3);
}
