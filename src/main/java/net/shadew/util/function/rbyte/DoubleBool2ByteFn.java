package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double} and a {@code boolean} and returns a {@code byte}.
 */
public interface DoubleBool2ByteFn {
    byte invoke(double p1, boolean p2);
}
