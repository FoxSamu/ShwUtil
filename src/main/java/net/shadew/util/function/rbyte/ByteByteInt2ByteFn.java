package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface ByteByteInt2ByteFn {
    byte invoke(byte p1, byte p2, int p3);
}
