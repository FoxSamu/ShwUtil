package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code double} and a {@code byte} and returns a {@code byte}.
 */
public interface RefDoubleByte2ByteFn<P1> {
    byte invoke(P1 p1, double p2, byte p3);
}
