package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns a {@code byte}.
 */
public interface ByteByteDouble2ByteFn {
    byte invoke(byte p1, byte p2, double p3);
}
