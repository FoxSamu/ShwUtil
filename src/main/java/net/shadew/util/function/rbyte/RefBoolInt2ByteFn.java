package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code boolean} and an {@code int} and returns a {@code byte}.
 */
public interface RefBoolInt2ByteFn<P1> {
    byte invoke(P1 p1, boolean p2, int p3);
}
