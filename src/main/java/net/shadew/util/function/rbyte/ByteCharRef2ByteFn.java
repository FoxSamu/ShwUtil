package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code char} and an object and returns a {@code byte}.
 */
public interface ByteCharRef2ByteFn<P3> {
    byte invoke(byte p1, char p2, P3 p3);
}
