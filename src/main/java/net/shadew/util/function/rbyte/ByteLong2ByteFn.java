package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte} and a {@code long} and returns a {@code byte}.
 */
public interface ByteLong2ByteFn {
    byte invoke(byte p1, long p2);
}
