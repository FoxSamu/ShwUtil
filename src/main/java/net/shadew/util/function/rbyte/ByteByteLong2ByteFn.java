package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code long} and returns a {@code byte}.
 */
public interface ByteByteLong2ByteFn {
    byte invoke(byte p1, byte p2, long p3);
}
