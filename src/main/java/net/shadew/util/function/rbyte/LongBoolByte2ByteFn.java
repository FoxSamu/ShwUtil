package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code byte} and returns a {@code byte}.
 */
public interface LongBoolByte2ByteFn {
    byte invoke(long p1, boolean p2, byte p3);
}
