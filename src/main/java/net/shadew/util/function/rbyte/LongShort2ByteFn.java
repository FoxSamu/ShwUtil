package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long} and a {@code short} and returns a {@code byte}.
 */
public interface LongShort2ByteFn {
    byte invoke(long p1, short p2);
}
