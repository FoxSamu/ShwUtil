package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code double} and returns a {@code byte}.
 */
public interface IntDoubleDouble2ByteFn {
    byte invoke(int p1, double p2, double p3);
}
