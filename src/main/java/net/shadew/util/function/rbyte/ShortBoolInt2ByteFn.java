package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code boolean} and an {@code int} and returns a {@code byte}.
 */
public interface ShortBoolInt2ByteFn {
    byte invoke(short p1, boolean p2, int p3);
}
