package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short} and an {@code int} and returns a {@code byte}.
 */
public interface ShortInt2ByteFn {
    byte invoke(short p1, int p2);
}
