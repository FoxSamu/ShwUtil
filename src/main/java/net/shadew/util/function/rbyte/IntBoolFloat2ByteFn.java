package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code float} and returns a {@code byte}.
 */
public interface IntBoolFloat2ByteFn {
    byte invoke(int p1, boolean p2, float p3);
}
