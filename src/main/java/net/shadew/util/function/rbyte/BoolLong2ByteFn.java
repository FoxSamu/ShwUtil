package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean} and a {@code long} and returns a {@code byte}.
 */
public interface BoolLong2ByteFn {
    byte invoke(boolean p1, long p2);
}
