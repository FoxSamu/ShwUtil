package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code short} and returns a {@code byte}.
 */
public interface FloatFloatShort2ByteFn {
    byte invoke(float p1, float p2, short p3);
}
