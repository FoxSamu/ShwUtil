package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code char} and a {@code double} and returns a {@code byte}.
 */
public interface CharCharDouble2ByteFn {
    byte invoke(char p1, char p2, double p3);
}
