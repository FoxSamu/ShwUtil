package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double} and a {@code byte} and returns a {@code byte}.
 */
public interface DoubleByte2ByteFn {
    byte invoke(Double p1, Byte p2);
}
