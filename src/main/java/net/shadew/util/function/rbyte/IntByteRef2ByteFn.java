package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code byte} and an object and returns a {@code byte}.
 */
public interface IntByteRef2ByteFn<P3> {
    byte invoke(int p1, byte p2, P3 p3);
}
