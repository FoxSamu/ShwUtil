package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code float} and returns a {@code byte}.
 */
public interface ShortCharFloat2ByteFn {
    byte invoke(short p1, char p2, float p3);
}
