package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte} and a {@code short} and returns a {@code byte}.
 */
public interface ByteShort2ByteFn {
    byte invoke(byte p1, short p2);
}
