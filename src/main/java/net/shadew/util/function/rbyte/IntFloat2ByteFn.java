package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int} and a {@code float} and returns a {@code byte}.
 */
public interface IntFloat2ByteFn {
    byte invoke(int p1, float p2);
}
