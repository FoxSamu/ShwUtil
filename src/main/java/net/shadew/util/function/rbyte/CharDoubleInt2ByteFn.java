package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code double} and an {@code int} and returns a {@code byte}.
 */
public interface CharDoubleInt2ByteFn {
    byte invoke(char p1, double p2, int p3);
}
