package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns a {@code byte}.
 */
public interface ShortCharByte2ByteFn {
    byte invoke(short p1, char p2, byte p3);
}
