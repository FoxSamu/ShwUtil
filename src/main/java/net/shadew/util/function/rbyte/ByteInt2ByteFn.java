package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface ByteInt2ByteFn {
    byte invoke(byte p1, int p2);
}
