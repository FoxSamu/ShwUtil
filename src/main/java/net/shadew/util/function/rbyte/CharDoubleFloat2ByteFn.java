package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code float} and returns a {@code byte}.
 */
public interface CharDoubleFloat2ByteFn {
    byte invoke(char p1, double p2, float p3);
}
