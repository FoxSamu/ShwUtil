package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code char} and returns a {@code byte}.
 */
public interface DoubleBoolChar2ByteFn {
    byte invoke(double p1, boolean p2, char p3);
}
