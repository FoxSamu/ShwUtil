package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code double} and returns a {@code byte}.
 */
public interface BoolShortDouble2ByteFn {
    byte invoke(boolean p1, short p2, double p3);
}
