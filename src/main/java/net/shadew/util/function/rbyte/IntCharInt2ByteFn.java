package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code char} and an {@code int} and returns a {@code byte}.
 */
public interface IntCharInt2ByteFn {
    byte invoke(int p1, char p2, int p3);
}
