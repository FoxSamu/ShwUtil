package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code short} and returns a {@code byte}.
 */
public interface ShortLongShort2ByteFn {
    byte invoke(short p1, long p2, short p3);
}
