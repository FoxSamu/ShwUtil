package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code short} and an object and returns a {@code byte}.
 */
public interface BoolShortRef2ByteFn<P3> {
    byte invoke(boolean p1, short p2, P3 p3);
}
