package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code double} and returns a {@code byte}.
 */
public interface BoolByteDouble2ByteFn {
    byte invoke(boolean p1, byte p2, double p3);
}
