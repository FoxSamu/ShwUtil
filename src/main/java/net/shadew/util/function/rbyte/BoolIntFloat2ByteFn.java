package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, an {@code int} and a {@code float} and returns a {@code byte}.
 */
public interface BoolIntFloat2ByteFn {
    byte invoke(boolean p1, int p2, float p3);
}
