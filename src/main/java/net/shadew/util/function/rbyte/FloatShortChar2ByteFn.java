package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code char} and returns a {@code byte}.
 */
public interface FloatShortChar2ByteFn {
    byte invoke(float p1, short p2, char p3);
}
