package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double} and a {@code double} and returns a {@code byte}.
 */
public interface DoubleDouble2ByteFn {
    byte invoke(double p1, double p2);
}
