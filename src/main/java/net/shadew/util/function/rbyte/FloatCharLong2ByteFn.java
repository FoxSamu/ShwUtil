package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code long} and returns a {@code byte}.
 */
public interface FloatCharLong2ByteFn {
    byte invoke(float p1, char p2, long p3);
}
