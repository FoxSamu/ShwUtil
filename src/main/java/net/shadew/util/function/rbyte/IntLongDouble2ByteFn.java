package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code long} and a {@code double} and returns a {@code byte}.
 */
public interface IntLongDouble2ByteFn {
    byte invoke(int p1, long p2, double p3);
}
