package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code byte} and returns a {@code byte}.
 */
public interface BoolByteByte2ByteFn {
    byte invoke(boolean p1, byte p2, byte p3);
}
