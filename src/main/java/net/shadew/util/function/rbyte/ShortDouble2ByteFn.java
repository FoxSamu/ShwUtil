package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short} and a {@code double} and returns a {@code byte}.
 */
public interface ShortDouble2ByteFn {
    byte invoke(short p1, double p2);
}
