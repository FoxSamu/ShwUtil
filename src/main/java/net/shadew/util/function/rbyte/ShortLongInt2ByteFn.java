package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code long} and an {@code int} and returns a {@code byte}.
 */
public interface ShortLongInt2ByteFn {
    byte invoke(short p1, long p2, int p3);
}
