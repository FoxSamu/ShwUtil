package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code byte} and returns a {@code byte}.
 */
public interface IntCharByte2ByteFn {
    byte invoke(int p1, char p2, byte p3);
}
