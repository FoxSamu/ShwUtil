package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int} and a {@code short} and returns a {@code byte}.
 */
public interface IntShort2ByteFn {
    byte invoke(int p1, short p2);
}
