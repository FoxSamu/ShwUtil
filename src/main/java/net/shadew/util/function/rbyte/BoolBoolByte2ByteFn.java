package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code byte} and returns a {@code byte}.
 */
public interface BoolBoolByte2ByteFn {
    byte invoke(boolean p1, boolean p2, byte p3);
}
