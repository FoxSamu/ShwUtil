package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code short} and an {@code int} and returns a {@code byte}.
 */
public interface LongShortInt2ByteFn {
    byte invoke(long p1, short p2, int p3);
}
