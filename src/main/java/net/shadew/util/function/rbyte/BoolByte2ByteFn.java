package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean} and a {@code byte} and returns a {@code byte}.
 */
public interface BoolByte2ByteFn {
    byte invoke(boolean p1, byte p2);
}
