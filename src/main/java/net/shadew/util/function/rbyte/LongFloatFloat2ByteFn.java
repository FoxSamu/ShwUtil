package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code float} and returns a {@code byte}.
 */
public interface LongFloatFloat2ByteFn {
    byte invoke(long p1, float p2, float p3);
}
