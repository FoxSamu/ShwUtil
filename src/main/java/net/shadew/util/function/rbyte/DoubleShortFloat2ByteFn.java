package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code float} and returns a {@code byte}.
 */
public interface DoubleShortFloat2ByteFn {
    byte invoke(double p1, short p2, float p3);
}
