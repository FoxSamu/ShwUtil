package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface BoolByteChar2ByteFn {
    byte invoke(boolean p1, byte p2, char p3);
}
