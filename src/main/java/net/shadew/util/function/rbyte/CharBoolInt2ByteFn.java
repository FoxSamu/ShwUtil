package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code boolean} and an {@code int} and returns a {@code byte}.
 */
public interface CharBoolInt2ByteFn {
    byte invoke(char p1, boolean p2, int p3);
}
