package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code byte} and returns a {@code byte}.
 */
public interface ShortDoubleByte2ByteFn {
    byte invoke(short p1, double p2, byte p3);
}
