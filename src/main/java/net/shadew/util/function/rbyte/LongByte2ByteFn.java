package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long} and a {@code byte} and returns a {@code byte}.
 */
public interface LongByte2ByteFn {
    byte invoke(long p1, byte p2);
}
