package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code float} and returns a {@code byte}.
 */
public interface DoubleBoolFloat2ByteFn {
    byte invoke(double p1, boolean p2, float p3);
}
