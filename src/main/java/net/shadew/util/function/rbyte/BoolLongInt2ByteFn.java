package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code long} and an {@code int} and returns a {@code byte}.
 */
public interface BoolLongInt2ByteFn {
    byte invoke(boolean p1, long p2, int p3);
}
