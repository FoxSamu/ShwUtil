package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code short} and an {@code int} and returns a {@code byte}.
 */
public interface BoolShortInt2ByteFn {
    byte invoke(boolean p1, short p2, int p3);
}
