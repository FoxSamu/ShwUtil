package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double} and an {@code int} and returns a {@code byte}.
 */
public interface DoubleInt2ByteFn {
    byte invoke(double p1, int p2);
}
