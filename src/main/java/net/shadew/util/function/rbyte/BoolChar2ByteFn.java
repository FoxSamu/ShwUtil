package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns a {@code byte}.
 */
public interface BoolChar2ByteFn {
    byte invoke(boolean p1, char p2);
}
