package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code char} and returns a {@code byte}.
 */
public interface ShortBoolChar2ByteFn {
    byte invoke(short p1, boolean p2, char p3);
}
