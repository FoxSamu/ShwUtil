package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code float} and an {@code int} and returns a {@code byte}.
 */
public interface RefFloatInt2ByteFn<P1> {
    byte invoke(P1 p1, float p2, int p3);
}
