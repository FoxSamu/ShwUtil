package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code char} and returns a {@code byte}.
 */
public interface IntShortChar2ByteFn {
    byte invoke(int p1, short p2, char p3);
}
