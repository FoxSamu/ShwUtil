package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code double} and an {@code int} and returns a {@code byte}.
 */
public interface FloatDoubleInt2ByteFn {
    byte invoke(Float p1, Double p2, Int p3);
}
