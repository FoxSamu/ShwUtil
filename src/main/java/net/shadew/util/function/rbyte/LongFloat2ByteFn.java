package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long} and a {@code float} and returns a {@code byte}.
 */
public interface LongFloat2ByteFn {
    byte invoke(long p1, float p2);
}
