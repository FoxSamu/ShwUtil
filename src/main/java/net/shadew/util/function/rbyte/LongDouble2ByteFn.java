package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long} and a {@code double} and returns a {@code byte}.
 */
public interface LongDouble2ByteFn {
    byte invoke(long p1, double p2);
}
