package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code short} and an {@code int} and returns a {@code byte}.
 */
public interface FloatShortInt2ByteFn {
    byte invoke(float p1, short p2, int p3);
}
