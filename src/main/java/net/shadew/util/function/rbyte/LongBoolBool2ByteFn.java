package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code boolean} and returns a {@code byte}.
 */
public interface LongBoolBool2ByteFn {
    byte invoke(long p1, boolean p2, boolean p3);
}
