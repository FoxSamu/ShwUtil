package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float} and a {@code boolean} and returns a {@code byte}.
 */
public interface FloatBool2ByteFn {
    byte invoke(float p1, boolean p2);
}
