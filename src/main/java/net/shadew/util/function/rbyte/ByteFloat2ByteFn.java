package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte} and a {@code float} and returns a {@code byte}.
 */
public interface ByteFloat2ByteFn {
    byte invoke(byte p1, float p2);
}
