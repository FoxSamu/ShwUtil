package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code short} and returns a {@code byte}.
 */
public interface BoolShortShort2ByteFn {
    byte invoke(boolean p1, short p2, short p3);
}
