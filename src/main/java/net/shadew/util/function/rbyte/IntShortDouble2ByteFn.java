package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code double} and returns a {@code byte}.
 */
public interface IntShortDouble2ByteFn {
    byte invoke(int p1, short p2, double p3);
}
