package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code byte} and returns a {@code byte}.
 */
public interface LongCharByte2ByteFn {
    byte invoke(long p1, char p2, byte p3);
}
