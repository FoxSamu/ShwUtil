package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code boolean} and returns a {@code byte}.
 */
public interface FloatDoubleBool2ByteFn {
    byte invoke(float p1, double p2, boolean p3);
}
