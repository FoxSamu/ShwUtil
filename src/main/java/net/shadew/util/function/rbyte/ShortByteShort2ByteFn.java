package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code short} and returns a {@code byte}.
 */
public interface ShortByteShort2ByteFn {
    byte invoke(short p1, byte p2, short p3);
}
