package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code double} and returns a {@code byte}.
 */
public interface BoolFloatDouble2ByteFn {
    byte invoke(boolean p1, float p2, double p3);
}
