package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code boolean} and a {@code char} and returns a {@code byte}.
 */
public interface CharBoolChar2ByteFn {
    byte invoke(char p1, boolean p2, char p3);
}
