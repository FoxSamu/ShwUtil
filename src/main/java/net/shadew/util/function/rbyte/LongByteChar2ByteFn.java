package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface LongByteChar2ByteFn {
    byte invoke(long p1, byte p2, char p3);
}
