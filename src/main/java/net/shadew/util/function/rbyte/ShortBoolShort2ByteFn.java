package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface ShortBoolShort2ByteFn {
    byte invoke(short p1, boolean p2, short p3);
}
