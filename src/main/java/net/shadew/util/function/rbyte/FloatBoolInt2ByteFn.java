package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code boolean} and an {@code int} and returns a {@code byte}.
 */
public interface FloatBoolInt2ByteFn {
    byte invoke(float p1, boolean p2, int p3);
}
