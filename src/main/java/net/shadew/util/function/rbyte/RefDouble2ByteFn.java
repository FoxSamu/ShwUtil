package net.shadew.util.function.rbyte;

/**
 * A function that takes an object and a {@code double} and returns a {@code byte}.
 */
public interface RefDouble2ByteFn<P1> {
    byte invoke(P1 p1, double p2);
}
