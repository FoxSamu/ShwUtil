package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface DoubleByteChar2ByteFn {
    byte invoke(double p1, byte p2, char p3);
}
