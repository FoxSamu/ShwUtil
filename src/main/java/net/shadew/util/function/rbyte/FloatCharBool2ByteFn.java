package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code boolean} and returns a {@code byte}.
 */
public interface FloatCharBool2ByteFn {
    byte invoke(Float p1, Char p2, Bool p3);
}
