package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code double} and returns a {@code byte}.
 */
public interface IntFloatDouble2ByteFn {
    byte invoke(int p1, float p2, double p3);
}
