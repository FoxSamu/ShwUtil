package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code long} and a {@code double} and returns a {@code byte}.
 */
public interface BoolLongDouble2ByteFn {
    byte invoke(boolean p1, long p2, double p3);
}
