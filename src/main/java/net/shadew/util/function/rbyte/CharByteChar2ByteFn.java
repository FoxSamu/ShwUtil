package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface CharByteChar2ByteFn {
    byte invoke(char p1, byte p2, char p3);
}
