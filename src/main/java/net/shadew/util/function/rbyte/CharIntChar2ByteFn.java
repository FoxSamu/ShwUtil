package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, an {@code int} and a {@code char} and returns a {@code byte}.
 */
public interface CharIntChar2ByteFn {
    byte invoke(char p1, int p2, char p3);
}
