package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code long} and returns a {@code byte}.
 */
public interface FloatByteLong2ByteFn {
    byte invoke(float p1, byte p2, long p3);
}
