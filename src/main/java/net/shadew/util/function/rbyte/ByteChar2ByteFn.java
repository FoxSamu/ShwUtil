package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface ByteChar2ByteFn {
    byte invoke(byte p1, char p2);
}
