package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code short} and returns a {@code byte}.
 */
public interface LongByteShort2ByteFn {
    byte invoke(Long p1, Byte p2, Short p3);
}
