package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code char} and returns a {@code byte}.
 */
public interface ByteShortChar2ByteFn {
    byte invoke(byte p1, short p2, char p3);
}
