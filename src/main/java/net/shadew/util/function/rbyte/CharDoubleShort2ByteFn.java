package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code short} and returns a {@code byte}.
 */
public interface CharDoubleShort2ByteFn {
    byte invoke(char p1, double p2, short p3);
}
