package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code boolean} and returns a {@code byte}.
 */
public interface CharByteBool2ByteFn {
    byte invoke(char p1, byte p2, boolean p3);
}
