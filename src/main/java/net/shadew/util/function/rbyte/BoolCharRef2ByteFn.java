package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code char} and an object and returns a {@code byte}.
 */
public interface BoolCharRef2ByteFn<P3> {
    byte invoke(boolean p1, char p2, P3 p3);
}
