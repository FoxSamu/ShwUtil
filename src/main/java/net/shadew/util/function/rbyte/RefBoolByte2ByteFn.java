package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code boolean} and a {@code byte} and returns a {@code byte}.
 */
public interface RefBoolByte2ByteFn<P1> {
    byte invoke(P1 p1, boolean p2, byte p3);
}
