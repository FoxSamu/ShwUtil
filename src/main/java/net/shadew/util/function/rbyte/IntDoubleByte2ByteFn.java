package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns a {@code byte}.
 */
public interface IntDoubleByte2ByteFn {
    byte invoke(int p1, double p2, byte p3);
}
