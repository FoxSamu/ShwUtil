package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code short} and an object and returns a {@code byte}.
 */
public interface FloatShortRef2ByteFn<P3> {
    byte invoke(float p1, short p2, P3 p3);
}
