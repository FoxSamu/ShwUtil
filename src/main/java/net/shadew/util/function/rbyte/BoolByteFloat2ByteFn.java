package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code float} and returns a {@code byte}.
 */
public interface BoolByteFloat2ByteFn {
    byte invoke(boolean p1, byte p2, float p3);
}
