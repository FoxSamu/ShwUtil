package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code double} and an object and returns a {@code byte}.
 */
public interface CharDoubleRef2ByteFn<P3> {
    byte invoke(char p1, double p2, P3 p3);
}
