package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code double} and a {@code float} and returns a {@code byte}.
 */
public interface RefDoubleFloat2ByteFn<P1> {
    byte invoke(P1 p1, double p2, float p3);
}
