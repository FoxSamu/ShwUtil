package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code char} and returns a {@code byte}.
 */
public interface BoolCharChar2ByteFn {
    byte invoke(boolean p1, char p2, char p3);
}
