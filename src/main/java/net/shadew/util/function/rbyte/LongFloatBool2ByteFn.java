package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code boolean} and returns a {@code byte}.
 */
public interface LongFloatBool2ByteFn {
    byte invoke(long p1, float p2, boolean p3);
}
