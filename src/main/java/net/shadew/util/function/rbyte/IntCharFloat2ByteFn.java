package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code float} and returns a {@code byte}.
 */
public interface IntCharFloat2ByteFn {
    byte invoke(int p1, char p2, float p3);
}
