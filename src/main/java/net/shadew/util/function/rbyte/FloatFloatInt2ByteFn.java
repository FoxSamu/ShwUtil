package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code float} and an {@code int} and returns a {@code byte}.
 */
public interface FloatFloatInt2ByteFn {
    byte invoke(float p1, float p2, int p3);
}
