package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code double} and returns a {@code byte}.
 */
public interface FloatLongDouble2ByteFn {
    byte invoke(float p1, long p2, double p3);
}
