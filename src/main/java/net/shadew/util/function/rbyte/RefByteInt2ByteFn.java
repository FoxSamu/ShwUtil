package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface RefByteInt2ByteFn<P1> {
    byte invoke(P1 p1, byte p2, int p3);
}
