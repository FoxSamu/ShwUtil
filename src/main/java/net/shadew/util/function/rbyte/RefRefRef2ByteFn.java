package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, an object and an object and returns a {@code byte}.
 */
public interface RefRefRef2ByteFn<P1, P2, P3> {
    byte invoke(P1 p1, P2 p2, P3 p3);
}
