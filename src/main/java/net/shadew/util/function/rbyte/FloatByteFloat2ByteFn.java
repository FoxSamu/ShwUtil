package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code float} and returns a {@code byte}.
 */
public interface FloatByteFloat2ByteFn {
    byte invoke(float p1, byte p2, float p3);
}
