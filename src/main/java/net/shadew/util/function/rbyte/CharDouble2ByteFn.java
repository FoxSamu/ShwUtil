package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char} and a {@code double} and returns a {@code byte}.
 */
public interface CharDouble2ByteFn {
    byte invoke(char p1, double p2);
}
