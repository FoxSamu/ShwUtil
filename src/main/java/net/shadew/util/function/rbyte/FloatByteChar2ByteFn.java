package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface FloatByteChar2ByteFn {
    byte invoke(float p1, byte p2, char p3);
}
