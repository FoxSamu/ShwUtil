package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code boolean} and returns a {@code byte}.
 */
public interface LongIntBool2ByteFn {
    byte invoke(long p1, int p2, boolean p3);
}
