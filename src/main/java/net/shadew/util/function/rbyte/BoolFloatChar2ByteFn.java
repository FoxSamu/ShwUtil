package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code char} and returns a {@code byte}.
 */
public interface BoolFloatChar2ByteFn {
    byte invoke(boolean p1, float p2, char p3);
}
