package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code boolean} and returns a {@code byte}.
 */
public interface LongDoubleBool2ByteFn {
    byte invoke(long p1, double p2, boolean p3);
}
