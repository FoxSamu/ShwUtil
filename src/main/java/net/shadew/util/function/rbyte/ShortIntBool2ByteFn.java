package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code boolean} and returns a {@code byte}.
 */
public interface ShortIntBool2ByteFn {
    byte invoke(short p1, int p2, boolean p3);
}
