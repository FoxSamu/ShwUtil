package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code short} and returns a {@code byte}.
 */
public interface FloatByteShort2ByteFn {
    byte invoke(float p1, byte p2, short p3);
}
