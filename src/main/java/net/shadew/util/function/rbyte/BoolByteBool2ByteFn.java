package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code boolean} and returns a {@code byte}.
 */
public interface BoolByteBool2ByteFn {
    byte invoke(boolean p1, byte p2, boolean p3);
}
