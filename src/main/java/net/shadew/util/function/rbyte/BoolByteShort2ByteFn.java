package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code short} and returns a {@code byte}.
 */
public interface BoolByteShort2ByteFn {
    byte invoke(boolean p1, byte p2, short p3);
}
