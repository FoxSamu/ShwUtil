package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code double} and returns a {@code byte}.
 */
public interface LongBoolDouble2ByteFn {
    byte invoke(long p1, boolean p2, double p3);
}
