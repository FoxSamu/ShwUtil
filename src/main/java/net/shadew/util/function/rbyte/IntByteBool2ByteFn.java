package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code boolean} and returns a {@code byte}.
 */
public interface IntByteBool2ByteFn {
    byte invoke(int p1, byte p2, boolean p3);
}
