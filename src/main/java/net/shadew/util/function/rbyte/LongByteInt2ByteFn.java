package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface LongByteInt2ByteFn {
    byte invoke(long p1, byte p2, int p3);
}
