package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an object and returns a {@code byte}.
 */
public interface BoolByteRef2ByteFn<P3> {
    byte invoke(boolean p1, byte p2, P3 p3);
}
