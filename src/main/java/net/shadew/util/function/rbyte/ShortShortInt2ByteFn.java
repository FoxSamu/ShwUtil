package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code short} and an {@code int} and returns a {@code byte}.
 */
public interface ShortShortInt2ByteFn {
    byte invoke(short p1, short p2, int p3);
}
