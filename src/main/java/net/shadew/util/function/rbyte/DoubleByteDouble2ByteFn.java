package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code double} and returns a {@code byte}.
 */
public interface DoubleByteDouble2ByteFn {
    byte invoke(double p1, byte p2, double p3);
}
