package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code double} and returns a {@code byte}.
 */
public interface ShortDoubleDouble2ByteFn {
    byte invoke(short p1, double p2, double p3);
}
