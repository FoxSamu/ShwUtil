package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code double} and returns a {@code byte}.
 */
public interface LongDoubleDouble2ByteFn {
    byte invoke(long p1, double p2, double p3);
}
