package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code float} and returns a {@code byte}.
 */
public interface LongByteFloat2ByteFn {
    byte invoke(long p1, byte p2, float p3);
}
