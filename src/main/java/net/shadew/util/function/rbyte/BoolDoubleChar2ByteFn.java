package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code char} and returns a {@code byte}.
 */
public interface BoolDoubleChar2ByteFn {
    byte invoke(boolean p1, double p2, char p3);
}
