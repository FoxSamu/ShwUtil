package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code short} and returns a {@code byte}.
 */
public interface LongDoubleShort2ByteFn {
    byte invoke(long p1, double p2, short p3);
}
