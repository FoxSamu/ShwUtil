package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code double} and returns a {@code byte}.
 */
public interface FloatFloatDouble2ByteFn {
    byte invoke(float p1, float p2, double p3);
}
