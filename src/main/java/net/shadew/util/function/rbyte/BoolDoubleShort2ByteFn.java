package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code short} and returns a {@code byte}.
 */
public interface BoolDoubleShort2ByteFn {
    byte invoke(boolean p1, double p2, short p3);
}
