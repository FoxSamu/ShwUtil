package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface LongBoolShort2ByteFn {
    byte invoke(long p1, boolean p2, short p3);
}
