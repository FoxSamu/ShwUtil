package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code boolean} and returns a {@code byte}.
 */
public interface ShortByteBool2ByteFn {
    byte invoke(short p1, byte p2, boolean p3);
}
