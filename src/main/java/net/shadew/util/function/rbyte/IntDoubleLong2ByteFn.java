package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code long} and returns a {@code byte}.
 */
public interface IntDoubleLong2ByteFn {
    byte invoke(int p1, double p2, long p3);
}
