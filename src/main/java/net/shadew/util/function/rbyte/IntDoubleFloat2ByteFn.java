package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code float} and returns a {@code byte}.
 */
public interface IntDoubleFloat2ByteFn {
    byte invoke(int p1, double p2, float p3);
}
