package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code float} and returns a {@code byte}.
 */
public interface IntShortFloat2ByteFn {
    byte invoke(int p1, short p2, float p3);
}
