package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code char} and returns a {@code byte}.
 */
public interface FloatIntChar2ByteFn {
    byte invoke(float p1, int p2, char p3);
}
