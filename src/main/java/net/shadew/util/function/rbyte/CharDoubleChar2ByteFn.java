package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code char} and returns a {@code byte}.
 */
public interface CharDoubleChar2ByteFn {
    byte invoke(char p1, double p2, char p3);
}
