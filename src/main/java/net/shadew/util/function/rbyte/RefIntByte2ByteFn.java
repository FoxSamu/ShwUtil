package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, an {@code int} and a {@code byte} and returns a {@code byte}.
 */
public interface RefIntByte2ByteFn<P1> {
    byte invoke(P1 p1, int p2, byte p3);
}
