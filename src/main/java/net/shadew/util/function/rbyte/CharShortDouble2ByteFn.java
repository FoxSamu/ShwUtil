package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code short} and a {@code double} and returns a {@code byte}.
 */
public interface CharShortDouble2ByteFn {
    byte invoke(char p1, short p2, double p3);
}
