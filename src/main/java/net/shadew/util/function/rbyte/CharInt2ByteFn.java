package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char} and an {@code int} and returns a {@code byte}.
 */
public interface CharInt2ByteFn {
    byte invoke(char p1, int p2);
}
