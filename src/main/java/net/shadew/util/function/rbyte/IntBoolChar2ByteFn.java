package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code char} and returns a {@code byte}.
 */
public interface IntBoolChar2ByteFn {
    byte invoke(int p1, boolean p2, char p3);
}
