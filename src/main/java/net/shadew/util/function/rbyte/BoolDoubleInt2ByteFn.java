package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code double} and an {@code int} and returns a {@code byte}.
 */
public interface BoolDoubleInt2ByteFn {
    byte invoke(boolean p1, double p2, int p3);
}
