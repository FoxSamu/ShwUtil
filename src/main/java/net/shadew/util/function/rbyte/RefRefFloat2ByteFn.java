package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, an object and a {@code float} and returns a {@code byte}.
 */
public interface RefRefFloat2ByteFn<P1, P2> {
    byte invoke(P1 p1, P2 p2, float p3);
}
