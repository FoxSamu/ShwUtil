package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code double} and returns a {@code byte}.
 */
public interface ShortIntDouble2ByteFn {
    byte invoke(short p1, int p2, double p3);
}
