package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code boolean} and returns a {@code byte}.
 */
public interface ShortFloatBool2ByteFn {
    byte invoke(short p1, float p2, boolean p3);
}
