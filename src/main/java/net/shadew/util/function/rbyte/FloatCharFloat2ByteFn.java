package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code float} and returns a {@code byte}.
 */
public interface FloatCharFloat2ByteFn {
    byte invoke(float p1, char p2, float p3);
}
