package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code float} and returns a {@code byte}.
 */
public interface ShortFloatFloat2ByteFn {
    byte invoke(short p1, float p2, float p3);
}
