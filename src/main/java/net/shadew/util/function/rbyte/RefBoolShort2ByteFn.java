package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface RefBoolShort2ByteFn<P1> {
    byte invoke(P1 p1, boolean p2, short p3);
}
