package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code float} and returns a {@code byte}.
 */
public interface DoubleByteFloat2ByteFn {
    byte invoke(double p1, byte p2, float p3);
}
