package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code float} and returns a {@code byte}.
 */
public interface IntByteFloat2ByteFn {
    byte invoke(int p1, byte p2, float p3);
}
