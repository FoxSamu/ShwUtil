package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float} and a {@code char} and returns a {@code byte}.
 */
public interface FloatChar2ByteFn {
    byte invoke(float p1, char p2);
}
