package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code short} and returns a {@code byte}.
 */
public interface CharByteShort2ByteFn {
    byte invoke(char p1, byte p2, short p3);
}
