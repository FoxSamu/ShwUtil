package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code double} and returns a {@code byte}.
 */
public interface LongShortDouble2ByteFn {
    byte invoke(long p1, short p2, double p3);
}
