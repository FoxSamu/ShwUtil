package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code float} and an {@code int} and returns a {@code byte}.
 */
public interface LongFloatInt2ByteFn {
    byte invoke(long p1, float p2, int p3);
}
