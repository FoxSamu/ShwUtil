package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code double} and returns a {@code byte}.
 */
public interface FloatIntDouble2ByteFn {
    byte invoke(float p1, int p2, double p3);
}
