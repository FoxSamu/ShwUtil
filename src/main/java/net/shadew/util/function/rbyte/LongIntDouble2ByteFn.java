package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code double} and returns a {@code byte}.
 */
public interface LongIntDouble2ByteFn {
    byte invoke(long p1, int p2, double p3);
}
