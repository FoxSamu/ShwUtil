package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code double} and returns a {@code byte}.
 */
public interface IntCharDouble2ByteFn {
    byte invoke(int p1, char p2, double p3);
}
