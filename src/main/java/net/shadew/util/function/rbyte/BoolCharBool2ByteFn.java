package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code boolean} and returns a {@code byte}.
 */
public interface BoolCharBool2ByteFn {
    byte invoke(boolean p1, char p2, boolean p3);
}
