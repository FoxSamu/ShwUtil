package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double} and a {@code float} and returns a {@code byte}.
 */
public interface DoubleFloat2ByteFn {
    byte invoke(double p1, float p2);
}
