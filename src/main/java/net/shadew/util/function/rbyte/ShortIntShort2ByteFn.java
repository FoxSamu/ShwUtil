package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code short} and returns a {@code byte}.
 */
public interface ShortIntShort2ByteFn {
    byte invoke(short p1, int p2, short p3);
}
