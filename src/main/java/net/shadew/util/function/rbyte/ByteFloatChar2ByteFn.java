package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code char} and returns a {@code byte}.
 */
public interface ByteFloatChar2ByteFn {
    byte invoke(byte p1, float p2, char p3);
}
