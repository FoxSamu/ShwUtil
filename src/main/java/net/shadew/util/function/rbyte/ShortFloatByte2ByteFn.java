package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code byte} and returns a {@code byte}.
 */
public interface ShortFloatByte2ByteFn {
    byte invoke(short p1, float p2, byte p3);
}
