package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code short} and returns a {@code byte}.
 */
public interface FloatLongShort2ByteFn {
    byte invoke(float p1, long p2, short p3);
}
