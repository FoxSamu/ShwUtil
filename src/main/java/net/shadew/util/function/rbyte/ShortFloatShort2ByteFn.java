package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code short} and returns a {@code byte}.
 */
public interface ShortFloatShort2ByteFn {
    byte invoke(short p1, float p2, short p3);
}
