package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code float} and returns a {@code byte}.
 */
public interface LongShortFloat2ByteFn {
    byte invoke(long p1, short p2, float p3);
}
