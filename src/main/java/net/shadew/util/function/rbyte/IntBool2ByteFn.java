package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int} and a {@code boolean} and returns a {@code byte}.
 */
public interface IntBool2ByteFn {
    byte invoke(int p1, boolean p2);
}
