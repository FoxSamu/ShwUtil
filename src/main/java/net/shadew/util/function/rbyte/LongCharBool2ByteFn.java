package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code boolean} and returns a {@code byte}.
 */
public interface LongCharBool2ByteFn {
    byte invoke(long p1, char p2, boolean p3);
}
