package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code short} and returns a {@code byte}.
 */
public interface ShortCharShort2ByteFn {
    byte invoke(short p1, char p2, short p3);
}
