package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns a {@code byte}.
 */
public interface FloatByteDouble2ByteFn {
    byte invoke(float p1, byte p2, double p3);
}
