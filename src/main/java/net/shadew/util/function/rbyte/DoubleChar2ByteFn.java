package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double} and a {@code char} and returns a {@code byte}.
 */
public interface DoubleChar2ByteFn {
    byte invoke(double p1, char p2);
}
