package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code boolean} and an {@code int} and returns a {@code byte}.
 */
public interface IntBoolInt2ByteFn {
    byte invoke(int p1, boolean p2, int p3);
}
