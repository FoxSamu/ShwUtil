package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code short} and returns a {@code byte}.
 */
public interface DoubleByteShort2ByteFn {
    byte invoke(double p1, byte p2, short p3);
}
