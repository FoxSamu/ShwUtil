package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte} and a {@code double} and returns a {@code byte}.
 */
public interface ByteDouble2ByteFn {
    byte invoke(byte p1, double p2);
}
