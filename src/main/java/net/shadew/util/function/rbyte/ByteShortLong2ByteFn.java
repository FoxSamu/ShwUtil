package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code long} and returns a {@code byte}.
 */
public interface ByteShortLong2ByteFn {
    byte invoke(byte p1, short p2, long p3);
}
