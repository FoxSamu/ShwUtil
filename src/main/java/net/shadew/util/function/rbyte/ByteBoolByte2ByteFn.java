package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code byte} and returns a {@code byte}.
 */
public interface ByteBoolByte2ByteFn {
    byte invoke(byte p1, boolean p2, byte p3);
}
