package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short} and a {@code float} and returns a {@code byte}.
 */
public interface ShortFloat2ByteFn {
    byte invoke(short p1, float p2);
}
