package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code short} and returns a {@code byte}.
 */
public interface IntFloatShort2ByteFn {
    byte invoke(int p1, float p2, short p3);
}
