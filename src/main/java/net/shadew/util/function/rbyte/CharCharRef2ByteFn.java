package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code char} and an object and returns a {@code byte}.
 */
public interface CharCharRef2ByteFn<P3> {
    byte invoke(char p1, char p2, P3 p3);
}
