package net.shadew.util.function.rbyte;

/**
 * A function that takes an object and a {@code byte} and returns a {@code byte}.
 */
public interface RefByte2ByteFn<P1> {
    byte invoke(P1 p1, byte p2);
}
