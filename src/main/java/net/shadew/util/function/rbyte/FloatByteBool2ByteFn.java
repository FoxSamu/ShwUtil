package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns a {@code byte}.
 */
public interface FloatByteBool2ByteFn {
    byte invoke(float p1, byte p2, boolean p3);
}
