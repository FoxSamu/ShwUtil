package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code boolean} and returns a {@code byte}.
 */
public interface LongByteBool2ByteFn {
    byte invoke(long p1, byte p2, boolean p3);
}
