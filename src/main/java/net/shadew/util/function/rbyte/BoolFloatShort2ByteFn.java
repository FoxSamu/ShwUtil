package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code short} and returns a {@code byte}.
 */
public interface BoolFloatShort2ByteFn {
    byte invoke(boolean p1, float p2, short p3);
}
