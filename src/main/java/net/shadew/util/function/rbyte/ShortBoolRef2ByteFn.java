package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code boolean} and an object and returns a {@code byte}.
 */
public interface ShortBoolRef2ByteFn<P3> {
    byte invoke(short p1, boolean p2, P3 p3);
}
