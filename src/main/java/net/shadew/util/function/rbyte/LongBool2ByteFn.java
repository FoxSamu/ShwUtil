package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long} and a {@code boolean} and returns a {@code byte}.
 */
public interface LongBool2ByteFn {
    byte invoke(long p1, boolean p2);
}
