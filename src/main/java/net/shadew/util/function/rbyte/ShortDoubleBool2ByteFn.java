package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code boolean} and returns a {@code byte}.
 */
public interface ShortDoubleBool2ByteFn {
    byte invoke(short p1, double p2, boolean p3);
}
