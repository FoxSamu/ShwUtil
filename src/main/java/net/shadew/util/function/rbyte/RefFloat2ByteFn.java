package net.shadew.util.function.rbyte;

/**
 * A function that takes an object and a {@code float} and returns a {@code byte}.
 */
public interface RefFloat2ByteFn<P1> {
    byte invoke(P1 p1, float p2);
}
