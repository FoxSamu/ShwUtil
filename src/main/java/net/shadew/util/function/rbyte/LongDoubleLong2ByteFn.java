package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code long} and returns a {@code byte}.
 */
public interface LongDoubleLong2ByteFn {
    byte invoke(long p1, double p2, long p3);
}
