package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code boolean} and returns a {@code byte}.
 */
public interface IntFloatBool2ByteFn {
    byte invoke(int p1, float p2, boolean p3);
}
