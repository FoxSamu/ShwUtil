package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface FloatBoolShort2ByteFn {
    byte invoke(float p1, boolean p2, short p3);
}
