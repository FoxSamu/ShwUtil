package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code char} and an {@code int} and returns a {@code byte}.
 */
public interface BoolCharInt2ByteFn {
    byte invoke(boolean p1, char p2, int p3);
}
