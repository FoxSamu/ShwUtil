package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, an {@code int} and a {@code short} and returns a {@code byte}.
 */
public interface RefIntShort2ByteFn<P1> {
    byte invoke(P1 p1, int p2, short p3);
}
