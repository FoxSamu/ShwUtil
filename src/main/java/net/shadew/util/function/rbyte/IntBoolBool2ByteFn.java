package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code boolean} and returns a {@code byte}.
 */
public interface IntBoolBool2ByteFn {
    byte invoke(int p1, boolean p2, boolean p3);
}
