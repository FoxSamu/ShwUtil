package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code double} and returns a {@code byte}.
 */
public interface BoolBoolDouble2ByteFn {
    byte invoke(boolean p1, boolean p2, double p3);
}
