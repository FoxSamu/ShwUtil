package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code short} and returns a {@code byte}.
 */
public interface IntDoubleShort2ByteFn {
    byte invoke(int p1, double p2, short p3);
}
