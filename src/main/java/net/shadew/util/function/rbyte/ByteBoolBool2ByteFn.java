package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code boolean} and returns a {@code byte}.
 */
public interface ByteBoolBool2ByteFn {
    byte invoke(byte p1, boolean p2, boolean p3);
}
