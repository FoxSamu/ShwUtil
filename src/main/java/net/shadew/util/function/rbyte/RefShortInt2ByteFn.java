package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code short} and an {@code int} and returns a {@code byte}.
 */
public interface RefShortInt2ByteFn<P1> {
    byte invoke(P1 p1, short p2, int p3);
}
