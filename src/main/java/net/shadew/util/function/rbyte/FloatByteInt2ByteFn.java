package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface FloatByteInt2ByteFn {
    byte invoke(float p1, byte p2, int p3);
}
