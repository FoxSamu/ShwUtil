package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an {@code int} and returns a {@code byte}.
 */
public interface ByteBoolInt2ByteFn {
    byte invoke(byte p1, boolean p2, int p3);
}
