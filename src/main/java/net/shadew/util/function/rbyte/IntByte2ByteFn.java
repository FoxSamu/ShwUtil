package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int} and a {@code byte} and returns a {@code byte}.
 */
public interface IntByte2ByteFn {
    byte invoke(int p1, byte p2);
}
