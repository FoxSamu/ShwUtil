package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code double} and returns a {@code byte}.
 */
public interface IntBoolDouble2ByteFn {
    byte invoke(int p1, boolean p2, double p3);
}
