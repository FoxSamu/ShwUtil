package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code boolean} and returns a {@code byte}.
 */
public interface FloatFloatBool2ByteFn {
    byte invoke(float p1, float p2, boolean p3);
}
