package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code double} and returns a {@code byte}.
 */
public interface ByteShortDouble2ByteFn {
    byte invoke(byte p1, short p2, double p3);
}
