package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns a {@code byte}.
 */
public interface BoolDouble2ByteFn {
    byte invoke(boolean p1, double p2);
}
