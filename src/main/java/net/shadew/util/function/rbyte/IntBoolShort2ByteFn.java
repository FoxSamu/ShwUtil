package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface IntBoolShort2ByteFn {
    byte invoke(int p1, boolean p2, short p3);
}
