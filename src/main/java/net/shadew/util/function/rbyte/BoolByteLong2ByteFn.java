package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code long} and returns a {@code byte}.
 */
public interface BoolByteLong2ByteFn {
    byte invoke(boolean p1, byte p2, long p3);
}
