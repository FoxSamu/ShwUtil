package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code double} and returns a {@code byte}.
 */
public interface FloatShortDouble2ByteFn {
    byte invoke(float p1, short p2, double p3);
}
