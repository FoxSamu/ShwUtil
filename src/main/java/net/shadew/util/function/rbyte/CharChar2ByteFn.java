package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char} and a {@code char} and returns a {@code byte}.
 */
public interface CharChar2ByteFn {
    byte invoke(char p1, char p2);
}
