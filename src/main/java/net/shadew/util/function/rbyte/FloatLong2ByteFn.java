package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float} and a {@code long} and returns a {@code byte}.
 */
public interface FloatLong2ByteFn {
    byte invoke(float p1, long p2);
}
