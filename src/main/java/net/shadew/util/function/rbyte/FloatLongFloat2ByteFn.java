package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code float} and returns a {@code byte}.
 */
public interface FloatLongFloat2ByteFn {
    byte invoke(float p1, long p2, float p3);
}
