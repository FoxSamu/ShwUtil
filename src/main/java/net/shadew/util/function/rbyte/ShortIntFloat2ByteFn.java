package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code float} and returns a {@code byte}.
 */
public interface ShortIntFloat2ByteFn {
    byte invoke(short p1, int p2, float p3);
}
