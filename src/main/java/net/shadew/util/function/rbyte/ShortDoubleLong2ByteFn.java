package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code long} and returns a {@code byte}.
 */
public interface ShortDoubleLong2ByteFn {
    byte invoke(short p1, double p2, long p3);
}
