package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code short} and returns a {@code byte}.
 */
public interface FloatCharShort2ByteFn {
    byte invoke(float p1, char p2, short p3);
}
