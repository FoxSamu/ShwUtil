package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long} and a {@code char} and returns a {@code byte}.
 */
public interface LongChar2ByteFn {
    byte invoke(long p1, char p2);
}
