package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code byte} and returns a {@code byte}.
 */
public interface FloatByteByte2ByteFn {
    byte invoke(float p1, byte p2, byte p3);
}
