package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float} and an {@code int} and returns a {@code byte}.
 */
public interface FloatInt2ByteFn {
    byte invoke(float p1, int p2);
}
