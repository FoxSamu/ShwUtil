package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code byte} and an {@code int} and returns a {@code byte}.
 */
public interface ShortByteInt2ByteFn {
    byte invoke(short p1, byte p2, int p3);
}
