package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float} and a {@code float} and returns a {@code byte}.
 */
public interface FloatFloat2ByteFn {
    byte invoke(float p1, float p2);
}
