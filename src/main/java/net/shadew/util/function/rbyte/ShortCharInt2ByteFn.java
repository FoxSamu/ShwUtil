package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code char} and an {@code int} and returns a {@code byte}.
 */
public interface ShortCharInt2ByteFn {
    byte invoke(short p1, char p2, int p3);
}
