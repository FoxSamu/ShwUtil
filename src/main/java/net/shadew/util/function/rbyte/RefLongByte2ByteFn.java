package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code long} and a {@code byte} and returns a {@code byte}.
 */
public interface RefLongByte2ByteFn<P1> {
    byte invoke(P1 p1, long p2, byte p3);
}
