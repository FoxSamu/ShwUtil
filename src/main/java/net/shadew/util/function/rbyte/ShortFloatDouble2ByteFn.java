package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code double} and returns a {@code byte}.
 */
public interface ShortFloatDouble2ByteFn {
    byte invoke(short p1, float p2, double p3);
}
