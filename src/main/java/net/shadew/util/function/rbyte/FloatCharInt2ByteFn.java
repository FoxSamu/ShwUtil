package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code char} and an {@code int} and returns a {@code byte}.
 */
public interface FloatCharInt2ByteFn {
    byte invoke(float p1, char p2, int p3);
}
