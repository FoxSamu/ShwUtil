package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code long} and returns a {@code byte}.
 */
public interface FloatDoubleLong2ByteFn {
    byte invoke(float p1, double p2, long p3);
}
