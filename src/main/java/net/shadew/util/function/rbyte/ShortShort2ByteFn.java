package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short} and a {@code short} and returns a {@code byte}.
 */
public interface ShortShort2ByteFn {
    byte invoke(short p1, short p2);
}
