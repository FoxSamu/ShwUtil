package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code long} and returns a {@code byte}.
 */
public interface ByteDoubleLong2ByteFn {
    byte invoke(byte p1, double p2, long p3);
}
