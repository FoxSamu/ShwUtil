package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns a {@code byte}.
 */
public interface IntByteChar2ByteFn {
    byte invoke(int p1, byte p2, char p3);
}
