package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns a {@code byte}.
 */
public interface BoolShort2ByteFn {
    byte invoke(boolean p1, short p2);
}
