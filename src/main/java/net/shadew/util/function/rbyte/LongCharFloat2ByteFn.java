package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code float} and returns a {@code byte}.
 */
public interface LongCharFloat2ByteFn {
    byte invoke(long p1, char p2, float p3);
}
