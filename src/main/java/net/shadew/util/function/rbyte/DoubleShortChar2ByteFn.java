package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code char} and returns a {@code byte}.
 */
public interface DoubleShortChar2ByteFn {
    byte invoke(double p1, short p2, char p3);
}
