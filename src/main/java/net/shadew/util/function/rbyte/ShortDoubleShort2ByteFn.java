package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code short} and returns a {@code byte}.
 */
public interface ShortDoubleShort2ByteFn {
    byte invoke(short p1, double p2, short p3);
}
