package net.shadew.util.function.rbyte;

/**
 * A function that takes an object, a {@code float} and a {@code boolean} and returns a {@code byte}.
 */
public interface RefFloatBool2ByteFn<P1> {
    byte invoke(P1 p1, float p2, boolean p3);
}
