package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code float} and returns a {@code byte}.
 */
public interface BoolShortFloat2ByteFn {
    byte invoke(boolean p1, short p2, float p3);
}
