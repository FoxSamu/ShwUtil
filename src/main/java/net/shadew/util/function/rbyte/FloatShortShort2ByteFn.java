package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code short} and returns a {@code byte}.
 */
public interface FloatShortShort2ByteFn {
    byte invoke(float p1, short p2, short p3);
}
