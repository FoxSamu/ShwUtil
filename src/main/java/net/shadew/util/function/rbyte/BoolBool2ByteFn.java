package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns a {@code byte}.
 */
public interface BoolBool2ByteFn {
    byte invoke(boolean p1, boolean p2);
}
