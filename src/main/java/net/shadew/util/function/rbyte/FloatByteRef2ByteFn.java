package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code byte} and an object and returns a {@code byte}.
 */
public interface FloatByteRef2ByteFn<P3> {
    byte invoke(float p1, byte p2, P3 p3);
}
