package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short} and a {@code byte} and returns a {@code byte}.
 */
public interface ShortByte2ByteFn {
    byte invoke(short p1, byte p2);
}
