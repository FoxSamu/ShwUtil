package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code float} and returns a {@code byte}.
 */
public interface ShortDoubleFloat2ByteFn {
    byte invoke(short p1, double p2, float p3);
}
