package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code long} and returns a {@code byte}.
 */
public interface CharDoubleLong2ByteFn {
    byte invoke(char p1, double p2, long p3);
}
