package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code double} and returns a {@code byte}.
 */
public interface ByteFloatDouble2ByteFn {
    byte invoke(byte p1, float p2, double p3);
}
