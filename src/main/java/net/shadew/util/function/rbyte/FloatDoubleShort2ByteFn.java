package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code short} and returns a {@code byte}.
 */
public interface FloatDoubleShort2ByteFn {
    byte invoke(float p1, double p2, short p3);
}
