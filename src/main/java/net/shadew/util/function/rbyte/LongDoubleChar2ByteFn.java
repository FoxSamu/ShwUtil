package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code char} and returns a {@code byte}.
 */
public interface LongDoubleChar2ByteFn {
    byte invoke(long p1, double p2, char p3);
}
