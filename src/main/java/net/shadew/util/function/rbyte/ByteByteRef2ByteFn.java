package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code byte} and an object and returns a {@code byte}.
 */
public interface ByteByteRef2ByteFn<P3> {
    byte invoke(byte p1, byte p2, P3 p3);
}
