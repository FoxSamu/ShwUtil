package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code long}, a {@code double} and an {@code int} and returns a {@code byte}.
 */
public interface LongDoubleInt2ByteFn {
    byte invoke(long p1, double p2, int p3);
}
