package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code float}, a {@code long} and an {@code int} and returns a {@code byte}.
 */
public interface FloatLongInt2ByteFn {
    byte invoke(float p1, long p2, int p3);
}
