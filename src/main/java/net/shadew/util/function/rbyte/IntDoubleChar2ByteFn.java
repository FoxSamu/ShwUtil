package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code char} and returns a {@code byte}.
 */
public interface IntDoubleChar2ByteFn {
    byte invoke(int p1, double p2, char p3);
}
