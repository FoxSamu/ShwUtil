package net.shadew.util.function.rbyte;

/**
 * A function that takes an {@code int} and an {@code int} and returns a {@code byte}.
 */
public interface IntInt2ByteFn {
    byte invoke(int p1, int p2);
}
