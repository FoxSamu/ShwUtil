package net.shadew.util.function.rbyte;

/**
 * A function that takes a {@code byte}, a {@code double} and an {@code int} and returns a {@code byte}.
 */
public interface ByteDoubleInt2ByteFn {
    byte invoke(byte p1, double p2, int p3);
}
