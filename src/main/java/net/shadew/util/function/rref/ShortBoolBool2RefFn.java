package net.shadew.util.function.rref;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code boolean} and returns an object.
 */
public interface ShortBoolBool2RefFn<R> {
    R invoke(short p1, boolean p2, boolean p3);
}
