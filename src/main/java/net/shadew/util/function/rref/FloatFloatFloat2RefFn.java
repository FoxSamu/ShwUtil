package net.shadew.util.function.rref;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code float} and returns an object.
 */
public interface FloatFloatFloat2RefFn<R> {
    R invoke(float p1, float p2, float p3);
}
