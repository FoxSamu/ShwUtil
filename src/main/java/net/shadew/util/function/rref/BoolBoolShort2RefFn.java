package net.shadew.util.function.rref;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code short} and returns an object.
 */
public interface BoolBoolShort2RefFn<R> {
    R invoke(boolean p1, boolean p2, short p3);
}
