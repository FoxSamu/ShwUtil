package net.shadew.util.function.rref;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code short} and returns an object.
 */
public interface FloatFloatShort2RefFn<R> {
    R invoke(float p1, float p2, short p3);
}
