package net.shadew.util.function.rref;

/**
 * A function that takes a {@code float} and a {@code float} and returns an object.
 */
public interface FloatFloat2RefFn<R> {
    R invoke(Float p1, Float p2);
}
