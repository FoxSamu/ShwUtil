package net.shadew.util.function.rref;

/**
 * A function that takes a {@code short} and a {@code short} and returns an object.
 */
public interface ShortShort2RefFn<R> {
    R invoke(short p1, short p2);
}
