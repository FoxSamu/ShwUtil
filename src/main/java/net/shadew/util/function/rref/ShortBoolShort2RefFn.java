package net.shadew.util.function.rref;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code short} and returns an object.
 */
public interface ShortBoolShort2RefFn<R> {
    R invoke(short p1, boolean p2, short p3);
}
