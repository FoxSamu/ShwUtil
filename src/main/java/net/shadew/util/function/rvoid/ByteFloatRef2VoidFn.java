package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code float} and an object and returns nothing.
 */
public interface ByteFloatRef2VoidFn<P3> {
    void invoke(byte p1, float p2, P3 p3);
}
