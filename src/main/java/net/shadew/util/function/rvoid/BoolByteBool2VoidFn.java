package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code boolean} and returns nothing.
 */
public interface BoolByteBool2VoidFn {
    void invoke(boolean p1, byte p2, boolean p3);
}
