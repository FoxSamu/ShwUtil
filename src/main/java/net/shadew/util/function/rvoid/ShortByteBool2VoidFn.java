package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code boolean} and returns nothing.
 */
public interface ShortByteBool2VoidFn {
    void invoke(short p1, byte p2, boolean p3);
}
