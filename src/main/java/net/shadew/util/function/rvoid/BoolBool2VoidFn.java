package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns nothing.
 */
public interface BoolBool2VoidFn {
    void invoke(boolean p1, boolean p2);
}
