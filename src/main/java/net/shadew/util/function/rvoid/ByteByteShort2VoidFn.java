package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code short} and returns nothing.
 */
public interface ByteByteShort2VoidFn {
    void invoke(byte p1, byte p2, short p3);
}
