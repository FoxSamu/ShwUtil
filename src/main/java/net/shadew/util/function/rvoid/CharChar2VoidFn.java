package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code char} and a {@code char} and returns nothing.
 */
public interface CharChar2VoidFn {
    void invoke(char p1, char p2);
}
