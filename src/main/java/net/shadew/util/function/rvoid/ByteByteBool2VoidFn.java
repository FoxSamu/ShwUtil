package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns nothing.
 */
public interface ByteByteBool2VoidFn {
    void invoke(byte p1, byte p2, boolean p3);
}
