package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and an {@code int} and returns nothing.
 */
public interface ByteByteInt2VoidFn {
    void invoke(byte p1, byte p2, int p3);
}
