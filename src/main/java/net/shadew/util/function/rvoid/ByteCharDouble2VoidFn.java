package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code double} and returns nothing.
 */
public interface ByteCharDouble2VoidFn {
    void invoke(byte p1, char p2, double p3);
}
