package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code byte} and returns nothing.
 */
public interface BoolDoubleByte2VoidFn {
    void invoke(boolean p1, double p2, byte p3);
}
