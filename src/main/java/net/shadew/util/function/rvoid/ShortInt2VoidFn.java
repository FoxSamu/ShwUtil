package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short} and an {@code int} and returns nothing.
 */
public interface ShortInt2VoidFn {
    void invoke(short p1, int p2);
}
