package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short} and a {@code double} and returns nothing.
 */
public interface ShortDouble2VoidFn {
    void invoke(short p1, double p2);
}
