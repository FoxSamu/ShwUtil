package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code short} and returns nothing.
 */
public interface FloatByteShort2VoidFn {
    void invoke(float p1, byte p2, short p3);
}
