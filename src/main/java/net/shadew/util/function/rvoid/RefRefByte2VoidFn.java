package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, an object and a {@code byte} and returns nothing.
 */
public interface RefRefByte2VoidFn<P1, P2> {
    void invoke(P1 p1, P2 p2, byte p3);
}
