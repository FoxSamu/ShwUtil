package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code char} and returns nothing.
 */
public interface BoolByteChar2VoidFn {
    void invoke(boolean p1, byte p2, char p3);
}
