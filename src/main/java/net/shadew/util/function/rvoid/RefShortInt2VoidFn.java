package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code short} and an {@code int} and returns nothing.
 */
public interface RefShortInt2VoidFn<P1> {
    void invoke(P1 p1, short p2, int p3);
}
