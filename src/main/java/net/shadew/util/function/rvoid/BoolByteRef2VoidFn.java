package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an object and returns nothing.
 */
public interface BoolByteRef2VoidFn<P3> {
    void invoke(boolean p1, byte p2, P3 p3);
}
