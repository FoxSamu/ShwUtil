package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, an object and a {@code boolean} and returns nothing.
 */
public interface RefRefBool2VoidFn<P1, P2> {
    void invoke(P1 p1, P2 p2, boolean p3);
}
