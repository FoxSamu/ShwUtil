package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code float} and a {@code byte} and returns nothing.
 */
public interface RefFloatByte2VoidFn<P1> {
    void invoke(P1 p1, float p2, byte p3);
}
