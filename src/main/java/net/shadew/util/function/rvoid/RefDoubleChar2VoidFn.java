package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code double} and a {@code char} and returns nothing.
 */
public interface RefDoubleChar2VoidFn<P1> {
    void invoke(P1 p1, double p2, char p3);
}
