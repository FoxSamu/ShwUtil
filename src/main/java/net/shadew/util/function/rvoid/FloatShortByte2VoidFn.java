package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code byte} and returns nothing.
 */
public interface FloatShortByte2VoidFn {
    void invoke(float p1, short p2, byte p3);
}
