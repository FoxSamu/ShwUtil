package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code double} and an {@code int} and returns nothing.
 */
public interface FloatDoubleInt2VoidFn {
    void invoke(float p1, double p2, int p3);
}
