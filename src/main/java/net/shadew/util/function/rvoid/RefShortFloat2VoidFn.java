package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code short} and a {@code float} and returns nothing.
 */
public interface RefShortFloat2VoidFn<P1> {
    void invoke(P1 p1, short p2, float p3);
}
