package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code byte} and returns nothing.
 */
public interface IntByteByte2VoidFn {
    void invoke(int p1, byte p2, byte p3);
}
