package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code char} and an {@code int} and returns nothing.
 */
public interface RefCharInt2VoidFn<P1> {
    void invoke(P1 p1, char p2, int p3);
}
