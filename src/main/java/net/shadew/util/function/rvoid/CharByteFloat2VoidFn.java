package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code float} and returns nothing.
 */
public interface CharByteFloat2VoidFn {
    void invoke(char p1, byte p2, float p3);
}
