package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short} and a {@code boolean} and returns nothing.
 */
public interface ShortBool2VoidFn {
    void invoke(short p1, boolean p2);
}
