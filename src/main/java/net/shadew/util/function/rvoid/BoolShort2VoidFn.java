package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns nothing.
 */
public interface BoolShort2VoidFn {
    void invoke(boolean p1, short p2);
}
