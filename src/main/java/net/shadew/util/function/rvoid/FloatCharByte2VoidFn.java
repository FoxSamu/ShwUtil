package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code byte} and returns nothing.
 */
public interface FloatCharByte2VoidFn {
    void invoke(float p1, char p2, byte p3);
}
