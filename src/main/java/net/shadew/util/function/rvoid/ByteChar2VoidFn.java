package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte} and a {@code char} and returns nothing.
 */
public interface ByteChar2VoidFn {
    void invoke(byte p1, char p2);
}
