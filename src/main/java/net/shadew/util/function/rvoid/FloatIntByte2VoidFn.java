package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code byte} and returns nothing.
 */
public interface FloatIntByte2VoidFn {
    void invoke(float p1, int p2, byte p3);
}
