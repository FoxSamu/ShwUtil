package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code short} and returns nothing.
 */
public interface IntByteShort2VoidFn {
    void invoke(int p1, byte p2, short p3);
}
