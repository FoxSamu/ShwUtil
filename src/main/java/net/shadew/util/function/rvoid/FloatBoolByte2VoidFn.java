package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code byte} and returns nothing.
 */
public interface FloatBoolByte2VoidFn {
    void invoke(float p1, boolean p2, byte p3);
}
