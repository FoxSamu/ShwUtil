package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code float} and returns nothing.
 */
public interface FloatByteFloat2VoidFn {
    void invoke(float p1, byte p2, float p3);
}
