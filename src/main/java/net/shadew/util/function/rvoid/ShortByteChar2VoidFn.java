package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code char} and returns nothing.
 */
public interface ShortByteChar2VoidFn {
    void invoke(short p1, byte p2, char p3);
}
