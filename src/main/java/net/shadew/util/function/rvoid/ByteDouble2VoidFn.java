package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte} and a {@code double} and returns nothing.
 */
public interface ByteDouble2VoidFn {
    void invoke(byte p1, double p2);
}
