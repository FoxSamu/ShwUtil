package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code float} and returns nothing.
 */
public interface ByteByteFloat2VoidFn {
    void invoke(byte p1, byte p2, float p3);
}
