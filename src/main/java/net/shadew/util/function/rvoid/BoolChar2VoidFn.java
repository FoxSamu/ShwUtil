package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns nothing.
 */
public interface BoolChar2VoidFn {
    void invoke(boolean p1, char p2);
}
