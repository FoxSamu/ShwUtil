package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code float} and returns nothing.
 */
public interface IntByteFloat2VoidFn {
    void invoke(int p1, byte p2, float p3);
}
