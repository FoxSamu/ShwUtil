package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code float} and an object and returns nothing.
 */
public interface RefFloatRef2VoidFn<P1, P3> {
    void invoke(P1 p1, float p2, P3 p3);
}
