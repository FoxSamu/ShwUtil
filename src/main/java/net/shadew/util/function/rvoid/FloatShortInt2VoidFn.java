package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code short} and an {@code int} and returns nothing.
 */
public interface FloatShortInt2VoidFn {
    void invoke(float p1, short p2, int p3);
}
