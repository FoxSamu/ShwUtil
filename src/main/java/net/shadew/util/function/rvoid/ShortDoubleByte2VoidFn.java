package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code byte} and returns nothing.
 */
public interface ShortDoubleByte2VoidFn {
    void invoke(short p1, double p2, byte p3);
}
