package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code short} and an object and returns nothing.
 */
public interface RefShortRef2VoidFn<P1, P3> {
    void invoke(P1 p1, short p2, P3 p3);
}
