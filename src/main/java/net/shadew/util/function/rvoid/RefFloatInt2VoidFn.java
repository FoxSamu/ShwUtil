package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code float} and an {@code int} and returns nothing.
 */
public interface RefFloatInt2VoidFn<P1> {
    void invoke(P1 p1, float p2, int p3);
}
