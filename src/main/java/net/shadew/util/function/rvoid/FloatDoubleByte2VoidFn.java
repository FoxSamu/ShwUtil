package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code byte} and returns nothing.
 */
public interface FloatDoubleByte2VoidFn {
    void invoke(float p1, double p2, byte p3);
}
