package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code byte} and an object and returns nothing.
 */
public interface ShortByteRef2VoidFn<P3> {
    void invoke(short p1, byte p2, P3 p3);
}
