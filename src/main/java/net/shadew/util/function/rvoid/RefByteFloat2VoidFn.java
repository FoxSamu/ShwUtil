package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code byte} and a {@code float} and returns nothing.
 */
public interface RefByteFloat2VoidFn<P1> {
    void invoke(P1 p1, byte p2, float p3);
}
