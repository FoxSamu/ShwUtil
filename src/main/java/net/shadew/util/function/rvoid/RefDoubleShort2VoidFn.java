package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code double} and a {@code short} and returns nothing.
 */
public interface RefDoubleShort2VoidFn<P1> {
    void invoke(P1 p1, double p2, short p3);
}
