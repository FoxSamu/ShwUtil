package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code boolean} and a {@code double} and returns nothing.
 */
public interface RefBoolDouble2VoidFn<P1> {
    void invoke(P1 p1, boolean p2, double p3);
}
