package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code char} and returns nothing.
 */
public interface ByteByteChar2VoidFn {
    void invoke(byte p1, byte p2, char p3);
}
