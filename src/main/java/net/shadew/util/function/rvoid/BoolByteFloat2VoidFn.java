package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code float} and returns nothing.
 */
public interface BoolByteFloat2VoidFn {
    void invoke(boolean p1, byte p2, float p3);
}
