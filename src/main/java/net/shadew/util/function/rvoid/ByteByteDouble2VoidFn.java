package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns nothing.
 */
public interface ByteByteDouble2VoidFn {
    void invoke(byte p1, byte p2, double p3);
}
