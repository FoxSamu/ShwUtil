package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code double} and returns nothing.
 */
public interface CharByteDouble2VoidFn {
    void invoke(char p1, byte p2, double p3);
}
