package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code char} and returns nothing.
 */
public interface FloatBoolChar2VoidFn {
    void invoke(float p1, boolean p2, char p3);
}
