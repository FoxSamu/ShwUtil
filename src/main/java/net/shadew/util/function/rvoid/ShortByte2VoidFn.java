package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short} and a {@code byte} and returns nothing.
 */
public interface ShortByte2VoidFn {
    void invoke(short p1, byte p2);
}
