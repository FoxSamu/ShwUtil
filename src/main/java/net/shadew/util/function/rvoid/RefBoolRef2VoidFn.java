package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code boolean} and an object and returns nothing.
 */
public interface RefBoolRef2VoidFn<P1, P3> {
    void invoke(P1 p1, boolean p2, P3 p3);
}
