package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code byte} and returns nothing.
 */
public interface BoolByteByte2VoidFn {
    void invoke(boolean p1, byte p2, byte p3);
}
