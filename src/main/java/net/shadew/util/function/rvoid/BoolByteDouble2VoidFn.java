package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code double} and returns nothing.
 */
public interface BoolByteDouble2VoidFn {
    void invoke(boolean p1, byte p2, double p3);
}
