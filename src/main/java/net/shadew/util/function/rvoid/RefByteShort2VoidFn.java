package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code byte} and a {@code short} and returns nothing.
 */
public interface RefByteShort2VoidFn<P1> {
    void invoke(P1 p1, Byte p2, Short p3);
}
