package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns nothing.
 */
public interface FloatByteDouble2VoidFn {
    void invoke(float p1, byte p2, double p3);
}
