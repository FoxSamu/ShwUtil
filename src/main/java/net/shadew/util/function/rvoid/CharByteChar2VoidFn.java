package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns nothing.
 */
public interface CharByteChar2VoidFn {
    void invoke(char p1, byte p2, char p3);
}
