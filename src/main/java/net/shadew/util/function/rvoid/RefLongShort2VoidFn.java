package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code long} and a {@code short} and returns nothing.
 */
public interface RefLongShort2VoidFn<P1> {
    void invoke(P1 p1, Long p2, Short p3);
}
