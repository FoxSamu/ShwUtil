package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code short} and returns nothing.
 */
public interface ShortByteShort2VoidFn {
    void invoke(short p1, byte p2, short p3);
}
