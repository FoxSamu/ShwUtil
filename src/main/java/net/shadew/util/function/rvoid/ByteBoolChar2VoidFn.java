package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns nothing.
 */
public interface ByteBoolChar2VoidFn {
    void invoke(byte p1, boolean p2, char p3);
}
