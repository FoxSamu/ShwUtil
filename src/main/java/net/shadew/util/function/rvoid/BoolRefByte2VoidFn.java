package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean}, an object and a {@code byte} and returns nothing.
 */
public interface BoolRefByte2VoidFn<P2> {
    void invoke(boolean p1, P2 p2, byte p3);
}
