package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code double} and returns nothing.
 */
public interface IntByteDouble2VoidFn {
    void invoke(int p1, byte p2, double p3);
}
