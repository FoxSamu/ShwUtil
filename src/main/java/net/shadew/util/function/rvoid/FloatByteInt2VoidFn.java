package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns nothing.
 */
public interface FloatByteInt2VoidFn {
    void invoke(float p1, byte p2, int p3);
}
