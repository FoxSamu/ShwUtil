package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code byte} and returns nothing.
 */
public interface ByteByteByte2VoidFn {
    void invoke(byte p1, byte p2, byte p3);
}
