package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code double} and returns nothing.
 */
public interface LongByteDouble2VoidFn {
    void invoke(long p1, byte p2, double p3);
}
