package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code short} and returns nothing.
 */
public interface ByteDoubleShort2VoidFn {
    void invoke(byte p1, double p2, short p3);
}
