package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code boolean} and a {@code byte} and returns nothing.
 */
public interface RefBoolByte2VoidFn<P1> {
    void invoke(P1 p1, boolean p2, byte p3);
}
