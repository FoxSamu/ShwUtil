package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code byte} and returns nothing.
 */
public interface LongByteByte2VoidFn {
    void invoke(long p1, byte p2, byte p3);
}
