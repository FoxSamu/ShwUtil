package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean} and a {@code byte} and returns nothing.
 */
public interface BoolByte2VoidFn {
    void invoke(boolean p1, byte p2);
}
