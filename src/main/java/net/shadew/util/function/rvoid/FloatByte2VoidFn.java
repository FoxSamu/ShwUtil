package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float} and a {@code byte} and returns nothing.
 */
public interface FloatByte2VoidFn {
    void invoke(float p1, byte p2);
}
