package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code char} and a {@code byte} and returns nothing.
 */
public interface CharByte2VoidFn {
    void invoke(char p1, byte p2);
}
