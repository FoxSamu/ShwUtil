package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code byte} and returns nothing.
 */
public interface ShortByteByte2VoidFn {
    void invoke(short p1, byte p2, byte p3);
}
