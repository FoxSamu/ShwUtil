package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code byte} and an object and returns nothing.
 */
public interface FloatByteRef2VoidFn<P3> {
    void invoke(float p1, byte p2, P3 p3);
}
