package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code byte} and returns nothing.
 */
public interface CharDoubleByte2VoidFn {
    void invoke(char p1, double p2, byte p3);
}
