package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code byte} and returns nothing.
 */
public interface IntBoolByte2VoidFn {
    void invoke(int p1, boolean p2, byte p3);
}
