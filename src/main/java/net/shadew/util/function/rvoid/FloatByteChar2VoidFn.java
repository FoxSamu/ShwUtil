package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code char} and returns nothing.
 */
public interface FloatByteChar2VoidFn {
    void invoke(float p1, byte p2, char p3);
}
