package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns nothing.
 */
public interface BoolDouble2VoidFn {
    void invoke(boolean p1, double p2);
}
