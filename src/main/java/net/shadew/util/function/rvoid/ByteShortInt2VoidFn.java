package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code short} and an {@code int} and returns nothing.
 */
public interface ByteShortInt2VoidFn {
    void invoke(byte p1, short p2, int p3);
}
