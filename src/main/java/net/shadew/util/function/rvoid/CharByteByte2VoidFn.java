package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code byte} and returns nothing.
 */
public interface CharByteByte2VoidFn {
    void invoke(char p1, byte p2, byte p3);
}
