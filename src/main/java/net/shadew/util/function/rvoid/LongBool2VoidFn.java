package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long} and a {@code boolean} and returns nothing.
 */
public interface LongBool2VoidFn {
    void invoke(long p1, boolean p2);
}
