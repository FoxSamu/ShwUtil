package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code boolean} and a {@code short} and returns nothing.
 */
public interface RefBoolShort2VoidFn<P1> {
    void invoke(P1 p1, boolean p2, short p3);
}
