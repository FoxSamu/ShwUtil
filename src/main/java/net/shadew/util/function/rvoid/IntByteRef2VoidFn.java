package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code byte} and an object and returns nothing.
 */
public interface IntByteRef2VoidFn<P3> {
    void invoke(int p1, byte p2, P3 p3);
}
