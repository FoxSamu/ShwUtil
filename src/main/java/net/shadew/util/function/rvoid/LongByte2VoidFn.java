package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long} and a {@code byte} and returns nothing.
 */
public interface LongByte2VoidFn {
    void invoke(long p1, byte p2);
}
