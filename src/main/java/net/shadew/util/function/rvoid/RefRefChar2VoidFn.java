package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, an object and a {@code char} and returns nothing.
 */
public interface RefRefChar2VoidFn<P1, P2> {
    void invoke(P1 p1, P2 p2, char p3);
}
