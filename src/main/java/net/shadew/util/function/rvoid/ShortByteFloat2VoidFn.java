package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code float} and returns nothing.
 */
public interface ShortByteFloat2VoidFn {
    void invoke(short p1, byte p2, float p3);
}
