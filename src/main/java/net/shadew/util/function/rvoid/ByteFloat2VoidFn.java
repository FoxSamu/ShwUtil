package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte} and a {@code float} and returns nothing.
 */
public interface ByteFloat2VoidFn {
    void invoke(byte p1, float p2);
}
