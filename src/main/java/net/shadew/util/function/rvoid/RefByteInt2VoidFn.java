package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code byte} and an {@code int} and returns nothing.
 */
public interface RefByteInt2VoidFn<P1> {
    void invoke(P1 p1, byte p2, int p3);
}
