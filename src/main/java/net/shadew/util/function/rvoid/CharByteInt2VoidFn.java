package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code char}, a {@code byte} and an {@code int} and returns nothing.
 */
public interface CharByteInt2VoidFn {
    void invoke(char p1, byte p2, int p3);
}
