package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code byte} and returns nothing.
 */
public interface ByteBoolByte2VoidFn {
    void invoke(byte p1, boolean p2, byte p3);
}
