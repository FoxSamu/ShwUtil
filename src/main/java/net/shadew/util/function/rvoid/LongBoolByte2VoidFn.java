package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code byte} and returns nothing.
 */
public interface LongBoolByte2VoidFn {
    void invoke(long p1, boolean p2, byte p3);
}
