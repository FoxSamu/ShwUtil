package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an {@code int} and returns nothing.
 */
public interface ByteBoolInt2VoidFn {
    void invoke(byte p1, boolean p2, int p3);
}
