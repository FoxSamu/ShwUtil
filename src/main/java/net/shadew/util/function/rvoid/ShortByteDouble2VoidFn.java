package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code double} and returns nothing.
 */
public interface ShortByteDouble2VoidFn {
    void invoke(short p1, byte p2, double p3);
}
