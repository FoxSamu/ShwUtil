package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code boolean} and an {@code int} and returns nothing.
 */
public interface RefBoolInt2VoidFn<P1> {
    void invoke(P1 p1, boolean p2, int p3);
}
