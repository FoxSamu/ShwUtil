package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code char} and returns nothing.
 */
public interface ByteShortChar2VoidFn {
    void invoke(byte p1, short p2, char p3);
}
