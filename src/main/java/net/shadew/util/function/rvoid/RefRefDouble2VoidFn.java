package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, an object and a {@code double} and returns nothing.
 */
public interface RefRefDouble2VoidFn<P1, P2> {
    void invoke(P1 p1, P2 p2, double p3);
}
