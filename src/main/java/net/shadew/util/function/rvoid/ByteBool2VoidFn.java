package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns nothing.
 */
public interface ByteBool2VoidFn {
    void invoke(byte p1, boolean p2);
}
