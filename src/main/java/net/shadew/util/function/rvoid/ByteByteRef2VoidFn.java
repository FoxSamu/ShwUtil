package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and an object and returns nothing.
 */
public interface ByteByteRef2VoidFn<P3> {
    void invoke(byte p1, byte p2, P3 p3);
}
