package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code double} and a {@code byte} and returns nothing.
 */
public interface DoubleByte2VoidFn {
    void invoke(double p1, byte p2);
}
