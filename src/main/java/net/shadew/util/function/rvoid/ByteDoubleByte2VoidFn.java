package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code byte} and returns nothing.
 */
public interface ByteDoubleByte2VoidFn {
    void invoke(byte p1, double p2, byte p3);
}
