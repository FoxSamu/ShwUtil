package net.shadew.util.function.rvoid;

/**
 * A function that takes an object, a {@code boolean} and a {@code char} and returns nothing.
 */
public interface RefBoolChar2VoidFn<P1> {
    void invoke(P1 p1, boolean p2, char p3);
}
