package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns nothing.
 */
public interface ShortDoubleInt2VoidFn {
    void invoke(short p1, double p2, int p3);
}
