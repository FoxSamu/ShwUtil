package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long}, a {@code double} and an {@code int} and returns nothing.
 */
public interface LongDoubleInt2VoidFn {
    void invoke(long p1, double p2, int p3);
}
