package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns nothing.
 */
public interface IntByteChar2VoidFn {
    void invoke(int p1, byte p2, char p3);
}
