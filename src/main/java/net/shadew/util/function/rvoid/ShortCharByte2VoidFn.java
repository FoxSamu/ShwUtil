package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns nothing.
 */
public interface ShortCharByte2VoidFn {
    void invoke(short p1, char p2, byte p3);
}
