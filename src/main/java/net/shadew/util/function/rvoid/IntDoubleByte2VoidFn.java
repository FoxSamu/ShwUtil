package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns nothing.
 */
public interface IntDoubleByte2VoidFn {
    void invoke(int p1, double p2, byte p3);
}
