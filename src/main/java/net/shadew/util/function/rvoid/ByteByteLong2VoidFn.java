package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code long} and returns nothing.
 */
public interface ByteByteLong2VoidFn {
    void invoke(byte p1, byte p2, long p3);
}
