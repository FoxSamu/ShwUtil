package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code double} and returns nothing.
 */
public interface ShortDoubleDouble2VoidFn {
    void invoke(short p1, double p2, double p3);
}
