package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code double} and returns nothing.
 */
public interface ByteShortDouble2VoidFn {
    void invoke(byte p1, short p2, double p3);
}
