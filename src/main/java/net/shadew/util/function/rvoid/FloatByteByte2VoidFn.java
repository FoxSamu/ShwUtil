package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code byte} and returns nothing.
 */
public interface FloatByteByte2VoidFn {
    void invoke(float p1, byte p2, byte p3);
}
