package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code boolean} and an {@code int} and returns nothing.
 */
public interface BoolInt2VoidFn {
    void invoke(boolean p1, int p2);
}
