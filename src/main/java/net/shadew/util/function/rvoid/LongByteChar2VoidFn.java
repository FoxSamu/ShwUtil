package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code char} and returns nothing.
 */
public interface LongByteChar2VoidFn {
    void invoke(long p1, byte p2, char p3);
}
