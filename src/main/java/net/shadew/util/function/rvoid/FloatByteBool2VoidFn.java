package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns nothing.
 */
public interface FloatByteBool2VoidFn {
    void invoke(float p1, byte p2, boolean p3);
}
