package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long}, a {@code byte} and an {@code int} and returns nothing.
 */
public interface LongByteInt2VoidFn {
    void invoke(long p1, byte p2, int p3);
}
