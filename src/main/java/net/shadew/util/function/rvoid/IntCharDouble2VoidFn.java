package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code double} and returns nothing.
 */
public interface IntCharDouble2VoidFn {
    void invoke(int p1, char p2, double p3);
}
