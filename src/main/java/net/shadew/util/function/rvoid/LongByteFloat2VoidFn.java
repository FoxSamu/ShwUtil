package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code float} and returns nothing.
 */
public interface LongByteFloat2VoidFn {
    void invoke(long p1, byte p2, float p3);
}
