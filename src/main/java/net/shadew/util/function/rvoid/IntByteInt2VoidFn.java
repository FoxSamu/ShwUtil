package net.shadew.util.function.rvoid;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns nothing.
 */
public interface IntByteInt2VoidFn {
    void invoke(int p1, byte p2, int p3);
}
