package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte} and a {@code short} and returns nothing.
 */
public interface ByteShort2VoidFn {
    void invoke(byte p1, short p2);
}
