package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code byte}, a {@code char} and an object and returns nothing.
 */
public interface ByteCharRef2VoidFn<P3> {
    void invoke(byte p1, char p2, P3 p3);
}
