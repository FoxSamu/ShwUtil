package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code byte} and returns nothing.
 */
public interface LongDoubleByte2VoidFn {
    void invoke(long p1, double p2, byte p3);
}
