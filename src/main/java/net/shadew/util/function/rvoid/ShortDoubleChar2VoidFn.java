package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code char} and returns nothing.
 */
public interface ShortDoubleChar2VoidFn {
    void invoke(short p1, double p2, char p3);
}
