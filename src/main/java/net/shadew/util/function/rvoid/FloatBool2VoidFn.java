package net.shadew.util.function.rvoid;

/**
 * A function that takes a {@code float} and a {@code boolean} and returns nothing.
 */
public interface FloatBool2VoidFn {
    void invoke(float p1, boolean p2);
}
