package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface ByteShortChar2IntFn {
    int invoke(byte p1, short p2, char p3);
}
