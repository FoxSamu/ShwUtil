package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code double} and returns an {@code int}.
 */
public interface FloatBoolDouble2IntFn {
    int invoke(float p1, boolean p2, double p3);
}
