package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code short} and returns an {@code int}.
 */
public interface ShortDoubleShort2IntFn {
    int invoke(short p1, double p2, short p3);
}
