package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code byte} and returns an {@code int}.
 */
public interface FloatIntByte2IntFn {
    int invoke(float p1, int p2, byte p3);
}
