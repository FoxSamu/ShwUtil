package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code short} and returns an {@code int}.
 */
public interface ByteByteShort2IntFn {
    int invoke(byte p1, byte p2, short p3);
}
