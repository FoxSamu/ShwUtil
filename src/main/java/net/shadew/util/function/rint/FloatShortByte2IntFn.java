package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code byte} and returns an {@code int}.
 */
public interface FloatShortByte2IntFn {
    int invoke(float p1, short p2, byte p3);
}
