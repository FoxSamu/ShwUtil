package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code boolean} and a {@code boolean} and returns an {@code int}.
 */
public interface RefBoolBool2IntFn<P1> {
    int invoke(P1 p1, boolean p2, boolean p3);
}
