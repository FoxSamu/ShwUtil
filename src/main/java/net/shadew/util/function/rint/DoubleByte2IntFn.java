package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double} and a {@code byte} and returns an {@code int}.
 */
public interface DoubleByte2IntFn {
    int invoke(double p1, byte p2);
}
