package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code byte} and returns an {@code int}.
 */
public interface FloatBoolByte2IntFn {
    int invoke(float p1, boolean p2, byte p3);
}
