package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code short} and a {@code double} and returns an {@code int}.
 */
public interface RefShortDouble2IntFn<P1> {
    int invoke(P1 p1, short p2, double p3);
}
