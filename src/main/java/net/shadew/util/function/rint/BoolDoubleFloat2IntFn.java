package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code float} and returns an {@code int}.
 */
public interface BoolDoubleFloat2IntFn {
    int invoke(boolean p1, double p2, float p3);
}
