package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code long} and an {@code int} and returns an {@code int}.
 */
public interface RefLongInt2IntFn<P1> {
    int invoke(P1 p1, long p2, int p3);
}
