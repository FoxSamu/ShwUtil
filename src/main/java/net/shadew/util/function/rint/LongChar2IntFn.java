package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long} and a {@code char} and returns an {@code int}.
 */
public interface LongChar2IntFn {
    int invoke(long p1, char p2);
}
