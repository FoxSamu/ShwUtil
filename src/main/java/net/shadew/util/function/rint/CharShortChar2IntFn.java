package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface CharShortChar2IntFn {
    int invoke(char p1, short p2, char p3);
}
