package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code short} and returns an {@code int}.
 */
public interface ShortFloatShort2IntFn {
    int invoke(short p1, float p2, short p3);
}
