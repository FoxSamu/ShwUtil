package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code char} and an object and returns an {@code int}.
 */
public interface FloatCharRef2IntFn<P3> {
    int invoke(float p1, char p2, P3 p3);
}
