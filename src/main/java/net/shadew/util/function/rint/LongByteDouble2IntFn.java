package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface LongByteDouble2IntFn {
    int invoke(long p1, byte p2, double p3);
}
