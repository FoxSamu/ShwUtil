package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code char} and returns an {@code int}.
 */
public interface BoolDoubleChar2IntFn {
    int invoke(boolean p1, double p2, char p3);
}
