package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface LongShortChar2IntFn {
    int invoke(long p1, short p2, char p3);
}
