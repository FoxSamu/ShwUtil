package net.shadew.util.function.rint;

/**
 * A function that takes an object, an {@code int} and a {@code boolean} and returns an {@code int}.
 */
public interface RefIntBool2IntFn<P1> {
    int invoke(P1 p1, int p2, boolean p3);
}
