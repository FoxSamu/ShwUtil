package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code byte} and returns an {@code int}.
 */
public interface FloatByteByte2IntFn {
    int invoke(float p1, byte p2, byte p3);
}
