package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code byte} and an {@code int} and returns an {@code int}.
 */
public interface RefByteInt2IntFn<P1> {
    int invoke(P1 p1, byte p2, int p3);
}
