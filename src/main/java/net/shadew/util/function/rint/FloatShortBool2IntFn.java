package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code boolean} and returns an {@code int}.
 */
public interface FloatShortBool2IntFn {
    int invoke(float p1, short p2, boolean p3);
}
