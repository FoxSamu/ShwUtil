package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char} and an {@code int} and returns an {@code int}.
 */
public interface CharInt2IntFn {
    int invoke(char p1, int p2);
}
