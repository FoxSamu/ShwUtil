package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code long} and a {@code short} and returns an {@code int}.
 */
public interface ByteLongShort2IntFn {
    int invoke(byte p1, long p2, short p3);
}
