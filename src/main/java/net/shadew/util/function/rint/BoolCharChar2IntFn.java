package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code char} and returns an {@code int}.
 */
public interface BoolCharChar2IntFn {
    int invoke(boolean p1, char p2, char p3);
}
