package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code float} and returns an {@code int}.
 */
public interface ShortBoolFloat2IntFn {
    int invoke(short p1, boolean p2, float p3);
}
