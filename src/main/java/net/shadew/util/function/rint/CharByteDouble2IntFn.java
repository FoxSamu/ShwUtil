package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface CharByteDouble2IntFn {
    int invoke(char p1, byte p2, double p3);
}
