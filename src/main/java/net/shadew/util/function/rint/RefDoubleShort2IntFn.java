package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code double} and a {@code short} and returns an {@code int}.
 */
public interface RefDoubleShort2IntFn<P1> {
    int invoke(P1 p1, double p2, short p3);
}
