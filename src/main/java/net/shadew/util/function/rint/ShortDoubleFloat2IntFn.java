package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code float} and returns an {@code int}.
 */
public interface ShortDoubleFloat2IntFn {
    int invoke(short p1, double p2, float p3);
}
