package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code char} and returns an {@code int}.
 */
public interface BoolFloatChar2IntFn {
    int invoke(boolean p1, float p2, char p3);
}
