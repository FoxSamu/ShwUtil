package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code char} and returns an {@code int}.
 */
public interface ByteDoubleChar2IntFn {
    int invoke(byte p1, double p2, char p3);
}
