package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code long} and a {@code short} and returns an {@code int}.
 */
public interface RefLongShort2IntFn<P1> {
    int invoke(P1 p1, long p2, short p3);
}
