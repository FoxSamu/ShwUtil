package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code float} and returns an {@code int}.
 */
public interface IntShortFloat2IntFn {
    int invoke(int p1, short p2, float p3);
}
