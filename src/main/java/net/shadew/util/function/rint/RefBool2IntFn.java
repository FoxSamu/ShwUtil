package net.shadew.util.function.rint;

/**
 * A function that takes an object and a {@code boolean} and returns an {@code int}.
 */
public interface RefBool2IntFn<P1> {
    int invoke(P1 p1, boolean p2);
}
