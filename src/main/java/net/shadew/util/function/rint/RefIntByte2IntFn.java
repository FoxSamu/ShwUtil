package net.shadew.util.function.rint;

/**
 * A function that takes an object, an {@code int} and a {@code byte} and returns an {@code int}.
 */
public interface RefIntByte2IntFn<P1> {
    int invoke(P1 p1, int p2, byte p3);
}
