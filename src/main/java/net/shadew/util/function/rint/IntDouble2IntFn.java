package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int} and a {@code double} and returns an {@code int}.
 */
public interface IntDouble2IntFn {
    int invoke(int p1, double p2);
}
