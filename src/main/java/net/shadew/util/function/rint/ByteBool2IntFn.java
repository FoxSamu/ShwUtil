package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns an {@code int}.
 */
public interface ByteBool2IntFn {
    int invoke(byte p1, boolean p2);
}
