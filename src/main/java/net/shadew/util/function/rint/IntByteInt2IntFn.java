package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns an {@code int}.
 */
public interface IntByteInt2IntFn {
    int invoke(int p1, byte p2, int p3);
}
