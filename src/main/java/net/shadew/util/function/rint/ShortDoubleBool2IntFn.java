package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code boolean} and returns an {@code int}.
 */
public interface ShortDoubleBool2IntFn {
    int invoke(short p1, double p2, boolean p3);
}
