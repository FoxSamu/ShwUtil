package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code boolean} and an {@code int} and returns an {@code int}.
 */
public interface ShortBoolInt2IntFn {
    int invoke(short p1, boolean p2, int p3);
}
