package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code float} and an object and returns an {@code int}.
 */
public interface CharFloatRef2IntFn<P3> {
    int invoke(char p1, float p2, P3 p3);
}
