package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long} and a {@code float} and returns an {@code int}.
 */
public interface LongFloat2IntFn {
    int invoke(long p1, float p2);
}
