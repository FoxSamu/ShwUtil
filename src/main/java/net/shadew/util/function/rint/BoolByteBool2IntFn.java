package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code boolean} and returns an {@code int}.
 */
public interface BoolByteBool2IntFn {
    int invoke(boolean p1, byte p2, boolean p3);
}
