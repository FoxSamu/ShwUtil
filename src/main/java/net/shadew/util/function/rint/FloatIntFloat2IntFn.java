package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code float} and returns an {@code int}.
 */
public interface FloatIntFloat2IntFn {
    int invoke(float p1, int p2, float p3);
}
