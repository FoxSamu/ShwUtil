package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code char} and an object and returns an {@code int}.
 */
public interface CharCharRef2IntFn<P3> {
    int invoke(char p1, char p2, P3 p3);
}
