package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code boolean} and an {@code int} and returns an {@code int}.
 */
public interface LongBoolInt2IntFn {
    int invoke(long p1, boolean p2, int p3);
}
