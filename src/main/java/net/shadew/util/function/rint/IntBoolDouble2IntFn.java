package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code double} and returns an {@code int}.
 */
public interface IntBoolDouble2IntFn {
    int invoke(int p1, boolean p2, double p3);
}
