package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code char} and returns an {@code int}.
 */
public interface ShortFloatChar2IntFn {
    int invoke(short p1, float p2, char p3);
}
