package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code boolean} and an {@code int} and returns an {@code int}.
 */
public interface RefBoolInt2IntFn<P1> {
    int invoke(P1 p1, boolean p2, int p3);
}
