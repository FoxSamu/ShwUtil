package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code char} and a {@code double} and returns an {@code int}.
 */
public interface RefCharDouble2IntFn<P1> {
    int invoke(P1 p1, char p2, double p3);
}
