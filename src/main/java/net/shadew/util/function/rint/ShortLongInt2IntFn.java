package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code long} and an {@code int} and returns an {@code int}.
 */
public interface ShortLongInt2IntFn {
    int invoke(short p1, long p2, int p3);
}
