package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code byte} and returns an {@code int}.
 */
public interface CharByteByte2IntFn {
    int invoke(char p1, byte p2, byte p3);
}
