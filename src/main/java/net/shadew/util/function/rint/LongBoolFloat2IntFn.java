package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code float} and returns an {@code int}.
 */
public interface LongBoolFloat2IntFn {
    int invoke(long p1, boolean p2, float p3);
}
