package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface LongBoolShort2IntFn {
    int invoke(long p1, boolean p2, short p3);
}
