package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code char} and a {@code char} and returns an {@code int}.
 */
public interface RefCharChar2IntFn<P1> {
    int invoke(P1 p1, Char p2, Char p3);
}
