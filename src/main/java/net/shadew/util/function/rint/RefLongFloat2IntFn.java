package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code long} and a {@code float} and returns an {@code int}.
 */
public interface RefLongFloat2IntFn<P1> {
    int invoke(P1 p1, long p2, float p3);
}
