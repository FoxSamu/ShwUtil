package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double} and a {@code double} and returns an {@code int}.
 */
public interface DoubleDouble2IntFn {
    int invoke(double p1, double p2);
}
