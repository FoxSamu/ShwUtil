package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code short} and returns an {@code int}.
 */
public interface FloatShortShort2IntFn {
    int invoke(float p1, short p2, short p3);
}
