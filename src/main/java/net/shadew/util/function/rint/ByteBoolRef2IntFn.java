package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an object and returns an {@code int}.
 */
public interface ByteBoolRef2IntFn<P3> {
    int invoke(byte p1, boolean p2, P3 p3);
}
