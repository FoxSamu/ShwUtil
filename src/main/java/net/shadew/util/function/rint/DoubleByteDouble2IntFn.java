package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface DoubleByteDouble2IntFn {
    int invoke(double p1, byte p2, double p3);
}
