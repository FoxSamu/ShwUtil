package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code char} and an {@code int} and returns an {@code int}.
 */
public interface ByteCharInt2IntFn {
    int invoke(byte p1, char p2, int p3);
}
