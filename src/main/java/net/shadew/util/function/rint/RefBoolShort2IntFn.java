package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface RefBoolShort2IntFn<P1> {
    int invoke(P1 p1, boolean p2, short p3);
}
