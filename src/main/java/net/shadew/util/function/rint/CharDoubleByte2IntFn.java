package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code byte} and returns an {@code int}.
 */
public interface CharDoubleByte2IntFn {
    int invoke(char p1, double p2, byte p3);
}
