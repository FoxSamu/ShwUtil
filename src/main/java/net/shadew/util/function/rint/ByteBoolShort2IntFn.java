package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface ByteBoolShort2IntFn {
    int invoke(byte p1, boolean p2, short p3);
}
