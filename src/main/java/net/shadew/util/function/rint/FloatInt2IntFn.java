package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float} and an {@code int} and returns an {@code int}.
 */
public interface FloatInt2IntFn {
    int invoke(float p1, int p2);
}
