package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code char} and a {@code double} and returns an {@code int}.
 */
public interface CharCharDouble2IntFn {
    int invoke(char p1, char p2, double p3);
}
