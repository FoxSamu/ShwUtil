package net.shadew.util.function.rint;

/**
 * A function that takes an object and a {@code char} and returns an {@code int}.
 */
public interface RefChar2IntFn<P1> {
    int invoke(P1 p1, char p2);
}
