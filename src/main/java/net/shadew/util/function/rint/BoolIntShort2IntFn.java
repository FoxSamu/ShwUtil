package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, an {@code int} and a {@code short} and returns an {@code int}.
 */
public interface BoolIntShort2IntFn {
    int invoke(boolean p1, int p2, short p3);
}
