package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code float} and returns an {@code int}.
 */
public interface ByteBoolFloat2IntFn {
    int invoke(byte p1, boolean p2, float p3);
}
