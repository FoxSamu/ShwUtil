package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double} and a {@code float} and returns an {@code int}.
 */
public interface DoubleFloat2IntFn {
    int invoke(double p1, float p2);
}
