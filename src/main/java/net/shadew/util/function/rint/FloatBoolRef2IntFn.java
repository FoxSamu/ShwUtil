package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code boolean} and an object and returns an {@code int}.
 */
public interface FloatBoolRef2IntFn<P3> {
    int invoke(float p1, boolean p2, P3 p3);
}
