package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code double} and returns an {@code int}.
 */
public interface FloatShortDouble2IntFn {
    int invoke(float p1, short p2, double p3);
}
