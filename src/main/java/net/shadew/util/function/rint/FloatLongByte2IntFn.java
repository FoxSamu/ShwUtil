package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code byte} and returns an {@code int}.
 */
public interface FloatLongByte2IntFn {
    int invoke(float p1, long p2, byte p3);
}
