package net.shadew.util.function.rint;

/**
 * A function that takes an object and a {@code float} and returns an {@code int}.
 */
public interface RefFloat2IntFn<P1> {
    int invoke(P1 p1, float p2);
}
