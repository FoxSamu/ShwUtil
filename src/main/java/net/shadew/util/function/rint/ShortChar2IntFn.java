package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface ShortChar2IntFn {
    int invoke(short p1, char p2);
}
