package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code byte} and returns an {@code int}.
 */
public interface ShortShortByte2IntFn {
    int invoke(short p1, short p2, byte p3);
}
