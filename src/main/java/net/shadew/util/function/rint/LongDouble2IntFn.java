package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long} and a {@code double} and returns an {@code int}.
 */
public interface LongDouble2IntFn {
    int invoke(long p1, double p2);
}
