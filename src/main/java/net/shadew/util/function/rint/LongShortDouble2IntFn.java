package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code double} and returns an {@code int}.
 */
public interface LongShortDouble2IntFn {
    int invoke(long p1, short p2, double p3);
}
