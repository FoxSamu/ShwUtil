package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code short} and returns an {@code int}.
 */
public interface CharDoubleShort2IntFn {
    int invoke(char p1, double p2, short p3);
}
