package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code boolean} and an object and returns an {@code int}.
 */
public interface CharBoolRef2IntFn<P3> {
    int invoke(char p1, boolean p2, P3 p3);
}
