package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code byte} and returns an {@code int}.
 */
public interface ByteByteByte2IntFn {
    int invoke(byte p1, byte p2, byte p3);
}
