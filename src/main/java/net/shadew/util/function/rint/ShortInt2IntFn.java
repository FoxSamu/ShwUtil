package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short} and an {@code int} and returns an {@code int}.
 */
public interface ShortInt2IntFn {
    int invoke(short p1, int p2);
}
