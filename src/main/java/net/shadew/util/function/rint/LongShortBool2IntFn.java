package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code boolean} and returns an {@code int}.
 */
public interface LongShortBool2IntFn {
    int invoke(long p1, short p2, boolean p3);
}
