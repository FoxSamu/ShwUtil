package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code float} and returns an {@code int}.
 */
public interface LongDoubleFloat2IntFn {
    int invoke(long p1, double p2, float p3);
}
