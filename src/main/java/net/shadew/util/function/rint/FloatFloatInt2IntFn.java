package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code float} and an {@code int} and returns an {@code int}.
 */
public interface FloatFloatInt2IntFn {
    int invoke(Float p1, Float p2, Int p3);
}
