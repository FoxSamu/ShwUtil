package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code boolean} and returns an {@code int}.
 */
public interface ShortShortBool2IntFn {
    int invoke(short p1, short p2, boolean p3);
}
