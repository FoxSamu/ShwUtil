package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface IntBoolShort2IntFn {
    int invoke(int p1, boolean p2, short p3);
}
