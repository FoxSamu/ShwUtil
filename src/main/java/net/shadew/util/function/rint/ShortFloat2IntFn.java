package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short} and a {@code float} and returns an {@code int}.
 */
public interface ShortFloat2IntFn {
    int invoke(short p1, float p2);
}
