package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns an {@code int}.
 */
public interface BoolBool2IntFn {
    int invoke(boolean p1, boolean p2);
}
