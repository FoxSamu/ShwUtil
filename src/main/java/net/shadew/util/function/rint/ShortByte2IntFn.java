package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short} and a {@code byte} and returns an {@code int}.
 */
public interface ShortByte2IntFn {
    int invoke(short p1, byte p2);
}
