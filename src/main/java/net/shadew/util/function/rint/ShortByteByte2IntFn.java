package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code byte} and returns an {@code int}.
 */
public interface ShortByteByte2IntFn {
    int invoke(short p1, byte p2, byte p3);
}
