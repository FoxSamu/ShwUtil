package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code byte} and an {@code int} and returns an {@code int}.
 */
public interface CharByteInt2IntFn {
    int invoke(char p1, byte p2, int p3);
}
