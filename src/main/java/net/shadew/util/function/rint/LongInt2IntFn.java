package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long} and an {@code int} and returns an {@code int}.
 */
public interface LongInt2IntFn {
    int invoke(long p1, int p2);
}
