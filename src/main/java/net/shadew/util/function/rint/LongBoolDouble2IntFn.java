package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code double} and returns an {@code int}.
 */
public interface LongBoolDouble2IntFn {
    int invoke(long p1, boolean p2, double p3);
}
