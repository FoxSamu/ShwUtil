package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, an {@code int} and an object and returns an {@code int}.
 */
public interface ShortIntRef2IntFn<P3> {
    int invoke(short p1, int p2, P3 p3);
}
