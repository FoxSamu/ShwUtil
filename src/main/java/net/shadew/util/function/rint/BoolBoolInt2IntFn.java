package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an {@code int} and returns an {@code int}.
 */
public interface BoolBoolInt2IntFn {
    int invoke(boolean p1, boolean p2, int p3);
}
