package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int} and a {@code char} and returns an {@code int}.
 */
public interface IntChar2IntFn {
    int invoke(int p1, char p2);
}
