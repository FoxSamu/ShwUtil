package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface IntByteDouble2IntFn {
    int invoke(int p1, byte p2, double p3);
}
