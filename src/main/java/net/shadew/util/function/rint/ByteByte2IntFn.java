package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte} and a {@code byte} and returns an {@code int}.
 */
public interface ByteByte2IntFn {
    int invoke(byte p1, byte p2);
}
