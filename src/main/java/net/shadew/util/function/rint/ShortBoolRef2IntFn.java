package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code boolean} and an object and returns an {@code int}.
 */
public interface ShortBoolRef2IntFn<P3> {
    int invoke(short p1, boolean p2, P3 p3);
}
