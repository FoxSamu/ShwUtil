package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code boolean} and returns an {@code int}.
 */
public interface FloatIntBool2IntFn {
    int invoke(float p1, int p2, boolean p3);
}
