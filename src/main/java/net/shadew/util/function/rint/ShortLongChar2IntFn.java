package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code char} and returns an {@code int}.
 */
public interface ShortLongChar2IntFn {
    int invoke(short p1, long p2, char p3);
}
