package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code boolean} and returns an {@code int}.
 */
public interface BoolFloatBool2IntFn {
    int invoke(boolean p1, float p2, boolean p3);
}
