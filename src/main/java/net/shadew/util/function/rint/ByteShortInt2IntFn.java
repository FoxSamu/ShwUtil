package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code short} and an {@code int} and returns an {@code int}.
 */
public interface ByteShortInt2IntFn {
    int invoke(byte p1, short p2, int p3);
}
