package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float} and a {@code short} and returns an {@code int}.
 */
public interface FloatShort2IntFn {
    int invoke(float p1, short p2);
}
