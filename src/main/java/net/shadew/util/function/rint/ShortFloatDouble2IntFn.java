package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code double} and returns an {@code int}.
 */
public interface ShortFloatDouble2IntFn {
    int invoke(short p1, float p2, double p3);
}
