package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code boolean} and returns an {@code int}.
 */
public interface IntByteBool2IntFn {
    int invoke(int p1, byte p2, boolean p3);
}
