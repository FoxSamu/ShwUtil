package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code char} and returns an {@code int}.
 */
public interface LongDoubleChar2IntFn {
    int invoke(long p1, double p2, char p3);
}
