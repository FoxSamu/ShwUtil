package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code short} and returns an {@code int}.
 */
public interface BoolByteShort2IntFn {
    int invoke(boolean p1, byte p2, short p3);
}
