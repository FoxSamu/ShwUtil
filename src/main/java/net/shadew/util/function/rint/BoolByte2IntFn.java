package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean} and a {@code byte} and returns an {@code int}.
 */
public interface BoolByte2IntFn {
    int invoke(boolean p1, byte p2);
}
