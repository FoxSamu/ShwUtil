package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code char} and returns an {@code int}.
 */
public interface FloatLongChar2IntFn {
    int invoke(Float p1, Long p2, Char p3);
}
