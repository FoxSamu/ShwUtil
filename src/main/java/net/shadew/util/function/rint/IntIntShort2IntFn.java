package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, an {@code int} and a {@code short} and returns an {@code int}.
 */
public interface IntIntShort2IntFn {
    int invoke(int p1, int p2, short p3);
}
