package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code short} and returns an {@code int}.
 */
public interface CharByteShort2IntFn {
    int invoke(char p1, byte p2, short p3);
}
