package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code char} and returns an {@code int}.
 */
public interface LongIntChar2IntFn {
    int invoke(long p1, int p2, char p3);
}
