package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code double} and returns an {@code int}.
 */
public interface IntCharDouble2IntFn {
    int invoke(int p1, char p2, double p3);
}
