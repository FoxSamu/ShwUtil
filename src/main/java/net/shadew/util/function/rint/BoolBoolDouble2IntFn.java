package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code double} and returns an {@code int}.
 */
public interface BoolBoolDouble2IntFn {
    int invoke(Bool p1, Bool p2, Double p3);
}
