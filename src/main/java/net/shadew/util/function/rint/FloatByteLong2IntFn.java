package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code long} and returns an {@code int}.
 */
public interface FloatByteLong2IntFn {
    int invoke(float p1, byte p2, long p3);
}
