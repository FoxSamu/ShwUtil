package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float} and a {@code byte} and returns an {@code int}.
 */
public interface FloatByte2IntFn {
    int invoke(float p1, byte p2);
}
