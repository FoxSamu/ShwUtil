package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code boolean} and returns an {@code int}.
 */
public interface ShortBoolBool2IntFn {
    int invoke(short p1, boolean p2, boolean p3);
}
