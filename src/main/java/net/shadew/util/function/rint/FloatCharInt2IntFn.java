package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code char} and an {@code int} and returns an {@code int}.
 */
public interface FloatCharInt2IntFn {
    int invoke(float p1, char p2, int p3);
}
