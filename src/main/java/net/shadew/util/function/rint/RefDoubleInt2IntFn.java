package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code double} and an {@code int} and returns an {@code int}.
 */
public interface RefDoubleInt2IntFn<P1> {
    int invoke(P1 p1, double p2, int p3);
}
