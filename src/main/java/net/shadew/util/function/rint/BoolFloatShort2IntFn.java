package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code short} and returns an {@code int}.
 */
public interface BoolFloatShort2IntFn {
    int invoke(boolean p1, float p2, short p3);
}
