package net.shadew.util.function.rint;

/**
 * A function that takes an object, an {@code int} and a {@code short} and returns an {@code int}.
 */
public interface RefIntShort2IntFn<P1> {
    int invoke(P1 p1, int p2, short p3);
}
