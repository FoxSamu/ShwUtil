package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface CharByteChar2IntFn {
    int invoke(char p1, byte p2, char p3);
}
