package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code char} and an object and returns an {@code int}.
 */
public interface ShortCharRef2IntFn<P3> {
    int invoke(short p1, char p2, P3 p3);
}
