package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface IntShortChar2IntFn {
    int invoke(int p1, short p2, char p3);
}
