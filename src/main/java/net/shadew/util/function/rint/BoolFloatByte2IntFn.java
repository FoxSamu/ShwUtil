package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code byte} and returns an {@code int}.
 */
public interface BoolFloatByte2IntFn {
    int invoke(boolean p1, float p2, byte p3);
}
