package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code char} and an {@code int} and returns an {@code int}.
 */
public interface CharCharInt2IntFn {
    int invoke(Char p1, Char p2, Int p3);
}
