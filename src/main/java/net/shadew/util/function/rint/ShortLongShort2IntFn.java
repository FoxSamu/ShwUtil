package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code short} and returns an {@code int}.
 */
public interface ShortLongShort2IntFn {
    int invoke(short p1, long p2, short p3);
}
