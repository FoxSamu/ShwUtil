package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code double} and a {@code boolean} and returns an {@code int}.
 */
public interface RefDoubleBool2IntFn<P1> {
    int invoke(P1 p1, double p2, boolean p3);
}
