package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code float} and a {@code boolean} and returns an {@code int}.
 */
public interface CharFloatBool2IntFn {
    int invoke(Char p1, Float p2, Bool p3);
}
