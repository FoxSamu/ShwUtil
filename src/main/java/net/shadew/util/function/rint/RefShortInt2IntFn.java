package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code short} and an {@code int} and returns an {@code int}.
 */
public interface RefShortInt2IntFn<P1> {
    int invoke(P1 p1, short p2, int p3);
}
