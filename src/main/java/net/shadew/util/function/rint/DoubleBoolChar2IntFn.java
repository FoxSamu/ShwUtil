package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code char} and returns an {@code int}.
 */
public interface DoubleBoolChar2IntFn {
    int invoke(double p1, boolean p2, char p3);
}
