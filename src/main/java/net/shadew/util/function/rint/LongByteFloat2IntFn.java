package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code float} and returns an {@code int}.
 */
public interface LongByteFloat2IntFn {
    int invoke(long p1, byte p2, float p3);
}
