package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code byte} and an object and returns an {@code int}.
 */
public interface IntByteRef2IntFn<P3> {
    int invoke(int p1, byte p2, P3 p3);
}
