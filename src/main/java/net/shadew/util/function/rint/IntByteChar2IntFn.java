package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface IntByteChar2IntFn {
    int invoke(int p1, byte p2, char p3);
}
