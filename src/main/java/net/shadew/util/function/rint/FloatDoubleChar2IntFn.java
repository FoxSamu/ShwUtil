package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code char} and returns an {@code int}.
 */
public interface FloatDoubleChar2IntFn {
    int invoke(float p1, double p2, char p3);
}
