package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code byte} and a {@code byte} and returns an {@code int}.
 */
public interface RefByteByte2IntFn<P1> {
    int invoke(P1 p1, byte p2, byte p3);
}
