package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short} and a {@code short} and returns an {@code int}.
 */
public interface ShortShort2IntFn {
    int invoke(short p1, short p2);
}
