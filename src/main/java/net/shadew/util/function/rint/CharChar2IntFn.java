package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char} and a {@code char} and returns an {@code int}.
 */
public interface CharChar2IntFn {
    int invoke(char p1, char p2);
}
