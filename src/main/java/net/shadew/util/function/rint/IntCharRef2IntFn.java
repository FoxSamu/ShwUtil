package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code char} and an object and returns an {@code int}.
 */
public interface IntCharRef2IntFn<P3> {
    int invoke(int p1, char p2, P3 p3);
}
