package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code short} and a {@code double} and returns an {@code int}.
 */
public interface IntShortDouble2IntFn {
    int invoke(int p1, short p2, double p3);
}
