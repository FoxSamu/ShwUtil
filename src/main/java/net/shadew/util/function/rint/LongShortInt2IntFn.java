package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code short} and an {@code int} and returns an {@code int}.
 */
public interface LongShortInt2IntFn {
    int invoke(long p1, short p2, int p3);
}
