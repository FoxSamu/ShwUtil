package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface LongByteChar2IntFn {
    int invoke(Long p1, Byte p2, Char p3);
}
