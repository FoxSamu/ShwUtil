package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code double} and returns an {@code int}.
 */
public interface FloatCharDouble2IntFn {
    int invoke(float p1, char p2, double p3);
}
