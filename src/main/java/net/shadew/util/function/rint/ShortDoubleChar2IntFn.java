package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code char} and returns an {@code int}.
 */
public interface ShortDoubleChar2IntFn {
    int invoke(short p1, double p2, char p3);
}
