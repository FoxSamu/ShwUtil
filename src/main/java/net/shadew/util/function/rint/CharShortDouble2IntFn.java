package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code short} and a {@code double} and returns an {@code int}.
 */
public interface CharShortDouble2IntFn {
    int invoke(char p1, short p2, double p3);
}
