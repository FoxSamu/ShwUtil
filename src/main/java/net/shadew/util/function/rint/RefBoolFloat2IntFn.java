package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code boolean} and a {@code float} and returns an {@code int}.
 */
public interface RefBoolFloat2IntFn<P1> {
    int invoke(P1 p1, boolean p2, float p3);
}
