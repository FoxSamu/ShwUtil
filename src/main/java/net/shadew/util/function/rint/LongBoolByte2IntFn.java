package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code byte} and returns an {@code int}.
 */
public interface LongBoolByte2IntFn {
    int invoke(long p1, boolean p2, byte p3);
}
