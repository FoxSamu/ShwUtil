package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code boolean} and returns an {@code int}.
 */
public interface ByteFloatBool2IntFn {
    int invoke(byte p1, float p2, boolean p3);
}
