package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns an {@code int}.
 */
public interface ShortCharByte2IntFn {
    int invoke(short p1, char p2, byte p3);
}
