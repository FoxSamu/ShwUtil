package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface BoolByteChar2IntFn {
    int invoke(boolean p1, byte p2, char p3);
}
