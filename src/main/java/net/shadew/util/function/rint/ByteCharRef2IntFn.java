package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code char} and an object and returns an {@code int}.
 */
public interface ByteCharRef2IntFn<P3> {
    int invoke(byte p1, char p2, P3 p3);
}
