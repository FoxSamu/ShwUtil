package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code float} and returns an {@code int}.
 */
public interface FloatByteFloat2IntFn {
    int invoke(float p1, byte p2, float p3);
}
