package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code char} and returns an {@code int}.
 */
public interface LongCharChar2IntFn {
    int invoke(long p1, char p2, char p3);
}
