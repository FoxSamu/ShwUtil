package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double}, a {@code double} and a {@code byte} and returns an {@code int}.
 */
public interface DoubleDoubleByte2IntFn {
    int invoke(double p1, double p2, byte p3);
}
