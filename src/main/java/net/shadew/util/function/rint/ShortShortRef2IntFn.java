package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code short} and an object and returns an {@code int}.
 */
public interface ShortShortRef2IntFn<P3> {
    int invoke(short p1, short p2, P3 p3);
}
