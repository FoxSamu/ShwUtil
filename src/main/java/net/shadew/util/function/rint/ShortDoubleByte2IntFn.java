package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code byte} and returns an {@code int}.
 */
public interface ShortDoubleByte2IntFn {
    int invoke(short p1, double p2, byte p3);
}
