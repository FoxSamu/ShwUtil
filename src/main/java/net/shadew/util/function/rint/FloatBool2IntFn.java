package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float} and a {@code boolean} and returns an {@code int}.
 */
public interface FloatBool2IntFn {
    int invoke(float p1, boolean p2);
}
