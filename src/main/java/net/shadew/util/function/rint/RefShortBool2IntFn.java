package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code short} and a {@code boolean} and returns an {@code int}.
 */
public interface RefShortBool2IntFn<P1> {
    int invoke(P1 p1, short p2, boolean p3);
}
