package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code boolean} and returns an {@code int}.
 */
public interface ShortFloatBool2IntFn {
    int invoke(short p1, float p2, boolean p3);
}
