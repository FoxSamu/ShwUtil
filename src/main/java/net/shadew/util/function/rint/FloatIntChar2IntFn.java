package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code char} and returns an {@code int}.
 */
public interface FloatIntChar2IntFn {
    int invoke(float p1, int p2, char p3);
}
