package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code long} and returns an {@code int}.
 */
public interface ByteByteLong2IntFn {
    int invoke(byte p1, byte p2, long p3);
}
