package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code byte} and returns an {@code int}.
 */
public interface FloatDoubleByte2IntFn {
    int invoke(float p1, double p2, byte p3);
}
