package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code boolean} and returns an {@code int}.
 */
public interface ShortCharBool2IntFn {
    int invoke(short p1, char p2, boolean p3);
}
