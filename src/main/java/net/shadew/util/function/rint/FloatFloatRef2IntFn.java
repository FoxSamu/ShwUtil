package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code float} and an object and returns an {@code int}.
 */
public interface FloatFloatRef2IntFn<P3> {
    int invoke(float p1, float p2, P3 p3);
}
