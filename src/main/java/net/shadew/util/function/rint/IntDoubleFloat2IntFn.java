package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code float} and returns an {@code int}.
 */
public interface IntDoubleFloat2IntFn {
    int invoke(int p1, double p2, float p3);
}
