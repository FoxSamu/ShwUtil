package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns an {@code int}.
 */
public interface BoolDouble2IntFn {
    int invoke(boolean p1, double p2);
}
