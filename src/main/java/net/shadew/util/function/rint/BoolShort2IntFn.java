package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface BoolShort2IntFn {
    int invoke(boolean p1, short p2);
}
