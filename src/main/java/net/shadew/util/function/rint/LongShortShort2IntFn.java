package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code short} and returns an {@code int}.
 */
public interface LongShortShort2IntFn {
    int invoke(long p1, short p2, short p3);
}
