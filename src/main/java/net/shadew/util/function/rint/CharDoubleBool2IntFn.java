package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code boolean} and returns an {@code int}.
 */
public interface CharDoubleBool2IntFn {
    int invoke(char p1, double p2, boolean p3);
}
