package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float} and a {@code double} and returns an {@code int}.
 */
public interface FloatDouble2IntFn {
    int invoke(float p1, double p2);
}
