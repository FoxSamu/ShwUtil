package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code double} and returns an {@code int}.
 */
public interface ShortShortDouble2IntFn {
    int invoke(short p1, short p2, double p3);
}
