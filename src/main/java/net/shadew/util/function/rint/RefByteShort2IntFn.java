package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code byte} and a {@code short} and returns an {@code int}.
 */
public interface RefByteShort2IntFn<P1> {
    int invoke(P1 p1, byte p2, short p3);
}
