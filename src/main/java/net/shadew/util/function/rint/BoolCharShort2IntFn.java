package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code short} and returns an {@code int}.
 */
public interface BoolCharShort2IntFn {
    int invoke(boolean p1, char p2, short p3);
}
