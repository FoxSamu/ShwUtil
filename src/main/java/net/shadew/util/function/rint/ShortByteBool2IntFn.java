package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code boolean} and returns an {@code int}.
 */
public interface ShortByteBool2IntFn {
    int invoke(short p1, byte p2, boolean p3);
}
