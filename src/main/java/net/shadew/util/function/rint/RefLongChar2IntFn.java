package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code long} and a {@code char} and returns an {@code int}.
 */
public interface RefLongChar2IntFn<P1> {
    int invoke(P1 p1, long p2, char p3);
}
