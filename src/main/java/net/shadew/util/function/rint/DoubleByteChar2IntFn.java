package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface DoubleByteChar2IntFn {
    int invoke(double p1, byte p2, char p3);
}
