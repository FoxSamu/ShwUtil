package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean} and an {@code int} and returns an {@code int}.
 */
public interface BoolInt2IntFn {
    int invoke(boolean p1, int p2);
}
