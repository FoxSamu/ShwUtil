package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int} and a {@code boolean} and returns an {@code int}.
 */
public interface IntBool2IntFn {
    int invoke(int p1, boolean p2);
}
