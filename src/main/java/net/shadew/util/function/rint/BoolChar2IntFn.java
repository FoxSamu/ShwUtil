package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns an {@code int}.
 */
public interface BoolChar2IntFn {
    int invoke(boolean p1, char p2);
}
