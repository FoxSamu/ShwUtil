package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code short} and returns an {@code int}.
 */
public interface IntByteShort2IntFn {
    int invoke(int p1, byte p2, short p3);
}
