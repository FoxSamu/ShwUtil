package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code double} and returns an {@code int}.
 */
public interface LongFloatDouble2IntFn {
    int invoke(long p1, float p2, double p3);
}
