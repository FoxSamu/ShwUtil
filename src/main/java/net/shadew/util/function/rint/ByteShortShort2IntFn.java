package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code short} and returns an {@code int}.
 */
public interface ByteShortShort2IntFn {
    int invoke(byte p1, short p2, short p3);
}
