package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code boolean} and a {@code char} and returns an {@code int}.
 */
public interface CharBoolChar2IntFn {
    int invoke(char p1, boolean p2, char p3);
}
