package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code char} and returns an {@code int}.
 */
public interface IntBoolChar2IntFn {
    int invoke(int p1, boolean p2, char p3);
}
