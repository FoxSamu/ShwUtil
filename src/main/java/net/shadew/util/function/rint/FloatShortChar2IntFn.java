package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface FloatShortChar2IntFn {
    int invoke(float p1, short p2, char p3);
}
