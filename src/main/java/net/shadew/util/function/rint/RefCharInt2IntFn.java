package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code char} and an {@code int} and returns an {@code int}.
 */
public interface RefCharInt2IntFn<P1> {
    int invoke(P1 p1, char p2, int p3);
}
