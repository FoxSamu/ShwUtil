package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code byte} and returns an {@code int}.
 */
public interface IntCharByte2IntFn {
    int invoke(int p1, char p2, byte p3);
}
