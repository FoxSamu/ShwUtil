package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code double} and returns an {@code int}.
 */
public interface ByteBoolDouble2IntFn {
    int invoke(byte p1, boolean p2, double p3);
}
