package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface ByteByteChar2IntFn {
    int invoke(byte p1, byte p2, char p3);
}
