package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code float} and returns an {@code int}.
 */
public interface ShortShortFloat2IntFn {
    int invoke(short p1, short p2, float p3);
}
