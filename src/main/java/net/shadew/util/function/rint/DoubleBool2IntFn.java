package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double} and a {@code boolean} and returns an {@code int}.
 */
public interface DoubleBool2IntFn {
    int invoke(double p1, boolean p2);
}
