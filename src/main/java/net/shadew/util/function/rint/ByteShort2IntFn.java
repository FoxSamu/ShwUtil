package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte} and a {@code short} and returns an {@code int}.
 */
public interface ByteShort2IntFn {
    int invoke(byte p1, short p2);
}
