package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code float} and an {@code int} and returns an {@code int}.
 */
public interface ShortFloatInt2IntFn {
    int invoke(short p1, float p2, int p3);
}
