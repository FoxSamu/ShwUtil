package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code short} and returns an {@code int}.
 */
public interface FloatLongShort2IntFn {
    int invoke(float p1, long p2, short p3);
}
