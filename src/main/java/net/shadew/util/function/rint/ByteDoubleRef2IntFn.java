package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code double} and an object and returns an {@code int}.
 */
public interface ByteDoubleRef2IntFn<P3> {
    int invoke(byte p1, double p2, P3 p3);
}
