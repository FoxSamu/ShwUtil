package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code char} and returns an {@code int}.
 */
public interface ShortCharChar2IntFn {
    int invoke(short p1, char p2, char p3);
}
