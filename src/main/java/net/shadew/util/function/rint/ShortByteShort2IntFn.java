package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code short} and returns an {@code int}.
 */
public interface ShortByteShort2IntFn {
    int invoke(short p1, byte p2, short p3);
}
