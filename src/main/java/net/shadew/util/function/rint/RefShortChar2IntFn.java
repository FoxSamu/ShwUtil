package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface RefShortChar2IntFn<P1> {
    int invoke(P1 p1, Short p2, Char p3);
}
