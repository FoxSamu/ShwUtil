package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code float} and returns an {@code int}.
 */
public interface ShortLongFloat2IntFn {
    int invoke(short p1, long p2, float p3);
}
