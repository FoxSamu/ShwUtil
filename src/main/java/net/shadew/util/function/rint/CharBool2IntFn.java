package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char} and a {@code boolean} and returns an {@code int}.
 */
public interface CharBool2IntFn {
    int invoke(char p1, boolean p2);
}
