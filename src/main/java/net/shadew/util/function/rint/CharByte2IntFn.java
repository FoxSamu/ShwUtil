package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char} and a {@code byte} and returns an {@code int}.
 */
public interface CharByte2IntFn {
    int invoke(char p1, byte p2);
}
