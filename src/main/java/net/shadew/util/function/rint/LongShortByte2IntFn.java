package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code byte} and returns an {@code int}.
 */
public interface LongShortByte2IntFn {
    int invoke(long p1, short p2, byte p3);
}
