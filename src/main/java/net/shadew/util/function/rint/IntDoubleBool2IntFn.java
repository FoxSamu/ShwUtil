package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code boolean} and returns an {@code int}.
 */
public interface IntDoubleBool2IntFn {
    int invoke(int p1, double p2, boolean p3);
}
