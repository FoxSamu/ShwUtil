package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int} and a {@code float} and returns an {@code int}.
 */
public interface IntFloat2IntFn {
    int invoke(int p1, float p2);
}
