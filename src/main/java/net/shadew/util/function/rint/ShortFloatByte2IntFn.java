package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code byte} and returns an {@code int}.
 */
public interface ShortFloatByte2IntFn {
    int invoke(short p1, float p2, byte p3);
}
