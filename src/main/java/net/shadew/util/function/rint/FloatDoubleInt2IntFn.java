package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code double} and an {@code int} and returns an {@code int}.
 */
public interface FloatDoubleInt2IntFn {
    int invoke(float p1, double p2, int p3);
}
