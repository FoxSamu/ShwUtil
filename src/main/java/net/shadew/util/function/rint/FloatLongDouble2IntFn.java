package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code double} and returns an {@code int}.
 */
public interface FloatLongDouble2IntFn {
    int invoke(float p1, long p2, double p3);
}
