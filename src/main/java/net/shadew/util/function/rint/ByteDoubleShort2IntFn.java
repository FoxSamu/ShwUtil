package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code short} and returns an {@code int}.
 */
public interface ByteDoubleShort2IntFn {
    int invoke(byte p1, double p2, short p3);
}
