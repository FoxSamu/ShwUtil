package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code float} and returns an {@code int}.
 */
public interface ShortCharFloat2IntFn {
    int invoke(short p1, char p2, float p3);
}
