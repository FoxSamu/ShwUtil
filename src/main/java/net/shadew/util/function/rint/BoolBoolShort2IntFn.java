package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface BoolBoolShort2IntFn {
    int invoke(boolean p1, boolean p2, short p3);
}
