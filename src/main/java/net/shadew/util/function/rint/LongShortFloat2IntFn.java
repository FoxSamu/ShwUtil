package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code float} and returns an {@code int}.
 */
public interface LongShortFloat2IntFn {
    int invoke(long p1, short p2, float p3);
}
