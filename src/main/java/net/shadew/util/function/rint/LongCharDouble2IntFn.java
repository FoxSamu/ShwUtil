package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code double} and returns an {@code int}.
 */
public interface LongCharDouble2IntFn {
    int invoke(long p1, char p2, double p3);
}
