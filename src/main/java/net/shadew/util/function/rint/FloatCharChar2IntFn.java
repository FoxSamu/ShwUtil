package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code char} and returns an {@code int}.
 */
public interface FloatCharChar2IntFn {
    int invoke(float p1, char p2, char p3);
}
