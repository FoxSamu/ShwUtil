package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code boolean} and returns an {@code int}.
 */
public interface LongFloatBool2IntFn {
    int invoke(long p1, float p2, boolean p3);
}
