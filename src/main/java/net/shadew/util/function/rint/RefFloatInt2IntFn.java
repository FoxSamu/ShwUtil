package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code float} and an {@code int} and returns an {@code int}.
 */
public interface RefFloatInt2IntFn<P1> {
    int invoke(P1 p1, float p2, int p3);
}
