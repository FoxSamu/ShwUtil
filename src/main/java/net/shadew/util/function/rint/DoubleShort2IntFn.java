package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double} and a {@code short} and returns an {@code int}.
 */
public interface DoubleShort2IntFn {
    int invoke(double p1, short p2);
}
