package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code byte} and returns an {@code int}.
 */
public interface BoolCharByte2IntFn {
    int invoke(boolean p1, char p2, byte p3);
}
