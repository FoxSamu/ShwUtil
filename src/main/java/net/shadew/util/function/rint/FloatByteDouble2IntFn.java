package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface FloatByteDouble2IntFn {
    int invoke(float p1, byte p2, double p3);
}
