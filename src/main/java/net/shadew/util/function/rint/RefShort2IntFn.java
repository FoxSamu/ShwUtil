package net.shadew.util.function.rint;

/**
 * A function that takes an object and a {@code short} and returns an {@code int}.
 */
public interface RefShort2IntFn<P1> {
    int invoke(P1 p1, short p2);
}
