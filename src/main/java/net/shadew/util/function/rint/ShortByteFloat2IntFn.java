package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code float} and returns an {@code int}.
 */
public interface ShortByteFloat2IntFn {
    int invoke(short p1, byte p2, float p3);
}
