package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code boolean} and returns an {@code int}.
 */
public interface ShortLongBool2IntFn {
    int invoke(short p1, long p2, boolean p3);
}
