package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface ByteByteDouble2IntFn {
    int invoke(byte p1, byte p2, double p3);
}
