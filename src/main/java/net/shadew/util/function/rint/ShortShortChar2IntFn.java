package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface ShortShortChar2IntFn {
    int invoke(short p1, short p2, char p3);
}
