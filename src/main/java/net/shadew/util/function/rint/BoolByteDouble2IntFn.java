package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface BoolByteDouble2IntFn {
    int invoke(boolean p1, byte p2, double p3);
}
