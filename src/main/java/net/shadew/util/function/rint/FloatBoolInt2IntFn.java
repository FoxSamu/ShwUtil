package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code boolean} and an {@code int} and returns an {@code int}.
 */
public interface FloatBoolInt2IntFn {
    int invoke(float p1, boolean p2, int p3);
}
