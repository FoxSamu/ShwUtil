package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double}, a {@code short} and an {@code int} and returns an {@code int}.
 */
public interface DoubleShortInt2IntFn {
    int invoke(double p1, short p2, int p3);
}
