package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code char} and an object and returns an {@code int}.
 */
public interface BoolCharRef2IntFn<P3> {
    int invoke(boolean p1, char p2, P3 p3);
}
