package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code short} and a {@code byte} and returns an {@code int}.
 */
public interface RefShortByte2IntFn<P1> {
    int invoke(P1 p1, short p2, byte p3);
}
