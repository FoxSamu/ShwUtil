package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char} and a {@code double} and returns an {@code int}.
 */
public interface CharDouble2IntFn {
    int invoke(char p1, double p2);
}
