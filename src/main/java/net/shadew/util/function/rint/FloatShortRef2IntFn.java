package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code short} and an object and returns an {@code int}.
 */
public interface FloatShortRef2IntFn<P3> {
    int invoke(float p1, short p2, P3 p3);
}
