package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface ShortBoolShort2IntFn {
    int invoke(short p1, boolean p2, short p3);
}
