package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns an {@code int}.
 */
public interface FloatByteInt2IntFn {
    int invoke(float p1, byte p2, int p3);
}
