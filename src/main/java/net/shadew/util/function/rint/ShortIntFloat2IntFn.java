package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code float} and returns an {@code int}.
 */
public interface ShortIntFloat2IntFn {
    int invoke(short p1, int p2, float p3);
}
