package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code long} and an {@code int} and returns an {@code int}.
 */
public interface FloatLongInt2IntFn {
    int invoke(float p1, long p2, int p3);
}
