package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code char} and an {@code int} and returns an {@code int}.
 */
public interface IntCharInt2IntFn {
    int invoke(int p1, char p2, int p3);
}
