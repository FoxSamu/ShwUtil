package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code short} and returns an {@code int}.
 */
public interface ShortCharShort2IntFn {
    int invoke(short p1, char p2, short p3);
}
