package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, an {@code int} and an object and returns an {@code int}.
 */
public interface FloatIntRef2IntFn<P3> {
    int invoke(float p1, int p2, P3 p3);
}
