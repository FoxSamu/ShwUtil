package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int} and a {@code byte} and returns an {@code int}.
 */
public interface IntByte2IntFn {
    int invoke(int p1, byte p2);
}
