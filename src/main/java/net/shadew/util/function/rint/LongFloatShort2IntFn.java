package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code short} and returns an {@code int}.
 */
public interface LongFloatShort2IntFn {
    int invoke(long p1, float p2, short p3);
}
