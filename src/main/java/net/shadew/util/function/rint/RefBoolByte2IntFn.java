package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code boolean} and a {@code byte} and returns an {@code int}.
 */
public interface RefBoolByte2IntFn<P1> {
    int invoke(P1 p1, boolean p2, byte p3);
}
