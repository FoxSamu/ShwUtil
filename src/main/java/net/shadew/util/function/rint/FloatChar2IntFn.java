package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float} and a {@code char} and returns an {@code int}.
 */
public interface FloatChar2IntFn {
    int invoke(float p1, char p2);
}
