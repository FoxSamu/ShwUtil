package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code double} and returns an {@code int}.
 */
public interface DoubleShortDouble2IntFn {
    int invoke(double p1, short p2, double p3);
}
