package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char} and a {@code float} and returns an {@code int}.
 */
public interface CharFloat2IntFn {
    int invoke(char p1, float p2);
}
