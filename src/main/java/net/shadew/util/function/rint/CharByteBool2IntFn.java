package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code boolean} and returns an {@code int}.
 */
public interface CharByteBool2IntFn {
    int invoke(char p1, byte p2, boolean p3);
}
