package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long} and a {@code boolean} and returns an {@code int}.
 */
public interface LongBool2IntFn {
    int invoke(long p1, boolean p2);
}
