package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code byte} and a {@code float} and returns an {@code int}.
 */
public interface RefByteFloat2IntFn<P1> {
    int invoke(P1 p1, byte p2, float p3);
}
