package net.shadew.util.function.rint;

/**
 * A function that takes an object and a {@code byte} and returns an {@code int}.
 */
public interface RefByte2IntFn<P1> {
    int invoke(P1 p1, byte p2);
}
