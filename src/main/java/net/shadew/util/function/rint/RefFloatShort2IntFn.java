package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code float} and a {@code short} and returns an {@code int}.
 */
public interface RefFloatShort2IntFn<P1> {
    int invoke(P1 p1, float p2, short p3);
}
