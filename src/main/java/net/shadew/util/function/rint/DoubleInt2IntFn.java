package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double} and an {@code int} and returns an {@code int}.
 */
public interface DoubleInt2IntFn {
    int invoke(double p1, int p2);
}
