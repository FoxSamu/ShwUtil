package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface ShortByteDouble2IntFn {
    int invoke(short p1, byte p2, double p3);
}
