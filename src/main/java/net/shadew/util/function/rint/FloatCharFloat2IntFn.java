package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code float} and returns an {@code int}.
 */
public interface FloatCharFloat2IntFn {
    int invoke(float p1, char p2, float p3);
}
