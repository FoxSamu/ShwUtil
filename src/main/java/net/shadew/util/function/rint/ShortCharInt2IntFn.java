package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code char} and an {@code int} and returns an {@code int}.
 */
public interface ShortCharInt2IntFn {
    int invoke(short p1, char p2, int p3);
}
