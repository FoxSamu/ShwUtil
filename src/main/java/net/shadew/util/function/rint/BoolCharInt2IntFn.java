package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code char} and an {@code int} and returns an {@code int}.
 */
public interface BoolCharInt2IntFn {
    int invoke(boolean p1, char p2, int p3);
}
