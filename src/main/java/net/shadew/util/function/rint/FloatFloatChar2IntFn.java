package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code char} and returns an {@code int}.
 */
public interface FloatFloatChar2IntFn {
    int invoke(Float p1, Float p2, Char p3);
}
