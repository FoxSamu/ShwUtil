package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code double} and returns an {@code int}.
 */
public interface ShortLongDouble2IntFn {
    int invoke(short p1, long p2, double p3);
}
