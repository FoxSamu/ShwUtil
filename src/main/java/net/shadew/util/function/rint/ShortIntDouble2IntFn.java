package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code double} and returns an {@code int}.
 */
public interface ShortIntDouble2IntFn {
    int invoke(short p1, int p2, double p3);
}
