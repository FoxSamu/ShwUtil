package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns an {@code int}.
 */
public interface ByteByteBool2IntFn {
    int invoke(byte p1, byte p2, boolean p3);
}
