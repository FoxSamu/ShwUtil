package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code double} and an {@code int} and returns an {@code int}.
 */
public interface ByteDoubleInt2IntFn {
    int invoke(byte p1, double p2, int p3);
}
