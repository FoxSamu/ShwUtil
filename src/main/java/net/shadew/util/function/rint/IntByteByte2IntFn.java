package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code byte} and returns an {@code int}.
 */
public interface IntByteByte2IntFn {
    int invoke(int p1, byte p2, byte p3);
}
