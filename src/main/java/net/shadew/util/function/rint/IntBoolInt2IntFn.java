package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code boolean} and an {@code int} and returns an {@code int}.
 */
public interface IntBoolInt2IntFn {
    int invoke(int p1, boolean p2, int p3);
}
