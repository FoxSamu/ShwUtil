package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns an {@code int}.
 */
public interface IntDoubleByte2IntFn {
    int invoke(int p1, double p2, byte p3);
}
