package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code double} and an {@code int} and returns an {@code int}.
 */
public interface LongDoubleInt2IntFn {
    int invoke(long p1, double p2, int p3);
}
