package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code byte} and returns an {@code int}.
 */
public interface LongByteByte2IntFn {
    int invoke(long p1, byte p2, byte p3);
}
