package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code byte} and returns an {@code int}.
 */
public interface FloatCharByte2IntFn {
    int invoke(float p1, char p2, byte p3);
}
