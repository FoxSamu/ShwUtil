package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte} and a {@code float} and returns an {@code int}.
 */
public interface ByteFloat2IntFn {
    int invoke(byte p1, float p2);
}
