package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code double} and an {@code int} and returns an {@code int}.
 */
public interface CharDoubleInt2IntFn {
    int invoke(char p1, double p2, int p3);
}
