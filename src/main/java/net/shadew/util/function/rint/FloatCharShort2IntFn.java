package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code short} and returns an {@code int}.
 */
public interface FloatCharShort2IntFn {
    int invoke(float p1, char p2, short p3);
}
