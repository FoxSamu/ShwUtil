package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code boolean} and returns an {@code int}.
 */
public interface FloatCharBool2IntFn {
    int invoke(float p1, char p2, boolean p3);
}
