package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code float} and returns an {@code int}.
 */
public interface FloatBoolFloat2IntFn {
    int invoke(float p1, boolean p2, float p3);
}
