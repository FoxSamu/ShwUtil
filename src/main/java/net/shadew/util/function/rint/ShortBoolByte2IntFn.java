package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code byte} and returns an {@code int}.
 */
public interface ShortBoolByte2IntFn {
    int invoke(Short p1, Bool p2, Byte p3);
}
