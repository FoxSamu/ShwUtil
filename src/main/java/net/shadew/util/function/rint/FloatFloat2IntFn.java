package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float} and a {@code float} and returns an {@code int}.
 */
public interface FloatFloat2IntFn {
    int invoke(float p1, float p2);
}
