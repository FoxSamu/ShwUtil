package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code char} and returns an {@code int}.
 */
public interface ShortBoolChar2IntFn {
    int invoke(short p1, boolean p2, char p3);
}
