package net.shadew.util.function.rint;

/**
 * A function that takes an object, an object and an object and returns an {@code int}.
 */
public interface RefRefRef2IntFn<P1, P2, P3> {
    int invoke(P1 p1, P2 p2, P3 p3);
}
