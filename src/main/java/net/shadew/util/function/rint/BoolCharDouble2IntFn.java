package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code double} and returns an {@code int}.
 */
public interface BoolCharDouble2IntFn {
    int invoke(Bool p1, Char p2, Double p3);
}
