package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns an {@code int}.
 */
public interface FloatByteBool2IntFn {
    int invoke(float p1, byte p2, boolean p3);
}
