package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code char} and returns an {@code int}.
 */
public interface LongFloatChar2IntFn {
    int invoke(long p1, float p2, char p3);
}
