package net.shadew.util.function.rint;

/**
 * A function that takes an object and a {@code double} and returns an {@code int}.
 */
public interface RefDouble2IntFn<P1> {
    int invoke(P1 p1, double p2);
}
