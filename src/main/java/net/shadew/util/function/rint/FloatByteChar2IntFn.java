package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface FloatByteChar2IntFn {
    int invoke(float p1, byte p2, char p3);
}
