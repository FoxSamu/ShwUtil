package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code byte} and a {@code double} and returns an {@code int}.
 */
public interface RefByteDouble2IntFn<P1> {
    int invoke(P1 p1, byte p2, double p3);
}
