package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns an {@code int}.
 */
public interface ByteBoolChar2IntFn {
    int invoke(byte p1, boolean p2, char p3);
}
