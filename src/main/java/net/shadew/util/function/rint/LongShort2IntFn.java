package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long} and a {@code short} and returns an {@code int}.
 */
public interface LongShort2IntFn {
    int invoke(long p1, short p2);
}
