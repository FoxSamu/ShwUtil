package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte} and an {@code int} and returns an {@code int}.
 */
public interface ByteInt2IntFn {
    int invoke(byte p1, int p2);
}
