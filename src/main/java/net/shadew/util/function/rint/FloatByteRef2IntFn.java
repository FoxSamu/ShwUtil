package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and an object and returns an {@code int}.
 */
public interface FloatByteRef2IntFn<P3> {
    int invoke(float p1, byte p2, P3 p3);
}
