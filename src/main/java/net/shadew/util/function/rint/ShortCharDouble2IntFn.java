package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code double} and returns an {@code int}.
 */
public interface ShortCharDouble2IntFn {
    int invoke(short p1, char p2, double p3);
}
