package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code byte} and an {@code int} and returns an {@code int}.
 */
public interface ByteByteInt2IntFn {
    int invoke(byte p1, byte p2, int p3);
}
