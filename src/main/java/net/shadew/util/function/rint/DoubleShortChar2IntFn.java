package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code char} and returns an {@code int}.
 */
public interface DoubleShortChar2IntFn {
    int invoke(double p1, short p2, char p3);
}
