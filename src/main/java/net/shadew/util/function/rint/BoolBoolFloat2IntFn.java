package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code float} and returns an {@code int}.
 */
public interface BoolBoolFloat2IntFn {
    int invoke(boolean p1, boolean p2, float p3);
}
