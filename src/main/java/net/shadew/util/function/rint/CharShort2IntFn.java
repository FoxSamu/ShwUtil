package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char} and a {@code short} and returns an {@code int}.
 */
public interface CharShort2IntFn {
    int invoke(char p1, short p2);
}
