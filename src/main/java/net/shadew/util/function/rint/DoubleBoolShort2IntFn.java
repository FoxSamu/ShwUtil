package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface DoubleBoolShort2IntFn {
    int invoke(double p1, boolean p2, short p3);
}
