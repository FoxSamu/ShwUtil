package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface ByteChar2IntFn {
    int invoke(byte p1, char p2);
}
