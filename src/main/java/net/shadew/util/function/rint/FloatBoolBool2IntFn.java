package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code boolean} and returns an {@code int}.
 */
public interface FloatBoolBool2IntFn {
    int invoke(float p1, boolean p2, boolean p3);
}
