package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean} and a {@code float} and returns an {@code int}.
 */
public interface BoolFloat2IntFn {
    int invoke(boolean p1, float p2);
}
