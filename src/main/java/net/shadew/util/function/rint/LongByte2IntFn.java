package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long} and a {@code byte} and returns an {@code int}.
 */
public interface LongByte2IntFn {
    int invoke(long p1, byte p2);
}
