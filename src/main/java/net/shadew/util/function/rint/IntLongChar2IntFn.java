package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code long} and a {@code char} and returns an {@code int}.
 */
public interface IntLongChar2IntFn {
    int invoke(int p1, long p2, char p3);
}
