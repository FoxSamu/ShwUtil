package net.shadew.util.function.rint;

/**
 * A function that takes a {@code double} and a {@code char} and returns an {@code int}.
 */
public interface DoubleChar2IntFn {
    int invoke(double p1, char p2);
}
