package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code double} and returns an {@code int}.
 */
public interface ShortDoubleDouble2IntFn {
    int invoke(short p1, double p2, double p3);
}
