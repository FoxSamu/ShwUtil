package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code float} and a {@code byte} and returns an {@code int}.
 */
public interface RefFloatByte2IntFn<P1> {
    int invoke(P1 p1, float p2, byte p3);
}
