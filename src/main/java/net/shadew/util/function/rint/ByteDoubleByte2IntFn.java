package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code byte} and returns an {@code int}.
 */
public interface ByteDoubleByte2IntFn {
    int invoke(byte p1, double p2, byte p3);
}
