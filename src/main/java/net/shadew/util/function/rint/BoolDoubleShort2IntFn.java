package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code short} and returns an {@code int}.
 */
public interface BoolDoubleShort2IntFn {
    int invoke(boolean p1, double p2, short p3);
}
