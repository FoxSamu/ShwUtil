package net.shadew.util.function.rint;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code boolean} and returns an {@code int}.
 */
public interface ByteDoubleBool2IntFn {
    int invoke(byte p1, double p2, boolean p3);
}
