package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code byte} and returns an {@code int}.
 */
public interface ShortLongByte2IntFn {
    int invoke(short p1, long p2, byte p3);
}
