package net.shadew.util.function.rint;

/**
 * A function that takes an object, an {@code int} and an {@code int} and returns an {@code int}.
 */
public interface RefIntInt2IntFn<P1> {
    int invoke(P1 p1, int p2, int p3);
}
