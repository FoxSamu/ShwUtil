package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code char} and returns an {@code int}.
 */
public interface ShortByteChar2IntFn {
    int invoke(short p1, byte p2, char p3);
}
