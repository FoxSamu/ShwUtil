package net.shadew.util.function.rint;

/**
 * A function that takes an object, a {@code long} and a {@code byte} and returns an {@code int}.
 */
public interface RefLongByte2IntFn<P1> {
    int invoke(P1 p1, long p2, byte p3);
}
