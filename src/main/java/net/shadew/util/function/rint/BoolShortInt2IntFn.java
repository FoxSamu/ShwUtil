package net.shadew.util.function.rint;

/**
 * A function that takes a {@code boolean}, a {@code short} and an {@code int} and returns an {@code int}.
 */
public interface BoolShortInt2IntFn {
    int invoke(boolean p1, short p2, int p3);
}
