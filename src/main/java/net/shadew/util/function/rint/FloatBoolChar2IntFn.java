package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code char} and returns an {@code int}.
 */
public interface FloatBoolChar2IntFn {
    int invoke(float p1, boolean p2, char p3);
}
