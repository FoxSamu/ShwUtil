package net.shadew.util.function.rint;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code float} and returns an {@code int}.
 */
public interface CharDoubleFloat2IntFn {
    int invoke(char p1, double p2, float p3);
}
