package net.shadew.util.function.rint;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code short} and returns an {@code int}.
 */
public interface LongDoubleShort2IntFn {
    int invoke(long p1, double p2, short p3);
}
