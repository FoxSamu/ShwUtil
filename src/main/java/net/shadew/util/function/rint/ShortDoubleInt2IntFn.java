package net.shadew.util.function.rint;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns an {@code int}.
 */
public interface ShortDoubleInt2IntFn {
    int invoke(short p1, double p2, int p3);
}
