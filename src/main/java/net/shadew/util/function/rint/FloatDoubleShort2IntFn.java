package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code short} and returns an {@code int}.
 */
public interface FloatDoubleShort2IntFn {
    int invoke(float p1, double p2, short p3);
}
