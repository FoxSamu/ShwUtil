package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code byte} and returns an {@code int}.
 */
public interface IntBoolByte2IntFn {
    int invoke(int p1, boolean p2, byte p3);
}
