package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code short} and returns an {@code int}.
 */
public interface FloatByteShort2IntFn {
    int invoke(float p1, byte p2, short p3);
}
