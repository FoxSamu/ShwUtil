package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code float} and returns an {@code int}.
 */
public interface IntBoolFloat2IntFn {
    int invoke(int p1, boolean p2, float p3);
}
