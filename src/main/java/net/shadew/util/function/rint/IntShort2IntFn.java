package net.shadew.util.function.rint;

/**
 * A function that takes an {@code int} and a {@code short} and returns an {@code int}.
 */
public interface IntShort2IntFn {
    int invoke(int p1, short p2);
}
