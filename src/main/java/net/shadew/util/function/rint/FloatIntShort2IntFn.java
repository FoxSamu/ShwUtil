package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code short} and returns an {@code int}.
 */
public interface FloatIntShort2IntFn {
    int invoke(float p1, int p2, short p3);
}
