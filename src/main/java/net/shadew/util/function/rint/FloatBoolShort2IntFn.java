package net.shadew.util.function.rint;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code short} and returns an {@code int}.
 */
public interface FloatBoolShort2IntFn {
    int invoke(float p1, boolean p2, short p3);
}
