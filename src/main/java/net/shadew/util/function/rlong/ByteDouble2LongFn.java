package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte} and a {@code double} and returns a {@code long}.
 */
public interface ByteDouble2LongFn {
    long invoke(byte p1, double p2);
}
