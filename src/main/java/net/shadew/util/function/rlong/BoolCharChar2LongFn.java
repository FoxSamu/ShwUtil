package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code char} and returns a {@code long}.
 */
public interface BoolCharChar2LongFn {
    long invoke(boolean p1, char p2, char p3);
}
