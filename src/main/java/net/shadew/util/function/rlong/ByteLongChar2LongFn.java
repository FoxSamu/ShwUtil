package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code long} and a {@code char} and returns a {@code long}.
 */
public interface ByteLongChar2LongFn {
    long invoke(byte p1, long p2, char p3);
}
