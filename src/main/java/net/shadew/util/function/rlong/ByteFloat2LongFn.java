package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte} and a {@code float} and returns a {@code long}.
 */
public interface ByteFloat2LongFn {
    long invoke(byte p1, float p2);
}
