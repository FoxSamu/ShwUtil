package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code char} and returns a {@code long}.
 */
public interface FloatFloatChar2LongFn {
    long invoke(float p1, float p2, char p3);
}
