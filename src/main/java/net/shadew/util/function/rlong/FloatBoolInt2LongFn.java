package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code boolean} and an {@code int} and returns a {@code long}.
 */
public interface FloatBoolInt2LongFn {
    long invoke(float p1, boolean p2, int p3);
}
