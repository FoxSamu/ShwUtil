package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns a {@code long}.
 */
public interface BoolChar2LongFn {
    long invoke(boolean p1, char p2);
}
