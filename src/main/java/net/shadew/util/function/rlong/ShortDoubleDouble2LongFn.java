package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code double} and returns a {@code long}.
 */
public interface ShortDoubleDouble2LongFn {
    long invoke(short p1, double p2, double p3);
}
