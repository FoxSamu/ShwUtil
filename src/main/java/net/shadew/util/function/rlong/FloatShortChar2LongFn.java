package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code char} and returns a {@code long}.
 */
public interface FloatShortChar2LongFn {
    long invoke(float p1, short p2, char p3);
}
