package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns a {@code long}.
 */
public interface ByteBool2LongFn {
    long invoke(byte p1, boolean p2);
}
