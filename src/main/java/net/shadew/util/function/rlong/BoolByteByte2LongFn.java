package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code byte} and returns a {@code long}.
 */
public interface BoolByteByte2LongFn {
    long invoke(boolean p1, byte p2, byte p3);
}
