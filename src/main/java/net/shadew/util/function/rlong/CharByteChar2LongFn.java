package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns a {@code long}.
 */
public interface CharByteChar2LongFn {
    long invoke(char p1, byte p2, char p3);
}
