package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short} and a {@code byte} and returns a {@code long}.
 */
public interface ShortByte2LongFn {
    long invoke(short p1, byte p2);
}
