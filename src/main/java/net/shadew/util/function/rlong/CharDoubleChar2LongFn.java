package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code char} and returns a {@code long}.
 */
public interface CharDoubleChar2LongFn {
    long invoke(char p1, double p2, char p3);
}
