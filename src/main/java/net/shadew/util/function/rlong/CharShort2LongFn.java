package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char} and a {@code short} and returns a {@code long}.
 */
public interface CharShort2LongFn {
    long invoke(char p1, short p2);
}
