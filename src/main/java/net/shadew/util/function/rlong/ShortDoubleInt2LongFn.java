package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns a {@code long}.
 */
public interface ShortDoubleInt2LongFn {
    long invoke(short p1, double p2, int p3);
}
