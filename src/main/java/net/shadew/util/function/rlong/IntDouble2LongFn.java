package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int} and a {@code double} and returns a {@code long}.
 */
public interface IntDouble2LongFn {
    long invoke(int p1, double p2);
}
