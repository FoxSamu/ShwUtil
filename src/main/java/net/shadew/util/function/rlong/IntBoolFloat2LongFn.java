package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code float} and returns a {@code long}.
 */
public interface IntBoolFloat2LongFn {
    long invoke(int p1, boolean p2, float p3);
}
