package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float} and a {@code short} and returns a {@code long}.
 */
public interface FloatShort2LongFn {
    long invoke(float p1, short p2);
}
