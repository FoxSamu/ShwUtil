package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code double} and an {@code int} and returns a {@code long}.
 */
public interface ByteDoubleInt2LongFn {
    long invoke(byte p1, double p2, int p3);
}
