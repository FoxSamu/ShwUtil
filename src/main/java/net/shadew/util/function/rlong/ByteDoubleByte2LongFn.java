package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code byte} and returns a {@code long}.
 */
public interface ByteDoubleByte2LongFn {
    long invoke(byte p1, double p2, byte p3);
}
