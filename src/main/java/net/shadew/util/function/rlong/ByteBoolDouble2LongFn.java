package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code double} and returns a {@code long}.
 */
public interface ByteBoolDouble2LongFn {
    long invoke(byte p1, boolean p2, double p3);
}
