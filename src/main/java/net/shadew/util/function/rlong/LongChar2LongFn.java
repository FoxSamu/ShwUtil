package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long} and a {@code char} and returns a {@code long}.
 */
public interface LongChar2LongFn {
    long invoke(long p1, char p2);
}
