package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code float} and returns a {@code long}.
 */
public interface ShortCharFloat2LongFn {
    long invoke(short p1, char p2, float p3);
}
