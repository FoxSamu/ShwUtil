package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code char} and returns a {@code long}.
 */
public interface ShortBoolChar2LongFn {
    long invoke(short p1, boolean p2, char p3);
}
