package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code char} and returns a {@code long}.
 */
public interface ShortCharChar2LongFn {
    long invoke(short p1, char p2, char p3);
}
