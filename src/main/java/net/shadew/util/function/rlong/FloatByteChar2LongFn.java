package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code char} and returns a {@code long}.
 */
public interface FloatByteChar2LongFn {
    long invoke(float p1, byte p2, char p3);
}
