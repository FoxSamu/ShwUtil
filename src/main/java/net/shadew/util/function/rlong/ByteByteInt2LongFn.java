package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code byte} and an {@code int} and returns a {@code long}.
 */
public interface ByteByteInt2LongFn {
    long invoke(byte p1, byte p2, int p3);
}
