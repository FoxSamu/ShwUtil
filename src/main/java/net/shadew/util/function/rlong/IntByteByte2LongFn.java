package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code byte} and returns a {@code long}.
 */
public interface IntByteByte2LongFn {
    long invoke(int p1, byte p2, byte p3);
}
