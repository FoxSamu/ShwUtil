package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code float} and returns a {@code long}.
 */
public interface ShortByteFloat2LongFn {
    long invoke(short p1, byte p2, float p3);
}
