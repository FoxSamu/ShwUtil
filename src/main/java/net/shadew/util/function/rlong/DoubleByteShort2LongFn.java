package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code short} and returns a {@code long}.
 */
public interface DoubleByteShort2LongFn {
    long invoke(double p1, byte p2, short p3);
}
