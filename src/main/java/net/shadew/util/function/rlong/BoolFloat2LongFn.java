package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean} and a {@code float} and returns a {@code long}.
 */
public interface BoolFloat2LongFn {
    long invoke(boolean p1, float p2);
}
