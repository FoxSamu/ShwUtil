package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code float} and returns a {@code long}.
 */
public interface IntByteFloat2LongFn {
    long invoke(int p1, byte p2, float p3);
}
