package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float} and a {@code boolean} and returns a {@code long}.
 */
public interface FloatBool2LongFn {
    long invoke(float p1, boolean p2);
}
