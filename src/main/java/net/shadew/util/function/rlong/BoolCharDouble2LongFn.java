package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code double} and returns a {@code long}.
 */
public interface BoolCharDouble2LongFn {
    long invoke(boolean p1, char p2, double p3);
}
