package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code char} and returns a {@code long}.
 */
public interface BoolShortChar2LongFn {
    long invoke(Bool p1, Short p2, Char p3);
}
