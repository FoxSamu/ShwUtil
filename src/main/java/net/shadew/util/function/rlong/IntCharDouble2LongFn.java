package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code double} and returns a {@code long}.
 */
public interface IntCharDouble2LongFn {
    long invoke(int p1, char p2, double p3);
}
