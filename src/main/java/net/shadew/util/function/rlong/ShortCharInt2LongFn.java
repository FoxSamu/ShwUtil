package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code char} and an {@code int} and returns a {@code long}.
 */
public interface ShortCharInt2LongFn {
    long invoke(short p1, char p2, int p3);
}
