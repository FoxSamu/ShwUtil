package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code short} and returns a {@code long}.
 */
public interface FloatIntShort2LongFn {
    long invoke(float p1, int p2, short p3);
}
