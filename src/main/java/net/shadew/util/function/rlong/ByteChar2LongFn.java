package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte} and a {@code char} and returns a {@code long}.
 */
public interface ByteChar2LongFn {
    long invoke(byte p1, char p2);
}
