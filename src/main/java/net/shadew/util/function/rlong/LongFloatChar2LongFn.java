package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code char} and returns a {@code long}.
 */
public interface LongFloatChar2LongFn {
    long invoke(long p1, float p2, char p3);
}
