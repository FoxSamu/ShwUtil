package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long} and an {@code int} and returns a {@code long}.
 */
public interface LongInt2LongFn {
    long invoke(long p1, int p2);
}
