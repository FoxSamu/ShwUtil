package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code short} and returns a {@code long}.
 */
public interface ByteBoolShort2LongFn {
    long invoke(byte p1, boolean p2, short p3);
}
