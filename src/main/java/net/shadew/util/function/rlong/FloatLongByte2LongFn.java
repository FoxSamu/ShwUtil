package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code byte} and returns a {@code long}.
 */
public interface FloatLongByte2LongFn {
    long invoke(float p1, long p2, byte p3);
}
