package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code short} and returns a {@code long}.
 */
public interface FloatCharShort2LongFn {
    long invoke(float p1, char p2, short p3);
}
