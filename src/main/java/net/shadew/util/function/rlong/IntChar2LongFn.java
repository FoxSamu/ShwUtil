package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int} and a {@code char} and returns a {@code long}.
 */
public interface IntChar2LongFn {
    long invoke(int p1, char p2);
}
