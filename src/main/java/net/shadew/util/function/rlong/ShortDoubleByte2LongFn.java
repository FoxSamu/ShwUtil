package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code byte} and returns a {@code long}.
 */
public interface ShortDoubleByte2LongFn {
    long invoke(short p1, double p2, byte p3);
}
