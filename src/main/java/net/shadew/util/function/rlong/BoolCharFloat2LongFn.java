package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code float} and returns a {@code long}.
 */
public interface BoolCharFloat2LongFn {
    long invoke(Bool p1, Char p2, Float p3);
}
