package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code char} and returns a {@code long}.
 */
public interface IntBoolChar2LongFn {
    long invoke(int p1, boolean p2, char p3);
}
