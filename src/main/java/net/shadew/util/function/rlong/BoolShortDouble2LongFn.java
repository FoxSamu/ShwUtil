package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code double} and returns a {@code long}.
 */
public interface BoolShortDouble2LongFn {
    long invoke(boolean p1, short p2, double p3);
}
