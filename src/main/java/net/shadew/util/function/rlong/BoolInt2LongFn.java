package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean} and an {@code int} and returns a {@code long}.
 */
public interface BoolInt2LongFn {
    long invoke(boolean p1, int p2);
}
