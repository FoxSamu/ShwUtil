package net.shadew.util.function.rlong;

/**
 * A function that takes an object, a {@code boolean} and a {@code short} and returns a {@code long}.
 */
public interface RefBoolShort2LongFn<P1> {
    long invoke(P1 p1, boolean p2, short p3);
}
