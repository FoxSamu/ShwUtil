package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code float} and returns a {@code long}.
 */
public interface FloatFloatFloat2LongFn {
    long invoke(Float p1, Float p2, Float p3);
}
