package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char} and a {@code byte} and returns a {@code long}.
 */
public interface CharByte2LongFn {
    long invoke(char p1, byte p2);
}
