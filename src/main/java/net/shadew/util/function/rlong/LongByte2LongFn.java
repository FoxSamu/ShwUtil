package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long} and a {@code byte} and returns a {@code long}.
 */
public interface LongByte2LongFn {
    long invoke(long p1, byte p2);
}
