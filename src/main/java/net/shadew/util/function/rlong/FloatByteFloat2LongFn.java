package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code float} and returns a {@code long}.
 */
public interface FloatByteFloat2LongFn {
    long invoke(float p1, byte p2, float p3);
}
