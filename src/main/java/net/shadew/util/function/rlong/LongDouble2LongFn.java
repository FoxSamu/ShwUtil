package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long} and a {@code double} and returns a {@code long}.
 */
public interface LongDouble2LongFn {
    long invoke(long p1, double p2);
}
