package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code double} and a {@code double} and returns a {@code long}.
 */
public interface DoubleDouble2LongFn {
    long invoke(double p1, double p2);
}
