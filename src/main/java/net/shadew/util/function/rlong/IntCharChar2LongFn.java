package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code char} and returns a {@code long}.
 */
public interface IntCharChar2LongFn {
    long invoke(int p1, char p2, char p3);
}
