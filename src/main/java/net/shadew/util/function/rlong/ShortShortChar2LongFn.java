package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code char} and returns a {@code long}.
 */
public interface ShortShortChar2LongFn {
    long invoke(short p1, short p2, char p3);
}
