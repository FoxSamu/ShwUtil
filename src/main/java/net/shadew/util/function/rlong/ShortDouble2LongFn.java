package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short} and a {@code double} and returns a {@code long}.
 */
public interface ShortDouble2LongFn {
    long invoke(short p1, double p2);
}
