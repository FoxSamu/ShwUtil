package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code char} and returns a {@code long}.
 */
public interface ShortFloatChar2LongFn {
    long invoke(short p1, float p2, char p3);
}
