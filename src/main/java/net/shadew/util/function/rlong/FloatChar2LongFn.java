package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float} and a {@code char} and returns a {@code long}.
 */
public interface FloatChar2LongFn {
    long invoke(float p1, char p2);
}
