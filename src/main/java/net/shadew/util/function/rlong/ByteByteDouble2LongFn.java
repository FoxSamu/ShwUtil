package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns a {@code long}.
 */
public interface ByteByteDouble2LongFn {
    long invoke(byte p1, byte p2, double p3);
}
