package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns a {@code long}.
 */
public interface BoolDouble2LongFn {
    long invoke(Bool p1, Double p2);
}
