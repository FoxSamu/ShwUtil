package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code byte} and returns a {@code long}.
 */
public interface FloatDoubleByte2LongFn {
    long invoke(float p1, double p2, byte p3);
}
