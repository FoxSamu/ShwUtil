package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code double} and returns a {@code long}.
 */
public interface FloatDoubleDouble2LongFn {
    long invoke(float p1, double p2, double p3);
}
