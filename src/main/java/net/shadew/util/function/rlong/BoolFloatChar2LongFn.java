package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code char} and returns a {@code long}.
 */
public interface BoolFloatChar2LongFn {
    long invoke(boolean p1, float p2, char p3);
}
