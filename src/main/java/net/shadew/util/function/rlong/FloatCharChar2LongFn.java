package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code char} and returns a {@code long}.
 */
public interface FloatCharChar2LongFn {
    long invoke(float p1, char p2, char p3);
}
