package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte} and a {@code short} and returns a {@code long}.
 */
public interface ByteShort2LongFn {
    long invoke(byte p1, short p2);
}
