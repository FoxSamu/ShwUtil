package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short} and a {@code boolean} and returns a {@code long}.
 */
public interface ShortBool2LongFn {
    long invoke(short p1, boolean p2);
}
