package net.shadew.util.function.rlong;

/**
 * A function that takes an object, a {@code char} and a {@code char} and returns a {@code long}.
 */
public interface RefCharChar2LongFn<P1> {
    long invoke(P1 p1, char p2, char p3);
}
