package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code short} and returns a {@code long}.
 */
public interface LongByteShort2LongFn {
    long invoke(long p1, byte p2, short p3);
}
