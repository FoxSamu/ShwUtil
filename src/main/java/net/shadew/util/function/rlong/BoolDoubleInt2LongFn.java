package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code double} and an {@code int} and returns a {@code long}.
 */
public interface BoolDoubleInt2LongFn {
    long invoke(boolean p1, double p2, int p3);
}
