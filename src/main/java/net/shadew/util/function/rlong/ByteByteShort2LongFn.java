package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code short} and returns a {@code long}.
 */
public interface ByteByteShort2LongFn {
    long invoke(byte p1, byte p2, short p3);
}
