package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code double} and returns a {@code long}.
 */
public interface IntByteDouble2LongFn {
    long invoke(int p1, byte p2, double p3);
}
