package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code char} and returns a {@code long}.
 */
public interface ByteDoubleChar2LongFn {
    long invoke(byte p1, double p2, char p3);
}
