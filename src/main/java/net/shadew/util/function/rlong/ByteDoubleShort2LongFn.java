package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code short} and returns a {@code long}.
 */
public interface ByteDoubleShort2LongFn {
    long invoke(byte p1, double p2, short p3);
}
