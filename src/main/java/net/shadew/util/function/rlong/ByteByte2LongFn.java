package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte} and a {@code byte} and returns a {@code long}.
 */
public interface ByteByte2LongFn {
    long invoke(byte p1, byte p2);
}
