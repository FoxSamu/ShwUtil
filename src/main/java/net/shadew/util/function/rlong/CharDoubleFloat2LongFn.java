package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code float} and returns a {@code long}.
 */
public interface CharDoubleFloat2LongFn {
    long invoke(char p1, double p2, float p3);
}
