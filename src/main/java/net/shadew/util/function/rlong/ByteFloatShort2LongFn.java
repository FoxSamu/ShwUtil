package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code short} and returns a {@code long}.
 */
public interface ByteFloatShort2LongFn {
    long invoke(byte p1, float p2, short p3);
}
