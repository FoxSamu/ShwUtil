package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int} and a {@code float} and returns a {@code long}.
 */
public interface IntFloat2LongFn {
    long invoke(int p1, float p2);
}
