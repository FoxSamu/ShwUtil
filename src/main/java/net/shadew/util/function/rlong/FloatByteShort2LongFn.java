package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code short} and returns a {@code long}.
 */
public interface FloatByteShort2LongFn {
    long invoke(float p1, byte p2, short p3);
}
