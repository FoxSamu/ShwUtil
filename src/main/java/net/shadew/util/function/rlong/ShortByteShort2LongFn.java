package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code short} and returns a {@code long}.
 */
public interface ShortByteShort2LongFn {
    long invoke(short p1, byte p2, short p3);
}
