package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float} and an {@code int} and returns a {@code long}.
 */
public interface FloatInt2LongFn {
    long invoke(float p1, int p2);
}
