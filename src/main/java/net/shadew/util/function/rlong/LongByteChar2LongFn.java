package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code char} and returns a {@code long}.
 */
public interface LongByteChar2LongFn {
    long invoke(long p1, byte p2, char p3);
}
