package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code double} and returns a {@code long}.
 */
public interface LongShortDouble2LongFn {
    long invoke(long p1, short p2, double p3);
}
