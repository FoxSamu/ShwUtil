package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns a {@code long}.
 */
public interface BoolShort2LongFn {
    long invoke(boolean p1, short p2);
}
