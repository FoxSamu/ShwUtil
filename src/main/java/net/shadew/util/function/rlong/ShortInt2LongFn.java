package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short} and an {@code int} and returns a {@code long}.
 */
public interface ShortInt2LongFn {
    long invoke(short p1, int p2);
}
