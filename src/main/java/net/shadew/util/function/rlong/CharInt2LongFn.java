package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char} and an {@code int} and returns a {@code long}.
 */
public interface CharInt2LongFn {
    long invoke(char p1, int p2);
}
