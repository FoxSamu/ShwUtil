package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns a {@code long}.
 */
public interface FloatByteBool2LongFn {
    long invoke(float p1, byte p2, boolean p3);
}
