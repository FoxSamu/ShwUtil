package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code char} and returns a {@code long}.
 */
public interface BoolByteChar2LongFn {
    long invoke(boolean p1, byte p2, char p3);
}
