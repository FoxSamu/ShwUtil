package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code double} and a {@code byte} and returns a {@code long}.
 */
public interface DoubleByte2LongFn {
    long invoke(double p1, byte p2);
}
