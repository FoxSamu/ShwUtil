package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code char} and returns a {@code long}.
 */
public interface BoolDoubleChar2LongFn {
    long invoke(boolean p1, double p2, char p3);
}
