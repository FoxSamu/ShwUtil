package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int} and a {@code short} and returns a {@code long}.
 */
public interface IntShort2LongFn {
    long invoke(int p1, short p2);
}
