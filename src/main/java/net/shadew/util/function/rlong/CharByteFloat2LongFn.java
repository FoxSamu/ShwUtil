package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code float} and returns a {@code long}.
 */
public interface CharByteFloat2LongFn {
    long invoke(char p1, byte p2, float p3);
}
