package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code double} and returns a {@code long}.
 */
public interface ByteShortDouble2LongFn {
    long invoke(byte p1, short p2, double p3);
}
