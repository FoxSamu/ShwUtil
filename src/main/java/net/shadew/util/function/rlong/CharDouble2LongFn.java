package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char} and a {@code double} and returns a {@code long}.
 */
public interface CharDouble2LongFn {
    long invoke(char p1, double p2);
}
