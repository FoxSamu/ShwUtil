package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short} and a {@code short} and returns a {@code long}.
 */
public interface ShortShort2LongFn {
    long invoke(short p1, short p2);
}
