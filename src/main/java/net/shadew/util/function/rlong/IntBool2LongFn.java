package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int} and a {@code boolean} and returns a {@code long}.
 */
public interface IntBool2LongFn {
    long invoke(int p1, boolean p2);
}
