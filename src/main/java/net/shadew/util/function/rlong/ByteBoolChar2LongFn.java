package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns a {@code long}.
 */
public interface ByteBoolChar2LongFn {
    long invoke(byte p1, boolean p2, char p3);
}
