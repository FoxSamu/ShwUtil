package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code char} and returns a {@code long}.
 */
public interface IntFloatChar2LongFn {
    long invoke(int p1, float p2, char p3);
}
