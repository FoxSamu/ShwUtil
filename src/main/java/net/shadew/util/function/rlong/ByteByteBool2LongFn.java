package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns a {@code long}.
 */
public interface ByteByteBool2LongFn {
    long invoke(byte p1, byte p2, boolean p3);
}
