package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code char} and returns a {@code long}.
 */
public interface ByteByteChar2LongFn {
    long invoke(byte p1, byte p2, char p3);
}
