package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code char} and returns a {@code long}.
 */
public interface ShortDoubleChar2LongFn {
    long invoke(short p1, double p2, char p3);
}
