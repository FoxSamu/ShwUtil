package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code double} and returns a {@code long}.
 */
public interface BoolByteDouble2LongFn {
    long invoke(boolean p1, byte p2, double p3);
}
