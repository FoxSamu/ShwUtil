package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte} and an {@code int} and returns a {@code long}.
 */
public interface ByteInt2LongFn {
    long invoke(byte p1, int p2);
}
