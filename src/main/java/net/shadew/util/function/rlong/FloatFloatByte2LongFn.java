package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code byte} and returns a {@code long}.
 */
public interface FloatFloatByte2LongFn {
    long invoke(float p1, float p2, byte p3);
}
