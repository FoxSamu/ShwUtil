package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code byte} and returns a {@code long}.
 */
public interface FloatBoolByte2LongFn {
    long invoke(float p1, boolean p2, byte p3);
}
