package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int} and a {@code byte} and returns a {@code long}.
 */
public interface IntByte2LongFn {
    long invoke(int p1, byte p2);
}
