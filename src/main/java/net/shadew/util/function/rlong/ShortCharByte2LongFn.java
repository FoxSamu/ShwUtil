package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns a {@code long}.
 */
public interface ShortCharByte2LongFn {
    long invoke(short p1, char p2, byte p3);
}
