package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char}, a {@code double} and an {@code int} and returns a {@code long}.
 */
public interface CharDoubleInt2LongFn {
    long invoke(char p1, double p2, int p3);
}
