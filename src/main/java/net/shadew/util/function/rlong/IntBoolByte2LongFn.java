package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code byte} and returns a {@code long}.
 */
public interface IntBoolByte2LongFn {
    long invoke(int p1, boolean p2, byte p3);
}
