package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns a {@code long}.
 */
public interface FloatByteDouble2LongFn {
    long invoke(float p1, byte p2, double p3);
}
