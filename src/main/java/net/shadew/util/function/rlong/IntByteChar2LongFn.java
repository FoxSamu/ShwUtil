package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns a {@code long}.
 */
public interface IntByteChar2LongFn {
    long invoke(int p1, byte p2, char p3);
}
