package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns a {@code long}.
 */
public interface FloatByteInt2LongFn {
    long invoke(float p1, byte p2, int p3);
}
