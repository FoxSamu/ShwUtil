package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code float} and returns a {@code long}.
 */
public interface LongByteFloat2LongFn {
    long invoke(long p1, byte p2, float p3);
}
