package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code short} and returns a {@code long}.
 */
public interface ShortFloatShort2LongFn {
    long invoke(Short p1, Float p2, Short p3);
}
