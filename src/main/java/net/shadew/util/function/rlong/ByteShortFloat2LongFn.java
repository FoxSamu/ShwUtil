package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code float} and returns a {@code long}.
 */
public interface ByteShortFloat2LongFn {
    long invoke(byte p1, short p2, float p3);
}
