package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code byte} and returns a {@code long}.
 */
public interface FloatByteByte2LongFn {
    long invoke(float p1, byte p2, byte p3);
}
