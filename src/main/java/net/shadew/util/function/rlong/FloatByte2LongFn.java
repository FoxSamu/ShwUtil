package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float} and a {@code byte} and returns a {@code long}.
 */
public interface FloatByte2LongFn {
    long invoke(float p1, byte p2);
}
