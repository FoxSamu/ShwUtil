package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code double} and returns a {@code long}.
 */
public interface FloatShortDouble2LongFn {
    long invoke(float p1, short p2, double p3);
}
