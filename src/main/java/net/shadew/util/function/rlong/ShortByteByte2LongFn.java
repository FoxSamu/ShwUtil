package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code byte} and returns a {@code long}.
 */
public interface ShortByteByte2LongFn {
    long invoke(short p1, byte p2, byte p3);
}
