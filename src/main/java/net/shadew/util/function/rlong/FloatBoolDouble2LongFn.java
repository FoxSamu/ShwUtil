package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code double} and returns a {@code long}.
 */
public interface FloatBoolDouble2LongFn {
    long invoke(float p1, boolean p2, double p3);
}
