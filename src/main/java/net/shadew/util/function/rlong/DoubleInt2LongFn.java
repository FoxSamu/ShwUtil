package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code double} and an {@code int} and returns a {@code long}.
 */
public interface DoubleInt2LongFn {
    long invoke(double p1, int p2);
}
