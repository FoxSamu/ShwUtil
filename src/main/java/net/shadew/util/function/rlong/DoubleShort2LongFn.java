package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code double} and a {@code short} and returns a {@code long}.
 */
public interface DoubleShort2LongFn {
    long invoke(double p1, short p2);
}
