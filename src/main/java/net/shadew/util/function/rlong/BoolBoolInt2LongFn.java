package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an {@code int} and returns a {@code long}.
 */
public interface BoolBoolInt2LongFn {
    long invoke(boolean p1, boolean p2, int p3);
}
