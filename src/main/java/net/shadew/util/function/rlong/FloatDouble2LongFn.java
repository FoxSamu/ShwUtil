package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float} and a {@code double} and returns a {@code long}.
 */
public interface FloatDouble2LongFn {
    long invoke(float p1, double p2);
}
