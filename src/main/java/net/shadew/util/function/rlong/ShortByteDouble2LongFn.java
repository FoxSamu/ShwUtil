package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code double} and returns a {@code long}.
 */
public interface ShortByteDouble2LongFn {
    long invoke(short p1, byte p2, double p3);
}
