package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code double} and returns a {@code long}.
 */
public interface FloatCharDouble2LongFn {
    long invoke(float p1, char p2, double p3);
}
