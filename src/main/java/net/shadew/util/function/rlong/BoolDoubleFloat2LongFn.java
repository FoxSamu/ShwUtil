package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code float} and returns a {@code long}.
 */
public interface BoolDoubleFloat2LongFn {
    long invoke(Bool p1, Double p2, Float p3);
}
