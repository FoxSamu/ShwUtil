package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns a {@code long}.
 */
public interface IntDoubleByte2LongFn {
    long invoke(int p1, double p2, byte p3);
}
