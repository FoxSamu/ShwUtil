package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code float} and returns a {@code long}.
 */
public interface BoolByteFloat2LongFn {
    long invoke(boolean p1, byte p2, float p3);
}
