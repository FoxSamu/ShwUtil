package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns a {@code long}.
 */
public interface BoolBool2LongFn {
    long invoke(boolean p1, boolean p2);
}
