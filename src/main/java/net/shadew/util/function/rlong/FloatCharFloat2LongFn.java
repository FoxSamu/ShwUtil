package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code float} and returns a {@code long}.
 */
public interface FloatCharFloat2LongFn {
    long invoke(float p1, char p2, float p3);
}
