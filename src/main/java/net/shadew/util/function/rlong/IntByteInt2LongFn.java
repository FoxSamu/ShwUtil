package net.shadew.util.function.rlong;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns a {@code long}.
 */
public interface IntByteInt2LongFn {
    long invoke(int p1, byte p2, int p3);
}
