package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code float} and returns a {@code long}.
 */
public interface ByteDoubleFloat2LongFn {
    long invoke(byte p1, double p2, float p3);
}
