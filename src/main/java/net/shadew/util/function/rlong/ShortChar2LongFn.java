package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short} and a {@code char} and returns a {@code long}.
 */
public interface ShortChar2LongFn {
    long invoke(short p1, char p2);
}
