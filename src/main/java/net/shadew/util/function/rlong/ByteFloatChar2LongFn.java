package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code char} and returns a {@code long}.
 */
public interface ByteFloatChar2LongFn {
    long invoke(byte p1, float p2, char p3);
}
