package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code short} and returns a {@code long}.
 */
public interface ShortDoubleShort2LongFn {
    long invoke(short p1, double p2, short p3);
}
