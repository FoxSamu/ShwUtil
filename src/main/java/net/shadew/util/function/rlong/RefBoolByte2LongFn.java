package net.shadew.util.function.rlong;

/**
 * A function that takes an object, a {@code boolean} and a {@code byte} and returns a {@code long}.
 */
public interface RefBoolByte2LongFn<P1> {
    long invoke(P1 p1, boolean p2, byte p3);
}
