package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code long} and a {@code short} and returns a {@code long}.
 */
public interface LongShort2LongFn {
    long invoke(long p1, short p2);
}
