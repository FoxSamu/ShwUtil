package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code double} and a {@code char} and returns a {@code long}.
 */
public interface DoubleChar2LongFn {
    long invoke(double p1, char p2);
}
