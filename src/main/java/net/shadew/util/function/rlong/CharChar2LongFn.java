package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code char} and a {@code char} and returns a {@code long}.
 */
public interface CharChar2LongFn {
    long invoke(char p1, char p2);
}
