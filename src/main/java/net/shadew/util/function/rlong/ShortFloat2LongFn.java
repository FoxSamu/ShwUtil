package net.shadew.util.function.rlong;

/**
 * A function that takes a {@code short} and a {@code float} and returns a {@code long}.
 */
public interface ShortFloat2LongFn {
    long invoke(short p1, float p2);
}
