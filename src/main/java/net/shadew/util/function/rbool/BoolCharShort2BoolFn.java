package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code short} and returns a {@code boolean}.
 */
public interface BoolCharShort2BoolFn {
    boolean invoke(boolean p1, char p2, short p3);
}
