package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code byte} and an {@code int} and returns a {@code boolean}.
 */
public interface ShortByteInt2BoolFn {
    boolean invoke(short p1, byte p2, int p3);
}
