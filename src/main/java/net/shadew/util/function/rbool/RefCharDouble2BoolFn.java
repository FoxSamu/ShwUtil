package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code char} and a {@code double} and returns a {@code boolean}.
 */
public interface RefCharDouble2BoolFn<P1> {
    boolean invoke(P1 p1, char p2, double p3);
}
