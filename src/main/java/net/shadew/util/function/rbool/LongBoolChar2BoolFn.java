package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code char} and returns a {@code boolean}.
 */
public interface LongBoolChar2BoolFn {
    boolean invoke(long p1, boolean p2, char p3);
}
