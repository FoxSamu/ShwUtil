package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code double} and returns a {@code boolean}.
 */
public interface IntBoolDouble2BoolFn {
    boolean invoke(int p1, boolean p2, double p3);
}
