package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code long} and a {@code char} and returns a {@code boolean}.
 */
public interface ByteLongChar2BoolFn {
    boolean invoke(byte p1, long p2, char p3);
}
