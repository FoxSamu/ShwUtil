package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code short} and an {@code int} and returns a {@code boolean}.
 */
public interface ByteShortInt2BoolFn {
    boolean invoke(byte p1, short p2, int p3);
}
