package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int} and a {@code float} and returns a {@code boolean}.
 */
public interface IntFloat2BoolFn {
    boolean invoke(int p1, float p2);
}
