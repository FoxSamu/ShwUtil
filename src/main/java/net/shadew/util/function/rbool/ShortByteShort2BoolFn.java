package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code short} and returns a {@code boolean}.
 */
public interface ShortByteShort2BoolFn {
    boolean invoke(short p1, byte p2, short p3);
}
