package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code char} and returns a {@code boolean}.
 */
public interface ByteFloatChar2BoolFn {
    boolean invoke(byte p1, float p2, char p3);
}
