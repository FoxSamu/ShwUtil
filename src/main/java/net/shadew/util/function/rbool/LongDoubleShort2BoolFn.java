package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code short} and returns a {@code boolean}.
 */
public interface LongDoubleShort2BoolFn {
    boolean invoke(long p1, double p2, short p3);
}
