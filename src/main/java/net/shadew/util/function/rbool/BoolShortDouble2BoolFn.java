package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code double} and returns a {@code boolean}.
 */
public interface BoolShortDouble2BoolFn {
    boolean invoke(boolean p1, short p2, double p3);
}
