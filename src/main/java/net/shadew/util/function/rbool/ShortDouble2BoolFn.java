package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short} and a {@code double} and returns a {@code boolean}.
 */
public interface ShortDouble2BoolFn {
    boolean invoke(short p1, double p2);
}
