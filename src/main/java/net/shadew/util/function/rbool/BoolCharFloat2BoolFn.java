package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code float} and returns a {@code boolean}.
 */
public interface BoolCharFloat2BoolFn {
    boolean invoke(boolean p1, char p2, float p3);
}
