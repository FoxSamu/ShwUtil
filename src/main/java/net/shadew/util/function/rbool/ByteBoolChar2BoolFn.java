package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns a {@code boolean}.
 */
public interface ByteBoolChar2BoolFn {
    boolean invoke(byte p1, boolean p2, char p3);
}
