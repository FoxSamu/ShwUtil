package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code float} and returns a {@code boolean}.
 */
public interface LongShortFloat2BoolFn {
    boolean invoke(long p1, short p2, float p3);
}
