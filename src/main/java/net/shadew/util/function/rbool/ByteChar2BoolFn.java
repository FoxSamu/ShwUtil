package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface ByteChar2BoolFn {
    boolean invoke(byte p1, char p2);
}
