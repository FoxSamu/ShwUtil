package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code double} and returns a {@code boolean}.
 */
public interface FloatLongDouble2BoolFn {
    boolean invoke(float p1, long p2, double p3);
}
