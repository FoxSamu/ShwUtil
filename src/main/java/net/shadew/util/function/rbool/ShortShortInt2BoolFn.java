package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code short} and an {@code int} and returns a {@code boolean}.
 */
public interface ShortShortInt2BoolFn {
    boolean invoke(Short p1, Short p2, Int p3);
}
