package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface ShortByteChar2BoolFn {
    boolean invoke(short p1, byte p2, char p3);
}
