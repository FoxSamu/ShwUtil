package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code float} and returns a {@code boolean}.
 */
public interface IntBoolFloat2BoolFn {
    boolean invoke(int p1, boolean p2, float p3);
}
