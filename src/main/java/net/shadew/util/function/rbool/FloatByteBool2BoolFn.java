package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns a {@code boolean}.
 */
public interface FloatByteBool2BoolFn {
    boolean invoke(float p1, byte p2, boolean p3);
}
