package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code byte} and returns a {@code boolean}.
 */
public interface CharByteByte2BoolFn {
    boolean invoke(char p1, byte p2, byte p3);
}
