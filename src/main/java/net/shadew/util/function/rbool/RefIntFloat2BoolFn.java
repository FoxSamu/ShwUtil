package net.shadew.util.function.rbool;

/**
 * A function that takes an object, an {@code int} and a {@code float} and returns a {@code boolean}.
 */
public interface RefIntFloat2BoolFn<P1> {
    boolean invoke(P1 p1, int p2, float p3);
}
