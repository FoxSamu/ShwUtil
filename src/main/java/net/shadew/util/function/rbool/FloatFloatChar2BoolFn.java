package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code char} and returns a {@code boolean}.
 */
public interface FloatFloatChar2BoolFn {
    boolean invoke(float p1, float p2, char p3);
}
