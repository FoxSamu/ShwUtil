package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code boolean} and an object and returns a {@code boolean}.
 */
public interface ShortBoolRef2BoolFn<P3> {
    boolean invoke(short p1, boolean p2, P3 p3);
}
