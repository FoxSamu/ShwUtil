package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code double} and returns a {@code boolean}.
 */
public interface ShortBoolDouble2BoolFn {
    boolean invoke(short p1, boolean p2, double p3);
}
