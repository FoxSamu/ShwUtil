package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long} and a {@code short} and returns a {@code boolean}.
 */
public interface LongShort2BoolFn {
    boolean invoke(long p1, short p2);
}
