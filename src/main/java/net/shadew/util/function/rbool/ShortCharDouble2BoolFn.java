package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code double} and returns a {@code boolean}.
 */
public interface ShortCharDouble2BoolFn {
    boolean invoke(short p1, char p2, double p3);
}
