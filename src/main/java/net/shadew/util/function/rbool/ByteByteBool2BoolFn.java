package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns a {@code boolean}.
 */
public interface ByteByteBool2BoolFn {
    boolean invoke(byte p1, byte p2, boolean p3);
}
