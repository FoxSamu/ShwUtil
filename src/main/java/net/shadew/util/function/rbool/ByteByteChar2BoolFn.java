package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface ByteByteChar2BoolFn {
    boolean invoke(byte p1, byte p2, char p3);
}
