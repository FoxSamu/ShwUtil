package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code byte} and returns a {@code boolean}.
 */
public interface LongFloatByte2BoolFn {
    boolean invoke(long p1, float p2, byte p3);
}
