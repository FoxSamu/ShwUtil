package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code boolean} and an {@code int} and returns a {@code boolean}.
 */
public interface FloatBoolInt2BoolFn {
    boolean invoke(float p1, boolean p2, int p3);
}
