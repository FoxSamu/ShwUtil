package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float} and a {@code byte} and returns a {@code boolean}.
 */
public interface FloatByte2BoolFn {
    boolean invoke(float p1, byte p2);
}
