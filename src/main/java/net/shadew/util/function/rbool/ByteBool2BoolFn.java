package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns a {@code boolean}.
 */
public interface ByteBool2BoolFn {
    boolean invoke(byte p1, boolean p2);
}
