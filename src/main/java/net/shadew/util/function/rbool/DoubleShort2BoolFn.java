package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code double} and a {@code short} and returns a {@code boolean}.
 */
public interface DoubleShort2BoolFn {
    boolean invoke(double p1, short p2);
}
