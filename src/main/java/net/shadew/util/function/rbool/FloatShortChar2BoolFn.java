package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code char} and returns a {@code boolean}.
 */
public interface FloatShortChar2BoolFn {
    boolean invoke(float p1, short p2, char p3);
}
