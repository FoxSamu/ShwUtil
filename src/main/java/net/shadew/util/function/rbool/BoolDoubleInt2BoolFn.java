package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code double} and an {@code int} and returns a {@code boolean}.
 */
public interface BoolDoubleInt2BoolFn {
    boolean invoke(boolean p1, double p2, int p3);
}
