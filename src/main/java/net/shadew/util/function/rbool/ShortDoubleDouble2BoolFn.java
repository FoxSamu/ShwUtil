package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code double} and returns a {@code boolean}.
 */
public interface ShortDoubleDouble2BoolFn {
    boolean invoke(short p1, double p2, double p3);
}
