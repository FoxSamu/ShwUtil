package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code double} and an {@code int} and returns a {@code boolean}.
 */
public interface LongDoubleInt2BoolFn {
    boolean invoke(long p1, double p2, int p3);
}
