package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code byte} and returns a {@code boolean}.
 */
public interface BoolFloatByte2BoolFn {
    boolean invoke(boolean p1, float p2, byte p3);
}
