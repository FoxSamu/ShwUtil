package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code double} and a {@code float} and returns a {@code boolean}.
 */
public interface DoubleFloat2BoolFn {
    boolean invoke(double p1, float p2);
}
