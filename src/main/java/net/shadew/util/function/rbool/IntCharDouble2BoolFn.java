package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code double} and returns a {@code boolean}.
 */
public interface IntCharDouble2BoolFn {
    boolean invoke(int p1, char p2, double p3);
}
