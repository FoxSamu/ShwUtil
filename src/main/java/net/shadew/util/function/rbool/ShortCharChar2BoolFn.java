package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code char} and returns a {@code boolean}.
 */
public interface ShortCharChar2BoolFn {
    boolean invoke(short p1, char p2, char p3);
}
