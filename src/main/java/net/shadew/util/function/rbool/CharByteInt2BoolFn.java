package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code byte} and an {@code int} and returns a {@code boolean}.
 */
public interface CharByteInt2BoolFn {
    boolean invoke(char p1, byte p2, int p3);
}
