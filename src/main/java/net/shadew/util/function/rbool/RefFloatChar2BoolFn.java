package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code float} and a {@code char} and returns a {@code boolean}.
 */
public interface RefFloatChar2BoolFn<P1> {
    boolean invoke(P1 p1, float p2, char p3);
}
