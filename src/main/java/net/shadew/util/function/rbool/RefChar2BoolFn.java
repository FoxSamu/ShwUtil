package net.shadew.util.function.rbool;

/**
 * A function that takes an object and a {@code char} and returns a {@code boolean}.
 */
public interface RefChar2BoolFn<P1> {
    boolean invoke(P1 p1, char p2);
}
