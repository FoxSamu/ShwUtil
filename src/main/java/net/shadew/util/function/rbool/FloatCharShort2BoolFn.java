package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code short} and returns a {@code boolean}.
 */
public interface FloatCharShort2BoolFn {
    boolean invoke(float p1, char p2, short p3);
}
