package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code float} and returns a {@code boolean}.
 */
public interface ShortShortFloat2BoolFn {
    boolean invoke(short p1, short p2, float p3);
}
