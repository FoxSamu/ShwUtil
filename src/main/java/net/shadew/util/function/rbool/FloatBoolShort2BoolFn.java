package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code short} and returns a {@code boolean}.
 */
public interface FloatBoolShort2BoolFn {
    boolean invoke(float p1, boolean p2, short p3);
}
