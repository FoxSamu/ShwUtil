package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code double} and returns a {@code boolean}.
 */
public interface FloatCharDouble2BoolFn {
    boolean invoke(float p1, char p2, double p3);
}
