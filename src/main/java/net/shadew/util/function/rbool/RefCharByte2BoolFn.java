package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code char} and a {@code byte} and returns a {@code boolean}.
 */
public interface RefCharByte2BoolFn<P1> {
    boolean invoke(P1 p1, char p2, byte p3);
}
