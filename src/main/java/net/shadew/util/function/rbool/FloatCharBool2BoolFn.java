package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code boolean} and returns a {@code boolean}.
 */
public interface FloatCharBool2BoolFn {
    boolean invoke(float p1, char p2, boolean p3);
}
