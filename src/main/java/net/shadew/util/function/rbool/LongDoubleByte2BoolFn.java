package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code byte} and returns a {@code boolean}.
 */
public interface LongDoubleByte2BoolFn {
    boolean invoke(long p1, double p2, byte p3);
}
