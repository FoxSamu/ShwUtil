package net.shadew.util.function.rbool;

/**
 * A function that takes an object, an object and a {@code float} and returns a {@code boolean}.
 */
public interface RefRefFloat2BoolFn<P1, P2> {
    boolean invoke(P1 p1, P2 p2, float p3);
}
