package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long} and a {@code char} and returns a {@code boolean}.
 */
public interface LongChar2BoolFn {
    boolean invoke(long p1, char p2);
}
