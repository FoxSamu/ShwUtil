package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an {@code int} and returns a {@code boolean}.
 */
public interface BoolByteInt2BoolFn {
    boolean invoke(boolean p1, byte p2, int p3);
}
