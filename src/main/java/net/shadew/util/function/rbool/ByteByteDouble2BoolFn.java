package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns a {@code boolean}.
 */
public interface ByteByteDouble2BoolFn {
    boolean invoke(byte p1, byte p2, double p3);
}
