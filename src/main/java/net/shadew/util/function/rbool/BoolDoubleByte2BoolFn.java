package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code byte} and returns a {@code boolean}.
 */
public interface BoolDoubleByte2BoolFn {
    boolean invoke(boolean p1, double p2, byte p3);
}
