package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code short} and returns a {@code boolean}.
 */
public interface FloatDoubleShort2BoolFn {
    boolean invoke(float p1, double p2, short p3);
}
