package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface IntByteChar2BoolFn {
    boolean invoke(int p1, byte p2, char p3);
}
