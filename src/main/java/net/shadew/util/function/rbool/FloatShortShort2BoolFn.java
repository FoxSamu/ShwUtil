package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code short} and returns a {@code boolean}.
 */
public interface FloatShortShort2BoolFn {
    boolean invoke(float p1, short p2, short p3);
}
