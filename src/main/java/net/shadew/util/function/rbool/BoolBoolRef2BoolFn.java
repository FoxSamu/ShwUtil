package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an object and returns a {@code boolean}.
 */
public interface BoolBoolRef2BoolFn<P3> {
    boolean invoke(boolean p1, boolean p2, P3 p3);
}
