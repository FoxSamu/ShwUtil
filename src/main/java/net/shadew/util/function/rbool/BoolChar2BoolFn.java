package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns a {@code boolean}.
 */
public interface BoolChar2BoolFn {
    boolean invoke(boolean p1, char p2);
}
