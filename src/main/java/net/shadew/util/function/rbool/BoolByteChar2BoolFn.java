package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface BoolByteChar2BoolFn {
    boolean invoke(boolean p1, byte p2, char p3);
}
