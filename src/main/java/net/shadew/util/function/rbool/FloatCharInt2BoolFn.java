package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code char} and an {@code int} and returns a {@code boolean}.
 */
public interface FloatCharInt2BoolFn {
    boolean invoke(float p1, char p2, int p3);
}
