package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code char} and returns a {@code boolean}.
 */
public interface FloatIntChar2BoolFn {
    boolean invoke(float p1, int p2, char p3);
}
