package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code byte} and returns a {@code boolean}.
 */
public interface FloatBoolByte2BoolFn {
    boolean invoke(float p1, boolean p2, byte p3);
}
