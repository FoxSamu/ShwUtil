package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code byte} and returns a {@code boolean}.
 */
public interface CharDoubleByte2BoolFn {
    boolean invoke(char p1, double p2, byte p3);
}
