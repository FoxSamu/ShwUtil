package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char} and an {@code int} and returns a {@code boolean}.
 */
public interface CharInt2BoolFn {
    boolean invoke(char p1, int p2);
}
