package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code float} and returns a {@code boolean}.
 */
public interface LongIntFloat2BoolFn {
    boolean invoke(Long p1, Int p2, Float p3);
}
