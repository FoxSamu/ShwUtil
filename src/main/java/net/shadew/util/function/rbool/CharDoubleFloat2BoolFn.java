package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code float} and returns a {@code boolean}.
 */
public interface CharDoubleFloat2BoolFn {
    boolean invoke(char p1, double p2, float p3);
}
