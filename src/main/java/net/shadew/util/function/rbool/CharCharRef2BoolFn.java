package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code char} and an object and returns a {@code boolean}.
 */
public interface CharCharRef2BoolFn<P3> {
    boolean invoke(char p1, char p2, P3 p3);
}
