package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code float} and a {@code byte} and returns a {@code boolean}.
 */
public interface RefFloatByte2BoolFn<P1> {
    boolean invoke(P1 p1, float p2, byte p3);
}
