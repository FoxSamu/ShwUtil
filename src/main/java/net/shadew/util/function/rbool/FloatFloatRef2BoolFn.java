package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code float} and an object and returns a {@code boolean}.
 */
public interface FloatFloatRef2BoolFn<P3> {
    boolean invoke(float p1, float p2, P3 p3);
}
