package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code short} and returns a {@code boolean}.
 */
public interface ByteBoolShort2BoolFn {
    boolean invoke(byte p1, boolean p2, short p3);
}
