package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code char} and returns a {@code boolean}.
 */
public interface ByteDoubleChar2BoolFn {
    boolean invoke(byte p1, double p2, char p3);
}
