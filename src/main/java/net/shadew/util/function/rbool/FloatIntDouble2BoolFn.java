package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code double} and returns a {@code boolean}.
 */
public interface FloatIntDouble2BoolFn {
    boolean invoke(float p1, int p2, double p3);
}
