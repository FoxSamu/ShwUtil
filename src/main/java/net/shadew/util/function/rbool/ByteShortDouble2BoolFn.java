package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code double} and returns a {@code boolean}.
 */
public interface ByteShortDouble2BoolFn {
    boolean invoke(byte p1, short p2, double p3);
}
