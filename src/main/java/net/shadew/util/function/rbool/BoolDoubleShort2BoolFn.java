package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code short} and returns a {@code boolean}.
 */
public interface BoolDoubleShort2BoolFn {
    boolean invoke(boolean p1, double p2, short p3);
}
