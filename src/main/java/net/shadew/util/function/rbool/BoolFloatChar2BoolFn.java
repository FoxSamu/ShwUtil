package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code char} and returns a {@code boolean}.
 */
public interface BoolFloatChar2BoolFn {
    boolean invoke(boolean p1, float p2, char p3);
}
