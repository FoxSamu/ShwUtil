package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code float} and an object and returns a {@code boolean}.
 */
public interface ShortFloatRef2BoolFn<P3> {
    boolean invoke(short p1, float p2, P3 p3);
}
