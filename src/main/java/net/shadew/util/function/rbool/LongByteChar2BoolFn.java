package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface LongByteChar2BoolFn {
    boolean invoke(long p1, byte p2, char p3);
}
