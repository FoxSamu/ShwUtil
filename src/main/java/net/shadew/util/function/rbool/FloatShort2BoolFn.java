package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float} and a {@code short} and returns a {@code boolean}.
 */
public interface FloatShort2BoolFn {
    boolean invoke(float p1, short p2);
}
