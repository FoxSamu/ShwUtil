package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code float} and returns a {@code boolean}.
 */
public interface ShortIntFloat2BoolFn {
    boolean invoke(short p1, int p2, float p3);
}
