package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code double} and returns a {@code boolean}.
 */
public interface BoolByteDouble2BoolFn {
    boolean invoke(boolean p1, byte p2, double p3);
}
