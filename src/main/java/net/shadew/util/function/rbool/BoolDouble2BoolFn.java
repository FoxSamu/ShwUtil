package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns a {@code boolean}.
 */
public interface BoolDouble2BoolFn {
    boolean invoke(boolean p1, double p2);
}
