package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns a {@code boolean}.
 */
public interface BoolBool2BoolFn {
    boolean invoke(boolean p1, boolean p2);
}
