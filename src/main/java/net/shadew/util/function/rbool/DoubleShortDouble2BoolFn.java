package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code double} and returns a {@code boolean}.
 */
public interface DoubleShortDouble2BoolFn {
    boolean invoke(double p1, short p2, double p3);
}
