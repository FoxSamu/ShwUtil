package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code short} and returns a {@code boolean}.
 */
public interface IntFloatShort2BoolFn {
    boolean invoke(int p1, float p2, short p3);
}
