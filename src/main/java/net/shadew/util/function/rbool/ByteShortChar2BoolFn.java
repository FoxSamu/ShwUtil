package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code char} and returns a {@code boolean}.
 */
public interface ByteShortChar2BoolFn {
    boolean invoke(byte p1, short p2, char p3);
}
