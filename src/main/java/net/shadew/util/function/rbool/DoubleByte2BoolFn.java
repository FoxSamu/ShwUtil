package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code double} and a {@code byte} and returns a {@code boolean}.
 */
public interface DoubleByte2BoolFn {
    boolean invoke(double p1, byte p2);
}
