package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code char} and an object and returns a {@code boolean}.
 */
public interface ShortCharRef2BoolFn<P3> {
    boolean invoke(short p1, char p2, P3 p3);
}
