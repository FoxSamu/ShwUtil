package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code byte} and returns a {@code boolean}.
 */
public interface FloatShortByte2BoolFn {
    boolean invoke(float p1, short p2, byte p3);
}
