package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code char} and an object and returns a {@code boolean}.
 */
public interface FloatCharRef2BoolFn<P3> {
    boolean invoke(Float p1, Char p2, P3 p3);
}
