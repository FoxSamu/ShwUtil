package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int} and a {@code byte} and returns a {@code boolean}.
 */
public interface IntByte2BoolFn {
    boolean invoke(int p1, byte p2);
}
