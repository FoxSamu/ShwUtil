package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code byte} and returns a {@code boolean}.
 */
public interface FloatDoubleByte2BoolFn {
    boolean invoke(float p1, double p2, byte p3);
}
