package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code short} and returns a {@code boolean}.
 */
public interface LongByteShort2BoolFn {
    boolean invoke(long p1, byte p2, short p3);
}
