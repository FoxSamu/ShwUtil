package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code char} and returns a {@code boolean}.
 */
public interface ShortFloatChar2BoolFn {
    boolean invoke(short p1, float p2, char p3);
}
