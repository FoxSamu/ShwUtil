package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short} and a {@code long} and returns a {@code boolean}.
 */
public interface ShortLong2BoolFn {
    boolean invoke(short p1, long p2);
}
