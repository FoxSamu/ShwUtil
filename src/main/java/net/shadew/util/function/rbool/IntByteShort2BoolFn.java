package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code short} and returns a {@code boolean}.
 */
public interface IntByteShort2BoolFn {
    boolean invoke(int p1, byte p2, short p3);
}
