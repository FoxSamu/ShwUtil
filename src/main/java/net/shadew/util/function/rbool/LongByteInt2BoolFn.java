package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code byte} and an {@code int} and returns a {@code boolean}.
 */
public interface LongByteInt2BoolFn {
    boolean invoke(long p1, byte p2, int p3);
}
