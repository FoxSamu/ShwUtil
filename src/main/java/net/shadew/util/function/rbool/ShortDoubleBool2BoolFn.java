package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code boolean} and returns a {@code boolean}.
 */
public interface ShortDoubleBool2BoolFn {
    boolean invoke(short p1, double p2, boolean p3);
}
