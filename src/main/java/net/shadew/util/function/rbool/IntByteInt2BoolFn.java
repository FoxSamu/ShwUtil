package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns a {@code boolean}.
 */
public interface IntByteInt2BoolFn {
    boolean invoke(int p1, byte p2, int p3);
}
