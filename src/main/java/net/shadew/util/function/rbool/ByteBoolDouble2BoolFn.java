package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code double} and returns a {@code boolean}.
 */
public interface ByteBoolDouble2BoolFn {
    boolean invoke(byte p1, boolean p2, double p3);
}
