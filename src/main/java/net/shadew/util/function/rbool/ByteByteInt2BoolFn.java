package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code byte} and an {@code int} and returns a {@code boolean}.
 */
public interface ByteByteInt2BoolFn {
    boolean invoke(byte p1, byte p2, int p3);
}
