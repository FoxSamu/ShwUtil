package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean} and a {@code float} and returns a {@code boolean}.
 */
public interface BoolFloat2BoolFn {
    boolean invoke(boolean p1, float p2);
}
