package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int} and a {@code double} and returns a {@code boolean}.
 */
public interface IntDouble2BoolFn {
    boolean invoke(int p1, double p2);
}
