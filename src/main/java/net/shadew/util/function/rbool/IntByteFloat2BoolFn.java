package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code float} and returns a {@code boolean}.
 */
public interface IntByteFloat2BoolFn {
    boolean invoke(int p1, byte p2, float p3);
}
