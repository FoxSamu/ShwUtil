package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int} and a {@code char} and returns a {@code boolean}.
 */
public interface IntChar2BoolFn {
    boolean invoke(int p1, char p2);
}
