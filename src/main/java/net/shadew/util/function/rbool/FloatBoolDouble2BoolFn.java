package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code double} and returns a {@code boolean}.
 */
public interface FloatBoolDouble2BoolFn {
    boolean invoke(float p1, boolean p2, double p3);
}
