package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code double} and returns a {@code boolean}.
 */
public interface ShortFloatDouble2BoolFn {
    boolean invoke(short p1, float p2, double p3);
}
