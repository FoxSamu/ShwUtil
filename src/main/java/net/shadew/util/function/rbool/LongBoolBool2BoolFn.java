package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code boolean} and returns a {@code boolean}.
 */
public interface LongBoolBool2BoolFn {
    boolean invoke(Long p1, Bool p2, Bool p3);
}
