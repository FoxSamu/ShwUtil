package net.shadew.util.function.rbool;

/**
 * A function that takes an object and a {@code double} and returns a {@code boolean}.
 */
public interface RefDouble2BoolFn<P1> {
    boolean invoke(P1 p1, double p2);
}
