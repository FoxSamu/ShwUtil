package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code char} and returns a {@code boolean}.
 */
public interface IntFloatChar2BoolFn {
    boolean invoke(int p1, float p2, char p3);
}
