package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code double} and returns a {@code boolean}.
 */
public interface DoubleByteDouble2BoolFn {
    boolean invoke(double p1, byte p2, double p3);
}
