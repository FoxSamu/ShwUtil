package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte} and an {@code int} and returns a {@code boolean}.
 */
public interface ByteInt2BoolFn {
    boolean invoke(byte p1, int p2);
}
