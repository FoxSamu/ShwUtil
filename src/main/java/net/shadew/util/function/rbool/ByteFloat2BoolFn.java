package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte} and a {@code float} and returns a {@code boolean}.
 */
public interface ByteFloat2BoolFn {
    boolean invoke(byte p1, float p2);
}
