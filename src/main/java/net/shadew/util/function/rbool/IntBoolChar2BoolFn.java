package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code char} and returns a {@code boolean}.
 */
public interface IntBoolChar2BoolFn {
    boolean invoke(int p1, boolean p2, char p3);
}
