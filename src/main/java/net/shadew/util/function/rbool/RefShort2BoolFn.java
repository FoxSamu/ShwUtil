package net.shadew.util.function.rbool;

/**
 * A function that takes an object and a {@code short} and returns a {@code boolean}.
 */
public interface RefShort2BoolFn<P1> {
    boolean invoke(P1 p1, short p2);
}
