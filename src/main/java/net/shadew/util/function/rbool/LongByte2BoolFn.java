package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long} and a {@code byte} and returns a {@code boolean}.
 */
public interface LongByte2BoolFn {
    boolean invoke(long p1, byte p2);
}
