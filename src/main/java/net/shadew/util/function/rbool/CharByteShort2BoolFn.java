package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code short} and returns a {@code boolean}.
 */
public interface CharByteShort2BoolFn {
    boolean invoke(char p1, byte p2, short p3);
}
