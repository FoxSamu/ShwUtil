package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code byte} and returns a {@code boolean}.
 */
public interface IntBoolByte2BoolFn {
    boolean invoke(int p1, boolean p2, byte p3);
}
