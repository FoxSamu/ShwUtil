package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int} and a {@code boolean} and returns a {@code boolean}.
 */
public interface IntBool2BoolFn {
    boolean invoke(int p1, boolean p2);
}
