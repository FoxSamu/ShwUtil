package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean} and a {@code byte} and returns a {@code boolean}.
 */
public interface BoolByte2BoolFn {
    boolean invoke(boolean p1, byte p2);
}
