package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code short} and returns a {@code boolean}.
 */
public interface ByteByteShort2BoolFn {
    boolean invoke(byte p1, byte p2, short p3);
}
