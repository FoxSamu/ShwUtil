package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface CharByteChar2BoolFn {
    boolean invoke(char p1, byte p2, char p3);
}
