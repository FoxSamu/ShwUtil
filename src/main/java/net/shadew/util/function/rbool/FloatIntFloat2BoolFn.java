package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code float} and returns a {@code boolean}.
 */
public interface FloatIntFloat2BoolFn {
    boolean invoke(float p1, int p2, float p3);
}
