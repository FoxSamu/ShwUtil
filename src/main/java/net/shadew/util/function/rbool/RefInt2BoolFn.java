package net.shadew.util.function.rbool;

/**
 * A function that takes an object and an {@code int} and returns a {@code boolean}.
 */
public interface RefInt2BoolFn<P1> {
    boolean invoke(P1 p1, int p2);
}
