package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code short} and an object and returns a {@code boolean}.
 */
public interface FloatShortRef2BoolFn<P3> {
    boolean invoke(float p1, short p2, P3 p3);
}
