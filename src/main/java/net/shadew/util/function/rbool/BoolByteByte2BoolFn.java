package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code byte} and returns a {@code boolean}.
 */
public interface BoolByteByte2BoolFn {
    boolean invoke(boolean p1, byte p2, byte p3);
}
