package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte} and a {@code long} and returns a {@code boolean}.
 */
public interface ByteLong2BoolFn {
    boolean invoke(byte p1, long p2);
}
