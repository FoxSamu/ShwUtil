package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code float} and returns a {@code boolean}.
 */
public interface FloatDoubleFloat2BoolFn {
    boolean invoke(float p1, double p2, float p3);
}
