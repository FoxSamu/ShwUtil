package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code boolean} and a {@code char} and returns a {@code boolean}.
 */
public interface RefBoolChar2BoolFn<P1> {
    boolean invoke(P1 p1, boolean p2, char p3);
}
