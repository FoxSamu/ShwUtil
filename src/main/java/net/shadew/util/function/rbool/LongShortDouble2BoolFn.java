package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code double} and returns a {@code boolean}.
 */
public interface LongShortDouble2BoolFn {
    boolean invoke(long p1, short p2, double p3);
}
