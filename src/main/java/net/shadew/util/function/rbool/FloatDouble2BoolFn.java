package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float} and a {@code double} and returns a {@code boolean}.
 */
public interface FloatDouble2BoolFn {
    boolean invoke(float p1, double p2);
}
