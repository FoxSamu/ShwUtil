package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code boolean} and returns a {@code boolean}.
 */
public interface BoolByteBool2BoolFn {
    boolean invoke(boolean p1, byte p2, boolean p3);
}
