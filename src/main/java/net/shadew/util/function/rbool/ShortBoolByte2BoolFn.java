package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code byte} and returns a {@code boolean}.
 */
public interface ShortBoolByte2BoolFn {
    boolean invoke(short p1, boolean p2, byte p3);
}
