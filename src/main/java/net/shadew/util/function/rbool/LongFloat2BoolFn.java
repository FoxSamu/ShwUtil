package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long} and a {@code float} and returns a {@code boolean}.
 */
public interface LongFloat2BoolFn {
    boolean invoke(long p1, float p2);
}
