package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code float} and returns a {@code boolean}.
 */
public interface ShortDoubleFloat2BoolFn {
    boolean invoke(short p1, double p2, float p3);
}
