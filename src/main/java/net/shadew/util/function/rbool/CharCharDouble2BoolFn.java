package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code char} and a {@code double} and returns a {@code boolean}.
 */
public interface CharCharDouble2BoolFn {
    boolean invoke(char p1, char p2, double p3);
}
