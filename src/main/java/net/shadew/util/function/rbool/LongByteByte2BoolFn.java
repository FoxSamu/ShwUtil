package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code byte} and returns a {@code boolean}.
 */
public interface LongByteByte2BoolFn {
    boolean invoke(long p1, byte p2, byte p3);
}
