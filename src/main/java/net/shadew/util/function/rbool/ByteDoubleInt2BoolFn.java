package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code double} and an {@code int} and returns a {@code boolean}.
 */
public interface ByteDoubleInt2BoolFn {
    boolean invoke(byte p1, double p2, int p3);
}
