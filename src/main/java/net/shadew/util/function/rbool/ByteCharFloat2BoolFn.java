package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code float} and returns a {@code boolean}.
 */
public interface ByteCharFloat2BoolFn {
    boolean invoke(byte p1, char p2, float p3);
}
