package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns a {@code boolean}.
 */
public interface ShortDoubleInt2BoolFn {
    boolean invoke(short p1, double p2, int p3);
}
