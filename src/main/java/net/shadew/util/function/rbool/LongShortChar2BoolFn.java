package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code char} and returns a {@code boolean}.
 */
public interface LongShortChar2BoolFn {
    boolean invoke(long p1, short p2, char p3);
}
