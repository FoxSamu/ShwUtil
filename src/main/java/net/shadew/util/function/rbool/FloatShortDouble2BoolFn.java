package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code double} and returns a {@code boolean}.
 */
public interface FloatShortDouble2BoolFn {
    boolean invoke(float p1, short p2, double p3);
}
