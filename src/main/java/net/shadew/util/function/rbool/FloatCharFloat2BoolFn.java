package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code float} and returns a {@code boolean}.
 */
public interface FloatCharFloat2BoolFn {
    boolean invoke(float p1, char p2, float p3);
}
