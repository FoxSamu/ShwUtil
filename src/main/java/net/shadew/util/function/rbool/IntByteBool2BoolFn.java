package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code boolean} and returns a {@code boolean}.
 */
public interface IntByteBool2BoolFn {
    boolean invoke(int p1, byte p2, boolean p3);
}
