package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code double} and an {@code int} and returns a {@code boolean}.
 */
public interface FloatDoubleInt2BoolFn {
    boolean invoke(float p1, double p2, int p3);
}
