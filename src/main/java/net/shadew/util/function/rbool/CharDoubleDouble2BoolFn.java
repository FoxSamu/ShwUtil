package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code double} and returns a {@code boolean}.
 */
public interface CharDoubleDouble2BoolFn {
    boolean invoke(char p1, double p2, double p3);
}
