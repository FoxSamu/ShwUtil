package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code byte} and returns a {@code boolean}.
 */
public interface ShortFloatByte2BoolFn {
    boolean invoke(short p1, float p2, byte p3);
}
