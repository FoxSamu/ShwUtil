package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long} and a {@code double} and returns a {@code boolean}.
 */
public interface LongDouble2BoolFn {
    boolean invoke(long p1, double p2);
}
