package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code float} and returns a {@code boolean}.
 */
public interface ByteShortFloat2BoolFn {
    boolean invoke(byte p1, short p2, float p3);
}
