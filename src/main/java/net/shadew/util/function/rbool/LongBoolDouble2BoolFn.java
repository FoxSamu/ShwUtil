package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code double} and returns a {@code boolean}.
 */
public interface LongBoolDouble2BoolFn {
    boolean invoke(long p1, boolean p2, double p3);
}
