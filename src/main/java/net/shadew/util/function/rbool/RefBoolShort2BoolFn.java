package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code boolean} and a {@code short} and returns a {@code boolean}.
 */
public interface RefBoolShort2BoolFn<P1> {
    boolean invoke(P1 p1, boolean p2, short p3);
}
