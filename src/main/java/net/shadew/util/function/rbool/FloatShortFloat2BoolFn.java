package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code float} and returns a {@code boolean}.
 */
public interface FloatShortFloat2BoolFn {
    boolean invoke(float p1, short p2, float p3);
}
