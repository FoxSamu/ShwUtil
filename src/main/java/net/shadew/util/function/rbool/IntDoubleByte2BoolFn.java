package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns a {@code boolean}.
 */
public interface IntDoubleByte2BoolFn {
    boolean invoke(int p1, double p2, byte p3);
}
