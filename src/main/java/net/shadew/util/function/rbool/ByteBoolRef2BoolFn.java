package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an object and returns a {@code boolean}.
 */
public interface ByteBoolRef2BoolFn<P3> {
    boolean invoke(byte p1, boolean p2, P3 p3);
}
