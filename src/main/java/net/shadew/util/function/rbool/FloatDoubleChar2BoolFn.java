package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code char} and returns a {@code boolean}.
 */
public interface FloatDoubleChar2BoolFn {
    boolean invoke(float p1, double p2, char p3);
}
