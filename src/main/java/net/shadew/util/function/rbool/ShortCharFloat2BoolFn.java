package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code float} and returns a {@code boolean}.
 */
public interface ShortCharFloat2BoolFn {
    boolean invoke(short p1, char p2, float p3);
}
