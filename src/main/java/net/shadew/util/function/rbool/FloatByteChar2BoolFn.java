package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface FloatByteChar2BoolFn {
    boolean invoke(float p1, byte p2, char p3);
}
