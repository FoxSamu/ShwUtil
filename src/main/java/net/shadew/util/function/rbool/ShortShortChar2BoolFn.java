package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code char} and returns a {@code boolean}.
 */
public interface ShortShortChar2BoolFn {
    boolean invoke(short p1, short p2, char p3);
}
