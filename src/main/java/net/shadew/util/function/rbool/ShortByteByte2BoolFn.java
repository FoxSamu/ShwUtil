package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code byte} and returns a {@code boolean}.
 */
public interface ShortByteByte2BoolFn {
    boolean invoke(short p1, byte p2, byte p3);
}
