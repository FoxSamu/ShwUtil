package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code char} and a {@code char} and returns a {@code boolean}.
 */
public interface RefCharChar2BoolFn<P1> {
    boolean invoke(P1 p1, char p2, char p3);
}
