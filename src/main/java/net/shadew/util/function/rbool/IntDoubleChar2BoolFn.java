package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code char} and returns a {@code boolean}.
 */
public interface IntDoubleChar2BoolFn {
    boolean invoke(int p1, double p2, char p3);
}
