package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short} and a {@code byte} and returns a {@code boolean}.
 */
public interface ShortByte2BoolFn {
    boolean invoke(short p1, byte p2);
}
