package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code double} and returns a {@code boolean}.
 */
public interface FloatDoubleDouble2BoolFn {
    boolean invoke(float p1, double p2, double p3);
}
