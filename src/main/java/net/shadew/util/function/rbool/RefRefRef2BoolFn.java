package net.shadew.util.function.rbool;

/**
 * A function that takes an object, an object and an object and returns a {@code boolean}.
 */
public interface RefRefRef2BoolFn<P1, P2, P3> {
    boolean invoke(P1 p1, P2 p2, P3 p3);
}
