package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char} and a {@code boolean} and returns a {@code boolean}.
 */
public interface CharBool2BoolFn {
    boolean invoke(char p1, boolean p2);
}
