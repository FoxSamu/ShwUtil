package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float} and an {@code int} and returns a {@code boolean}.
 */
public interface FloatInt2BoolFn {
    boolean invoke(float p1, int p2);
}
