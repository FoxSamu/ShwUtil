package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code char} and a {@code short} and returns a {@code boolean}.
 */
public interface RefCharShort2BoolFn<P1> {
    boolean invoke(P1 p1, char p2, short p3);
}
