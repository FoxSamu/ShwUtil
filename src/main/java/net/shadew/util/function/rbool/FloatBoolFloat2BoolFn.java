package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code float} and returns a {@code boolean}.
 */
public interface FloatBoolFloat2BoolFn {
    boolean invoke(float p1, boolean p2, float p3);
}
