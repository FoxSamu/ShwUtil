package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean} and an {@code int} and returns a {@code boolean}.
 */
public interface BoolInt2BoolFn {
    boolean invoke(boolean p1, int p2);
}
