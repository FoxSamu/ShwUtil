package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code double} and returns a {@code boolean}.
 */
public interface IntByteDouble2BoolFn {
    boolean invoke(int p1, byte p2, double p3);
}
