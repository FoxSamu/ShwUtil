package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code char} and returns a {@code boolean}.
 */
public interface DoubleByteChar2BoolFn {
    boolean invoke(double p1, byte p2, char p3);
}
