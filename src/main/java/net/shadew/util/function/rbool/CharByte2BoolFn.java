package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char} and a {@code byte} and returns a {@code boolean}.
 */
public interface CharByte2BoolFn {
    boolean invoke(char p1, byte p2);
}
