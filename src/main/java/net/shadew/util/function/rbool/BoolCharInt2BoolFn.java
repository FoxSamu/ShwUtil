package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code char} and an {@code int} and returns a {@code boolean}.
 */
public interface BoolCharInt2BoolFn {
    boolean invoke(boolean p1, char p2, int p3);
}
