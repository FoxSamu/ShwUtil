package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short} and an {@code int} and returns a {@code boolean}.
 */
public interface ShortInt2BoolFn {
    boolean invoke(short p1, int p2);
}
