package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code double} and returns a {@code boolean}.
 */
public interface CharByteDouble2BoolFn {
    boolean invoke(char p1, byte p2, double p3);
}
