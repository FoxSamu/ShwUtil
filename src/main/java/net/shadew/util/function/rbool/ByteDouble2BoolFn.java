package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte} and a {@code double} and returns a {@code boolean}.
 */
public interface ByteDouble2BoolFn {
    boolean invoke(byte p1, double p2);
}
