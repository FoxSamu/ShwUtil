package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns a {@code boolean}.
 */
public interface FloatByteInt2BoolFn {
    boolean invoke(float p1, byte p2, int p3);
}
