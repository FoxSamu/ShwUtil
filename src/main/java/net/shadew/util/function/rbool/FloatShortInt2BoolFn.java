package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code short} and an {@code int} and returns a {@code boolean}.
 */
public interface FloatShortInt2BoolFn {
    boolean invoke(float p1, short p2, int p3);
}
