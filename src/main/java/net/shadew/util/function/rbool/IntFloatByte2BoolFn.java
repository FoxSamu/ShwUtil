package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code byte} and returns a {@code boolean}.
 */
public interface IntFloatByte2BoolFn {
    boolean invoke(int p1, float p2, byte p3);
}
