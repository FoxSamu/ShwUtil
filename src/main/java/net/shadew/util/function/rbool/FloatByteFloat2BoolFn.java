package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code float} and returns a {@code boolean}.
 */
public interface FloatByteFloat2BoolFn {
    boolean invoke(float p1, byte p2, float p3);
}
