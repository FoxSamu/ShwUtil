package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte} and a {@code byte} and returns a {@code boolean}.
 */
public interface ByteByte2BoolFn {
    boolean invoke(byte p1, byte p2);
}
