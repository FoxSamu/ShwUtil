package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code boolean} and an object and returns a {@code boolean}.
 */
public interface FloatBoolRef2BoolFn<P3> {
    boolean invoke(float p1, boolean p2, P3 p3);
}
