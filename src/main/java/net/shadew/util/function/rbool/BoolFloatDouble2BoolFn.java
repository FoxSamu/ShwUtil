package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code double} and returns a {@code boolean}.
 */
public interface BoolFloatDouble2BoolFn {
    boolean invoke(boolean p1, float p2, double p3);
}
