package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code float} and returns a {@code boolean}.
 */
public interface ByteByteFloat2BoolFn {
    boolean invoke(byte p1, byte p2, float p3);
}
