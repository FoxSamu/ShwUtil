package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code boolean} and a {@code boolean} and returns a {@code boolean}.
 */
public interface RefBoolBool2BoolFn<P1> {
    boolean invoke(P1 p1, boolean p2, boolean p3);
}
