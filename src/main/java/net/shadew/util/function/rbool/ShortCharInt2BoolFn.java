package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code char} and an {@code int} and returns a {@code boolean}.
 */
public interface ShortCharInt2BoolFn {
    boolean invoke(short p1, char p2, int p3);
}
