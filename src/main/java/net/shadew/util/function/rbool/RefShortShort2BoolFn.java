package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code short} and a {@code short} and returns a {@code boolean}.
 */
public interface RefShortShort2BoolFn<P1> {
    boolean invoke(P1 p1, short p2, short p3);
}
