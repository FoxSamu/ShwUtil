package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code double} and a {@code short} and returns a {@code boolean}.
 */
public interface RefDoubleShort2BoolFn<P1> {
    boolean invoke(P1 p1, double p2, short p3);
}
