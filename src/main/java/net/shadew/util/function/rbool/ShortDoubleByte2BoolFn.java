package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code byte} and returns a {@code boolean}.
 */
public interface ShortDoubleByte2BoolFn {
    boolean invoke(short p1, double p2, byte p3);
}
