package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns a {@code boolean}.
 */
public interface BoolShort2BoolFn {
    boolean invoke(boolean p1, short p2);
}
