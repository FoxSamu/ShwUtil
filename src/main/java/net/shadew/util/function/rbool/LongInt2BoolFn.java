package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long} and an {@code int} and returns a {@code boolean}.
 */
public interface LongInt2BoolFn {
    boolean invoke(long p1, int p2);
}
