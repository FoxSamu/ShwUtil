package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code float} and returns a {@code boolean}.
 */
public interface DoubleByteFloat2BoolFn {
    boolean invoke(double p1, byte p2, float p3);
}
