package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns a {@code boolean}.
 */
public interface FloatByteDouble2BoolFn {
    boolean invoke(float p1, byte p2, double p3);
}
