package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code float} and returns a {@code boolean}.
 */
public interface ByteBoolFloat2BoolFn {
    boolean invoke(byte p1, boolean p2, float p3);
}
