package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short} and a {@code char} and returns a {@code boolean}.
 */
public interface ShortChar2BoolFn {
    boolean invoke(short p1, char p2);
}
