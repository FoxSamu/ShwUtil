package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code char} and returns a {@code boolean}.
 */
public interface BoolDoubleChar2BoolFn {
    boolean invoke(boolean p1, double p2, char p3);
}
