package net.shadew.util.function.rbool;

/**
 * A function that takes an object, a {@code boolean} and a {@code float} and returns a {@code boolean}.
 */
public interface RefBoolFloat2BoolFn<P1> {
    boolean invoke(P1 p1, boolean p2, float p3);
}
