package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code float} and returns a {@code boolean}.
 */
public interface ByteDoubleFloat2BoolFn {
    boolean invoke(byte p1, double p2, float p3);
}
