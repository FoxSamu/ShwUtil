package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code byte} and returns a {@code boolean}.
 */
public interface FloatByteByte2BoolFn {
    boolean invoke(float p1, byte p2, byte p3);
}
