package net.shadew.util.function.rbool;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code byte} and returns a {@code boolean}.
 */
public interface IntByteByte2BoolFn {
    boolean invoke(int p1, byte p2, byte p3);
}
