package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code byte} and returns a {@code boolean}.
 */
public interface FloatIntByte2BoolFn {
    boolean invoke(float p1, int p2, byte p3);
}
