package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code double} and returns a {@code boolean}.
 */
public interface ShortShortDouble2BoolFn {
    boolean invoke(short p1, short p2, double p3);
}
