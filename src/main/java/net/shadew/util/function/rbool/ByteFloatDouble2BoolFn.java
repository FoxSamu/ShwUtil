package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code double} and returns a {@code boolean}.
 */
public interface ByteFloatDouble2BoolFn {
    boolean invoke(byte p1, float p2, double p3);
}
