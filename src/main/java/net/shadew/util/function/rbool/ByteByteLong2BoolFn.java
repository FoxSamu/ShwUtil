package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code long} and returns a {@code boolean}.
 */
public interface ByteByteLong2BoolFn {
    boolean invoke(byte p1, byte p2, long p3);
}
