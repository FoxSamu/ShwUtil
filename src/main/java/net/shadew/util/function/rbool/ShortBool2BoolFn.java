package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short} and a {@code boolean} and returns a {@code boolean}.
 */
public interface ShortBool2BoolFn {
    boolean invoke(short p1, boolean p2);
}
