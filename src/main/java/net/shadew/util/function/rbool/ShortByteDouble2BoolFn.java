package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code double} and returns a {@code boolean}.
 */
public interface ShortByteDouble2BoolFn {
    boolean invoke(short p1, byte p2, double p3);
}
