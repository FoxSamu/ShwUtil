package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code float} and returns a {@code boolean}.
 */
public interface CharByteFloat2BoolFn {
    boolean invoke(char p1, byte p2, float p3);
}
