package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code char} and returns a {@code boolean}.
 */
public interface LongFloatChar2BoolFn {
    boolean invoke(long p1, float p2, char p3);
}
