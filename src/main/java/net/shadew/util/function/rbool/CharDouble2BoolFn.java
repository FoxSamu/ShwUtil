package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code char} and a {@code double} and returns a {@code boolean}.
 */
public interface CharDouble2BoolFn {
    boolean invoke(char p1, double p2);
}
