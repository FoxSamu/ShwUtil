package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code double} and an {@code int} and returns a {@code boolean}.
 */
public interface DoubleInt2BoolFn {
    boolean invoke(double p1, int p2);
}
