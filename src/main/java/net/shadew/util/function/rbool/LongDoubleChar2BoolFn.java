package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code char} and returns a {@code boolean}.
 */
public interface LongDoubleChar2BoolFn {
    boolean invoke(long p1, double p2, char p3);
}
