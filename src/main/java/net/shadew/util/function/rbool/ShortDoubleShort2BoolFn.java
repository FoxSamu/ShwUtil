package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code short} and returns a {@code boolean}.
 */
public interface ShortDoubleShort2BoolFn {
    boolean invoke(short p1, double p2, short p3);
}
