package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code float} and an {@code int} and returns a {@code boolean}.
 */
public interface ByteFloatInt2BoolFn {
    boolean invoke(byte p1, float p2, int p3);
}
