package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short} and a {@code float} and returns a {@code boolean}.
 */
public interface ShortFloat2BoolFn {
    boolean invoke(short p1, float p2);
}
