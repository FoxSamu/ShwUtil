package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code char} and returns a {@code boolean}.
 */
public interface FloatBoolChar2BoolFn {
    boolean invoke(float p1, boolean p2, char p3);
}
