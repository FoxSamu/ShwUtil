package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns a {@code boolean}.
 */
public interface ShortCharByte2BoolFn {
    boolean invoke(short p1, char p2, byte p3);
}
