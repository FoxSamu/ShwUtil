package net.shadew.util.function.rbool;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code byte} and returns a {@code boolean}.
 */
public interface ByteDoubleByte2BoolFn {
    boolean invoke(byte p1, double p2, byte p3);
}
