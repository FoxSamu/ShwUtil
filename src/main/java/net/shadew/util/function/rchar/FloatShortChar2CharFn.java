package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code char} and returns a {@code char}.
 */
public interface FloatShortChar2CharFn {
    char invoke(float p1, short p2, char p3);
}
