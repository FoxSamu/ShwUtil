package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code boolean} and returns a {@code char}.
 */
public interface ByteDoubleBool2CharFn {
    char invoke(byte p1, double p2, boolean p3);
}
