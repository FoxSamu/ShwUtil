package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code byte} and an {@code int} and returns a {@code char}.
 */
public interface ShortByteInt2CharFn {
    char invoke(short p1, byte p2, int p3);
}
