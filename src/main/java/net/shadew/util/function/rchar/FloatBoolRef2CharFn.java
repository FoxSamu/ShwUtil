package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code boolean} and an object and returns a {@code char}.
 */
public interface FloatBoolRef2CharFn<P3> {
    char invoke(float p1, boolean p2, P3 p3);
}
