package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code double} and returns a {@code char}.
 */
public interface DoubleByteDouble2CharFn {
    char invoke(double p1, byte p2, double p3);
}
