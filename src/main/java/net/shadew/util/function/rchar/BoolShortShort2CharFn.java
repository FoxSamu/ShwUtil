package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code short} and returns a {@code char}.
 */
public interface BoolShortShort2CharFn {
    char invoke(Bool p1, Short p2, Short p3);
}
