package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code boolean} and returns a {@code char}.
 */
public interface IntDoubleBool2CharFn {
    char invoke(int p1, double p2, boolean p3);
}
