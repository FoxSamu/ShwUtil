package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code char} and returns a {@code char}.
 */
public interface IntBoolChar2CharFn {
    char invoke(int p1, boolean p2, char p3);
}
