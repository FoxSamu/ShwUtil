package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code byte} and returns a {@code char}.
 */
public interface ByteBoolByte2CharFn {
    char invoke(byte p1, boolean p2, byte p3);
}
