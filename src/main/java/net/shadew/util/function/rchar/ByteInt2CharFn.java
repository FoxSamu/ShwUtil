package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte} and an {@code int} and returns a {@code char}.
 */
public interface ByteInt2CharFn {
    char invoke(byte p1, int p2);
}
