package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code char} and returns a {@code char}.
 */
public interface LongBoolChar2CharFn {
    char invoke(long p1, boolean p2, char p3);
}
