package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code float} and returns a {@code char}.
 */
public interface FloatDoubleFloat2CharFn {
    char invoke(float p1, double p2, float p3);
}
