package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code short} and returns a {@code char}.
 */
public interface ByteDoubleShort2CharFn {
    char invoke(byte p1, double p2, short p3);
}
