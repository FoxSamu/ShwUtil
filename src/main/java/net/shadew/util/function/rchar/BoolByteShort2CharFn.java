package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code short} and returns a {@code char}.
 */
public interface BoolByteShort2CharFn {
    char invoke(boolean p1, byte p2, short p3);
}
