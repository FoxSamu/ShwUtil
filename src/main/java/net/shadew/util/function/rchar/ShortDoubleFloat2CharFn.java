package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code float} and returns a {@code char}.
 */
public interface ShortDoubleFloat2CharFn {
    char invoke(short p1, double p2, float p3);
}
