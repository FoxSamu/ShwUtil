package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, a {@code short} and a {@code char} and returns a {@code char}.
 */
public interface CharShortChar2CharFn {
    char invoke(char p1, short p2, char p3);
}
