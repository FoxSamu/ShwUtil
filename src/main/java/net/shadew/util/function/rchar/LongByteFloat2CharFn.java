package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code float} and returns a {@code char}.
 */
public interface LongByteFloat2CharFn {
    char invoke(long p1, byte p2, float p3);
}
