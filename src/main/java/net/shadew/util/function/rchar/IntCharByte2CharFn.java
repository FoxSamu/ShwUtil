package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code byte} and returns a {@code char}.
 */
public interface IntCharByte2CharFn {
    char invoke(int p1, char p2, byte p3);
}
