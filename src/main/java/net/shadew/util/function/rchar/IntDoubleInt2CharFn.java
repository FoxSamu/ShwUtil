package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code double} and an {@code int} and returns a {@code char}.
 */
public interface IntDoubleInt2CharFn {
    char invoke(int p1, double p2, int p3);
}
