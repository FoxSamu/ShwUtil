package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code boolean} and an {@code int} and returns a {@code char}.
 */
public interface LongBoolInt2CharFn {
    char invoke(long p1, boolean p2, int p3);
}
