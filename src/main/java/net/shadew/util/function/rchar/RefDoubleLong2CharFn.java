package net.shadew.util.function.rchar;

/**
 * A function that takes an object, a {@code double} and a {@code long} and returns a {@code char}.
 */
public interface RefDoubleLong2CharFn<P1> {
    char invoke(P1 p1, double p2, long p3);
}
