package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface FloatByteChar2CharFn {
    char invoke(float p1, byte p2, char p3);
}
