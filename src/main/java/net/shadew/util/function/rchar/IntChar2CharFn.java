package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int} and a {@code char} and returns a {@code char}.
 */
public interface IntChar2CharFn {
    char invoke(int p1, char p2);
}
