package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code double} and an {@code int} and returns a {@code char}.
 */
public interface FloatDoubleInt2CharFn {
    char invoke(float p1, double p2, int p3);
}
