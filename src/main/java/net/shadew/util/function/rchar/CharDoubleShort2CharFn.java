package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code short} and returns a {@code char}.
 */
public interface CharDoubleShort2CharFn {
    char invoke(char p1, double p2, short p3);
}
