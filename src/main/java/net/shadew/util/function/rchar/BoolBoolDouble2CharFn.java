package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code double} and returns a {@code char}.
 */
public interface BoolBoolDouble2CharFn {
    char invoke(Bool p1, Bool p2, Double p3);
}
