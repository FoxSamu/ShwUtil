package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char} and a {@code float} and returns a {@code char}.
 */
public interface CharFloat2CharFn {
    char invoke(char p1, float p2);
}
