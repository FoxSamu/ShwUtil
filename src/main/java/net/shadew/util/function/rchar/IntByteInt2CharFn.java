package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code byte} and an {@code int} and returns a {@code char}.
 */
public interface IntByteInt2CharFn {
    char invoke(int p1, byte p2, int p3);
}
