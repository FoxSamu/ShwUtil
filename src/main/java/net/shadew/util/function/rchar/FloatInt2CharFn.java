package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float} and an {@code int} and returns a {@code char}.
 */
public interface FloatInt2CharFn {
    char invoke(float p1, int p2);
}
