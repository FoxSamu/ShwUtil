package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code char} and returns a {@code char}.
 */
public interface BoolShortChar2CharFn {
    char invoke(boolean p1, short p2, char p3);
}
