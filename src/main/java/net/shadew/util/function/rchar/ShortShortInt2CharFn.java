package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code short} and an {@code int} and returns a {@code char}.
 */
public interface ShortShortInt2CharFn {
    char invoke(short p1, short p2, int p3);
}
