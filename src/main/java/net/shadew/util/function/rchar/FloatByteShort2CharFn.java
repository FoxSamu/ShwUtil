package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code short} and returns a {@code char}.
 */
public interface FloatByteShort2CharFn {
    char invoke(float p1, byte p2, short p3);
}
