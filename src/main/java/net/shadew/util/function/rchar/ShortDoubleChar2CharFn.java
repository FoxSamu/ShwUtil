package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code char} and returns a {@code char}.
 */
public interface ShortDoubleChar2CharFn {
    char invoke(short p1, double p2, char p3);
}
