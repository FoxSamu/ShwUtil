package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float} and a {@code byte} and returns a {@code char}.
 */
public interface FloatByte2CharFn {
    char invoke(float p1, byte p2);
}
