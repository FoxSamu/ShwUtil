package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code char} and returns a {@code char}.
 */
public interface ByteBoolChar2CharFn {
    char invoke(byte p1, boolean p2, char p3);
}
