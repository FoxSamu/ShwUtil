package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code char} and returns a {@code char}.
 */
public interface ShortBoolChar2CharFn {
    char invoke(short p1, boolean p2, char p3);
}
