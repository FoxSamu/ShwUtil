package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code boolean} and returns a {@code char}.
 */
public interface ShortDoubleBool2CharFn {
    char invoke(short p1, double p2, boolean p3);
}
