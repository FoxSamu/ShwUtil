package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code double}, a {@code boolean} and a {@code float} and returns a {@code char}.
 */
public interface DoubleBoolFloat2CharFn {
    char invoke(double p1, boolean p2, float p3);
}
