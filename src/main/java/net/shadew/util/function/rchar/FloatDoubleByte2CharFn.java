package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code byte} and returns a {@code char}.
 */
public interface FloatDoubleByte2CharFn {
    char invoke(float p1, double p2, byte p3);
}
