package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code char} and returns a {@code char}.
 */
public interface CharDoubleChar2CharFn {
    char invoke(char p1, double p2, char p3);
}
