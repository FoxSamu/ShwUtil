package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code float} and a {@code double} and returns a {@code char}.
 */
public interface ByteFloatDouble2CharFn {
    char invoke(byte p1, float p2, double p3);
}
