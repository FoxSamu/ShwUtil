package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code short} and an object and returns a {@code char}.
 */
public interface FloatShortRef2CharFn<P3> {
    char invoke(float p1, short p2, P3 p3);
}
