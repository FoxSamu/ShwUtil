package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code double} and an {@code int} and returns a {@code char}.
 */
public interface DoubleInt2CharFn {
    char invoke(double p1, int p2);
}
