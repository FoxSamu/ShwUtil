package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long} and a {@code byte} and returns a {@code char}.
 */
public interface LongByte2CharFn {
    char invoke(long p1, byte p2);
}
