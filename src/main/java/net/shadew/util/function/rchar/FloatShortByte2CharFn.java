package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code byte} and returns a {@code char}.
 */
public interface FloatShortByte2CharFn {
    char invoke(float p1, short p2, byte p3);
}
