package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char} and a {@code short} and returns a {@code char}.
 */
public interface CharShort2CharFn {
    char invoke(char p1, short p2);
}
