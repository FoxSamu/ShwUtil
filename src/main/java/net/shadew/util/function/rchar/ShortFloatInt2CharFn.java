package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code float} and an {@code int} and returns a {@code char}.
 */
public interface ShortFloatInt2CharFn {
    char invoke(short p1, float p2, int p3);
}
