package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code boolean} and returns a {@code char}.
 */
public interface FloatDoubleBool2CharFn {
    char invoke(float p1, double p2, boolean p3);
}
