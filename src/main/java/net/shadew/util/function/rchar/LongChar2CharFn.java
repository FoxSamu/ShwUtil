package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long} and a {@code char} and returns a {@code char}.
 */
public interface LongChar2CharFn {
    char invoke(long p1, char p2);
}
