package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code float} and a {@code byte} and returns a {@code char}.
 */
public interface BoolFloatByte2CharFn {
    char invoke(boolean p1, float p2, byte p3);
}
