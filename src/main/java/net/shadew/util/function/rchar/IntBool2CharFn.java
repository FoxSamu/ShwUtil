package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int} and a {@code boolean} and returns a {@code char}.
 */
public interface IntBool2CharFn {
    char invoke(int p1, boolean p2);
}
