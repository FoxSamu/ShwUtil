package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, an {@code int} and a {@code byte} and returns a {@code char}.
 */
public interface BoolIntByte2CharFn {
    char invoke(boolean p1, int p2, byte p3);
}
