package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long} and an {@code int} and returns a {@code char}.
 */
public interface LongInt2CharFn {
    char invoke(long p1, int p2);
}
