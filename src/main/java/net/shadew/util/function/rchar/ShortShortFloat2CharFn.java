package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code float} and returns a {@code char}.
 */
public interface ShortShortFloat2CharFn {
    char invoke(short p1, short p2, float p3);
}
