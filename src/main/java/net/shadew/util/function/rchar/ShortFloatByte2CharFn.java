package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code byte} and returns a {@code char}.
 */
public interface ShortFloatByte2CharFn {
    char invoke(short p1, float p2, byte p3);
}
