package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float} and a {@code double} and returns a {@code char}.
 */
public interface FloatDouble2CharFn {
    char invoke(float p1, double p2);
}
