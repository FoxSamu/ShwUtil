package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code short} and an {@code int} and returns a {@code char}.
 */
public interface LongShortInt2CharFn {
    char invoke(long p1, short p2, int p3);
}
