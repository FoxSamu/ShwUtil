package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code byte} and returns a {@code char}.
 */
public interface LongDoubleByte2CharFn {
    char invoke(long p1, double p2, byte p3);
}
