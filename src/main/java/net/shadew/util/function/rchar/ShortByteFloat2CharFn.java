package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code float} and returns a {@code char}.
 */
public interface ShortByteFloat2CharFn {
    char invoke(short p1, byte p2, float p3);
}
