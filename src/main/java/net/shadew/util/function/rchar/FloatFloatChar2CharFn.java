package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code char} and returns a {@code char}.
 */
public interface FloatFloatChar2CharFn {
    char invoke(float p1, float p2, char p3);
}
