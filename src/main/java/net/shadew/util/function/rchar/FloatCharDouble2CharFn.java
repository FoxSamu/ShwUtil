package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code double} and returns a {@code char}.
 */
public interface FloatCharDouble2CharFn {
    char invoke(float p1, char p2, double p3);
}
