package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code char} and returns a {@code char}.
 */
public interface LongShortChar2CharFn {
    char invoke(long p1, short p2, char p3);
}
