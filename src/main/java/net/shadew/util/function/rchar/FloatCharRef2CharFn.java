package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code char} and an object and returns a {@code char}.
 */
public interface FloatCharRef2CharFn<P3> {
    char invoke(float p1, char p2, P3 p3);
}
