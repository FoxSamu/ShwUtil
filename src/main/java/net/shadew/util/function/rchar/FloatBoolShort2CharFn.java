package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code short} and returns a {@code char}.
 */
public interface FloatBoolShort2CharFn {
    char invoke(float p1, boolean p2, short p3);
}
