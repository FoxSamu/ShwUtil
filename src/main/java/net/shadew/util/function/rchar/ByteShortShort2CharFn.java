package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code short} and returns a {@code char}.
 */
public interface ByteShortShort2CharFn {
    char invoke(byte p1, short p2, short p3);
}
