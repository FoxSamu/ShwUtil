package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code byte} and an {@code int} and returns a {@code char}.
 */
public interface FloatByteInt2CharFn {
    char invoke(float p1, byte p2, int p3);
}
