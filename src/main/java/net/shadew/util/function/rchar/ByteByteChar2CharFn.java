package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface ByteByteChar2CharFn {
    char invoke(byte p1, byte p2, char p3);
}
