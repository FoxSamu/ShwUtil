package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code double} and returns a {@code char}.
 */
public interface FloatDoubleDouble2CharFn {
    char invoke(float p1, double p2, double p3);
}
