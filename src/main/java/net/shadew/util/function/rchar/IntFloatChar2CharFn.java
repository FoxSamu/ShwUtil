package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code float} and a {@code char} and returns a {@code char}.
 */
public interface IntFloatChar2CharFn {
    char invoke(int p1, float p2, char p3);
}
