package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code boolean} and returns a {@code char}.
 */
public interface LongByteBool2CharFn {
    char invoke(long p1, byte p2, boolean p3);
}
