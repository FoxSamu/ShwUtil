package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code boolean} and an {@code int} and returns a {@code char}.
 */
public interface FloatBoolInt2CharFn {
    char invoke(float p1, boolean p2, int p3);
}
