package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean} and a {@code boolean} and returns a {@code char}.
 */
public interface BoolBool2CharFn {
    char invoke(boolean p1, boolean p2);
}
