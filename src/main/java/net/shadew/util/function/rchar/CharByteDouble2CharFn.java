package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code double} and returns a {@code char}.
 */
public interface CharByteDouble2CharFn {
    char invoke(char p1, byte p2, double p3);
}
