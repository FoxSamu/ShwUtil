package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code byte} and returns a {@code char}.
 */
public interface ShortShortByte2CharFn {
    char invoke(short p1, short p2, byte p3);
}
