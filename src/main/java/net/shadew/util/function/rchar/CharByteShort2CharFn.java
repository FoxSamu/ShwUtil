package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code short} and returns a {@code char}.
 */
public interface CharByteShort2CharFn {
    char invoke(char p1, byte p2, short p3);
}
