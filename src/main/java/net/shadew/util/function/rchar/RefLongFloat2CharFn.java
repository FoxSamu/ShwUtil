package net.shadew.util.function.rchar;

/**
 * A function that takes an object, a {@code long} and a {@code float} and returns a {@code char}.
 */
public interface RefLongFloat2CharFn<P1> {
    char invoke(P1 p1, long p2, float p3);
}
