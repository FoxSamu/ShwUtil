package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int} and a {@code byte} and returns a {@code char}.
 */
public interface IntByte2CharFn {
    char invoke(int p1, byte p2);
}
