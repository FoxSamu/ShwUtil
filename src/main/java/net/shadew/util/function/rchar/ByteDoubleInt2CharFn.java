package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code double} and an {@code int} and returns a {@code char}.
 */
public interface ByteDoubleInt2CharFn {
    char invoke(byte p1, double p2, int p3);
}
