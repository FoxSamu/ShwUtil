package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code short} and returns a {@code char}.
 */
public interface ShortCharShort2CharFn {
    char invoke(short p1, char p2, short p3);
}
