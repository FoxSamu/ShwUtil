package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code double} and returns a {@code char}.
 */
public interface ByteBoolDouble2CharFn {
    char invoke(byte p1, boolean p2, double p3);
}
