package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code float} and returns a {@code char}.
 */
public interface ByteDoubleFloat2CharFn {
    char invoke(byte p1, double p2, float p3);
}
