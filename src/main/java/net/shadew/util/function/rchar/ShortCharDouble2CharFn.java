package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code double} and returns a {@code char}.
 */
public interface ShortCharDouble2CharFn {
    char invoke(short p1, char p2, double p3);
}
