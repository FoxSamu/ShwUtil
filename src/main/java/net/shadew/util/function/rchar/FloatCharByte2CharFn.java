package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code byte} and returns a {@code char}.
 */
public interface FloatCharByte2CharFn {
    char invoke(float p1, char p2, byte p3);
}
