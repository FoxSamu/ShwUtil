package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code boolean} and returns a {@code char}.
 */
public interface FloatByteBool2CharFn {
    char invoke(Float p1, Byte p2, Bool p3);
}
