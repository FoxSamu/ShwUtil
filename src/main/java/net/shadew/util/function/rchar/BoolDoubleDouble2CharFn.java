package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code double} and returns a {@code char}.
 */
public interface BoolDoubleDouble2CharFn {
    char invoke(boolean p1, double p2, double p3);
}
