package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code double} and returns a {@code char}.
 */
public interface ShortDoubleDouble2CharFn {
    char invoke(short p1, double p2, double p3);
}
