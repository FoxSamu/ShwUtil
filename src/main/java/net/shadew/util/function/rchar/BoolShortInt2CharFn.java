package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code short} and an {@code int} and returns a {@code char}.
 */
public interface BoolShortInt2CharFn {
    char invoke(boolean p1, short p2, int p3);
}
