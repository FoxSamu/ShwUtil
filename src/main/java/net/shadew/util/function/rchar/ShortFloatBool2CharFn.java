package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code boolean} and returns a {@code char}.
 */
public interface ShortFloatBool2CharFn {
    char invoke(short p1, float p2, boolean p3);
}
