package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface BoolByteChar2CharFn {
    char invoke(boolean p1, byte p2, char p3);
}
