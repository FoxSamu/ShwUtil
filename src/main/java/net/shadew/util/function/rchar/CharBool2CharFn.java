package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char} and a {@code boolean} and returns a {@code char}.
 */
public interface CharBool2CharFn {
    char invoke(char p1, boolean p2);
}
