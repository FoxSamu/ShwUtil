package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code char} and returns a {@code char}.
 */
public interface ByteShortChar2CharFn {
    char invoke(byte p1, short p2, char p3);
}
