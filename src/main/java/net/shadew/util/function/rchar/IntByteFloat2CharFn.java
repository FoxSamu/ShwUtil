package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code float} and returns a {@code char}.
 */
public interface IntByteFloat2CharFn {
    char invoke(int p1, byte p2, float p3);
}
