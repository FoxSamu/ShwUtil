package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code boolean} and returns a {@code char}.
 */
public interface ByteByteBool2CharFn {
    char invoke(byte p1, byte p2, boolean p3);
}
