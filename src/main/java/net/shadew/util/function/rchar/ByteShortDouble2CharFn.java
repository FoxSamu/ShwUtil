package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code short} and a {@code double} and returns a {@code char}.
 */
public interface ByteShortDouble2CharFn {
    char invoke(byte p1, short p2, double p3);
}
