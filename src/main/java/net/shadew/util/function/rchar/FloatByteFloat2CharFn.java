package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code float} and returns a {@code char}.
 */
public interface FloatByteFloat2CharFn {
    char invoke(float p1, byte p2, float p3);
}
