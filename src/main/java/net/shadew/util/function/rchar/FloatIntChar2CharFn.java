package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code char} and returns a {@code char}.
 */
public interface FloatIntChar2CharFn {
    char invoke(float p1, int p2, char p3);
}
