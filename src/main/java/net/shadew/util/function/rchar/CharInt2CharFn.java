package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char} and an {@code int} and returns a {@code char}.
 */
public interface CharInt2CharFn {
    char invoke(char p1, int p2);
}
