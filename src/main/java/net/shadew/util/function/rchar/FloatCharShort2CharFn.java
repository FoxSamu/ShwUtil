package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code short} and returns a {@code char}.
 */
public interface FloatCharShort2CharFn {
    char invoke(Float p1, Char p2, Short p3);
}
