package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code float} and returns a {@code char}.
 */
public interface FloatBoolFloat2CharFn {
    char invoke(float p1, boolean p2, float p3);
}
