package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short} and a {@code double} and returns a {@code char}.
 */
public interface ShortDouble2CharFn {
    char invoke(short p1, double p2);
}
