package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code char} and an {@code int} and returns a {@code char}.
 */
public interface FloatCharInt2CharFn {
    char invoke(float p1, char p2, int p3);
}
