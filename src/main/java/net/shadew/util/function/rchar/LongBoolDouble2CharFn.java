package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code double} and returns a {@code char}.
 */
public interface LongBoolDouble2CharFn {
    char invoke(long p1, boolean p2, double p3);
}
