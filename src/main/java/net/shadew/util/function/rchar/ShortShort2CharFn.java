package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short} and a {@code short} and returns a {@code char}.
 */
public interface ShortShort2CharFn {
    char invoke(Short p1, Short p2);
}
