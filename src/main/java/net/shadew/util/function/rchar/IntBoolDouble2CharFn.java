package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code double} and returns a {@code char}.
 */
public interface IntBoolDouble2CharFn {
    char invoke(int p1, boolean p2, double p3);
}
