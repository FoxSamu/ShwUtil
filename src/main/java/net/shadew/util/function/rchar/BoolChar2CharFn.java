package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean} and a {@code char} and returns a {@code char}.
 */
public interface BoolChar2CharFn {
    char invoke(boolean p1, char p2);
}
