package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code float} and returns a {@code char}.
 */
public interface BoolShortFloat2CharFn {
    char invoke(boolean p1, short p2, float p3);
}
