package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code char} and returns a {@code char}.
 */
public interface LongDoubleChar2CharFn {
    char invoke(long p1, double p2, char p3);
}
