package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code double}, a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface DoubleByteChar2CharFn {
    char invoke(double p1, byte p2, char p3);
}
