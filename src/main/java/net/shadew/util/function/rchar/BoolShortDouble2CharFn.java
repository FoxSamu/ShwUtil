package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code double} and returns a {@code char}.
 */
public interface BoolShortDouble2CharFn {
    char invoke(boolean p1, short p2, double p3);
}
