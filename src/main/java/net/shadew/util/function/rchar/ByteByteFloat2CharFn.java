package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code float} and returns a {@code char}.
 */
public interface ByteByteFloat2CharFn {
    char invoke(byte p1, byte p2, float p3);
}
