package net.shadew.util.function.rchar;

/**
 * A function that takes an object, a {@code double} and a {@code byte} and returns a {@code char}.
 */
public interface RefDoubleByte2CharFn<P1> {
    char invoke(P1 p1, double p2, byte p3);
}
