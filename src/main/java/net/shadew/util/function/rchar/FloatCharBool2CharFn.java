package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code char} and a {@code boolean} and returns a {@code char}.
 */
public interface FloatCharBool2CharFn {
    char invoke(float p1, char p2, boolean p3);
}
