package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface IntByteChar2CharFn {
    char invoke(int p1, byte p2, char p3);
}
