package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code char} and returns a {@code char}.
 */
public interface FloatDoubleChar2CharFn {
    char invoke(float p1, double p2, char p3);
}
