package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, an {@code int} and a {@code char} and returns a {@code char}.
 */
public interface LongIntChar2CharFn {
    char invoke(long p1, int p2, char p3);
}
