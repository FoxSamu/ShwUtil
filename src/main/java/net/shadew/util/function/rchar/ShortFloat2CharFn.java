package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short} and a {@code float} and returns a {@code char}.
 */
public interface ShortFloat2CharFn {
    char invoke(short p1, float p2);
}
