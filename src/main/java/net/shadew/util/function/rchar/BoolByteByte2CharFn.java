package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code byte} and returns a {@code char}.
 */
public interface BoolByteByte2CharFn {
    char invoke(boolean p1, byte p2, byte p3);
}
