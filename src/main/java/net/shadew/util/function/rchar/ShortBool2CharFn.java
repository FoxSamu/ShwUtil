package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short} and a {@code boolean} and returns a {@code char}.
 */
public interface ShortBool2CharFn {
    char invoke(short p1, boolean p2);
}
