package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code short} and returns a {@code char}.
 */
public interface IntByteShort2CharFn {
    char invoke(int p1, byte p2, short p3);
}
