package net.shadew.util.function.rchar;

/**
 * A function that takes an object, an {@code int} and an {@code int} and returns a {@code char}.
 */
public interface RefIntInt2CharFn<P1> {
    char invoke(P1 p1, int p2, int p3);
}
