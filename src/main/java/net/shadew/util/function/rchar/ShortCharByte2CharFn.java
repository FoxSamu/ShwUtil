package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code byte} and returns a {@code char}.
 */
public interface ShortCharByte2CharFn {
    char invoke(short p1, char p2, byte p3);
}
