package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code byte} and a {@code double} and returns a {@code char}.
 */
public interface ByteByteDouble2CharFn {
    char invoke(byte p1, byte p2, double p3);
}
