package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code char} and a {@code float} and returns a {@code char}.
 */
public interface ShortCharFloat2CharFn {
    char invoke(short p1, char p2, float p3);
}
