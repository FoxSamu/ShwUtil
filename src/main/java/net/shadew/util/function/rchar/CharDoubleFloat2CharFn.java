package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, a {@code double} and a {@code float} and returns a {@code char}.
 */
public interface CharDoubleFloat2CharFn {
    char invoke(char p1, double p2, float p3);
}
