package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code double} and returns a {@code char}.
 */
public interface ShortIntDouble2CharFn {
    char invoke(short p1, int p2, double p3);
}
