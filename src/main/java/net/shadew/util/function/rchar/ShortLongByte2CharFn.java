package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code long} and a {@code byte} and returns a {@code char}.
 */
public interface ShortLongByte2CharFn {
    char invoke(short p1, long p2, byte p3);
}
