package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code byte} and returns a {@code char}.
 */
public interface CharByteByte2CharFn {
    char invoke(char p1, byte p2, byte p3);
}
