package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code boolean} and a {@code byte} and returns a {@code char}.
 */
public interface ShortBoolByte2CharFn {
    char invoke(short p1, boolean p2, byte p3);
}
