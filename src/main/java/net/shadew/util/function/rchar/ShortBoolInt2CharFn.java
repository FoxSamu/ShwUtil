package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code boolean} and an {@code int} and returns a {@code char}.
 */
public interface ShortBoolInt2CharFn {
    char invoke(short p1, boolean p2, int p3);
}
