package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code boolean} and returns a {@code char}.
 */
public interface BoolByteBool2CharFn {
    char invoke(boolean p1, byte p2, boolean p3);
}
