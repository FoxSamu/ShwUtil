package net.shadew.util.function.rchar;

/**
 * A function that takes an object, a {@code double} and a {@code short} and returns a {@code char}.
 */
public interface RefDoubleShort2CharFn<P1> {
    char invoke(P1 p1, double p2, short p3);
}
