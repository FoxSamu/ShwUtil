package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte} and a {@code float} and returns a {@code char}.
 */
public interface ByteFloat2CharFn {
    char invoke(byte p1, float p2);
}
