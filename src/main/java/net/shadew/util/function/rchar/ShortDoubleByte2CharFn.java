package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code byte} and returns a {@code char}.
 */
public interface ShortDoubleByte2CharFn {
    char invoke(short p1, double p2, byte p3);
}
