package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code byte} and an {@code int} and returns a {@code char}.
 */
public interface BoolByteInt2CharFn {
    char invoke(boolean p1, byte p2, int p3);
}
