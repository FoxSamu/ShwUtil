package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code double} and returns a {@code char}.
 */
public interface FloatByteDouble2CharFn {
    char invoke(float p1, byte p2, double p3);
}
