package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code float} and returns a {@code char}.
 */
public interface BoolDoubleFloat2CharFn {
    char invoke(boolean p1, double p2, float p3);
}
