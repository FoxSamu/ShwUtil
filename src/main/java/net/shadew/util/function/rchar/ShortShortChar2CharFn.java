package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code char} and returns a {@code char}.
 */
public interface ShortShortChar2CharFn {
    char invoke(short p1, short p2, char p3);
}
