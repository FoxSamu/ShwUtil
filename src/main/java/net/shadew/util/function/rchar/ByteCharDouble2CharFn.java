package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code char} and a {@code double} and returns a {@code char}.
 */
public interface ByteCharDouble2CharFn {
    char invoke(byte p1, char p2, double p3);
}
