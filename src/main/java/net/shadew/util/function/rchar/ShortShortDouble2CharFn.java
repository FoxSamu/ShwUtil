package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code short} and a {@code double} and returns a {@code char}.
 */
public interface ShortShortDouble2CharFn {
    char invoke(short p1, short p2, double p3);
}
