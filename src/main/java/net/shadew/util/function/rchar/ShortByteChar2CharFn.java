package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface ShortByteChar2CharFn {
    char invoke(short p1, byte p2, char p3);
}
