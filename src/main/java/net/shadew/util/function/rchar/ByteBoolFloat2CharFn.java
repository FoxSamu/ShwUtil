package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code float} and returns a {@code char}.
 */
public interface ByteBoolFloat2CharFn {
    char invoke(byte p1, boolean p2, float p3);
}
