package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code byte} and returns a {@code char}.
 */
public interface ShortByteByte2CharFn {
    char invoke(short p1, byte p2, byte p3);
}
