package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code boolean} and an {@code int} and returns a {@code char}.
 */
public interface ByteBoolInt2CharFn {
    char invoke(byte p1, boolean p2, int p3);
}
