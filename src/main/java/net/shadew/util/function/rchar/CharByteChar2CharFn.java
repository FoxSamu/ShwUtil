package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface CharByteChar2CharFn {
    char invoke(char p1, byte p2, char p3);
}
