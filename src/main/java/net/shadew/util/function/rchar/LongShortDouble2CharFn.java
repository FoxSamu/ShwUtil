package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code short} and a {@code double} and returns a {@code char}.
 */
public interface LongShortDouble2CharFn {
    char invoke(long p1, short p2, double p3);
}
