package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code double} and a {@code char} and returns a {@code char}.
 */
public interface DoubleChar2CharFn {
    char invoke(double p1, char p2);
}
