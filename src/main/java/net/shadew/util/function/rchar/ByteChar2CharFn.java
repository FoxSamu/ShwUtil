package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface ByteChar2CharFn {
    char invoke(byte p1, char p2);
}
