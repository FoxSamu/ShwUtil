package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean} and an {@code int} and returns a {@code char}.
 */
public interface BoolInt2CharFn {
    char invoke(boolean p1, int p2);
}
