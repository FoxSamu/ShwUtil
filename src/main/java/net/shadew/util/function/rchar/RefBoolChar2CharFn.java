package net.shadew.util.function.rchar;

/**
 * A function that takes an object, a {@code boolean} and a {@code char} and returns a {@code char}.
 */
public interface RefBoolChar2CharFn<P1> {
    char invoke(P1 p1, boolean p2, char p3);
}
