package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code long} and a {@code char} and returns a {@code char}.
 */
public interface FloatLongChar2CharFn {
    char invoke(float p1, long p2, char p3);
}
