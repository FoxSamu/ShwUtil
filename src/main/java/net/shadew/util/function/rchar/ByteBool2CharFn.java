package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte} and a {@code boolean} and returns a {@code char}.
 */
public interface ByteBool2CharFn {
    char invoke(byte p1, boolean p2);
}
