package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code char} and a {@code double} and returns a {@code char}.
 */
public interface LongCharDouble2CharFn {
    char invoke(long p1, char p2, double p3);
}
