package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code float} and a {@code char} and returns a {@code char}.
 */
public interface LongFloatChar2CharFn {
    char invoke(long p1, float p2, char p3);
}
