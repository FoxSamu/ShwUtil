package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean} and a {@code float} and returns a {@code char}.
 */
public interface BoolFloat2CharFn {
    char invoke(boolean p1, float p2);
}
