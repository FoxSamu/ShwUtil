package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code double} and returns a {@code char}.
 */
public interface IntCharDouble2CharFn {
    char invoke(int p1, char p2, double p3);
}
