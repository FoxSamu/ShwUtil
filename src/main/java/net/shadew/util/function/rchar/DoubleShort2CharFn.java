package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code double} and a {@code short} and returns a {@code char}.
 */
public interface DoubleShort2CharFn {
    char invoke(double p1, short p2);
}
