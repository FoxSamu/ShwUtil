package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code char} and returns a {@code char}.
 */
public interface IntDoubleChar2CharFn {
    char invoke(int p1, double p2, char p3);
}
