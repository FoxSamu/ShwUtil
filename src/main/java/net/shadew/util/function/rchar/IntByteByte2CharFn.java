package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code byte} and a {@code byte} and returns a {@code char}.
 */
public interface IntByteByte2CharFn {
    char invoke(int p1, byte p2, byte p3);
}
