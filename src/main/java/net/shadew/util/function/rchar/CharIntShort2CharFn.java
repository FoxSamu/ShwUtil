package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char}, an {@code int} and a {@code short} and returns a {@code char}.
 */
public interface CharIntShort2CharFn {
    char invoke(Char p1, Int p2, Short p3);
}
