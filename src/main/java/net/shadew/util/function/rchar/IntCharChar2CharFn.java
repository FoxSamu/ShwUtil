package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code char} and a {@code char} and returns a {@code char}.
 */
public interface IntCharChar2CharFn {
    char invoke(int p1, char p2, char p3);
}
