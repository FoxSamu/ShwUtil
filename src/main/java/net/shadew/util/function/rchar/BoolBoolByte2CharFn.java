package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code byte} and returns a {@code char}.
 */
public interface BoolBoolByte2CharFn {
    char invoke(boolean p1, boolean p2, byte p3);
}
