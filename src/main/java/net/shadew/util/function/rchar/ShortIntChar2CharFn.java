package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, an {@code int} and a {@code char} and returns a {@code char}.
 */
public interface ShortIntChar2CharFn {
    char invoke(short p1, int p2, char p3);
}
