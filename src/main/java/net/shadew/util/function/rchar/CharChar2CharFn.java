package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char} and a {@code char} and returns a {@code char}.
 */
public interface CharChar2CharFn {
    char invoke(char p1, char p2);
}
