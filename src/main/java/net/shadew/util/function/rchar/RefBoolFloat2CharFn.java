package net.shadew.util.function.rchar;

/**
 * A function that takes an object, a {@code boolean} and a {@code float} and returns a {@code char}.
 */
public interface RefBoolFloat2CharFn<P1> {
    char invoke(P1 p1, boolean p2, float p3);
}
