package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code double} and an {@code int} and returns a {@code char}.
 */
public interface ShortDoubleInt2CharFn {
    char invoke(short p1, double p2, int p3);
}
