package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short} and a {@code char} and returns a {@code char}.
 */
public interface ShortChar2CharFn {
    char invoke(short p1, char p2);
}
