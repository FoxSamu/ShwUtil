package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code char} and an {@code int} and returns a {@code char}.
 */
public interface BoolCharInt2CharFn {
    char invoke(boolean p1, char p2, int p3);
}
