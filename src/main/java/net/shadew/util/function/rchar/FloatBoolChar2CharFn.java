package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code char} and returns a {@code char}.
 */
public interface FloatBoolChar2CharFn {
    char invoke(float p1, boolean p2, char p3);
}
