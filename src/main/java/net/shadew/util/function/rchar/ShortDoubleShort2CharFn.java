package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code double} and a {@code short} and returns a {@code char}.
 */
public interface ShortDoubleShort2CharFn {
    char invoke(short p1, double p2, short p3);
}
