package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code float} and a {@code char} and returns a {@code char}.
 */
public interface ShortFloatChar2CharFn {
    char invoke(short p1, float p2, char p3);
}
