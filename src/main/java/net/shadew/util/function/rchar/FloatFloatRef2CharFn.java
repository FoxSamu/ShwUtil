package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code float} and an object and returns a {@code char}.
 */
public interface FloatFloatRef2CharFn<P3> {
    char invoke(float p1, float p2, P3 p3);
}
