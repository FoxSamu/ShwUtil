package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short} and an {@code int} and returns a {@code char}.
 */
public interface ShortInt2CharFn {
    char invoke(short p1, int p2);
}
