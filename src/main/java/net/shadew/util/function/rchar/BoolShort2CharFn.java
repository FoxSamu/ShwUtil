package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean} and a {@code short} and returns a {@code char}.
 */
public interface BoolShort2CharFn {
    char invoke(boolean p1, short p2);
}
