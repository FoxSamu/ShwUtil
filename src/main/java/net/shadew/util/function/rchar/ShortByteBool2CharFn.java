package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code byte} and a {@code boolean} and returns a {@code char}.
 */
public interface ShortByteBool2CharFn {
    char invoke(short p1, byte p2, boolean p3);
}
