package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code double} and an {@code int} and returns a {@code char}.
 */
public interface BoolDoubleInt2CharFn {
    char invoke(boolean p1, double p2, int p3);
}
