package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code byte} and returns a {@code char}.
 */
public interface FloatByteByte2CharFn {
    char invoke(float p1, byte p2, byte p3);
}
