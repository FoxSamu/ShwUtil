package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code double}, a {@code short} and a {@code char} and returns a {@code char}.
 */
public interface DoubleShortChar2CharFn {
    char invoke(double p1, short p2, char p3);
}
