package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean} and a {@code double} and returns a {@code char}.
 */
public interface BoolDouble2CharFn {
    char invoke(boolean p1, double p2);
}
