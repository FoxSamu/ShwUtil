package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code char} and a {@code byte} and returns a {@code char}.
 */
public interface CharByte2CharFn {
    char invoke(char p1, byte p2);
}
