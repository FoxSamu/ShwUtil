package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code byte} and returns a {@code char}.
 */
public interface FloatBoolByte2CharFn {
    char invoke(float p1, boolean p2, byte p3);
}
