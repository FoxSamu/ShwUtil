package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code byte} and a {@code long} and returns a {@code char}.
 */
public interface FloatByteLong2CharFn {
    char invoke(float p1, byte p2, long p3);
}
