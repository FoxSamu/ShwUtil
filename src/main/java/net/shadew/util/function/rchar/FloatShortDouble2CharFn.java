package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code double} and returns a {@code char}.
 */
public interface FloatShortDouble2CharFn {
    char invoke(float p1, short p2, double p3);
}
