package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code double} and a {@code float} and returns a {@code char}.
 */
public interface LongDoubleFloat2CharFn {
    char invoke(long p1, double p2, float p3);
}
