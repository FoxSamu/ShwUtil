package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code byte} and a {@code float} and returns a {@code char}.
 */
public interface BoolByteFloat2CharFn {
    char invoke(boolean p1, byte p2, float p3);
}
