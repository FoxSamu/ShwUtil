package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code short} and returns a {@code char}.
 */
public interface IntBoolShort2CharFn {
    char invoke(int p1, boolean p2, short p3);
}
