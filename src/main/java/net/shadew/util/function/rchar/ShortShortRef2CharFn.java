package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short}, a {@code short} and an object and returns a {@code char}.
 */
public interface ShortShortRef2CharFn<P3> {
    char invoke(short p1, short p2, P3 p3);
}
