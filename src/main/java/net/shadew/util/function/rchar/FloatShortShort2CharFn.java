package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code short} and a {@code short} and returns a {@code char}.
 */
public interface FloatShortShort2CharFn {
    char invoke(float p1, short p2, short p3);
}
