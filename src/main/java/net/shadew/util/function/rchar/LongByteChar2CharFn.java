package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code byte} and a {@code char} and returns a {@code char}.
 */
public interface LongByteChar2CharFn {
    char invoke(long p1, byte p2, char p3);
}
