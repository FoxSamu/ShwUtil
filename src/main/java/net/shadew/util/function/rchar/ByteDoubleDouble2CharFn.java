package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code double} and returns a {@code char}.
 */
public interface ByteDoubleDouble2CharFn {
    char invoke(byte p1, double p2, double p3);
}
