package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and a {@code char} and returns a {@code char}.
 */
public interface BoolBoolChar2CharFn {
    char invoke(boolean p1, boolean p2, char p3);
}
