package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code boolean} and a {@code float} and returns a {@code char}.
 */
public interface IntBoolFloat2CharFn {
    char invoke(int p1, boolean p2, float p3);
}
