package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code boolean} and returns a {@code char}.
 */
public interface FloatBoolBool2CharFn {
    char invoke(float p1, boolean p2, boolean p3);
}
