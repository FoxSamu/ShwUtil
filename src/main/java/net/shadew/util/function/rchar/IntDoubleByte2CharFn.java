package net.shadew.util.function.rchar;

/**
 * A function that takes an {@code int}, a {@code double} and a {@code byte} and returns a {@code char}.
 */
public interface IntDoubleByte2CharFn {
    char invoke(int p1, double p2, byte p3);
}
