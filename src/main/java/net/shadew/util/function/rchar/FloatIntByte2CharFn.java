package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code byte} and returns a {@code char}.
 */
public interface FloatIntByte2CharFn {
    char invoke(float p1, int p2, byte p3);
}
