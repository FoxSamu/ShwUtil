package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code double} and a {@code char} and returns a {@code char}.
 */
public interface ByteDoubleChar2CharFn {
    char invoke(byte p1, double p2, char p3);
}
