package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code double} and an {@code int} and returns a {@code char}.
 */
public interface LongDoubleInt2CharFn {
    char invoke(long p1, double p2, int p3);
}
