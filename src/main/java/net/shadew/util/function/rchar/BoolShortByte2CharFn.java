package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code short} and a {@code byte} and returns a {@code char}.
 */
public interface BoolShortByte2CharFn {
    char invoke(boolean p1, short p2, byte p3);
}
