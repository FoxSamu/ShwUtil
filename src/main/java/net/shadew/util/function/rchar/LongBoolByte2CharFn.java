package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code long}, a {@code boolean} and a {@code byte} and returns a {@code char}.
 */
public interface LongBoolByte2CharFn {
    char invoke(long p1, boolean p2, byte p3);
}
