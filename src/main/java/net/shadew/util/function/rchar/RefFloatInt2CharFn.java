package net.shadew.util.function.rchar;

/**
 * A function that takes an object, a {@code float} and an {@code int} and returns a {@code char}.
 */
public interface RefFloatInt2CharFn<P1> {
    char invoke(P1 p1, float p2, int p3);
}
