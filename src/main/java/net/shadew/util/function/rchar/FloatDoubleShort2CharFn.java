package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code double} and a {@code short} and returns a {@code char}.
 */
public interface FloatDoubleShort2CharFn {
    char invoke(float p1, double p2, short p3);
}
