package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code char} and a {@code double} and returns a {@code char}.
 */
public interface BoolCharDouble2CharFn {
    char invoke(boolean p1, char p2, double p3);
}
