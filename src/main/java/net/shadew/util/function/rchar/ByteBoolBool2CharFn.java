package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte}, a {@code boolean} and a {@code boolean} and returns a {@code char}.
 */
public interface ByteBoolBool2CharFn {
    char invoke(byte p1, boolean p2, boolean p3);
}
