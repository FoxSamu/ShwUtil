package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code double} and returns a {@code char}.
 */
public interface FloatIntDouble2CharFn {
    char invoke(Float p1, Int p2, Double p3);
}
