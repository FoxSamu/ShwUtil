package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, an {@code int} and a {@code boolean} and returns a {@code char}.
 */
public interface FloatIntBool2CharFn {
    char invoke(Float p1, Int p2, Bool p3);
}
