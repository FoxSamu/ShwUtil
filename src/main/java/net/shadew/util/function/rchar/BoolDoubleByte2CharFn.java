package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code double} and a {@code byte} and returns a {@code char}.
 */
public interface BoolDoubleByte2CharFn {
    char invoke(boolean p1, double p2, byte p3);
}
