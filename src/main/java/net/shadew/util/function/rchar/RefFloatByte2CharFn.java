package net.shadew.util.function.rchar;

/**
 * A function that takes an object, a {@code float} and a {@code byte} and returns a {@code char}.
 */
public interface RefFloatByte2CharFn<P1> {
    char invoke(P1 p1, float p2, byte p3);
}
