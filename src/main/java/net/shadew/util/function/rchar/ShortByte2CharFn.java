package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code short} and a {@code byte} and returns a {@code char}.
 */
public interface ShortByte2CharFn {
    char invoke(short p1, byte p2);
}
