package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code byte} and a {@code double} and returns a {@code char}.
 */
public interface ByteDouble2CharFn {
    char invoke(byte p1, double p2);
}
