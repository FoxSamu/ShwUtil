package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code float} and a {@code byte} and returns a {@code char}.
 */
public interface FloatFloatByte2CharFn {
    char invoke(float p1, float p2, byte p3);
}
