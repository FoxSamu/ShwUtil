package net.shadew.util.function.rchar;

/**
 * A function that takes an object and a {@code double} and returns a {@code char}.
 */
public interface RefDouble2CharFn<P1> {
    char invoke(P1 p1, double p2);
}
