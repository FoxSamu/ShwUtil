package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code float}, a {@code boolean} and a {@code double} and returns a {@code char}.
 */
public interface FloatBoolDouble2CharFn {
    char invoke(float p1, boolean p2, double p3);
}
