package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code double} and a {@code byte} and returns a {@code char}.
 */
public interface DoubleByte2CharFn {
    char invoke(double p1, byte p2);
}
