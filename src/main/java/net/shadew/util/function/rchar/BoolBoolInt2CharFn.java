package net.shadew.util.function.rchar;

/**
 * A function that takes a {@code boolean}, a {@code boolean} and an {@code int} and returns a {@code char}.
 */
public interface BoolBoolInt2CharFn {
    char invoke(boolean p1, boolean p2, int p3);
}
