package net.shadew.util.function.rchar;

/**
 * A function that takes an object and a {@code short} and returns a {@code char}.
 */
public interface RefShort2CharFn<P1> {
    char invoke(P1 p1, short p2);
}
